**██████████████████████**

◆ HIKER VIEW & JU YUE ◆

**📱 觚阅 / 海阔视界**

**规则开发完全手册**

─────────────◆─────────────

V3.66 · 2026 · 76 章 · 新版海阔 ES6 安全子集 / 聚阅旧版 ES5

*基于海阔视界底层 \| 已通过 Anime7 · CZBooks · hanime1.me 实战验证*

> **第零章：两款 App 的关系与底层架构**

聚阅 是基于 海阔视界（HikerView）底层引擎
封装的二次开发平台。两者共用同一套 Rhino JS
引擎和相同的内置函数库（fetch/request/pd/pdfa/pdfh
等）。新版海阔视界引擎（2026+）支持 ES6
安全子集（const/let/箭头函数/模板字符串）；聚阅及旧版引擎仍须使用
ES5（全量 var）。开发时统一以海阔视界 API
规范为底层基准，再叠加聚阅特有的变量注入规则和协议扩展。

  ------------------------------------------------------------------------------------------------------------------------------
  **维度**         **海阔视界（底层）**                                           **聚阅（上层）**
  ---------------- -------------------------------------------------------------- ----------------------------------------------
  JS 引擎          自研 Rhino-ES5 引擎                                            继承，完全相同

  内置函数         fetch/request/pd/pdfh/pdfa/log                                 继承，完全相同

  分页变量         MY_PAGE / MY_URL                                               type:&quot;主页&quot;
                                                                                  即注入 MY_PAGE（与 fypage 占位符无关，见 2.3.1）

  视频协议         video:// + ;{Header@Val}                                       相同

  图片/漫画        pics:// 协议                                                   解析函数必须 return，禁用 setResult

  小说/阅读        无专属                                                         #isJiexi=1#readTheme##autoPage# 后缀

  语法限制         新版海阔：ES6                                                  完全相同，全量用 var
                   安全子集（const/let/箭头函数可用）；聚阅/旧版引擎：ES5（全量   
                   var）。见第三十一章。                                          
  ------------------------------------------------------------------------------------------------------------------------------

> **第一章：parse 对象标准骨架**

所有规则必须封装在 parse 对象中，严禁在全局作用域散放变量。

> var parse = {
>
> 作者: &quot;dev&quot;,
>
> 版本: &quot;20260101.V1&quot;,
>
> host: &quot;https://example.com&quot;,
>
> UA: &quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64)
> AppleWebKit/537.36&quot;,
>
> 页码: { &quot;主页&quot;: 1, &quot;分类&quot;: 1, &quot;搜索&quot;: 1
> },  // value 可写 1 / true，二者等效（引擎只判断真值）；想关翻页用 0 / false / 不写该 key
>
> 静态分类: { type:&quot;主页&quot;, url:&quot;fypage&quot;,
> class_name:&quot;\...&quot;, class_url:&quot;\...&quot; },
>
> 主页: function() { /\* return d; \*/ },
>
> 二级: function(url){ /\* return {\...}; \*/ },
>
> 解析: function(url){ /\* return &quot;video://\...&quot;; \*/},
>
> 搜索: function(name, page){ /\* return d; \*/ },  // page 同 MY_PAGE，GBK站用 encodeStr(name,'GBK')
>
> // ── 工具函数 ────────────────────────────────────────
>
> // 双保险 Base64 解码
>
> \_base64Decode: function(str) {
>
> if (!str) return &quot;&quot;;
>
> str = str.replace(/-/g,&quot;+&quot;).replace(/\_/g,&quot;/&quot;);
>
> while (str.length % 4) str += &quot;=&quot;;
>
> try {
>
> var b = android.util.Base64.decode(str, android.util.Base64.DEFAULT);
>
> return new java.lang.String(b, &quot;UTF-8&quot;);
>
> } catch(e) {
>
> try { return decodeURIComponent(escape(atob(str))); }
>
> catch(e2) { log(&quot;Base64 解码失败&quot;); return &quot;&quot;; }
>
> }
>
> },
>
> // 自动脱壳（最多 3 层，防死循环）
>
> \_autoDeshell: function(html) {
>
> if (!html) return html;
>
> for (var i = 0; i \< 3; i++) {
>
> if (html.indexOf(&quot;decodeURIComponent(\\&quot;&quot;) \> -1) {
>
> var m =
> html.match(/decodeURIComponent\\(\\&quot;(\[\^\\&quot;\]+)\\&quot;\\)/);
>
> if (m) html = decodeURIComponent(m\[1\]);
>
> } else break;
>
> }
>
> return html;
>
> }
>
> // ⚠️ 注意：parse 骨架中不应定义 \_safeRequest 等未实现的占位函数
>
> // 如需带重试的请求，在各自函数内用 try/catch + log 自行实现
>
> };
>
> **第二章：静态分类------三种写法与五维占位符**

**2.1 三种写法对比**

  -------------------------------------------------------------------------------------------------------------
  **写法**        **type**           **url**                                      **适用场景**
  --------------- ------------------ -------------------------------------------- -----------------------------
  自管理 URL      &quot;主页&quot;   &quot;fypage&quot;                           主页内自渲染分类行 + 自己拼请求 URL，
                                                                                  fypage 仅作翻页触发占位

  带选单+翻页     &quot;主页&quot;   &quot;hiker://empty##fyclass##fypage&quot;   顶部有选单，MY_URL 带分类路径，
                                                                                  主页函数 split('##') 取分类和页码

  矩阵方案        &quot;主页&quot;   &quot;0&quot;                                第六章；主页函数完全接管，
                                                                                  url 不替换任何占位符

  无 MY_PAGE      0 / 1              任意                                         分类全在主页内渲染，MY_PAGE
                                                                                  不注入，⚠ 不推荐
  -------------------------------------------------------------------------------------------------------------

> ⚠️ **MY_PAGE 注入的真正条件:** **`type:"主页"`** 即注入 MY_PAGE（同时需要 `页码:{ "主页": 1 }` 在 parse 对象顶层声明）。url 是否含 `fypage` **只影响引擎是否帮你做 URL 占位符替换**，与 MY_PAGE 注入无关。第六章矩阵方案 `url:"0"` 同样能用 `MY_PAGE`，即为铁证。
>
> ⚠️ **type:0 / type:1 的真实表现:** MY_PAGE 不会被注入，但**不是词法分析报错**——是运行期访问 `MY_PAGE` 时为 `undefined`，引起后续比较异常（如 `MY_PAGE == 1` 仍能跑，但 `MY_PAGE * 20` 会变 NaN）。统一用 `type:"主页"` 即可避坑。

**2.2 五维多级筛选（完整写法）**

> &quot;静态分类&quot;: {
>
> &quot;type&quot;: &quot;主页&quot;,
>
> &quot;url&quot;:
> &quot;https://example.com/list/fyclass-fyarea-fysort\-\--fyyear\-\--fypage.html&quot;,
>
> &quot;class_name&quot;: &quot;电影&电视剧&动漫&quot;,
> &quot;class_url&quot;: &quot;1&2&3&quot;,
>
> &quot;area_name&quot;: &quot;全部&大陆&日本&美国&quot;,
> &quot;area_url&quot;: &quot;&大陆&日本&美国&quot;,
>
> &quot;year_name&quot;: &quot;全部&2026&2025&2024&quot;,
> &quot;year_url&quot;: &quot;&2026&2025&2024&quot;,
>
> &quot;sort_name&quot;: &quot;时间&热度&评分&quot;,
> &quot;sort_url&quot;: &quot;time&hits&score&quot;
>
> }

**2.3 五大占位符对照表**

  ------------------------------------------------------------------------------
  **占位符**   **对应 name 字段**    **对应 url       **说明**
                                     字段**           
  ------------ --------------------- ---------------- --------------------------
  fyclass      class_name            class_url        主分类

  fyarea       area_name             area_url         地区筛选

  fyyear       year_name             year_url         年份筛选

  fysort       sort_name             sort_url         排序方式

  fypage       ---（引擎自动管理）   ---              页码占位符，引擎请求 URL 前替换为页码字符串
  ------------------------------------------------------------------------------

> 📌 **铁律:** name 与 url 数量必须严格一一对应（&
> 分隔），&quot;全部&quot;选项对应的 url 留空字符串。fypage
> 由引擎自动管理，只需放在模板中即可。

**2.3.1 fypage / MY_PAGE / MY_URL 本质区分（极易混淆）**

  -----------------------------------------------------------------------------------------------------------------------
  **名词**    **本质**          **生命周期**              **常见用法**
  ----------- ----------------- ------------------------- -----------------------------------------------------------
  fypage      字符串占位符       URL 模板编译期（请求前）   写在 `静态分类.url` / 首页链接，引擎替换为页码字符串

  MY_PAGE     JS 注入变量        函数执行期（运行时）       函数体内直接读：`var p = MY_PAGE;`（1, 2, 3 ...）

  MY_URL      JS 注入变量        函数执行期               已完成所有占位符替换后的完整请求 URL
  -----------------------------------------------------------------------------------------------------------------------

> 📌 **三句话区分:**
> - `fypage` 是占位符，**只在 url 字符串里有意义**；不写它也不影响 MY_PAGE 注入
> - `MY_PAGE` 是引擎注入的变量，**只要 `type:"主页"` 即可用**，恒为 1, 2, 3...（不受偏移量语法影响）
> - `MY_URL` 是引擎替换占位符后的最终 URL；可用 `MY_URL.split('##')` 同时取分类和页码（如 BBYS 实战模式）
>
> ⚠️ **偏移量语法下的差异:** `fypage@-1@*20@` 让 URL 里的 fypage 替换成 0, 20, 40...，但 **JS 内的 `MY_PAGE` 仍是 1, 2, 3**。所以**翻页判断永远用 `MY_PAGE`，拼自定义 API URL 时用 `(MY_PAGE - 1) * 20` 计算 offset**，不要混用。

**2.3.2 parse 对象周边字段速查（含易忽略字段）**

第 1 章骨架给出了 parse 的核心字段，但实战规则里还会出现一些次要字段，常被读者忽略。下表汇总：

  -----------------------------------------------------------------------------------------------------
  **字段**        **类型**          **作用**                                          **值规范**
  --------------- ----------------- ------------------------------------------------- ------------------
  `作者`          string            规则署名                                          任意

  `版本`          string            版本号，更新规则时引擎据此提示用户                建议 `YYYYMMDD.Vx`

  `host`          string            站点基础 URL                                      末尾不带 `/`，与 fetch 拼接时手动加

  `UA`            string            默认 User-Agent，通过 `this.UA` 引用              移动 UA / PC UA / 自定义

  `页码`          object            **声明哪些入口支持翻页**；value 可写 `1` / `true` 二者等效        想关翻页就不写该 key 或写 `0` / `false`

  `静态分类`      object            主页分类入口配置（type / url / class_* / area_*） 见 2.1 / 2.2

  `二级标识`      string            **可选**——给所有 `/二级` URL 自动附加的标签       如 `"#gameTheme#"`、`"#immersiveTheme#"`；不写则不附加

  `预处理`        function          首页/搜索调用前自动跑一次，常用于域名自愈、获取 Cookie 见第 13 章

  `主页/分类/搜索/二级/解析/最新`    function    入口函数                              见各章

  `_xxx`          自定义            内部工具函数/常量，按约定下划线起头不被引擎当入口  随意
  -----------------------------------------------------------------------------------------------------

> 📌 **`二级标识` 字段（V3.66 新增说明）:** 该字段是引擎层"全局二级 URL 标签注入器"——只要点击 item 后引擎要进入二级页，就会自动把该字符串拼到 url 末尾。
>
> ```javascript
> 二级标识: "#gameTheme#"
> // 点击任意卡片进二级页时，URL 自动变成 https://host.com/play/xxx#gameTheme#
> // 等价于在每一条 d.push({ url: "...#gameTheme#" }) 上手写
> ```
>
> 常用值：`"#gameTheme#"`（全屏游戏式二级）/ `"#immersiveTheme#"`（沉浸式）/ `"#readTheme#"`（阅读模式，小说站常用）。**搜索/主页函数 push 的 item url 上不需要重复加**，引擎会统一处理。
>
> ⚠️ `二级标识` 仅影响真正进入二级函数的链接；`video://`、`pics://`、`toast://` 等协议路由不进二级，标签自动忽略。
>
> ⚠️ **未在官方 help 文档收录（实测建议）：** 本字段来自 BBYS 等实战规则，**官方 help_rules.json / help_js.json 均未提及**，疑似旧版海阔遗留或非公开字段。如设置后无效（点击 item 进二级未带标签），降级方案是**手动在每条 `d.push({url:...})` 末尾拼标签**，例如 `url: vidUrl + "#gameTheme#"`，效果等价、兼容性更高。

**2.4 fypage 高级语法------偏移量与步长**

当站点分页参数不是从 1 递增而是从 0 开始或以固定步长递增（如
0,20,40\...）时，使用偏移量语法：

> // 语法：fypage@偏移量@\*步长@
>
> // 偏移量：fypage 的初始值基础上加减的数字
>
> // 步长：乘以页码的倍数（默认步长=1）
>
> // 示例1：从 0 开始，步长 20（即 0,20,40\...）
>
> &quot;url&quot;:
> &quot;https://api.example.com/list?start=fypage@-1@\*20@&size=20&quot;
>
> // 第1页 → start=0，第2页 → start=20，第3页 → start=40
>
> // 示例2：从 0 开始，步长 1（即 0,1,2\...）
>
> &quot;url&quot;:
> &quot;https://api.example.com/list?page=fypage@-1@&quot;
>
> // 注意：fypage 不能放在 URL 最末尾，否则可能翻页异常
>
> // 解决方法：末尾加无效参数，如 ?\_t=0
>
> &quot;url&quot;: &quot;https://example.com/fypage.html?\_t=0&quot;
>
> // 示例3：第一页使用不同地址（部分站点第一页无 /1/）
>
> &quot;url&quot;:
> &quot;https://example.com/list/fypage/\[firstPage=https://example.com/list/\]&quot;
>
> 💡 **说明:** fyAll
> 是一个特殊占位符：当分类只有一个维度但选项太多时，class/area/year
> 的替换词都会替换 fyAll，有 fyAll 就不能再有其他替换词。

**2.5 URL 模板写法（速查 → 详见第二十八章）**

> 📌 完整四种写法 + 实战变体见 **第二十八章 静态分类多维筛选补充**。本节只给三种最常见形态：

```
路径型（MACCMS）:  /vod-show/fyclass-fyarea-fysort---fyyear---fypage---.html
参数型（API 站）:  /api?type=fyclass&area=fyarea&year=fyyear&page=fypage&_t=0
纯路径拼接:        /list/fyclass/fyarea/fyyear/fypage/
```

第四种"BBYS 混合型 `hiker://empty##fyclass##fypage`"详见第 28 章和第 75 章。
>
> **第三章：海阔视界底层内置函数参考**

**3.1 HTML 解析三件套**

  -----------------------------------------------------------------------------------------------
  **函数**                    **缩写**   **返回值**           **说明**
  --------------------------- ---------- -------------------- -----------------------------------
  parseDom(html, rule,        pd         String（智能补全）   自动补全域名/http，返回第一个匹配
  baseUrl)                                                    

  parseDomForHtml(html, rule) pdfh       String（原始）       不处理域名，原始返回

  parseDomForArray(html,      pdfa       Array\<String\>      返回匹配列表，用于循环
  rule)                                                       
  -----------------------------------------------------------------------------------------------

> var items = pdfa(html, &quot;body&&.list&&li&quot;);
>
> for (var i = 0; i \< items.length; i++) {
>
> var \_title = pdfh(items\[i\], &quot;a&&Text&quot;);
>
> var \_href = pd(items\[i\], &quot;a&&href&quot;, host); //
> 第三参数=baseUrl，自动补全域名
>
> var \_pic = pd(items\[i\], &quot;img&&data-original&quot;);
>
> }

**3.2 选择器语法速查**

  ----------------------------------------------------------------------------------------------------------------------------------------
  **语法**                  **说明**                     **示例**
  ------------------------- ---------------------------- ---------------------------------------------------------------------------------
  &&                        取子元素                     &quot;body&&.list&&li&quot;

  \--                       排除元素                     &quot;body\--script&&a&&href&quot;

  tag,N                     取第 N 个（0起）             &quot;body&&a,2&quot; 取第3个 a

  tag,-1                    倒数取                       &quot;body&&li,-1&quot; 取最后一个 li

  Text                      获取文本内容                 &quot;h2&&Text&quot;

  Html                      获取含标签的 HTML            &quot;div&&Html&quot;

  attr                      获取属性                     &quot;img&&src&quot; / &quot;a&&href&quot;

  \[attr\*=&quot;&quot;\]   模糊类名匹配                 \[class\*=&quot;item&quot;\]（比固定类名稳健）

  \|\|                      或语法（先找前者再找后者）   &quot;#app\|\|#app2&&Text&quot;

  .js:expr                  对结果进行 JS 处理           &quot;a&&href.js:input.replace(\\&quot;\\\\\\\\\\&quot;,\\&quot;\\&quot;)&quot;

  js:代码                   完全用 JS 写规则             &quot;js:parseDom(html,\'\...\') + \'/page/1\'&quot;

  tag,N:                    从第 N 个开始取（切片）      &quot;body&&a,1:&quot; 跳过第一个 a，取其余全部

  tag,N:M                   取第 N 到第 M 个（不含 M）   &quot;body&&a,1:3&quot; 取第2、3个 a

  :has(selector)            包含指定子元素的父节点       &quot;.list&&li:has(img)&quot; 只取含图片的 li
  ----------------------------------------------------------------------------------------------------------------------------------------

> 📌 **索引切片语法说明：** `tag,N` 取第 N 个（0起），`tag,N:` 从第 N 个取到末尾，`tag,N:M` 取 N 到 M-1。常用于跳过导航链接、广告等干扰元素，直接定位内容区的目标节点。`:has()` 是 CSS4 伪类，用于过滤"包含特定子元素"的父节点，比用 JS filter 更简洁。

**3.2.1 旧版聚阅 JSoup 嵌套解析 Bug 及绕过方法**

旧版聚阅引擎（JSoup 解析器）在处理**深度嵌套**的 HTML 时存在已知 Bug：`pdfa(html, 'body&&article')` 等嵌套选择器有时只返回 1 条结果，而实际页面有多个 `<article>` 元素。

**原因：** 旧版 JSoup 对某些 HTML 结构的嵌套层级解析不完整，导致只匹配到第一个节点就停止。

**绕过方法：改用字符串 `split()` + 正则提取：**

```javascript
// ❌ 旧版聚阅有时只返回1条
var items = pdfa(html, 'body&&article');

// ✅ 绕过：用字符串分割，每段独立提取
var cards = html.split('</article>');
cards.pop();  // 去掉最后一个空元素
cards.forEach(function(card) {
    var link  = (card.match(/href="([^"]+)"/)          || [])[1] || '';
    var title = (card.match(/class="[^"]*title[^"]*"[^>]*>([^<]+)</) || [])[1] || '';
    var img   = (card.match(/src="(https?:\/\/[^"]+)"/) || [])[1] || '';
    if (!link || !title) return;
    d.push({ title: title, img: img, url: link, col_type: 'movie_3' });
});

// 或按开始标签分割（更精确，防止嵌套 article 干扰）
var cards2 = html.split(/<article\b/);
cards2.shift();  // 去掉分割前的无效头部
```

> 📌 此 Bug 在新版海阔引擎里已修复。判断是否需要绕过：先用 `pdfa` 测试，如果明显少于实际数量，改用 `split()` 方案。

**3.3 网络请求函数**

  ----------------------------------------------------------------------------------------------------
  **函数**                  **说明**                                    **常用参数**
  ------------------------- ------------------------------------------- ------------------------------
  fetch(url, opts)          标准 GET/POST 请求                          {headers, body, method,
                                                                        timeout, withHeaders,
                                                                        withStatusCode, redirect}

  fetchPC(url, opts)        PC UA 请求                                  同 fetch

  request(url, opts)        fetch 的别名                                同 fetch

  post(url, {body})         POST，自动表单编码                          {body: {key: val}}

  batchFetch(arr)           并发请求（最多16个/批，超出自动分批串行）   \[{url, options}, \...\]，缩写
                                                                        bf

  fetchCookie(url, opts)    只获取 Set-Cookie                           返回 Cookie 数组的 JSON 字符串

  fetchCodeByWebView(url,   用 WebView 加载获取源码                     {headers, blockRules, timeout,
  opts)                                                                 checkJs}
  ----------------------------------------------------------------------------------------------------

> // 带 Header 请求
>
> var html = fetch(url, { headers: { &quot;User-Agent&quot;: parse.UA,
> &quot;Referer&quot;: parse.host+&quot;/&quot; } });
>
> // 获取返回 Header（如 Set-Cookie）
>
> var res = JSON.parse(fetch(url, { withHeaders: true }));
>
> var ck = res.headers\[&quot;Set-Cookie&quot;\]\[0\];
>
> // GBK 站点
>
> var html = fetch(url, { headers: { &quot;content-type&quot;:
> &quot;text/html; charset=GBK&quot; } });
>
> // 禁止自动 Cookie（避免串 Cookie）
>
> var html = fetch(url, { headers: { Cookie: &quot;#noCookie#&quot; }
> });
>
> ⚠️ **警告:** batchFetch 并发上限为 16。传入超过 16 个 URL
> 时会自动分批串行执行，并非全部同时发出。

**3.3.1 `requireCache(url, hours)` —— 带缓存的远程 JS 加载**

引擎原生 API，加载远程 JS 文件并在本地缓存指定小时数，缓存期内不重复请求：

```javascript
// 加载远程 JS，缓存 24 小时
requireCache('https://example.com/utils.js', 24);

// 加载后远程 JS 里定义的函数/变量即可在当前作用域使用
var result = someFunction();   // 调用远程 JS 里定义的函数
```

| 参数 | 类型 | 说明 |
|---|---|---|
| `url` | string | 远程 JS 文件地址 |
| `hours` | number | 缓存有效时长（小时），到期后重新请求 |

> ⚠️ **与 `$.require(url)` 的区别**：`$.require` 是引擎的模块加载，返回模块导出对象（需要 `$.exports`）；`requireCache` 是直接 `eval` 远程 JS 内容并缓存，加载后的函数/变量直接在当前作用域可用，无需显式导入。
>
> ⚠️ **依赖风险**：`requireCache` 加载的是第三方托管文件，文件失效或服务器下线会导致规则完全不可用。自己的工具函数应直接内联到规则里，不要用 `requireCache` 托管（Law 88 精神延伸）。生产规则中只在确实需要复用大型共享库时使用。

**3.4 变量存取------juItem.set vs putMyVar 的关键区别**

  -------------------------------------------------------------------------------------------------------------
  **函数**                   **作用域**       **生命周期**                       **典型用途**
  -------------------------- ---------------- ---------------------------------- ------------------------------
  juItem.set(k,v) /             当前规则         持久化（重启不丢，规则删除才丢）   CF Cookie、Token、动态域名
  juItem.get(k,def)                                                                 

  putMyVar(k,v) /            当前规则         会话级（App 重启后丢失）           筛选状态、页码、临时选中索引
  getMyVar(k,def)                                                                

  putVar(k,v) /              全局（跨规则）   会话级（App 重启后丢失）           规则间传递一次性数据
  getVar(k,def)                                                                  

  storage0.putMyVar(k,obj)   当前规则         会话级                             存储 JSON 对象

  juItem.set(k,obj)    当前规则         持久化                             持久存储 JSON 对象
  -------------------------------------------------------------------------------------------------------------

> ⚠️ **警告:** CF Cookie 必须用 juItem.set 持久化，putMyVar 在 App
> 重启后会丢失，导致每次都需重新验证。
>
> 📌 **命名空间:** 用 host
> 字符串作前缀（getMyVar(host+&quot;cindex0&quot;,&quot;0&quot;)），但部分版本不允许
> key 含 &quot;://&quot;，改用纯字母前缀如
> &quot;sitename_cindex0&quot;。

**3.4.1 三层存储分层使用策略（R星精选实战提炼）**

在多频道聚合型规则中，三种存储应按职责严格分层，混用会导致数据丢失或 key 冲突：

```javascript
// ── 层1：juItem —— 跨 App 重启的凭证（token、动态域名）──────
juItem.set('token', JSON.parse(fetch(authUrl)).data.token);
juItem.set('host',  'https://example.com/api/');
var host = juItem.get('host', 'https://default.com/api/');

// ── 层2：storage0 —— 接口返回的大型分类/列表数据（避免翻页重复请求）
if (!storage0.getMyVar('prefix' + 'comic_fenlei')) {
    storage0.putMyVar('prefix' + 'comic_fenlei', this.post('manhua/fenglei'));
}
var fenlei = storage0.getMyVar('prefix' + 'comic_fenlei');  // 取出即为对象，无需 parse

// ── 层3：putMyVar/getMyVar —— 用户当前操作状态（分类选中、排序、折叠）
putMyVar('prefix' + 'comic', data.id);             // 存分类选中 ID
putMyVar('prefix' + 'movie_flod', '1');            // 存展开/折叠状态
var sort = getMyVar('prefix' + 'pic_', 'time');    // 第二参数为默认值
```

**防 key 冲突规范：** 所有 key 必须加固定规则前缀，避免多规则并存时相互污染：

```javascript
// ✅ 固定前缀 + 功能名
'myrule' + '首页'        // 当前主 tab
'myrule' + 'comic'       // 漫画分类选中 ID
'myrule' + 'keyword'     // 搜索关键词

// ❌ 裸 key，极易与其他规则冲突
'首页'
'keyword'
```

> 📌 `key` 禁止包含 `://`，部分版本会报错。用纯字母/汉字前缀替代域名前缀。

**3.4.2 分类按钮防重复刷新（点已选中项不触发刷新）**

手册标准写法的分类切换按钮每次点击都会 `putMyVar + refreshPage`，即使点的是已选中项也会白刷一次。更优的写法是先比较新旧值，只有真正切换时才刷新：

```javascript
// ❌ 标准写法：点任何按钮都刷页，包括已选中项
url: $('#noLoading#').lazyRule(function(key, newid) {
    putMyVar(key, newid);
    refreshPage(false);
    return 'hiker://empty';
}, 'prefix_tab', data.id),

// ✅ 优化写法：nowid vs newid，相同则跳过刷新
url: $('#noLoading#').lazyRule(function(key, nowid, newid) {
    if (nowid != newid) {             // 只有切换了才刷
        putMyVar(key, newid);
        refreshPage(false);
    }
    return 'hiker://empty';
}, 'prefix_tab', curId, data.id),
// ↑ curId 是当前选中值，data.id 是按钮对应的值，都作为参数传入
```

效果：点已选中的分类按钮不触发页面刷新，避免重复请求和闪屏。代价是多传一个参数，可忽略不计。

**3.4.3 分类默认值动态化（用接口返回的第一项作为默认值）**

手册里 `getMyVar` 的默认值都是硬编码字符串（如 `'0'`、`'time'`），但当分类列表来自接口且顺序可能变化时，应该把接口返回的第一项 ID 存起来作为默认值，而不是硬编码：

```javascript
// ── 第一页：接口拿分类列表，把第一项 ID 存入 juItem 作为动态默认值
if (MY_PAGE == 1) {
    if (!storage0.getMyVar('prefix_tags')) {
        var tags = JSON.parse(request(host + 'type=tags')).list;
        storage0.putMyVar('prefix_tags', tags);
    }
    var tags_list = storage0.getMyVar('prefix_tags');

    // 把接口返回的第一个分类 ID 持久化为默认值
    juItem.set('tags_index', tags_list[0].tagid + '');

    // getMyVar 的默认值从 juItem 读，而非硬编码
    var cur = getMyVar('prefix_tags_sel', juItem.get('tags_index'));
    //                                    ↑ 动态默认值，运营调整顺序后自动跟着变

    tags_list.forEach(function(data) {
        d.push({
            title: cur == data.tagid ? highlight(data.tagtitle) : data.tagtitle,
            col_type: 'scroll_button',
            extra: { backgroundColor: cur == data.tagid ? '#20FA7298' : '' },
            url: $('#noLoading#').lazyRule(function(key, nowid, newid) {
                if (nowid != newid) {
                    putMyVar(key, newid);
                    refreshPage(false);
                }
                return 'hiker://empty';
            }, 'prefix_tags_sel', cur, data.tagid + '')
        });
    });
}

// ── 翻页时读取当前选中（默认值同样从 juItem 取）
var cur = getMyVar('prefix_tags_sel', juItem.get('tags_index', ''));
```

**两种默认值策略对比：**

| 策略 | 写法 | 适用场景 |
|---|---|---|
| 硬编码默认值 | `getMyVar(key, '0')` | 分类固定、ID稳定（如排序 `time/tj/rand`）|
| 动态默认值 | `getMyVar(key, juItem.get('xxx_index'))` | 分类列表来自接口，第一项可能变化 |

**3.4.4 token / deviceId 初始化持久化范式**

需要设备标识和登录态的 API 型规则，标准初始化流程：

```javascript
_initToken: function() {
    // ── deviceId：首次随机生成后永久持久化 ────────────────────
    var deviceId = juItem.get('deviceId', '');
    if (!deviceId) {
        deviceId = this._generateHex(32);  // 随机32位 hex
        juItem.set('deviceId', deviceId);
    }

    // ── token：不存在或已失效时重新请求 ──────────────────────
    if (!juItem.get('token', '')) {
        var res = JSON.parse(fetch(this.host + '/api/user/traveler', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ deviceId: deviceId, client: 'pwa' })
        }));
        juItem.set('token', res.data.token);
        juItem.set('imgDomain', res.data.imgDomain || '');
    }
},

_generateHex: function(len) {
    var chars = '0123456789abcdef', res = '';
    for (var i = 0; i < len; i++) res += chars[Math.floor(Math.random() * 16)];
    return res;
},
```

> 📌 `getItem`（壳子专用）必须替换为 `juItem.get`（引擎原生）。沙箱内 `juItem` 可能不可用（取决于引擎版本），统一在沙箱外取好 token/deviceId 后通过参数显式传入。

**3.5 其他常用内置**

  ----------------------------------------------------------------------------------------
  **函数/变量**                             **说明**
  ----------------------------------------- ----------------------------------------------
  log(msg)                                  打印调试日志

  MY_PAGE                                   当前页码（需 type:&quot;主页&quot;
                                            注入，url 无需含 fypage；详见 2.3.1）

  MY_URL                                    当前请求 URL（含静态分类替换后的完整 URL）

  MY_HOME                                   从 MY_URL 计算出的根域名，如 https://a.com

  MY_RULE.title / MY_RULE.find_rule         当前规则信息对象

  MOBILE_UA / PC_UA                         内置移动端/PC UA 变量

  sourcename                                当前规则名称字符串（引擎注入，可存入 extra.sname 用于多源标注）

  base64Encode(s) / base64Decode(s)         Base64 编解码

  encodeStr(s, charset)                     指定字符集 URL 编码（GBK 站点搜索必用，encodeURIComponent 只做 UTF-8）

  aesDecode(key,data) / aesEncode(key,data) AES 加解密

  buildUrl(url, params)                     GET 参数拼接

  refreshPage(false)                        刷新页面，false=不滚动到顶部

  back(true/false)                          关闭当前页，true=同时刷新上一页（仅限二级）

  toast(msg)                                弹出短提示

  confirm({title,content,confirm,cancel})   弹出确认框

  setPageTitle(title)                       动态修改当前页标题

  updateItem(id, obj)                       动态更新某个列表项（无需整页刷新）

  addItemAfter(id, obj)                     在指定 id 的项后插入新项

  deleteItem(id)                            删除指定 id 的列表项

  setPreResult(d) + setResult(d)            先返回顶部元素再追加列表（海阔用）
  ----------------------------------------------------------------------------------------

**3.5.2 `encodeStr(s, charset)` —— GBK 站点搜索编码**

`encodeURIComponent` 只能编码 UTF-8，对 GBK 编码的老站点搜索参数会产生乱码。引擎内置 `encodeStr` 专门解决这个问题：

```javascript
// ❌ 错误：encodeURIComponent 只做 UTF-8，GBK 站点会乱码
var url = host + '/search?q=' + encodeURIComponent(name);

// ✅ 正确：encodeStr 指定 GBK 编码
var key = encodeStr(name, 'GBK');
var url = host + '/search/' + page + '.html?k=' + key;

// 其他字符集（少见，按需使用）
encodeStr(name, 'GB2312');
encodeStr(name, 'Big5');
```

> 📌 判断站点是否需要 GBK 编码：在浏览器 DevTools Network 面板里看搜索请求的关键词参数，如果是 `%C4%E3%BA%C3` 这种双字节格式就是 GBK，如果是 `%E4%BD%A0%E5%A5%BD` 就是 UTF-8。

**3.5.3 `sourcename` —— 当前规则名称变量**

引擎在运行规则时自动注入 `sourcename` 变量，值为当前规则的名称字符串。常见用途是在多源聚合场景中给卡片打来源标注：

```javascript
// 卡片 extra 里传入来源信息，供聚合层识别
d.push({
    title: data.title,
    img:   data.img,
    url:   data.url,
    extra: {
        sname: sourcename,   // 当前规则名称，如"番茄听书"
        stype: '听书',       // 内容类型标注（视频/听书/漫画/小说等）
    }
});
```

| extra 字段 | 说明 |
|---|---|
| `sname` | 来源规则名称，填 `sourcename` 即可自动取当前规则名 |
| `stype` | 内容类型，告诉聚合层用哪种播放器处理 |

`updateItem` / `deleteItemByCls` / `addItemAfter` / `addItemBefore` 联合使用时，**顺序错误会导致锚点找不到或内容错位**。

```javascript
// ❌ 错误：先 add 再 delete，add 时锚点可能已不存在
addItemBefore('anchor', newItems);
deleteItemByCls('old_cls');

// ✅ 正确时序：
// 【折叠】先 delete 多余项，前N项始终在 DOM 里无需 add
deleteItemByCls('hidden_cls');

// 【展开】先确认锚点已 push 且未被删除，再插入
addItemBefore('anchor_id', hiddenItems);
```

**展开/折叠完整模板：**

```javascript
// ── 初始渲染：前N项打 visible_cls，其余存 storage0 ──────────
var allItems = buildItems();  // 构建全部卡片数组
storage0.putMyVar('prefix_hidden', allItems.slice(4));
allItems.slice(0, 4).forEach(function(item) { d.push(item); });

// ── 锚点（折叠区结束位置，addItemBefore 以此为目标）──────────
d.push({ col_type: 'blank_block', extra: { id: 'list_anchor' } });

// ── 展开/折叠按钮 ────────────────────────────────────────────
d.push({
    title: '精选频道',
    desc:  getMyVar('fold', '0') == '0' ? '点击展开' : '点击折叠',
    col_type: 'avatar',
    extra: { id: 'fold_btn' },
    url: $('#noLoading#').lazyRule(function() {
        putMyVar('fold', getMyVar('fold', '0') == '0' ? '1' : '0');
        updateItem('fold_btn', {
            desc: getMyVar('fold', '0') == '0' ? '点击展开' : '点击折叠'
        });
        if (getMyVar('fold', '0') == '0') {
            deleteItemByCls('hidden_cls');            // 折叠：删除多余项
        } else {
            var hidden = storage0.getMyVar('prefix_hidden');
            addItemBefore('list_anchor', hidden);     // 展开：插入锚点之前
        }
        return 'hiker://empty';
    })
});
```

**常见坑速查：**

| 问题 | 原因 | 解决 |
|---|---|---|
| `addItemBefore` 位置不对或报错 | 锚点 id 不存在或已被删 | 先确认锚点已 push 且未被 delete |
| `deleteItemByCls` 没效果 | cls 写在了 `extra` 外层 | 必须是 `extra: { cls: 'xxx' }` |
| `updateItem` 报找不到 id | id 写在了 `extra` 外层 | 必须是 `extra: { id: 'xxx' }` |
| 展开后翻页重复插入 | 展开状态没持久化，翻页重跑了 push | 翻页判断 `MY_PAGE == 1` 才做初始渲染 |
| `findItemsByCls` 取到空数组 | cls 拼写错误或项未渲染 | `log` 确认 cls 名称一致 |

> **第四章：URL 标签快速入口**

在页面 URL 或按钮 URL 中追加 `#xxx#` 标签可改变行为，请求时引擎自动剥除。

> 📌 **完整标签表请见第七十三章 URL 标签全集**（共 25+ 标签，含进阶用法）。本章只保留高频标签速记 + 4.1 `#noLoading#` 适用场景判断（属于陷阱级注意事项，独立成节）。

**最常用 8 个标签速记：**

| 标签 | 一句话用途 |
|---|---|
| `#noLoading#` | 切筛选不闪屏（见 4.1 判断标准） |
| `#noHistory#` | 不记录足迹（加在**前一级**链接） |
| `#readTheme#` | 阅读模式（点击/音量翻页） |
| `#autoPage#` | 滚到底自动加载下一项（小说章节按钮） |
| `#immersiveTheme#` | 沉浸式（标题栏不占位） |
| `#isJiexi=1#` | 触发聚阅内置播放器（漫画/小说**必须**） |
| `#concat#` | 拼接多段视频 |
| `#noCookie#` | 单次请求不带 Cookie（写在 `headers.Cookie`） |

> 📌 **典型组合:**
> ```javascript
> // 聚阅小说章节 URL 标准写法
> chapterUrl + "#isJiexi=1#readTheme##autoPage#"
> // 分段视频
> "video://seg1.m3u8#concat#seg2.m3u8"
> ```

**4.1 `#noLoading#` 适用场景判断（R星精选实战提炼）**

一句话判断标准：**操作结果是"原地更新当前页面内容"→ 加；操作结果是"跳转/加载新内容"→ 不加。**

```javascript
// ✅ 应该加 #noLoading# 的场景

// 1. 切换分类/排序（putMyVar + refreshPage，页面内刷新）
url: $('#noLoading#').lazyRule(function(n, id) {
    putMyVar('prefix' + n, id);
    refreshPage(false);
    return 'hiker://empty';
}, 'comic', data.id);

// 2. 展开/折叠（updateItem + deleteItemByCls，纯 DOM 操作）
url: $('#noLoading#').lazyRule(function() {
    putMyVar('fold', getMyVar('fold', '0') == '0' ? '1' : '0');
    updateItem('fold_btn', { desc: '...' });
    deleteItemByCls('hidden_cls');
    return 'hiker://empty';
});

// 3. 不刷页面替换内容（deleteItemByCls + addItemAfter）
url: $('#noLoading#').lazyRule(function(jkdata) {
    return $.require("jiekou").parse(jkdata).movie_refresh();
}, jkdata);

// 4. 触发 select:// 原生弹窗（立即弹出，无网络请求）
url: $('#noLoading#').lazyRule(function() {
    return 'select://' + JSON.stringify({ title: '切换', options: [...], js: '...' });
});

// ❌ 不应该加 #noLoading# 的场景

// 1. 跳转新页面（需要请求 API 渲染新页面，用户需要知道在加载）
url: $('hiker://empty?page=fypage&#gameTheme#').rule(function(jkdata) {
    $.require("jiekou").parse(jkdata).comic_new();
}, jkdata);
// ↑ 不加 #noLoading#

// 2. 视频直播/播放（加载需要时间，Loading 有意义）
url: 'https://example.com/video.mp4';

// 3. lazyRule 返回的是新页面沙箱（内部请求 API）
url: $(JSON.stringify(imgUrls)).lazyRule(function(urlsJson) {
    return $('hiker://empty?#immersiveTheme#').rule(function(urlList) {
        // 渲染图集需要时间
    }, JSON.parse(urlsJson));
}, JSON.stringify(imgUrls));
// ↑ lazyRule 层可不加，内层 rule 也不加
```

> ⚠️ `#noLoading#` 必须放在 `$()` 参数里（如 `$('#noLoading#').lazyRule(...)`），不能拼接在 `return` 值的末尾——lazyRule 会替换整个 url，拼接在 return 里不生效。

> **第五章：col_type 显示样式速查**

  -----------------------------------------------------------------------------
  **col_type**                **说明**
  --------------------------- -------------------------------------------------
  movie_3 / movie_3_marquee   一行3列，圆角图片+标题（默认样式）

  movie_2                     一行2列，圆角图片+标题

  movie_1                     一行1列，标题描述较多时使用

  movie_1_left_pic            一行1列，图片在左

  movie_1_vertical_pic        竖向图片在左，适合书籍/漫画封面

  movie_1_vertical_pic_blur   同上，背景高斯模糊

  text_1 \~ text_5            文本1\~5列，支持红色&quot;&quot;和橙色\'\'混排

  long_text                   不限长度长文本，extra:{textSize:18}

  rich_text                   富文本，支持 HTML
                              标签，extra:{textSize,lineSpacing}

  pic_1_full                  全宽图片，高度按比例自适应

  pic_3 / pic_2               纯图片布局，3列/2列

  scroll_button               横向滚动按钮（支持 HTML），多个连续 push 自动聚合

  flex_button                 流式自适应按钮（支持 HTML），多个连续 push
                              自动聚合

  blank_block                 空白块（高度1dp）

  line                        分割线

  input                       单行输入框，extra:{type,defaultValue,onChange}

  x5_webview_single           X5 浏览器组件，desc 控制高度/模式

  avatar                      头像样式（图片+标题+右侧 desc）

  card_pic_2 / card_pic_1     方形卡片，可配合 card_pic_2_2 组合

  icon_4 / icon_round_4       桌面图标样式，4列

  video                       视频组件（与 x5_webview_single 不能共存）
  -----------------------------------------------------------------------------

> ⚠️ **警告:** x5_webview_single 一个页面只能有一行使用。⚠️
> scroll_button 不支持 HTML 渲染（fontcolor/bold
> 会直接显示原始标签字符串）；flex_button 支持 HTML 标题。scroll_button
> 选中状态只能靠 backgroundColor + 纯文本符号（▶ title 或 ●
> title）标记，不能用 fontcolor().bold()。

**5.1 `x5_webview_single` 嵌入 WebView 登录（`extra.js` 注入）**

`x5_webview_single` 除了用于 CF 过盾，还可以将登录页直接嵌入规则页面，通过 `extra.js` 在 WebView 内执行检测脚本，登录成功后自动桥接 Cookie 到规则侧：

```javascript
d.push({
    col_type: 'x5_webview_single',
    desc:     '100&&float',    // 高度100%，float模式（覆盖整个页面）
    url:      loginUrl,        // 登录页地址
    extra: {
        js: $.toString(function() {
            // 这段 JS 在 WebView 内执行，使用 fba.* 桥接 API
            // 检测是否已登录（如检查页面是否含某个登录后才有的元素）
            if (document.querySelector('.user-avatar')) {
                // 已登录：取 Cookie 并传回规则侧
                var ck = fba.getCookie(location.href);
                fba.putVar('site_cookie', ck);   // 写入规则侧变量
                fba.toast('登录成功');
                fba.back();                       // 关闭 WebView，返回规则页
            }
        })
    }
});
```

> 📌 `extra.js` 里的代码在 WebView 沙箱内执行，只能使用 `fba.*` 桥接 API，不能直接调用规则侧的 `juItem`/`putMyVar` 等函数。Cookie 取回后由规则主函数用 `getVar('site_cookie')` 读取，再写入 `juItem.set` 持久化（见 5.2 节）。

**5.2 `'''''` 前缀——在 title 字段内渲染 HTML 标签**

手册中 `''''`（四个单引号）是富文本激活前缀，但部分版本/场景需要 `'''''`（**五个单引号**）才能触发完整 HTML 解析（包括 `<font>` 标签的颜色渲染）：

```javascript
// 四个单引号：标准富文本前缀（detail1/detail2 常用）
title: "''''<b>加粗文字</b>",

// 五个单引号：强制 HTML 渲染，适用于 scroll_button 以外的所有 col_type
title: "'''''<font color='#FF0000'>💬 全部回复</font>",

// 区别：四个单引号在某些 col_type 下只激活部分标签，
// 五个单引号触发更完整的 HTML 解析上下文
```

> ⚠️ `scroll_button` 对任意数量的单引号前缀均不生效，仍会原样显示标签字符串。需要着色的 scroll_button 只能用 `backgroundColor` 字段。

> **第六章：主页函数------矩阵静态分类完整方案**

矩阵静态分类是将所有分类数据硬编码在规则内部，通过多行 scroll_button
实现多维筛选的方案。优点是零网络请求、点击即开、支持多行联动。以下内容基于
XOJAV V34 实战代码提炼。

**6.1 静态分类声明------矩阵模式固定写法**

矩阵模式不依赖引擎的静态分类替换，仅用它声明入口。url 固定写
&quot;0&quot;，class_name/class_url
填任意占位值，主页函数自己全权管理所有分类渲染和 URL 构建。

> // ✅ 矩阵模式固定写法：url 写 &quot;0&quot;，引擎不替换任何占位符
>
> // 主页函数内通过 getMyVar 读取 cindex/curl 自行构建请求 URL
>
> 静态分类: {
>
> &quot;type&quot;: &quot;主页&quot;,
>
> &quot;url&quot;: &quot;0&quot;,
>
> &quot;class_name&quot;: &quot;静态矩阵旗舰版&quot;,
>
> &quot;class_url&quot;: &quot;0&quot;
>
> },
>
> 页码: { &quot;主页&quot;: 1 },
>
> 📌 **与 fypage 方案的区别:** fypage
> 方案（第二章）依赖引擎替换占位符，适合单维/多维但分类较少、URL
> 规律固定的站点。矩阵方案适合分类极多（如 9 行×数十列）、URL
> 需要按行单选拼接的复杂站点。
>
> ⚠️ **MY_PAGE 注入说明（修订）:** 本方案 `url:"0"` 不含 fypage 占位符，但因为 `type:"主页"` + 顶层声明 `页码:{ "主页": 1 }`，**MY_PAGE 仍然正常注入**（恒为 1, 2, 3...）。不要被"url 含 fypage 才注入 MY_PAGE"的旧说法误导，真正的注入条件见 2.3.1。

**6.2 分类数据字典------物理硬编码**

将所有分类以数组形式硬编码在主页函数内。每一项对应一行筛选条，t
是显示标题，i 是对应的 URL 路径值，用 &
分隔，数量严格一一对应。第一项固定为&quot;全部&quot;，i 值为
&quot;0&quot;（表示不选）。

> // 每个对象 = 一行筛选条
>
> // t：显示标题（& 分隔）
>
> // i：对应 URL 路径值（& 分隔，与 t 数量严格一一对应）
>
> // 第一项固定为全部/重置项，i 值用 &quot;0&quot; 表示不选中
>
> var dataClass = \[
>
> // 第 1 行（cindex1）
>
> { t: &quot;按主題&中文字幕&無碼解放&台灣AV&日本AV&quot;,
>
> i:
> &quot;0&/categories/chinese-subtitle&/categories/uncensored&/categories/taiwan-av&/categories/jav&quot;
> },
>
> // 第 2 行（cindex2）
>
> { t: &quot;按衣着&婚紗&水着&吊帶襪&校服&quot;,
>
> i:
> &quot;0&/tags/wedding-dress&/tags/swimsuit&/tags/stockings&/tags/school-uniform&quot;
> },
>
> // 第 3 行（cindex3）\... 以此类推，最多支持到 cindex9
>
> \];
>
> ⚠️ **警告:** t 和 i 中 &
> 分隔的项数必须严格相等，数量不匹配会导致越界取到 undefined，点击后
> curl 写入空值。

**6.3 renderRow------多行筛选渲染函数（唯一正确版本）**

renderRow
在主页函数内部定义为局部函数，参数：rowIdx（行号）、titleStr（标题串）、idStr（值串）、d（结果数组）、h（命名空间前缀，传
host 字符串）。

> var renderRow = function(rowIdx, titleStr, idStr, d, h) {
>
> var tArr = titleStr.split(&quot;&&quot;);
>
> var iArr = idStr.split(&quot;&&quot;);
>
> var curIdx = getMyVar(h + &quot;cindex&quot; + rowIdx, &quot;0&quot;);
>
> for (var i = 0; i \< tArr.length; i++) {
>
> var isSel = (i == curIdx);
>
> d.push({
>
> title: isSel ? &quot;❆ &quot; + tArr\[i\] + &quot; ❆&quot; :
> tArr\[i\],
>
> url: \$(&quot;#noLoading#&quot;).lazyRule(function(rIdx, idx, val, h)
> {
>
> putMyVar(h + &quot;cindex&quot; + rIdx, idx + &quot;&quot;);
>
> putMyVar(h + &quot;curl&quot; + rIdx, val);
>
> // ── 联动重置规则 ──────────────────────────────
>
> if (rIdx == 0) {
>
> // Row0（主切换）：重置所有子行
>
> for (var n = 1; n \< 10; n++) {
>
> putMyVar(h + &quot;cindex&quot; + n, &quot;0&quot;);
>
> putMyVar(h + &quot;curl&quot; + n, &quot;&quot;);
>
> }
>
> } else {
>
> // 分类行：其他分类行互斥单选（可选，按需开启）
>
> for (var m = 1; m \< 10; m++) {
>
> if (m != rIdx) putMyVar(h + &quot;cindex&quot; + m, &quot;0&quot;);
>
> }
>
> }
>
> // ────────────────────────────────────────────
>
> refreshPage(false);
>
> return &quot;hiker://empty&quot;;
>
> }, rowIdx, i, iArr\[i\], h),
>
> col_type: &quot;scroll_button&quot;
>
> });
>
> }
>
> d.push({ col_type: &quot;blank_block&quot; });
>
> };

  --------------------------------------------------------------------------------------------
  **联动规则**         **行为**                   **适用场景**
  -------------------- -------------------------- --------------------------------------------
  Row0 切换            重置 cindex1\~9 全部归 0   主模式切换（如：最近更新 / 热门 / 分类）

  分类行互斥（可选）   点击某行时，其他分类行归 0 各行分类互不干扰，同一时刻只有一行有效选中

  不互斥（各行独立）   各行 cindex 互不影响       多维筛选（如分类+地区+年份同时选）
  --------------------------------------------------------------------------------------------

> 💡 **两种联动策略的选择:**
> 如果各行分类是&quot;或&quot;关系（选了按主题就不能同时选按衣着），用互斥单选。如果各行是独立维度（分类+地区+年份可叠加），去掉
> else 分支，各行 cindex 互不影响。

**6.4 分类行渲染时机------仅第一页渲染**

分类行只在 MY_PAGE == 1 时渲染，翻页时不重复渲染。Row0 固定渲染；Row1\~N
是否渲染取决于当前 Row0
的选中状态（例如只有选中&quot;分类模式&quot;才展开 9 行矩阵）。

> if (MY_PAGE == 1) {
>
> // Row0：主模式切换，始终渲染
>
> renderRow(0, &quot;🏠最近更新&🔥热门影片&📂视频分类&quot;,
>
> &quot;/&hot&categories&quot;, \_resList, host);
>
> var index0 = getMyVar(host + &quot;cindex0&quot;, &quot;0&quot;);
>
> if (index0 == &quot;2&quot;) {
>
> // 仅在&quot;视频分类&quot;模式下展开 9 行矩阵
>
> for (var j = 0; j \< dataClass.length; j++) {
>
> renderRow(j + 1, dataClass\[j\].t, dataClass\[j\].i, \_resList, host);
>
> }
>
> }
>
> }

**6.5 URL 构建------读取 cindex/curl 拼接请求地址**

URL 构建完全由主页函数自己负责。读取 cindex0
判断当前主模式，分类模式下回溯 cindex1\~9 找到被选中的那一行，取其 curl
值拼接请求 URL。

> var \_fetchUrl = host;
>
> var c0 = getMyVar(host + &quot;cindex0&quot;, &quot;0&quot;);
>
> if (c0 == &quot;0&quot;) {
>
> // 模式0：最近更新
>
> \_fetchUrl = host + &quot;/?sort_by=release_at&from=&quot; + MY_PAGE;
>
> } else if (c0 == &quot;1&quot;) {
>
> // 模式1：热门影片
>
> \_fetchUrl = host + &quot;/?sort_by=views&from=&quot; + MY_PAGE;
>
> } else {
>
> // 模式2：矩阵分类------回溯 cindex1\~9，找到第一个非 0 的选中项
>
> var \_slug = &quot;/categories/default/&quot;; // 兜底默认分类
>
> for (var k = 1; k \< 10; k++) {
>
> var \_idx = getMyVar(host + &quot;cindex&quot; + k, &quot;0&quot;);
>
> if (\_idx != &quot;0&quot;) {
>
> \_slug = getMyVar(host + &quot;curl&quot; + k, \_slug);
>
> break; // 找到即停，单选
>
> }
>
> }
>
> // 拼接 URL，兼容路径已含 ? 的情况
>
> \_fetchUrl = host + \_slug
>
> \+ (\_slug.indexOf(&quot;?&quot;) \> -1 ? &quot;&&quot; :
> &quot;?&quot;)
>
> \+ &quot;sort_by=release_at&from=&quot; + MY_PAGE;
>
> }
>
> 📌 **变量命名:** 注意主页函数内要用 var \_fetchUrl
> 等自定义变量，不要直接给 MY_URL 赋值------MY_URL
> 是引擎注入的只读变量，覆写它在部分版本会导致引擎异常。

**6.6 多分类行互不干扰------完整工作流程**

以下是用户点击一个分类按钮后，各行状态如何变化的完整流程图解：

> 用户点击 Row3（按身材）中的&quot;巨乳&quot;
>
> → lazyRule 执行：
>
> putMyVar(host+&quot;cindex3&quot;, &quot;10&quot;) // 记录 Row3
> 选中索引
>
> putMyVar(host+&quot;curl3&quot;, &quot;/tags/big-tits&quot;) // 记录
> Row3 选中值
>
> // 互斥模式：其他行归0
>
> putMyVar(host+&quot;cindex1&quot;, &quot;0&quot;) // Row1 重置
>
> putMyVar(host+&quot;cindex2&quot;, &quot;0&quot;) // Row2 重置
>
> // cindex4\~9 同理\...
>
> → refreshPage(false)
>
> 主页函数重新执行：
>
> → c0 == &quot;2&quot;（仍在分类模式）
>
> → 渲染 Row0（✦ 视频分类 ✦），Row1（全部），Row2（全部），Row3（✦ 巨乳
> ✦）\...
>
> → URL 构建：回溯 k=1（cindex1=0 跳过），k=2（跳过），k=3（cindex3=10
> 命中）
>
> \_slug = &quot;/tags/big-tits&quot;
>
> \_fetchUrl = host +
> &quot;/tags/big-tits?sort_by=release_at&from=1&quot;
>
> → 请求并渲染列表

**6.7 矩阵模式主页函数完整骨架**

> 主页: function() {
>
> var \_resList = \[\];
>
> var \_host = this.host;
>
> var \_ua = this.UA;
>
> // ── 1. renderRow 定义（主页函数内局部，不污染全局）─────
>
> var renderRow = function(rowIdx, titleStr, idStr, d, h) {
>
> var tArr = titleStr.split(&quot;&&quot;);
>
> var iArr = idStr.split(&quot;&&quot;);
>
> var curIdx = getMyVar(h + &quot;cindex&quot; + rowIdx, &quot;0&quot;);
>
> for (var i = 0; i \< tArr.length; i++) {
>
> var isSel = (i == curIdx);
>
> d.push({
>
> title: isSel ? &quot;❆ &quot; + tArr\[i\] + &quot; ❆&quot; :
> tArr\[i\],
>
> url: \$(&quot;#noLoading#&quot;).lazyRule(function(rIdx, idx, val, h)
> {
>
> putMyVar(h + &quot;cindex&quot; + rIdx, idx + &quot;&quot;);
>
> putMyVar(h + &quot;curl&quot; + rIdx, val);
>
> if (rIdx == 0) {
>
> for (var n = 1; n \< 10; n++) putMyVar(h + &quot;cindex&quot; + n,
> &quot;0&quot;);
>
> } else {
>
> for (var m = 1; m \< 10; m++) {
>
> if (m != rIdx) putMyVar(h + &quot;cindex&quot; + m, &quot;0&quot;);
>
> }
>
> }
>
> refreshPage(false);
>
> return &quot;hiker://empty&quot;;
>
> }, rowIdx, i, iArr\[i\], h),
>
> col_type: &quot;scroll_button&quot;
>
> });
>
> }
>
> d.push({ col_type: &quot;blank_block&quot; });
>
> };
>
> // ── 2. 分类数据字典（硬编码）────────────────────────────
>
> var dataClass = \[
>
> { t: &quot;行1标题A&行1标题B&行1标题C&quot;, i:
> &quot;0&/path/b&/path/c&quot; },
>
> { t: &quot;行2标题A&行2标题B&quot;, i: &quot;0&/path/b2&quot; },
>
> // \... 最多到 dataClass\[8\]（对应 cindex1\~cindex9）
>
> \];
>
> // ── 3. 分类行渲染（仅第一页）────────────────────────────
>
> if (MY_PAGE == 1) {
>
> renderRow(0, &quot;最近更新&热门&分类&quot;,
> &quot;/&/hot&/categories&quot;, \_resList, \_host);
>
> if (getMyVar(\_host + &quot;cindex0&quot;, &quot;0&quot;) ==
> &quot;2&quot;) {
>
> for (var j = 0; j \< dataClass.length; j++) {
>
> renderRow(j + 1, dataClass\[j\].t, dataClass\[j\].i, \_resList,
> \_host);
>
> }
>
> }
>
> }
>
> // ── 4. URL 构建 ──────────────────────────────────────────
>
> var \_url = \_host;
>
> var c0 = getMyVar(\_host + &quot;cindex0&quot;, &quot;0&quot;);
>
> if (c0 == &quot;0&quot;) {
>
> \_url = \_host + &quot;/?sort_by=release_at&from=&quot; + MY_PAGE;
>
> } else if (c0 == &quot;1&quot;) {
>
> \_url = \_host + &quot;/?sort_by=views&from=&quot; + MY_PAGE;
>
> } else {
>
> var \_slug = &quot;/categories/default/&quot;;
>
> for (var k = 1; k \< 10; k++) {
>
> if (getMyVar(\_host + &quot;cindex&quot; + k, &quot;0&quot;) !=
> &quot;0&quot;) {
>
> \_slug = getMyVar(\_host + &quot;curl&quot; + k, \_slug);
>
> break;
>
> }
>
> }
>
> \_url = \_host + \_slug + (\_slug.indexOf(&quot;?&quot;) \> -1 ?
> &quot;&&quot; : &quot;?&quot;) + &quot;from=&quot; + MY_PAGE;
>
> }
>
> // ── 5. 请求与列表解析 ────────────────────────────────────
>
> var \_html = fetch(\_url, { headers: { &quot;User-Agent&quot;: \_ua }
> });
>
> // \... 解析 \_html，push 到 \_resList \...
>
> return \_resList; // ✅ Law 43：绝对不能漏
>
> },

**6.8 fypage 方案 vs 矩阵方案------如何选择**

  -----------------------------------------------------------------------------------------------
  **对比维度**        **fypage 方案（第二章）**             **矩阵方案（本章）**
  ------------------- ------------------------------------- -------------------------------------
  静态分类 url 写法   &quot;fypage&quot; 或含占位符的完整 URL   &quot;0&quot;（固定，不替换任何占位符）

  分类数据来源        写在 class_name/area_name 等字段       硬编码在主页函数内的 dataClass 数组

  URL 构建            引擎自动替换占位符                     主页函数完全自己构建

  适用行数            1\~4 行，结构规律                      1\~9 行，结构复杂或 URL 不规律

  适用分类数          每行几个到十几个                       每行可以几十个

  互斥单选逻辑        引擎自动处理                           renderRow lazyRule 内手动 putMyVar 重置

  多行叠加筛选        受限（引擎只替换对应占位符）           完全自由，主页函数自己读多行 curl 拼接

  MY_PAGE 可用        ✅（type:&quot;主页&quot;即注入）       ✅（type:&quot;主页&quot;即注入；url:"0" 同样有效）

  BBYS 类 API 站      ✅ 适用                                ✅ 适用（如果需要 4 行以上联动筛选）
  -----------------------------------------------------------------------------------------------

> 📌 **选型补充（V3.66）：** 第七十五章 BBYS 实战模式用的是另一种路径——`url: 'hiker://empty##fyclass##fypage'` + 主页函数 `MY_URL.split('##')`，介于上面两种方案之间，适合"顶部 Tab 走 fyclass，列表查询走自定义 API"的混合站点。

> **第七章：二级函数------详情页**

**7.1 视频源格式（有播放列表）**

> 二级: function(url) {
>
> var \_host = this.host;
>
> var \_ua = this.UA;
>
> var \_html = request(url, { headers: { &quot;User-Agent&quot;: \_ua }
> });
>
> var tabs = \[\];
>
> var lists = \[\];
>
> // MACCMS SID 强关联：用 data-sid 匹配对应的集数面板（Law 16）
>
> var tabNodes = pdfa(\_html,
> &quot;body&&.module-tab-item\[data-sid\]&quot;);
>
> for (var a = 0; a \< tabNodes.length; a++) {
>
> var sid = tabNodes\[a\].match(/data-sid=&quot;(\\d+)&quot;/)\[1\];
>
> var epItems = pdfa(\_html, &quot;body&&#pane&quot; + sid +
> &quot;&&.module-play-list-link&quot;);
>
> if (epItems.length \> 0) {
>
> // 集数下标美化（Law 21）
>
> var cnt = epItems.length;
>
> var subNum = (cnt + &quot;&quot;).split(&quot;&quot;).map(function(n)
> {
>
> return String.fromCharCode(parseInt(n) + 8320);
>
> }).join(&quot;&quot;);
>
> tabs.push(pdfh(tabNodes\[a\], &quot;span&&Text&quot;) + subNum);
>
> var subList = \[\];
>
> for (var i = 0; i \< epItems.length; i++) {
>
> subList.push({
>
> title: pdfh(epItems\[i\], &quot;Text&quot;),
>
> // ✅ 直接构造 video:// 协议（V102 最稳定方案）
>
> url: &quot;video://&quot; + pd(epItems\[i\], &quot;a&&href&quot;,
> \_host)
>
> \+ &quot;;{User-Agent@&quot; + \_ua + &quot;&&Referer@&quot; +
> \_host + &quot;/}&quot;
>
> });
>
> }
>
> lists.push(subList);
>
> }
>
> }
>
> return {
>
> vod_name: pdfh(\_html, &quot;h1&&Text&quot;),
>
> vod_pic: pd(\_html, &quot;.cover&&img&&src&quot;, \_host),
>
> vod_content: pdfh(\_html, &quot;.intro&&Text&quot;),
>
> line: tabs,
>
> list: lists
>
> };
>
> },

**7.2 漫画/小说格式**

> 二级: function(url) {
>
> var \_host = this.host;
>
> var \_html = request(url);
>
> var chapters = pdfa(\_html, &quot;ul.chapter&&li&quot;);
>
> var list = \[\];
>
> for (var i = 0; i \< chapters.length; i++) {
>
> list.push({
>
> title: pdfh(chapters\[i\], &quot;a&&Text&quot;),
>
> // ✅ 聚阅铁律：必须有 #isJiexi=1 才能触发内置阅读器
>
> url: pd(chapters\[i\], &quot;a&&href&quot;, \_host) +
> &quot;#isJiexi=1#readTheme##autoPage#&quot;
>
> });
>
> }
>
> return { detail1: pdfh(\_html,&quot;h1&&Text&quot;), img:
> pd(\_html,&quot;.cover&&img&&src&quot;,\_host),
>
> desc: pdfh(\_html,&quot;.desc&&Text&quot;), list: \[list\] };
>
> },

**7.3 二级返回结构完整字段**

  ------------------------------------------------------------------------------------------
  **字段**            **类型**                        **说明**
  ------------------- ------------------------------- --------------------------------------
  vod_name / detail1  String                          标题

  vod_pic / img       String                          封面图 URL

  vod_remarks         String                          简短备注（如&quot;更新至09集&quot;）

  vod_content / desc  String                          详细简介

  line                Array\<String\>                 线路名称数组

  list                Array\<Array\<{title,url}\>\>   与 line 一一对应的选集二维数组

  noShow              Object                          {封面:false, 简介:false, 选集:false}
                                                      控制不显示的组件

  moreitems           Array                           选集上方的扩展项

  extenditems         Array                           选集下方的扩展项（图文详情常用）

  novel               Boolean                         是否为小说模式
  ------------------------------------------------------------------------------------------

**7.3.0 `detail1` / `detail2` / `desc` 展示位置与最佳实践**

三个字段展示在封面卡片的不同区域，职责各不相同：

| 字段 | 展示位置 | 建议内容 | 特点 |
|---|---|---|---|
| `detail1` | 封面右侧/上方，标题旁 | 关键信息：更新状态、年份、地区、导演 | 支持 `\n` 换行，支持 `''''` 富文本 |
| `detail2` | 封面下方，简介上方 | 辅助信息：类型、评分、演员 | 支持 `\n` 换行，支持 `''''` 富文本 |
| `desc` | `detail2` 之后 | 详细简介，纯文本段落 | 纯文本，不渲染 HTML 标签 |

**富文本激活方式：** 字段值以 `''''`（四个单引号）开头时，引擎将该字段作为富文本渲染，可使用 `<font color>` / `<b>` / `<small>` 等 HTML 标签：

```javascript
detail1: "''''<font color=#FF6600>热播中</font>\n📅 2026 | 日本\n🎬 导演：久藤瞬",
detail2: "''''📂 漫画改 / 奇幻 / TV\n⭐ 5.2",
desc: "轮回循环，才能将在此世复苏...",  // 纯文本，不加 ''''
```

**完整提取代码模板（视频站通用）：**

```javascript
二级: function(url) {
    var html = request(url);

    // ── detail1：关键信息（更新状态 + 年份 + 地区 + 导演）─────────
    var remarks  = (html.match(/slide-info-remarks[^>]*>([^<]+)</) || [])[1] || '';
    var year     = (html.match(/<a[^>]*>(\d{4})<\/a>/) || [])[1] || '';
    var area     = (html.match(/<a[^>]*>\d{4}<\/a>[\s\S]*?<a[^>]*>([^<]+)</) || [])[1] || '';
    var director = (html.match(/导演\s*:<\/strong>([^<]+)/) || [])[1] || '';
    var detail1  = (remarks  ? '📺 ' + remarks.trim()          : '')
                 + (year     ? '\n📅 ' + year                  : '')
                 + (area     ? ' | ' + area.trim()             : '')
                 + (director ? '\n🎬 导演：' + director.trim() : '');

    // ── detail2：辅助信息（类型 + 评分 + 演员）───────────────────
    var typesRaw = (html.match(/类型\s*:<\/strong>([\s\S]*?)<\/div>/) || [])[1] || '';
    var actorRaw = (html.match(/演员\s*:<\/strong>([\s\S]*?)<\/div>/) || [])[1] || '';
    var score    = (html.match(/分数[^>]*>(\d+\.?\d*)</)             || [])[1] || '';
    var detail2  = (typesRaw ? '📂 ' + typesRaw.replace(/<[^>]+>/g, ' ').trim() : '')
                 + (actorRaw ? '\n👥 ' + actorRaw.replace(/<[^>]+>/g, ' ').trim() : '')
                 + (score    ? '\n⭐ ' + score                                    : '');

    // ── desc：纯文本简介 ──────────────────────────────────────────
    var descRaw = (html.match(/id="height_limit"[^>]*>([\s\S]*?)<\/div>/) || [])[1] || '';
    var desc    = descRaw.replace(/<[^>]+>/g, '').replace(/&nbsp;/g, '').trim();

    return {
        detail1: detail1 ? "'''' " + detail1 : '',
        detail2: detail2 ? "'''' " + detail2 : '',
        desc:    desc,
        img:     pd(html, '.cover&&img&&src'),
        list:    [...],
    };
}
```

**使用注意事项：**

| 规则 | 说明 |
|---|---|
| 省略规则 | 三个字段均可省略，引擎不显示对应区域，不会报错 |
| `''''` 前缀 | 只需在字符串最开头加一次，不能放在 `\n` 后的每行 |
| `desc` 不渲染 HTML | `desc` 是纯文本区域，`<font>` 等标签会原样显示；富文本简介应用 `extenditems` 里的 `rich_text` 卡片实现 |
| `\n` 换行 | `detail1` / `detail2` 支持 `\n`，实际渲染为换行；`desc` 里的 `\n` 也会换行 |
| 正则兜底 | 提取正则加 `|| [])[1] || ''` 兜底，避免匹配失败时 `.replace` 报错 |

**7.3.1 `MY_PARAMS`——`extra` 字段透传参数到二级（省去重复请求）**

列表页卡片的 `extra` 里放的字段，在二级函数里可以通过 `MY_PARAMS` 直接读取。常用来把列表页已经拿到的封面、标题、简介等数据带进二级，**避免二级再发一次网络请求**。

```javascript
// ── 列表页：把数据塞进 extra ──────────────────────────────────
comic_list.forEach(function(data) {
    d.push({
        title: data.title,
        img:   data.image,
        url:   host + data.comicid,   // 二级函数收到的 MY_URL
        col_type: 'movie_3',
        extra: {
            comic_id: data.comicid,   // 自定义字段，二级用 MY_PARAMS.comic_id 读取
            img:      data.image,
            title:    data.title,
            desc:     data.uptext,
        }
    });
});

// ── 二级函数：直接读 MY_PARAMS，不用再请求详情页拿标题/封面 ──
二级: function() {
    var title = MY_PARAMS.title;      // ✅ 直接取，零网络请求
    var id    = MY_PARAMS.comic_id;
    var img   = MY_PARAMS.img;
    var desc  = MY_PARAMS.desc;

    // 只需请求章节列表，封面/标题/简介已经有了
    var chapter = JSON.parse(request(host + 'type=chapterlist&comicid=' + id)).list;

    return {
        detail1:   title,
        detail2:   desc,
        detailurl: img,
        img:       img,
        list:      chapter.map(function(c) {
            return { title: c.title, url: c.chapterid + '' };
        })
    };
}
```

**适用场景与注意事项：**

| 规则 | 说明 |
|---|---|
| 适用场景 | 列表页 API 已包含封面/标题/简介，二级只需拿章节/选集列表 |
| 不适用场景 | 二级需要请求详情页才能拿到完整数据（简介很长、有多个线路等）|
| extra key 命名 | 避免与引擎保留字段冲突，推荐用业务前缀如 `comic_id` 而非 `id` |
| `MY_URL` 仍然是卡片的 `url` 字段 | `MY_PARAMS` 是 `extra` 里的自定义字段，两者独立 |
| extra 字段只传字符串/数字 | 避免传复杂对象，防止序列化异常（Law 52 延伸）|

> 📌 这个模式在漫画/有声/图集类规则里极为常见：列表接口往往已经返回封面、标题、状态等所有展示需要的字段，二级只需要用 `id` 换取章节列表，`MY_PARAMS` 是避免冗余请求的标准方式。

**7.4 erji 沙箱 vs 标准二级的选择依据**

**标准二级机制**

规则对象里有 二级 函数，引擎在用户点击列表卡片时自动调用，input 是卡片的
url 字段值。适用条件：详情页是独立 URL，可以直接 fetch/request 拿到完整
HTML；返回结构是标准的 vod_name / img / line / list
对象；不需要在详情页内部再跳转到其他同类详情页。

**erji 沙箱机制**

没有 二级 函数，卡片的 url 字段末尾拼接
\$('').rule(parse.erji)，点击后进入独立的列表沙箱页面，getResCode()
拿详情页 HTML，setResult(d)
输出渲染结果。适用条件：（1）详情页内部存在嵌套跳转------详情页里还有指向其他详情页的链接，需要递归进入同一套解析逻辑；（2）详情页里混合了图文、视频、关联跳转等多种内容类型，标准二级的
line/list
结构装不下；（3）图片需要解密，且解密逻辑（tlazy）需要在沙箱内重新初始化。

**判断流程**

> 详情页内部有没有指向同类详情页的链接？
>
> ├── 有 → 用 erji 沙算（递归拼 \$('').rule(parse.erji)）
>
> └── 没有
>
> 详情页是纯章节列表 + 简介？
>
> ├── 是 → 用标准二级（返回 line/list 结构）
>
> └── 否（图文混排/视频/复杂布局） → 用 erji 沙算

以海角吃瓜为例，选 erji
沙箱的原因是前两条同时成立：详情页里有相关帖子链接（/archives/xxx）需要再次进入同一解析逻辑，且内容是图文混排加视频，标准二级的
list 二维数组无法表达这种结构。

> **第八章：解析函数------播放/阅读协议**

  ----------------------------------------------------------------------------
  **内容类型**    **返回格式**                          **说明**
  --------------- ------------------------------------- ----------------------
  视频直链        return \'video://\' + url +           分号后为 Header 配置
                  \';{User-Agent@UA&&Referer@host/}\'   

  漫画/图集       return \'pics://\' + url1 + \'&&\' +  每个 URL 可追加
                  url2 + \'\...\'                       \@Referer=xxx

  嗅探兜底        return url + \'#嗅探\'                无法解析时触发 App
                                                        内置嗅探器
  ----------------------------------------------------------------------------

> ⚠️ **警告:** 解析函数（parse.解析）只认 return 协议字符串，严禁使用
> setResult()，否则漫画/小说内容无法显示！（Law 47a）⚠️
> 注意：\$().rule() 沙箱内是列表场景，必须用 setResult(d)，不能
> return；两者场景不同，请勿混淆。

> ⚠️ **反面案例——二级函数 `return` 之后的 `setResult` 是死代码（静默 Bug）：**
> ```javascript
> 二级: function() {
>     // ...构建 list...
>     return { detail1: title, img: img, list: list };
>     setResult(d);   // ← 永远不会执行！return 之后的代码是死代码
> }
> ```
> `return` 语句执行后函数立即退出，后面的 `setResult(d)` **不会执行也不会报错**，属于静默 Bug。二级函数本身就只认 `return` 返回的对象，`setResult` 写在任何位置（return 前或后）对二级都没有意义。常见原因是从 `$().rule()` 沙箱写法复制代码过来时忘记删掉。

**8.1 图片防盗链写法（两种格式均可用）**

> // ✅ 推荐新格式（支持多 header）
>
> pics.push(picUrl +
> &quot;@headers={\\&quot;Referer\\&quot;:\\&quot;https://cdn.example.com/\\&quot;,\\&quot;User-Agent\\&quot;:\\&quot;&quot; +
> parse.UA + &quot;\\&quot;}&quot;);
>
> // 简写格式（实测可靠；Referer 填图片 CDN 域名，非站点主域名）
>
> pics.push(picUrl + &quot;@Referer=https://cdn.example.com/&quot;);
>
> // nhentai 专用（t2 → i2 域名 + 去文件名末尾 t）
>
> var big = src.split(&quot; &quot;)\[0\]
>
> .replace(&quot;t2.nhentai.net&quot;, &quot;i2.nhentai.net&quot;)
>
> .replace(/(\\d+)t(\\.\[a-z\]+)\$/, &quot;\$1\$2&quot;);
>
> pics.push(big +
> &quot;@headers={\\&quot;Referer\\&quot;:\\&quot;https://nhentai.net/\\&quot;}&quot;);

**8.2 三种 lazyRule 播放模式**

**模式1：直接 video:// 协议（最稳定，优先选用）**

> // ✅ V102 验证最稳定：能直接构造协议就绝不用 lazyRule
>
> url: &quot;video://&quot; + epHref + &quot;;{User-Agent@&quot; +
> parse.UA + &quot;&&Referer@&quot; + parse.host + &quot;/}&quot;

**模式2：lazyRule 内联（需进一步解析时）**

> // ✅ 用于需要请求详情页解密的场景
>
> // 注意：lazyRule 内无法访问外部变量，所有需要的值必须通过参数传入
>
> url: \$(&quot;#noLoading#&quot;).lazyRule(function(pUrl, ua, host) {
>
> var \_html = request(pUrl, { headers: { &quot;User-Agent&quot;: ua }
> });
>
> var \_slash = String.fromCharCode(92); // ✅ 反斜杠隔离术
>
> var m =
> \_html.match(/https?:\\/\\/\[\^\\s\'&quot;\]+\\.m3u8\[\^\\s\'&quot;.\]\*/);
>
> if (m) return &quot;video://&quot; +
> m\[0\].split(\_slash).join(&quot;&quot;) + &quot;;{User-Agent@&quot; +
> ua + &quot;&&Referer@&quot; + host + &quot;/}&quot;;
>
> return pUrl + &quot;#嗅探&quot;;
>
> }, detailUrl, parse.UA, parse.host)

**模式3：MACCMS player_aaaa 双重解密（见第九章）**

**8.3 `video://页面URL` 嗅探模式 + extra 辅助参数（BBYS 实战提炼）**

当播放地址藏在网页 JS 里、规则侧无法静态提取时，可以把按钮 url 写成 `video://` + 页面地址，引擎会自动用 WebView 加载该页并嗅探音视频流。`extra` 字段是给嗅探器的钩子，能显著加快定位、屏蔽无关资源、绕过反爬。

```javascript
{
    title: cleanTitle,
    url: "video://" + playPageUrl,         // 注意：是页面 URL，不是直链
    extra: {
        videoRules: ["videoType=1", "*.m3u8"],   // 命中规则即视为目标视频
        videoExcludeRules: ["?url="],             // 排除假视频地址
        blockRules: [".png", ".jpg", "stats."],   // 屏蔽图片/统计请求加快加载
        ua: PC_UA,                                // 嗅探用 UA（部分站只对 PC 出流）
        referer: parse.host + "/",                // 嗅探用 Referer
        cacheM3u8: true,                          // 自动缓存 m3u8 索引（防过期）
        js: "fy_bridge_app.log('loaded')"         // 网页加载完执行的 JS
    }
}
```

| extra 字段 | 类型 | 作用 |
|---|---|---|
| `videoRules` | string[] | 包含匹配，`*` = 任意字符（不是正则）；命中即视为视频 |
| `videoExcludeRules` | string[] | 包含匹配，命中视为**非**视频，跳过 |
| `blockRules` | string[] | 拦截资源加快加载；支持 `!!xxx!!` 反向匹配（不包含才拦截） |
| `ua` | string | 嗅探时使用的 UA；可用 `MOBILE_UA` / `PC_UA` 常量 |
| `referer` | string | 嗅探时使用的 Referer |
| `cacheM3u8` | boolean | 自动 cacheM3u8 索引文件，防一次性链接失效 |
| `js` | string | 网页加载完执行的 JS 字符串（WebView 沙箱内） |

> 📌 **与第八章模式1（直接 video:// 直链）区别:** 模式1 是 `video://直链.m3u8;{Header}`——规则已经拿到 m3u8，引擎不嗅探。本模式（8.3）是 `video://页面URL` + extra——引擎用 WebView 加载页面后自己嗅探。**能用模式1 就不用 8.3**，嗅探有不确定性。
>
> 📌 **配合 video:// 模式1 的 cacheM3u8:** 拿到 m3u8 直链后，对一次性签名链接可用 `cacheM3u8(url, opts)` 提前缓存（见第 72.6 节），返回的 `file://...##原地址##` 作为最终 video:// 协议地址。

**8.4 视频解密章节地图（按算法/特征查询）**

视频地址解密散落在多章，按站点特征查表：

| 加密/混淆类型 | 对应章节 | 典型站点 |
|---|---|---|
| **MACCMS `player_aaaa`** 双重 unescape + base64 | 第 9 章 | 苹果 CMS V10 全家族 |
| **KVS 播放器 Hash** 时间戳 + MD5 | 第 23 章 | x1hub / 同类成人站 |
| **内嵌视频通用算法** + VidHide | 第 24 章 | streamtape / vidhide |
| **VidHide Dean Edwards Unpacker** | 第 25 章 | vidhide 进阶 |
| **Next.js 加密视频** + 分类独立页 | 第 27 章 | Next.js SSR 站 |
| **Dean Edwards Packer** 通用解包 | 第 34 章 | JS 混淆播放器 |
| **AES-CBC + 自托管解析器** | 第 44 章 | 大型聚合站 |
| **间接 m3u8 模式** 双重 Base64 + 写文件 | 第 57 章 | API 返回 base64 包裹的 base64 |
| **player_xxx 内联 JSON** + 时间戳 AES key | 第 63 章 | 综合站常见，含 encrypt 三态字段 |
| **AES-CBC 签名 Header** App 逆向 | 第 68 章 | 移动端 App 接口 |
| **页面嗅探** (video:// 页面URL + WebView) | 第 8.3 节 | 加密无法静态提取的兜底 |

> 📌 **选型流程：先看页面源码** → 找到 `var url = "..."` 直接 video:// → 没有则找 `player_aaaa` 走第 9 章 → 还没有看是 JS 混淆找第 25/34 章 → 都不行才走 8.3 嗅探。

> **第九章：MACCMS V10 播放加密穿透（Law 39）**

**9.1 加密特征识别**

> // 播放页源码特征：player_aaaa 配置块
>
> // encrypt: 0=不加密, 1=URL编码, 2=Base64后再URL编码
>
> var player_aaaa = {
>
> &quot;flag&quot;: &quot;play&quot;,
>
> &quot;encrypt&quot;: 2,
>
> &quot;url&quot;: &quot;JTY4JTc0\...&quot;,
>
> &quot;from&quot;: &quot;ddao&quot;
>
> }

**9.2 完整解密实现**

> \_parseVideo: function(playUrl) {
>
> var \_ua = this.UA;
>
> var \_host = this.host;
>
> var html = request(playUrl, { headers: { &quot;User-Agent&quot;: \_ua
> } });
>
> html = this.\_autoDeshell(html);
>
> var match = html.match(/var
> player_aaaa\\s\*=\\s\*(\\{\[\\s\\S\]\*?\\})(?=;\\s\*\<\\/script\|}\|\\\]\\\]\>)/);
>
> if (!match) { log(&quot;player_aaaa 未找到，嗅探&quot;); return
> playUrl + &quot;#嗅探&quot;; }
>
> try {
>
> var cfg = JSON.parse(match\[1\]);
>
> var encUrl = cfg.url;
>
> var realUrl = &quot;&quot;;
>
> if (cfg.encrypt == 2) realUrl = unescape(this.\_base64Decode(encUrl));
>
> else if (cfg.encrypt == 1) realUrl = unescape(encUrl);
>
> else realUrl = encUrl;
>
> var slash = String.fromCharCode(92);
>
> realUrl = realUrl.split(slash).join(&quot;&quot;);
>
> return &quot;video://&quot; + realUrl + &quot;;{User-Agent@&quot; +
> \_ua + &quot;&&Referer@&quot; + \_host + &quot;/}&quot;;
>
> } catch(e) {
>
> log(&quot;解析失败: &quot; + e);
>
> return playUrl + &quot;#嗅探&quot;;
>
> }
>
> }
>
> **第十章：图片处理------字节流优先（Law 28）**

JS 层处理 Base64 图片或大图时极易触发内存溢出（Error 36）。必须使用
FileUtil 字节流方案：

> var \_pic = pd(it, &quot;img&&data-original&quot;) \|\| pd(it,
> &quot;img&&src&quot;);
>
> if (\_pic && \_pic.indexOf(&quot;http&quot;) === 0) {
>
> \_pic = \$(\_pic).image({
>
> image: function() {
>
> var FileUtil = com.example.hikerview.utils.FileUtil;
>
> try {
>
> var raw = FileUtil.readBytes(input, {
>
> &quot;User-Agent&quot;: parse.UA,
>
> &quot;Referer&quot;: parse.host + &quot;/&quot;
>
> });
>
> return FileUtil.toInputStream(raw);
>
> } catch(e) {
>
> log(&quot;图片字节流失败: &quot; + e);
>
> return null;
>
> }
>
> }
>
> });
>
> }
>
> ⚠️ **警告:** 严禁在 JS 层用字符串处理 Base64 大图。必须走
> FileUtil.readBytes + toInputStream 字节流通道。

如需图片异或解密（如 Maomi/欢乐谷站点），在 readBytes 后、toInputStream
前加解密逻辑：

> var raw = FileUtil.readBytes(input, { &quot;User-Agent&quot;: parse.UA
> });
>
> var key = &quot;2019ysapp&quot;;
>
> for (var i = 0; i \< Math.min(raw.length, 100); i++) {
>
> var res = (raw\[i\] \^ key.charCodeAt(i % key.length)) & 0xff;
>
> raw\[i\] = res \> 127 ? res - 256 : res;
>
> }
>
> return FileUtil.toInputStream(raw);

**10.2 图片 AES 解密------\$(url).image() + CryptoUtil 方案**

当图片本身是 AES 加密的二进制数据（下载后不能直接显示）时，需要用
\$(url).image() 语法在下载完成后解密。这是和字节流防盗链完全不同的场景。

  ----------------------------------------------------------------------------------------------
  **场景**                      **方案**                  **核心 API**
  ----------------------------- ------------------------- --------------------------------------
  图片防盗链/大图，内容未加密   FileUtil.readBytes +      com.example.hikerview.utils.FileUtil
                                toInputStream             

  图片内容 AES                  \$(url).image(() =\>      hiker://assets/crypto-java.js
  加密，下载后需解密            CryptoUtil.AES.decrypt)   

  图片前 N 字节异或加密         FileUtil.readBytes +      同字节流方案
                                手动异或循环              
  ----------------------------------------------------------------------------------------------

**方案一：作为图片 lazyRule 字符串（拼接到 pic_url 末尾）**

\_getImageDecrypt() 返回一段 lazyRule 字符串，直接拼接到图片 URL
末尾，适合列表页封面批量解密。提前调用一次赋给变量，循环内复用。

> // 定义解密 lazyRule 生成函数
>
> \_getImageDecrypt: function() {
>
> return \$(&quot;&quot;).image(function() {
>
> try {
>
> var CryptoUtil =
> \$.require(&quot;hiker://assets/crypto-java.js&quot;);
>
> var key = CryptoUtil.Data.parseUTF8(&quot;your_aes_key_here&quot;);
>
> var iv = CryptoUtil.Data.parseUTF8(&quot;your_aes_iv_here&quot;);
>
> var rawData = CryptoUtil.Data.parseInputStream(input);
>
> var dec = CryptoUtil.AES.decrypt(rawData, key, {
>
> mode: &quot;AES/CBC/PKCS7Padding&quot;, iv: iv
>
> });
>
> return dec.toInputStream();
>
> } catch(e) { log(&quot;解密失败: &quot;+e); return input; }
>
> });
>
> },
>
> // ✅ 使用：提前调用一次，循环内复用（禁止在循环内每次调用）
>
> \_parseList: function(html) {
>
> var tlazy = this.\_getImageDecrypt(); // 只调用一次
>
> var items = pdfa(html, &quot;ul.list&&li&quot;);
>
> for (var i = 0; i \< items.length; i++) {
>
> var imgSrc = pd(items\[i\], &quot;img&&data-src&quot;);
>
> d.push({ pic_url: imgSrc + tlazy }); // 直接拼接
>
> }
>
> },

**方案二：在 lazyRule 内部重新定义（用于阅读页解析）**

lazyRule 内部无法访问外部的 \_getImageDecrypt()
返回值（作用域隔离），需要在 lazyRule
内部重新定义解密逻辑，再拼接到每张图片 URL 末尾。

> \_getComicReaderRule: function() {
>
> return \$(&quot;&quot;).lazyRule(function() {
>
> var host = &quot;https://example.com&quot;;
>
> // lazyRule 内部重新定义解密 lazyRule（不能从外部传入）
>
> var tlazy = \$(&quot;&quot;).image(function() {
>
> try {
>
> var CryptoUtil =
> \$.require(&quot;hiker://assets/crypto-java.js&quot;);
>
> var key = CryptoUtil.Data.parseUTF8(&quot;your_aes_key&quot;);
>
> var iv = CryptoUtil.Data.parseUTF8(&quot;your_aes_iv&quot;);
>
> var dec = CryptoUtil.AES.decrypt(
>
> CryptoUtil.Data.parseInputStream(input), key,
>
> { mode: &quot;AES/CBC/PKCS7Padding&quot;, iv: iv }
>
> );
>
> return dec.toInputStream();
>
> } catch(e) { log(&quot;解密失败: &quot;+e); return input; }
>
> });
>
> var html = request(input, { headers: { &quot;User-Agent&quot;:
> &quot;pc&quot;, &quot;Referer&quot;: host+&quot;/&quot; } });
>
> var pics = \[\];
>
> var items = pdfa(html, &quot;.reader&&img&quot;);
>
> for (var i = 0; i \< items.length; i++) {
>
> var src = pdfh(items\[i\], &quot;img&&data-src&quot;) \|\|
> pdfh(items\[i\], &quot;img&&src&quot;);
>
> if (src && src.indexOf(&quot;loading&quot;) == -1) {
>
> if (src.indexOf(&quot;http&quot;) !== 0) src = host + src;
>
> pics.push(src + tlazy); // 每张图拼上解密 lazyRule
>
> }
>
> }
>
> if (pics.length == 0) return &quot;toast://未找到图片&quot;;
>
> return &quot;pics://&quot; + pics.join(&quot;&&quot;);
>
> });
>
> },
>
> ⚠️ **警告:** \_getImageDecrypt()
> 每次调用都生成一段新字符串，严禁在循环内每次调用。必须提前 var tlazy =
> this.\_getImageDecrypt() 调用一次，循环内直接用 tlazy。
>
> 💡 **CryptoUtil 支持的加密模式:**

**10.3 CryptoUtil 完整模式参考**

所有模式均通过 `CryptoUtil.AES.decrypt(data, key, { mode, iv })` 调用，仅 `mode` 字段不同：

| 模式字符串 | 说明 | 是否需要 iv | 常见场景 |
|---|---|---|---|
| `AES/CBC/PKCS7Padding` | 最常用，有填充 | ✅ 必须 | 绝大多数加密图片站 |
| `AES/CBC/NoPadding` | 无填充，需手动对齐 | ✅ 必须 | 数据长度已对齐的场景 |
| `AES/ECB/PKCS7Padding` | 无 iv，分块加密 | ❌ 不需要 | 老站点、简单加密 |
| `AES/ECB/NoPadding` | 无 iv，无填充 | ❌ 不需要 | 少见 |
| `AES/CTR/NoPadding` | 流密码，无需块对齐 | ✅ 必须 | 图片头部跳过场景 |

**10.4 文件头跳过 + 流解密（skip 写法）**

部分站点图片加了固定字节数的混淆头部，需先跳过再解密。**必须用 `input.skip(N)` 先跳过，再 `parseInputStream` 读流**，顺序不能反：

```javascript
// ✅ 标准写法：key/iv 通过参数传入，避免闭包捕获 this 失效（Law 119）
_getImageLazy: function() {
    var key = this._imgKey;
    var iv  = this._imgIv;
    return $('').image(function(k, iv_s) {
        try {
            var CryptoUtil = $.require('hiker://assets/crypto-java.js');
            input.skip(276);                                     // 跳过头部 N 字节
            var hexData   = CryptoUtil.Data.parseInputStream(input);
            var keyBytes  = CryptoUtil.Data.parseUTF8(k);
            var ivBytes   = CryptoUtil.Data.parseUTF8(iv_s);
            var decrypted = CryptoUtil.AES.decrypt(hexData, keyBytes, {
                mode: 'AES/CTR/NoPadding',
                iv: ivBytes
            });
            return decrypted.toInputStream();
        } catch (e) {
            return input;
        }
    }, key, iv);
},
```

> ❌ **错误写法（已废弃）：**
> `parseInputStream(input)` 读全部 → `copyOfRange(bytes, N, bytes.length)` 切片。
> 逻辑等价但产生冗余内存拷贝，统一改用 `skip` 写法。

> 📌 **跳过字节数 N 的确认方法：** 抓包下载原始图片二进制，用十六进制查看器找到 JPEG/PNG/WEBP 文件头的起始偏移量即为 N。常见值：`276`、`512`、`1024`。

> 💡 key/iv 通常在站点 JS 源码中搜索 `key`/`iv`/`secret` 关键词找到。ECB 模式不需要 iv，`$.image()` 回调参数只传 key 即可。

## 10.5 $.image() 回调完整用法速查（R星精选实战提炼）

`$.image(func, pic)` 的回调 `func` 可返回三种类型，适用于不同解密场景：

| 返回类型 | 适用场景 | 说明 |
|---|---|---|
| 新 URL 字符串 | 防盗链替换、协议升级 | 引擎用新 URL 重新请求图片 |
| `InputStream` | 解密后直接渲染 | 引擎不再发网络请求，直接渲染流 |
| `null` | 加载失败兜底 | 显示占位图 |

> ⚠️ **关键区分（V3.66 二修）：** `$.image()` 与 `lazyRule()` 同理，**有两种模式**：① 独立形式 `$(url).image(fn)` 放在 `img` 字段，`input` 是引擎下载完成的 `InputStream`（本节 5 个用法均属此类）；② 拼接形式 `imgUrl + '@Referer=...' + $('').image(fn)` 整段作为 `pic_url`，`input` 是拼在前面的字符串前缀（带 Cookie 防盗链场景，详见 26 章和 41.4 节）。两种模式互斥，看代码结构选其一。`$.image()` 放在 `url` 字段无效。

**用法一：HTML 页面提取真实 URL**

```javascript
$(pic).image(function(url) {
    if (url.indexOf('.html') !== -1) {
        var html = fetch(url);
        return pdfa(html, 'img&&src')[0];
    }
    return url;
}, pic);
```

**用法二：HTML 页面 Base64 解码后返回 InputStream**

```javascript
$(pic).image(function(url) {
    if (url.indexOf('.html') !== -1) {
        var htmlContent = fetch(url);
        htmlContent = htmlContent.replace(/[\s\n\r]+/g, '');
        var CryptoUtil = $.require("hiker://assets/crypto-java.js");
        var decoded = CryptoUtil.Data.parseBase64(htmlContent, _base64.NO_WRAP);
        return decoded.toInputStream();  // 返回流，引擎直接渲染
    }
    return url;
}, pic);
```

**用法三：XOR 字节解密（只解密文件头，R星精选完整案例）**

```javascript
$(pic).image(function(url) {
    var javaImport = new JavaImporter();
    javaImport.importPackage(Packages.com.example.hikerview.utils);
    with(javaImport) {
        var bytes = FileUtil.toBytes(input);          // input = 已下载的图片字节流
        var key = [50, 48, 49, 57, 121, 115, 97, 112, 112, 55, 53, 50, 55];
        var limit = Math.min(100, bytes.length);      // 只解密前100字节（文件头）
        for (var i = 0; i < limit; i++) {
            var b = bytes[i];
            if (b < 0) b = b + 256;                  // Java byte 有符号转无符号
            b = b ^ key[i % key.length];             // XOR 解密
            if (b > 127) b = b - 256;                // 无符号转回有符号
            bytes[i] = b;
        }
        return FileUtil.toInputStream(bytes);         // 返回解密后的流
    }
}, pic);
```

> 📌 只解密前 N 字节（文件头）是常见优化：图片格式标识（JPEG/PNG 魔数）只在头部，加密方通常也只混淆头部，解密头部后引擎即可正常识别并渲染。

**用法四：AES-ECB 图片解密（含 base64 数据 URI 提取）**

部分站点图片以 `data:image/...;base64,` 形式嵌在 AES-ECB 加密结果里：

```javascript
$(src).image(function(k) {
    var CryptoUtil = $.require('hiker://assets/crypto-java.js');
    var key      = CryptoUtil.Data.parseUTF8(k);
    // input 是下载后的图片字节流，先 parseInputStream 再 base64Decode 得到加密数据
    var textData = CryptoUtil.Data.parseInputStream(input).base64Decode();
    var decrypted = CryptoUtil.AES.decrypt(textData, key, {
        mode: 'AES/ECB/NoPadding'  // ECB 模式不需要 iv
    });
    // 解密结果是 "data:image/jpeg;base64,/9j/..." 格式，取 base64 部分再转流
    var base64Text = decrypted.toString().split('base64,')[1];
    return CryptoUtil.Data.parseBase64(base64Text, _base64.NO_WRAP).toInputStream();
}, key)
```

**用法五：AES-CBC 加密 POST 请求体 + encData 响应解密**

当接口请求体和响应体都需要 AES-CBC 加解密时，使用 `CryptoUtil` 而非 `eval(getCryptoJS())`——后者在部分聚阅版本里会报 `CryptoJS 未定义`：

```javascript
_post: function(apiUrl, plainData) {
    var CryptoUtil = $.require('hiker://assets/crypto-java.js');
    var key = CryptoUtil.Data.parseUTF8('32字节密钥字符串xxx');   // 从站点 JS 提取
    var iv  = CryptoUtil.Data.parseUTF8('16字节IV字符串xxxx');

    // ── 加密请求体 ────────────────────────────────────────────
    var endata = CryptoUtil.AES.encrypt(
        CryptoUtil.Data.parseUTF8(typeof plainData === 'string' ? plainData : JSON.stringify(plainData)),
        key,
        { mode: 'AES/CBC/PKCS7Padding', iv: iv }
    ).toBase64();

    var body = JSON.stringify({ endata: endata, ents: '时间戳或其他签名字段' });

    // ── 发送请求 ──────────────────────────────────────────────
    var raw = fetch(apiUrl, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + juItem.get('token', '') },
        body: body
    });
    var obj = JSON.parse(raw);

    // ── 解密响应（部分接口返回加密的 encData 字段）────────────
    if (obj.encData) {
        var dec = CryptoUtil.AES.decrypt(
            CryptoUtil.Data.parseBase64(obj.encData),
            key,
            { mode: 'AES/CBC/PKCS7Padding', iv: iv }
        ).toText();
        obj = JSON.parse(dec);
    }
    return obj;
},
```

**`CryptoUtil` 可靠 API 速查（优先于 `eval(getCryptoJS())`）：**

| API | 说明 |
|---|---|
| `CryptoUtil.Data.parseUTF8(str)` | 字符串 → 字节数组 |
| `CryptoUtil.Data.parseBase64(str)` | Base64 字符串 → 字节数组 |
| `CryptoUtil.Data.parseInputStream(input)` | InputStream → 字节操作对象 |
| `.base64Decode()` | 字节对象 Base64 解码 |
| `.toBase64()` | 字节对象 → Base64 字符串 |
| `.toText()` | 字节对象 → UTF-8 字符串 |
| `.toInputStream()` | 字节对象 → InputStream（用于 `$.image()` 返回）|
| `CryptoUtil.AES.encrypt(data, key, opts)` | AES 加密 |
| `CryptoUtil.AES.decrypt(data, key, opts)` | AES 解密 |

> ⚠️ `eval(getCryptoJS())` 在部分聚阅旧版本里不生效（`CryptoJS` 对象注入失败），遇到此问题时改用 `CryptoUtil` 方案。两套 API 功能等价但接口不同，不可混用。

---

**10.6 多模态站点：搜索/列表混合渲染 + 双协议详情（Uu 视频实战提炼）**

针对一个站点同时承载多种内容形态（普通视频 / 图文社区 / 视频+图文混合）的场景，传统"所有卡片走二级"的写法把不同形态硬塞进同一模板，体验割裂。下面给出经实战验证的混合模式：**同一组 `setResult` 内不同 item 用不同 `col_type` + 不同 `url` 协议**，把详情决策前置到列表层。

**10.6.1 列表内 col_type / url 协议混排**

```javascript
搜索: function(name, page) {
    var d = [];
    var host = this._getHost();
    var data = this._fetchAPI(host + "/api/search?keyword=" + encodeURIComponent(name));
    var items = (data && data.data) || [];

    for (var i = 0; i < items.length; i++) {
        var it = items[i];
        var vid = it.id;
        var isImageType = this._isImagePost(it);   // 形态判定（label/classify/标志位）

        d.push({
            title: (isImageType ? "[图文] " : "") + it.title,
            pic_url: this._picEnc(it.cover),
            desc: it.label || "",
            url: isImageType
                ? this._imgSandboxUrl(host, vid)   // 图文 → .rule() 沙箱页
                : this._playLazyUrl(host, vid),    // 视频 → .lazyRule() 直接 video://
            col_type: isImageType ? "pic_1_card" : "movie_2"
        });
    }
    return d;
}
```

**关键点**：

| 维度 | 选择 | 适用形态 |
|---|---|---|
| `col_type: "pic_1_card"` | 单行大封面卡，title + desc 完整展示 | 图文社区、专题、合集 |
| `col_type: "movie_2"` | 一行 2 个标准影视卡 | 普通视频 |
| `url: $().lazyRule(fn)` | fn 内拉详情接口、解密、拼 `video://`，引擎直接拉起播放器 | 点击即播，无需二级 |
| `url: $('hiker://empty?theme=movie_1').rule(fn)` | fn 内 `setResult` 渲染独立沙箱页 | 自定义图文/混合详情 |

> 📌 **混排零代价**：聚阅/海阔的 `setResult` 对同一 `d` 数组里的混合 `col_type` 完整支持，不需要额外分组。视觉上会按 push 顺序铺排，相邻 item 的 col_type 不同也能正常显示。

**10.6.2 lazyRule vs rule 二分速查（v3.66 三修）**

手册 §1761 已说明 "`$().rule()` 沙箱必须用 `setResult`、严禁 `return`"，§8.2 模式2 给出 lazyRule 用法，二者对照如下：

| 维度 | `.lazyRule(fn)` | `.rule(fn)` |
|---|---|---|
| 用途 | 返回最终 url 字符串 | 进入独立沙箱页面 |
| 函数体内出口 | **必须 `return`** url（`video://...` / `pics://...` / `toast://...` / `hiker://...`） | **必须 `setResult(d)`**，严禁 return（return 后 setResult 不执行） |
| 引擎行为 | 用返回值替换 url，触发跳转/播放 | 跳到 `hiker://empty?theme=xxx` 沙箱页，执行 fn 渲染列表 |
| 闭包隔离 | 沙盒，不可引用外层 `this`/`parse` | 同左 |
| 典型场景 | 点击即播、动态嗅探、加密 m3u8 拼装 | 自定义详情页、图集页、跨接口聚合页 |

> 📌 **口诀**：**跳转/播放走 lazyRule（return url），自定义详情页走 rule（setResult）**。混用是 V3 阶段最高频的 "点击没反应" 根因——`.lazyRule` 内调用 `setResult` 引擎当作普通脚本运行后看返回值,`undefined` → 卡死无反馈。

**10.6.3 沙箱内整套解密自包含模板**

`.rule()` / `.lazyRule()` 沙盒内不可访问外层 `parse.*`，所有依赖必须通过参数注入或就地内联。当详情页要做完整的 "fetch + 信封解密 + 字段提取" 时，把解密原语就地复刻：

```javascript
_playLazyUrl: function(host, videoId) {
    return $("hiker://empty").lazyRule(function(h, ua, vid, PLAY_HOST) {
        var BASE64 = android.util.Base64;
        function caesar(s) { /* ... */ }
        function b64dec(s) { /* ... */ }
        function b64enc(b) { return BASE64.encodeToString(b, BASE64.NO_WRAP); }
        function pbkdf2(p, salt) { /* javax.crypto.SecretKeyFactory */ }
        function gcm(b64, keyB64) { /* javax.crypto.Cipher AES/GCM */ }
        function envelope(d, pwd) { /* caesar → b64 → split[salt|iv|ct] → pbkdf2 → gcm → JSON */ }
        function fetchAPI(url) {
            var ck = juItem.get("xx_ck", "") || "#noCookie#";
            var resp = fetch(url, { headers: {
                "User-Agent": ua, "Referer": h + "/", "Cookie": ck
            }});
            var obj = JSON.parse(resp);
            return envelope(obj.data, obj.key || "test");
        }

        var data = fetchAPI(h + "/api/detail?id=" + vid);
        if (!data) return "toast://加载失败";
        return "video://" + data.play_url + ";{User-Agent@" + ua + "&&Referer@" + h + "/}";
    }, host, this.UA, String(videoId), this.PLAY_HOST);
}
```

> 📌 **参数注入规则**：lazyRule/rule 第二个参数起的位置参数会按顺序映射给 fn 形参。**所有外层依赖（host、UA、KEY、PLAY_HOST 等）必须显式传入**。`this` 在沙盒内不可用，直接复刻 `parse._xxx` 函数体到沙盒里。

> 📌 **持久化共享**：沙盒 fn 内仍可访问 `juItem.get/set`，过盾 Cookie、动态域名等"全规则共享状态"通过 juItem 流通。**不要**在沙盒里重新过盾 / 重新探活——外层主流程已做过。

**10.6.4 JS Challenge 过盾后的 Cookie 抓取三选一**

WebView 过盾后 Cookie 被写入 Android 的 CookieManager，但 `fetch` 后续请求未必能自动带上（取决于聚阅版本）。三种抓取方式按可靠度排：

| # | 方式 | 优势 | 风险 |
|---|---|---|---|
| 1 | `android.webkit.CookieManager.getInstance().getCookie(host)` | 直读 WebView 持久化区，过盾后立即可用，返回 `"name1=v1; name2=v2"` 标准 Cookie 头格式 | 部分 Android 12+ 严格模式可能 SecurityException |
| 2 | `fetchCookie(url)` | 海阔内置 API，纯 JS 调用 | 返回 JSON 数组字符串，**空响应时 raw 为空字符串**，`JSON.parse("")` 抛 SyntaxError。**必须校验** `raw.length > 2 && raw.charAt(0) === '['` |
| 3 | `fetch(url, {withHeaders: true})` 取 `res.headers["Set-Cookie"][0]` | 兼容性最广 | 拿到的是单次响应 Set-Cookie，不含已存在 Cookie；需自己拼接 |

实战写法：

```javascript
_ensureSession: function() {
    var ck = juItem.get("xx_ck", "");
    if (ck && (Date.now() - parseInt(juItem.get("xx_ck_ts","0"))) < 25*60*1000) return ck;

    fetchCodeByWebView(host + "/", {
        timeout: 15000, ua: this.UA,
        blockRules: [".png", ".jpg", "analytics"],
        checkJs: "document.cookie.indexOf('xx_token')>=0"
    });

    try {                                          // 方式 1 优先
        var s = android.webkit.CookieManager.getInstance().getCookie(host);
        if (s) ck = String(s);
    } catch (e) {}

    if (!ck) try {                                 // 方式 2 兜底
        var raw = fetchCookie(host + "/");
        if (raw && raw.trim().length > 2 && raw.charAt(0) === '[') {
            var arr = JSON.parse(raw);
            ck = arr.map(function(c){return c.split(";")[0].trim();}).join("; ");
        }
    } catch (e) {}

    if (ck) {
        juItem.set("xx_ck", ck);
        juItem.set("xx_ck_ts", String(Date.now()));
    }
    return ck;
}
```

**10.6.5 AES-256-GCM 反射写法（CryptoUtil 不支持 GCM 时的兜底）**

§10.3 的 CryptoUtil 模式列表只覆盖 CBC/ECB/CFB/CTR/OFB，**不含 GCM**。当 API 用 AES-GCM 时（如 PBKDF2 派生 key + 12B iv + 16B tag 的标准信封），走 `javax.crypto` 反射：

```javascript
_pbkdf2DeriveKey: function(password, salt, iter) {
    var SKF = javax.crypto.SecretKeyFactory.getInstance("PBKDF2WithHmacSHA256");
    var arr = java.lang.reflect.Array.newInstance(java.lang.Character.TYPE, password.length);
    for (var i = 0; i < password.length; i++) arr[i] = password.charAt(i);
    var spec = new javax.crypto.spec.PBEKeySpec(arr, salt, iter || 1000, 256);
    return SKF.generateSecret(spec).getEncoded();
},

_decryptGCM: function(b64, keyB64) {
    var Base64 = android.util.Base64;
    var keyBytes = Base64.decode(keyB64, Base64.DEFAULT);
    var raw = Base64.decode(b64, Base64.DEFAULT);
    var iv = java.util.Arrays.copyOfRange(raw, 0, 12);
    var ct = java.util.Arrays.copyOfRange(raw, 12, raw.length);
    var cipher = javax.crypto.Cipher.getInstance("AES/GCM/NoPadding");
    cipher.init(javax.crypto.Cipher.DECRYPT_MODE,
        new javax.crypto.spec.SecretKeySpec(keyBytes, "AES"),
        new javax.crypto.spec.GCMParameterSpec(128, iv));   // tag 长度 128 bit
    return new java.lang.String(cipher.doFinal(ct), "UTF-8") + "";
}
```

> 📌 **关键坑**：
> - `PBEKeySpec` 第一参数必须是 `char[]`，**不能直接传 JS 字符串**。用 `java.lang.reflect.Array.newInstance(java.lang.Character.TYPE, n)` 显式构造。
> - GCM 的 IV/tag 长度有约定：常见 `iv=12B` + `tag=128bit`，与 CBC 的 `iv=16B` 不同。
> - 解密后 `new java.lang.String(bytes, "UTF-8") + ""` 末尾的 `+ ""` 是为了把 Java String 强转 JS string，否则后续 `JSON.parse` 可能失败。

**10.6.6 域名自愈三段式（发布页 → ping → 缓存）**

老站经常切域名，硬编码 `host` 在 host 失效后规则直接死。三段式自愈：

```javascript
_resolveHosts: function() {
    var html = fetch(this.PUBLISH_PAGE, { headers: {"User-Agent": this.UA}, timeout: 8000 });
    var re = /<div[^>]*class="btnLink"[^>]*>\s*(https?:\/\/[^\s<]+?)\s*<\/div>/g;
    var hosts = [], m;
    while ((m = re.exec(html)) !== null) hosts.push(m[1].trim().replace(/\/+$/, ""));
    return hosts;
},
_pingHost: function(h) {
    try {
        var r = fetch(h + "/api/index/notice", { timeout: 5000 });
        return r && r.length > 10;          // 长度兜底比 status code 跨版本更稳
    } catch (e) { return false; }
},
_getHost: function() {
    var cached = juItem.get("xx_host", "");
    var ts = parseInt(juItem.get("xx_host_ts", "0"));
    if (cached && (Date.now() - ts) < 6*3600*1000) return cached;       // 6h 缓存
    if (cached && this._pingHost(cached)) { /* 续期 */ return cached; }

    var all = this._resolveHosts().concat(this.__hosts);                // 发布页优先,本地兜底
    for (var i = 0; i < all.length; i++) {
        if (this._pingHost(all[i])) {
            juItem.set("xx_host", all[i]);
            juItem.set("xx_host_ts", String(Date.now()));
            juItem.set("xx_ck", "");                                    // ⚠ host 切换必清旧 Cookie
            return all[i];
        }
    }
    return cached || this.__hosts[0];
}
```

> 📌 **切换 host 必清 Cookie**：JS challenge / Cookie 与 host+IP 绑定，host 一换旧 Cookie 立刻失效（403）。`_getHost` 切换分支里必须同步 `juItem.set("xx_ck", "")`。

**10.6.7 `.enc` 图片：AES-CBC + IV 头切片解密**

§10.2 用法三 / 用法四覆盖了 XOR 与 AES-ECB，但更常见的是 **IV 嵌在密文头 16 字节 + AES-CBC/PKCS7** 的形态（uu/海角/部分漫画站）：

```javascript
_picEnc: function(url) {
    if (!url || url.indexOf(".enc") < 0) return url;
    return $(url).image(function(IMG_KEY) {
        var FileUtil = Packages.com.example.hikerview.utils.FileUtil;
        var bytes = FileUtil.toBytes(input);
        if (!bytes || bytes.length < 16) return input;
        var iv = java.util.Arrays.copyOfRange(bytes, 0, 16);
        var ct = java.util.Arrays.copyOfRange(bytes, 16, bytes.length);
        var keyBytes = new java.lang.String(IMG_KEY).getBytes("UTF-8");
        var c = javax.crypto.Cipher.getInstance("AES/CBC/PKCS7Padding");
        c.init(javax.crypto.Cipher.DECRYPT_MODE,
            new javax.crypto.spec.SecretKeySpec(keyBytes, "AES"),
            new javax.crypto.spec.IvParameterSpec(iv));
        return FileUtil.toInputStream(c.doFinal(ct));
    }, this.IMG_KEY);
}
```

要点：
- 函数体内 `input` 是引擎已下载的 `InputStream`（手册 §10.2 已说明）
- KEY 通过 `$(url).image(fn, KEY)` 第二参数注入，对应 fn 第一形参
- 切片用 `java.util.Arrays.copyOfRange`，不要用 JS slice（bytes 是 Java byte[]）
- 返回 `FileUtil.toInputStream(plainBytes)` 让引擎接管渲染

---

> **第十一章：JS 引擎报错速查与修复**

  ----------------------------------------------------------------------------
  **报错码**   **错误原因**             **解决方案**
  ------------ ------------------------ --------------------------------------
  Error 14     变量未定义 / 用了        直接
               let/const / 作用域泄漏   video://，或将变量显式传参；全量改用
                                        var

  Error 31     语法错误（含正则拼接）   用 new RegExp() 动态创建正则

  Error 35     lazyRule 内嵌套复杂对象  简化 lazyRule 内代码

  Error 36     字符串过长/内存溢出      图片走字节流；弃用长字符串拼接
                                        lazyRule

  Error 38     代码未闭合               检查字符串中的换行符，用 \\n 转义

  Error 43     非法反斜杠               用 String.fromCharCode(92)，禁止直接写
                                        \\
  ----------------------------------------------------------------------------

**11.1 高频陷阱对照表**

  --------------------------------------------------------------------------------
  **问题现象**        **原因**                     **解决方案**
  ------------------- ---------------------------- -------------------------------
  MY_PAGE 未定义      type:0 / type:1 不注入       改用 type:&quot;主页&quot;
                      MY_PAGE                      （url 无需含 fypage；运行期 undefined 不是词法报错）

  视频点击跳网页      fetch 返回空，#嗅探 被触发   加 log 调试，检查 html.length

  scroll_button       部分机型 HTML 渲染不一致     用纯文本符号 ✦ title ✦ 标记选中
  选中状态不稳定                                   

  getMyVar key 报错   key 含特殊字符 ://           改用纯字母前缀如
                                                   sitename_cindex0

  漫画图片空白        图片懒加载，src 是占位符     提取 data-original / data-src

  双域名拼接          putMyVar 存了完整 URL        存前
                                                   .replace(host,&quot;&quot;)
                                                   去域名

  返回列表停在末页    页码未重置                   返回按钮同时 putMyVar
                                                   page=&quot;1&quot;

  CF Cookie           用了 putMyVar 存 Cookie      改用 juItem.set 持久化
  每次重新验证                                     

  lazyRule 内变量     闭包隔离，无法访问外部变量   显式传参或硬编码到 lazyRule 内
  undefined                                        

  this.UA 在          this 指向变了                提前 var \_ua = this.UA; 再传参
  rule/lazyRule                                    
  内报错                                           
  --------------------------------------------------------------------------------

**11.2 安全编码规范**

**变量命名避锋（Law 23）**

> // ❌ 以 t/v/g 开头可能触发正则标志
>
> var title = &quot;\...&quot;; var videoUrl = &quot;\...&quot;;
>
> // ✅ 以下划线开头
>
> var \_title = &quot;\...&quot;; var \_videoUrl = &quot;\...&quot;;

**反斜杠隔离术（Law 30）**

> // ❌ 直接写反斜杠会触发转义 Bug
>
> var cleanUrl = realUrl.replace(/\\\\/g, &quot;&quot;);
>
> // ✅ 动态生成 ASCII 92
>
> var slash = String.fromCharCode(92);
>
> var cleanUrl = realUrl.split(slash).join(&quot;&quot;);

**ES5 全量降级（Law 34）**

> // ❌ 禁用
>
> let a = 1; const b = 2; c?.d; arr.map(x =\> x); \`\${a}\`;
>
> // ✅ 只能用
>
> var a = 1; var b = 2; if(c && c.d){} arr.map(function(x){return x;});
> a+&quot;&quot;;

**this 指向保护**

> // ❌ 错误：在 \$().rule 或 lazyRule 中直接用 this
>
> url: \$().rule(function(targetUrl) {
>
> var html = fetch(targetUrl, { headers: { &quot;User-Agent&quot;:
> this.UA } }); // this 指向错误！
>
> }, url)
>
> // ✅ 正确：提前取出，通过参数传入
>
> var \_ua = this.UA;
>
> var \_host = this.host;
>
> url: \$().rule(function(targetUrl, ua, host) {
>
> var html = fetch(targetUrl, { headers: { &quot;User-Agent&quot;: ua }
> });
>
> }, url, \_ua, \_host)
>
> **第十二章：Cloudflare 过盾标准方案**

CF 过盾有两个关键细节，错一个就失效：①检测条件要用页面内容判断（不用
html.length）；②过盾成功条件必须同时满足&quot;页面有真实内容&quot;和&quot;Cookie
有 cf_clearance&quot;------只有 Cookie
不等页面加载完，返回太早下次请求还是被 CF 拦截。

**12.1 完整标准模板（实战验证版）**

> // ── CF 过盾标准模板 ──────────────────────────────────────
>
> // UA 必须用 Android Chrome，和系统 WebView 默认 UA 一致
>
> // cf_clearance Cookie 与验证时的 UA+IP 绑定，UA 不匹配则 Cookie 无效
>
> UA: &quot;Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36
> (KHTML, like Gecko) Chrome/124.0.0.0 Mobile Safari/537.36&quot;,
>
> \_fetch: function(url) {
>
> var d = \[\];
>
> var \_host = this.host;
>
> var \_ua = this.UA;
>
> // Step1: putVar(临时中转) → juItem.set(持久化) 两步流程
>
> var tempCk = getVar(&quot;site_ck&quot;, &quot;&quot;);
>
> if (tempCk !== &quot;&quot;) {
>
> juItem.set(&quot;site_cookie&quot;, tempCk);
>
> clearVar(&quot;site_ck&quot;);
>
> }
>
> var \_cookie = juItem.get(&quot;site_cookie&quot;, &quot;&quot;);
>
> var html = fetch(url, {
>
> headers: { &quot;User-Agent&quot;: \_ua, &quot;Referer&quot;:
> \_host+&quot;/&quot;, &quot;Cookie&quot;: \_cookie }
>
> });
>
> // ✅ 检测条件：用页面内容判断，不用 html.length（长度不稳定）
>
> // 把 &quot;列表元素的 class&quot; 替换为实际站点的视频卡片 class
>
> if (html.indexOf(&quot;视频卡片class&quot;) === -1) {
>
> d.push({
>
> title: &quot;🛡️ CF 验证 -\> 点击过盾&quot;,
>
> col_type: &quot;text_center_1&quot;,
>
> url: \$().rule(function(targetUrl, ua) {
>
> var res = \[{
>
> title: &quot;验证沙箱（过盾后自动返回）&quot;,
>
> url: targetUrl,
>
> col_type: &quot;x5_webview_single&quot;,
>
> desc: &quot;float&&100%&quot;,
>
> extra: {
>
> ua: ua,
>
> js: \$.toString(function() {
>
> function check() {
>
> var c = fba.getCookie(location.href);
>
> // ✅ 核心：必须同时满足两个条件才返回
>
> // 1. 页面已加载出真实内容（视频列表节点存在）
>
> // 2. Cookie 里有 cf_clearance
>
> // 只检查 Cookie 会返回太早，下次请求还被拦截
>
> var nodes = document.querySelectorAll(&quot;.视频卡片class&quot;);
>
> if (nodes && nodes.length \> 0 &&
>
> c && c.indexOf(&quot;cf_clearance&quot;) \> -1) {
>
> fba.putVar(&quot;site_ck&quot;, c); // putVar 临时中转
>
> fba.toast(&quot;✅ 过盾成功！&quot;);
>
> fba.back();
>
> } else {
>
> setTimeout(check, 1000);
>
> }
>
> }
>
> check();
>
> })
>
> }
>
> }\];
>
> setResult(res);
>
> }, url, \_ua) // ✅ 传 \_ua 字符串，不传 this
>
> });
>
> return d;
>
> }
>
> return this.\_parseList(html);
>
> },

**12.2 四个关键点**

  -------------------------------------------------------------------------------------------------------------
  **关键点**      **错误做法**       **正确做法**                          **原因**
  --------------- ------------------ ------------------------------------- ------------------------------------
  UA              用 iPhone/PC UA    用 Android Chrome UA                  cf_clearance 与验证时的 UA
                                                                           绑定，不匹配则 Cookie 无效

  CF 检测条件     html.length \<     html.indexOf(&quot;视频class&quot;)   页面长度不稳定，内容判断更准确
                  1500               === -1                                

  过盾成功条件    只检查             节点存在 AND cf_clearance 存在        只有 Cookie
                  cf_clearance 存在                                        会返回太早，页面未渲染完下次还被拦

  Cookie 持久化   putVar 直接存      putVar 临时中转 → 主页函数 juItem.set    putVar 是会话级，App 重启后丢失
                                     持久化                                
  -------------------------------------------------------------------------------------------------------------

**12.3 this.UA 在 \$().rule 内无效**

\$().rule 内部是独立作用域，this 指向不是 parse 对象。UA 必须在调用
\$().rule 之前取出赋给局部变量，通过参数传入。

> // ❌ 错误：\$().rule 内 this.UA 是 undefined
>
> url: \$().rule(function(targetUrl) {
>
> var res = \[{ extra: { ua: this.UA } }\]; // this.UA = undefined
>
> }, url)
>
> // ✅ 正确：提前取出，通过参数传入
>
> var \_ua = this.UA; // 提前取出
>
> url: \$().rule(function(targetUrl, ua) {
>
> var res = \[{ extra: { ua: ua } }\]; // ✅ 正常
>
> }, url, \_ua) // 通过参数传入
>
> **第十三章：域名自愈系统（Law 14）**
>
> 预处理: function() {
>
> var \_host = juItem.get(&quot;current_host&quot;, this.host);
>
> var \_test = fetch(\_host + &quot;/&quot;, { headers: {
> &quot;User-Agent&quot;: this.UA }, timeout: 5000 });
>
> if (!\_test \|\| \_test.indexOf(&quot;特征关键词&quot;) === -1) {
>
> log(&quot;域名失效，重置为默认&quot;);
>
> juItem.set(&quot;current_host&quot;, this.host);
>
> } else {
>
> juItem.set(&quot;current_host&quot;, \_host);
>
> }
>
> },
>
> // 所有函数中统一获取（不要硬编码 this.host）
>
> var \_host = juItem.get(&quot;current_host&quot;, this.host);

**13.1 域名自愈进阶方案索引**

本章给出最基础的"预处理探测 + juItem 持久化"模板。生产规则中常需更强健的方案，按场景查表：

| 进阶方案 | 章节 | 适用场景 |
|---|---|---|
| 多域名并发测速（`batchFetch` + 特征关键词） | 第 45.8 节 | 站点有多个备用域名，要选最快的 |
| 远程调用清洗对照表（含域名自愈行） | 第 58.2 节 | 旧规则用了壳子 `getFastestDomain()`，要换成内联实现 |
| 双重保障（发布页探测 + 备用域名 batchFetch） | 第 59.1 节 | 域名更换频率高、有专门的"发布页"公告新域名的站点 |

> 📌 选择思路：单一固定域名 → 13 章基础；多备用域名 → 45.8；有发布页 → 59.1。三者**不是替代关系**，是按站点复杂度递进。

> **第十四章：聚阅专属规则**

**14.1 变量注入规律（核心必读）**

> ⚠️ **MY_PAGE 真实注入条件:** `type:"主页"` 即注入 MY_PAGE（需在 parse 顶层声明 `页码:{ "主页": 1 }`）。url 是否含 `fypage` **不影响** MY_PAGE 注入——它只影响引擎是否替换 URL 中的页码占位符。第六章矩阵方案 `url:"0"` 即可证明这一点。
>
> ⚠️ **type:0 / type:1 实际后果:** MY_PAGE 不注入，但**不是词法分析阶段报错**——是运行期访问 MY_PAGE 时为 `undefined`。后续表达式（如 `MY_PAGE * 20`、`encodeURIComponent(MY_PAGE)`）才会异常。统一用 `type:"主页"` 避坑。
>
> // ✅ 三种合法写法（任选其一）
>
> // 写法 A：url 含 fypage（引擎帮你做翻页 URL 替换）
>
> 静态分类: {
>
> &quot;type&quot;: &quot;主页&quot;,
>
> &quot;url&quot;: &quot;fypage&quot;,
>
> &quot;class_name&quot;: &quot;全部&分类1&分类2&quot;,
>
> &quot;class_url&quot;: &quot;0&1&2&quot;
>
> },
>
> 页码: { &quot;主页&quot;: 1 },
>
> 主页: function() {
>
> var \_url = this.host + &quot;/?page=&quot; + MY_PAGE; // MY_PAGE 直接用
>
> }
>
> // 写法 B：BBYS 风格——url 含 fyclass + fypage，主页函数 split MY_URL
>
> 静态分类: { type: &quot;主页&quot;, url: &quot;hiker://empty##fyclass##fypage&quot;, ... }
>
> 主页: function() {
>
> var parts = MY_URL.split(&quot;##&quot;);
>
> var \_url = this.host + &quot;/api?type=&quot; + parts[1] + &quot;&page=&quot; + parts[2];
>
> }
>
> // 写法 C：矩阵方案 url:"0"（第六章），MY_PAGE 仍然可用
>
> 静态分类: { type: &quot;主页&quot;, url: &quot;0&quot;, class_url: &quot;0&quot; }

**14.2 解析函数协议——聚阅特有补充**

> 📌 **三种核心协议（`video://` / `pics://` / `#嗅探` 兜底）见第八章 8.0 协议对照表**，本节只补充聚阅独有的"小说章节 URL 后缀"。

**聚阅小说章节 URL 必须的标签组合：**

```javascript
url: chapterUrl + "#isJiexi=1#readTheme##autoPage#"
// #isJiexi=1#  → 触发聚阅内置阅读器（海阔无此标签，仅聚阅识别）
// #readTheme#  → 阅读模式（点击/音量翻页）
// #autoPage#   → 滚到底自动加载下一章
```

> ⚠️ **警告:** 解析函数（parse.解析）只认 return，严禁 setResult()（Law
> 47a）。lazyRule 中也一样，lazyRule 返回的字符串就是解析结果。⚠️
> 区分：\$().rule() 列表沙箱内必须用 setResult(d)（Law 47b），不能
> return。

**14.3 putMyVar 存路径而非完整 URL（Law 45）**

> // ❌ 错误：存完整 URL，之后拼接时出现双域名
>
> var \_bHref = pd(btn, &quot;a&&href&quot;, host);
>
> putMyVar(vk, \_bHref);
>
> var \_detailUrl = host + \_selItem; // 变成
> https://host/https://host/path
>
> // ✅ 正确：存入前去掉域名
>
> var \_bPath = \_bHref.replace(host, &quot;&quot;);
>
> putMyVar(vk, \_bPath);
>
> var \_detailUrl = host + \_selItem; // 正确

**14.4 返回列表同时重置页码（Law 46）**

> // 返回按钮：清除选中项，同时重置页码为 1，防止停留在末页
>
> url: \$(&quot;#noLoading#&quot;).lazyRule(function(vk, h) {
>
> putMyVar(vk, &quot;&quot;);
>
> putMyVar(h + &quot;page&quot;, &quot;1&quot;);
>
> refreshPage(false);
>
> return &quot;hiker://empty&quot;;
>
> }, \_varKey, host)
>
> **第十五章：开发铁律速查全表**

  ----------------------------------------------------------------------------------------------------
  **编号**   **铁律名称**       **核心要义**                       **关键口诀**
  ---------- ------------------ ---------------------------------- -----------------------------------
  Law 1      入口固定化         type:&quot;主页&quot;              禁用 type:0/1；url 写法见 2.1（fypage/##fyclass##fypage/0
                                （MY_PAGE 即注入）                三选一）

  Law 2      作用域隔离         lazyRule 内通过参数传值，禁用 this 显式传参：fn(a,b,c), val1, val2,
                                                                   val3

  Law 3      静默嗅探           返回直链用 video:// 协议           video://直链;{Referer@host/}

  Law 4      防盗链补全         资源必须挂载 Referer               Referer 漏了就 403

  Law 6      模糊选择器         类名随机变化时用                   \[class\*=&quot;item&quot;\]
                                \[class\*=&quot;&quot;\]           比固定类名稳健

  Law 8      Header 零空格      ; 和 && 前后无空格                 &quot;video://url;{UA@val}&quot;
                                                                   有空格就崩

  Law 11     CF 破盾            X5 沙箱 + fba.getCookie            过盾后必须 juItem.set 持久化

  Law 14     域名自愈           预处理探测域名，juItem.set 持久化     juItem.get(&quot;host&quot;,default)

  Law 16     SID 强关联         data-sid 匹配 paneID               #pane+sid 防线路错位

  Law 17     路径编码           非英文路径执行 encodeURI           忘了就 404

  Law 18     持久化存储         动态数据存入 juItem.set               CF Cookie 必须 juItem.set

  Law 19     禁止内部循环分页   禁止 lazyRule 内 while 循环抓多页  用&quot;加载更多&quot;按钮

  Law 21     UI 字符增强        Unicode 下标美化集数               String.fromCharCode(n+8320)

  Law 22     指纹二次同步       getVar 转 juItem.set 持久化           clearVar 清临时值

  Law 23     变量名避锋         不以 t/v/g 开头，用下划线          var \_title, var \_url

  Law 26     传参透传           \$().lazyRule 显式传参             不能用 this，必须传参

  Law 28     字节流图片         图片走 FileUtil.readBytes          \$(pic).image({image:fn})

  Law 30     反斜杠隔离         String.fromCharCode(92)            禁止直接写 \\

  Law 34     ES5 全量降级       禁用 let/const/=\>/?./             全量 var + function

  Law 37     Base64 修正        用 Java android.util.Base64        双保险：Java → atob 降级

  Law 39     自动脱壳           decodeURIComponent 最多脱 3 层     \_autoDeshell 防死循环

  Law 41     聚阅变量注入       type:&quot;主页&quot;             不能用 type:0/1
                                即注入 MY_PAGE（url 不必含 fypage）

  Law 42     scroll_button      建议纯文本标记选中（兼容性最好）   \'✦ \'+title+\' ✦\'

  Law 43     主页必须 return    函数末尾 return \_res 不能漏       写完后检查 return

  Law 44     命名空间隔离       多模式各用独立 key                 host + \'sel\_\' + mode

  Law 45     存纯路径           putMyVar 存路径，不存完整 URL      .replace(host,\'\')

  Law 46     返回重置页码       返回列表同时 putMyVar page=1       防止末页空白

  Law 47     解析函数 return    禁用 setResult，只认 return        return \'pics://\'

  Law 48     nhentai 大图       t2→i2 域名 + 去末尾 t              .replace(\'t2\',\'i2\')

  Law 49     五维占位对应       name 与 url 数量一一对应           数量不匹配→越界 undefined

  Law 50     空值即全部         &quot;全部&quot;选项 url           &quot;area_url&quot;:
                                留空字符串                         &quot;&国&日&quot;
  ----------------------------------------------------------------------------------------------------

> **第十六章：完整规则骨架模板（可直接复用）**
>
> var parse = {
>
> 作者: &quot;dev&quot;, 版本: &quot;20260101.V1&quot;,
>
> host: &quot;https://example.com&quot;,
>
> UA: &quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64)
> AppleWebKit/537.36&quot;,
>
> 页码: { &quot;主页&quot;: 1, &quot;分类&quot;: 1, &quot;搜索&quot;: 1
> },  // value 可写 1 / true，二者等效（关翻页用 0 / false / 不写该 key）
>
> 静态分类: {
>
> type: &quot;主页&quot;, url: &quot;fypage&quot;,
>
> class_name: &quot;全部&分类1&分类2&quot;,
>
> class_url: &quot;0&1&2&quot;
>
> },
>
> 主页: function() {
>
> var \_host = this.host; var \_ua = this.UA;
>
> var \_h = \_host; // getMyVar 命名空间前缀
>
> var \_res = \[\];
>
> if (MY_PAGE == 1) {
>
> renderRow(0, &quot;全部&分类1&分类2&quot;, &quot;0&1&2&quot;, \_res,
> \_h);
>
> }
>
> var \_c0 = getMyVar(\_h + &quot;curl0&quot;, &quot;0&quot;);
>
> var \_url = \_host + &quot;/list/&quot; + \_c0 + &quot;/page/&quot; +
> MY_PAGE + &quot;/&quot;;
>
> var \_html = fetch(\_url, { headers: { &quot;User-Agent&quot;: \_ua }
> });
>
> var \_items = pdfa(\_html, &quot;body&&.list&&li&quot;);
>
> for (var i = 0; i \< \_items.length; i++) {
>
> \_res.push({
>
> title: pdfh(\_items\[i\], &quot;a&&title&quot;),
>
> url: pd(\_items\[i\], &quot;a&&href&quot;, \_host),
>
> pic_url: pd(\_items\[i\], &quot;img&&data-original&quot;),
>
> col_type: &quot;movie_3&quot;
>
> });
>
> }
>
> return \_res; // ✅ Law 43：绝对不能漏
>
> },
>
> 二级: function(url) {
>
> var \_host = this.host; var \_ua = this.UA;
>
> var \_html = request(url, { headers: { &quot;User-Agent&quot;: \_ua }
> });
>
> var \_eps = pdfa(\_html, &quot;.episode&&a&quot;);
>
> var \_list = \[\];
>
> for (var i = 0; i \< \_eps.length; i++) {
>
> \_list.push({
>
> title: pdfh(\_eps\[i\], &quot;Text&quot;),
>
> url: &quot;video://&quot; + pd(\_eps\[i\], &quot;href&quot;, \_host)
>
> \+ &quot;;{User-Agent@&quot; + \_ua + &quot;&&Referer@&quot; +
> \_host + &quot;/}&quot;
>
> });
>
> }
>
> return {
>
> vod_name: pdfh(\_html, &quot;h1&&Text&quot;),
>
> vod_pic: pd(\_html, &quot;.cover&&img&&src&quot;, \_host),
>
> desc: pdfh(\_html, &quot;.intro&&Text&quot;),
>
> line: \[&quot;默认线路&quot;\],
>
> list: \[\_list\]
>
> };
>
> },
>
> 解析: function(url) {
>
> var \_url = url.split(&quot;#&quot;)\[0\];
>
> var \_ua = this.UA;
>
> var \_host = this.host;
>
> var \_html = request(\_url, { headers: { &quot;User-Agent&quot;: \_ua
> } });
>
> \_html = this.\_autoDeshell(\_html);
>
> var m =
> \_html.match(/https?:\\/\\/\[\^\\s\'&quot;\]+\\.m3u8\[\^\\s\'&quot;.\]\*/);
>
> if (m) {
>
> var \_slash = String.fromCharCode(92);
>
> return &quot;video://&quot; + m\[0\].split(\_slash).join(&quot;&quot;)
>
> \+ &quot;;{User-Agent@&quot; + \_ua + &quot;&&Referer@&quot; +
> \_host + &quot;/}&quot;;
>
> }
>
> return \_url + &quot;#嗅探&quot;;
>
> },
>
> 搜索: function(name) {
>
> var \_host = this.host; var \_ua = this.UA;
>
> var \_url = \_host + &quot;/search/&quot; + encodeURIComponent(name) +
> &quot;/page/&quot; + MY_PAGE + &quot;/&quot;;
>
> var \_html = fetch(\_url, { headers: { &quot;User-Agent&quot;: \_ua }
> });
>
> var \_res = \[\];
>
> var \_items = pdfa(\_html, &quot;body&&.search-item&quot;);
>
> for (var i = 0; i \< \_items.length; i++) {
>
> \_res.push({
>
> title: pdfh(\_items\[i\], &quot;h3&&Text&quot;),
>
> url: pd(\_items\[i\], &quot;a&&href&quot;, \_host),
>
> pic_url: pd(\_items\[i\], &quot;img&&src&quot;),
>
> col_type: &quot;movie_1_vertical_pic_blur&quot;
>
> });
>
> }
>
> return \_res;
>
> },
>
> \_base64Decode: function(str) {
>
> if (!str) return &quot;&quot;;
>
> str = str.replace(/-/g,&quot;+&quot;).replace(/\_/g,&quot;/&quot;);
>
> while (str.length % 4) str += &quot;=&quot;;
>
> try {
>
> var b = android.util.Base64.decode(str, android.util.Base64.DEFAULT);
>
> return new java.lang.String(b, &quot;UTF-8&quot;);
>
> } catch(e) {
>
> try { return decodeURIComponent(escape(atob(str))); }
>
> catch(e2) { log(&quot;Base64 fail&quot;); return &quot;&quot;; }
>
> }
>
> },
>
> \_autoDeshell: function(html) {
>
> if (!html) return html;
>
> for (var i = 0; i \< 3; i++) {
>
> if (html.indexOf(&quot;decodeURIComponent(\\&quot;&quot;) \> -1) {
>
> var m =
> html.match(/decodeURIComponent\\(\\&quot;(\[\^\\&quot;\]+)\\&quot;\\)/);
>
> if (m) html = decodeURIComponent(m\[1\]);
>
> } else break;
>
> }
>
> return html;
>
> }
>
> };
>
> // ── renderRow（筛选行渲染，唯一正确版本）────────────────────────
>
> var renderRow = function(rowIdx, titleStr, idStr, d, h) {
>
> var tArr = titleStr.split(&quot;&&quot;);
>
> var iArr = idStr.split(&quot;&&quot;);
>
> var curIdx = getMyVar(h + &quot;cindex&quot; + rowIdx, &quot;0&quot;);
>
> for (var i = 0; i \< tArr.length; i++) {
>
> var isSel = (i == curIdx);
>
> d.push({
>
> title: isSel ? &quot;❆ &quot; + tArr\[i\] + &quot; ❆&quot; :
> tArr\[i\],
>
> url: \$(&quot;#noLoading#&quot;).lazyRule(function(rIdx, idx, val, h)
> {
>
> putMyVar(h + &quot;cindex&quot; + rIdx, idx + &quot;&quot;);
>
> putMyVar(h + &quot;curl&quot; + rIdx, val);
>
> if (rIdx == 0) {
>
> for (var n = 1; n \< 5; n++) {
>
> putMyVar(h + &quot;cindex&quot; + n, &quot;0&quot;);
>
> putMyVar(h + &quot;curl&quot; + n, &quot;&quot;);
>
> }
>
> }
>
> refreshPage(false);
>
> return &quot;hiker://empty&quot;;
>
> }, rowIdx, i, iArr\[i\], h),
>
> col_type: &quot;scroll_button&quot;
>
> });
>
> }
>
> d.push({ col_type: &quot;blank_block&quot; });
>
> };
>
> **附录：致命错误红牌榜**

  ----------------------------------------------------------------------------------------
  **❌ 错误行为**        **💀 后果**           **✅ 正确做法**
  ---------------------- --------------------- -------------------------------------------
  lazyRule 内 while      内存溢出，App 闪退    用&quot;加载更多&quot;按钮逐页加载
  循环抓多页                                   

  type:0 或 type:1       MY_PAGE 运行期 undefined 改 type:&quot;主页&quot;
  的静态分类             （非词法报错，访问后才异常） （url 无需含 fypage）

  JS 层字符串处理 Base64 内存溢出，图片空白    \$(pic).image({image:FileUtil.readBytes})
  大图                                         

  解析函数用 setResult   漫画/小说内容不显示   return \'pics://\' + urls.join(\'&&\')

  lazyRule 内直接用 this this 指向不是 parse   提前 var \_ua=this.UA 再传参
                         对象                  

  用 let / const / =\> / 引擎崩溃 Error 14     全量 var + function(){}
  ?.                                           

  反斜杠直接写 \\        Error 43              String.fromCharCode(92)

  putMyVar 存完整 URL    拼接时双域名          .replace(host,&quot;&quot;) 去域名后再存

  CF Cookie 用 putMyVar  App 重启后每次重验    改用 juItem.set 持久化
  存                                           

  renderRow 用           col_type              col_type: &quot;scroll_button&quot;
  scroll_text_1          不存在，不渲染        

  name 与 url 数量不对应 越界取 undefined      检查 & 分隔后元素数量严格对应

  主页函数漏写 return    主页空白              函数末尾加 return \_res
  \_res                                        
  ----------------------------------------------------------------------------------------

> **第十七章：各类站点实战写法**

本章汇总了普通视频、多分类隔离、图文混合、小说、音频、图片、动漫/漫画各类站点的核心架构要点与代码模板，均来自实战验证规则。

> 📌 **实战案例章节地图（17~20 + 后续专项章）：**
>
> | 章节 | 站点类型 | 实战案例 |
> |---|---|---|
> | 17.1 | 普通视频 (MissAV 型) | MissAV |
> | 17.2 | 图文/视频/小说混合 | 多分类隔离写法 |
> | 17.3 | 小说站 | CZBooks / 集书阁 |
> | 17.4 | 小说站高级 | 域名自愈 + 翻页路径 |
> | 17.5 | 音频站 (WordPress) | 通用模板 |
> | 17.6 | 图片站 | Photos18 |
> | 17.7 | 动漫/视频 (矩阵 + MACCMS) | 综合 |
> | 17.8 | 漫画站高级 | 多源切换 + 分页 |
> | **18** | 多模式矩阵 | hanime1.me（8 个高频坑） |
> | **19** | 视频站总结 | 17AV (Law 59/60 + 提速套路) |
> | **20** | API 接口型源 | 熊猫 APP 类型 |
> | **23** | KVS Hash 解密 | x1hub |
> | **27** | Next.js 站点 | 加密视频 + 分类独立页 |
> | **34** | JS 混淆播放器 | Dean Edwards Packer |
> | **35** | 多站点总结 | 禁漫/7mmtv/Pornhub/XVideos/MissAV |
> | **44** | 双重加密 | AES + 自托管解析器 |
> | **46-47** | 吃瓜/Elementor | WordPress 主题型 |
> | **50** | 图集站 | JunMeiTu |
> | **56** | MissAV 完全指南 | 独立章节 |
> | **57** | 间接 m3u8 | 双重 Base64 接口 |
> | **58-59** | 吃瓜站清洗/进阶 | 1024 吃瓜 / 海角 |
> | **60A** | 自定义二级页 | 配合远程模板 |
> | **63** | 综合站 player 内联 | 时间戳 AES key |
> | **64** | Discuz! 论坛 | formhash + REPORTURL |
> | **68** | App 逆向 | AES-CBC 签名 Header |
> | **75** | BBYS 实战 | 四维筛选 + 远程模板二级 |

**17.1 普通视频站点（MissAV 型）**

特征：fyclass 静态分类 + 翻页参数拼接 + lazyRule 内硬编码 host/ua + m3u8
匹配解析。

> 📌 **关键架构:** \_getVideoRule() 返回一个 lazyRule
> 字符串，在列表构建时直接拼接到 url 末尾（url: link +
> this.\_getVideoRule()）。所有 host/UA 在 lazyRule
> 内部硬编码，不依赖外部 this。
>
> var parse = {
>
> host: &quot;https://example.to&quot;,
>
> UA: &quot;Mozilla/5.0 (Linux; Android 10; Mobile)
> AppleWebKit/537.36&quot;,
>
> 页码: { 主页: 1, 分类: 1, 搜索: 1 },
>
> 静态分类: {
>
> &quot;type&quot;: &quot;主页&quot;, &quot;url&quot;:
> &quot;fyclass&quot;,
>
> &quot;class_name&quot;: &quot;最新&热门&无码&有码&quot;,
>
> &quot;class_url&quot;:
> &quot;/cn/new&/cn/hot&/cn/uncensored&/cn/censored&quot;
>
> },
>
> // ── 翻页 URL 构建工具 ─────────────────────────────────
>
> \_buildUrl: function(baseUrl) {
>
> if (MY_PAGE \> 1) {
>
> return baseUrl + (baseUrl.indexOf(&quot;?&quot;) \> -1 ? &quot;&&quot;
> : &quot;?&quot;) + &quot;page=&quot; + MY_PAGE;
>
> }
>
> return baseUrl;
>
> },
>
> // ── 视频解析 lazyRule（内部硬编码 host/UA，不依赖 this）─
>
> \_getVideoRule: function() {
>
> return \$(&quot;&quot;).lazyRule(function() {
>
> var host = &quot;https://example.to&quot;; // ✅ 硬编码，不用 this
>
> var ua = &quot;Mozilla/5.0 (Linux; Android 10; Mobile)
> AppleWebKit/537.36&quot;;
>
> var html = request(input, { headers: { &quot;User-Agent&quot;: ua,
> &quot;Referer&quot;: host+&quot;/&quot; } });
>
> // 优先匹配 data-config 中的视频
>
> var cfgM =
> html.match(/data-config=\[&quot;\'\](\[\^&quot;\'\]+)\[&quot;\'\]/);
>
> if (cfgM) {
>
> try {
>
> var cfg = JSON.parse(cfgM\[1\].replace(/&quot;/g,
> &quot;&quot;&quot;));
>
> if (cfg.video && cfg.video.url)
>
> return cfg.video.url + &quot;;{Referer@&quot; + host +
> &quot;/}#isVideo=true#&quot;;
>
> } catch(e) {}
>
> }
>
> // 直接匹配 m3u8
>
> var m3u8M =
> html.match(/\[&quot;\'\](\[\^&quot;\'\]+\\.m3u8\[\^&quot;\'\]\*)/);
>
> if (m3u8M) {
>
> var slash = String.fromCharCode(92);
>
> return m3u8M\[1\].split(slash).join(&quot;&quot;) +
> &quot;;{Referer@&quot; + host + &quot;/}&quot;;
>
> }
>
> return input + &quot;#嗅探&quot;;
>
> });
>
> },
>
> 主页: function() {
>
> var \_url = this.host + MY_URL;
>
> \_url = this.\_buildUrl(\_url);
>
> var html = request(\_url, { headers: { &quot;User-Agent&quot;: this.UA
> } });
>
> var list = pdfa(html, &quot;body&&.item&quot;);
>
> var d = \[\];
>
> var \_rule = this.\_getVideoRule(); // ✅ 提前生成，循环内复用
>
> for (var i = 0; i \< list.length; i++) {
>
> var it = list\[i\];
>
> var img = pd(it, &quot;img&&data-src&quot;) \|\| pd(it,
> &quot;img&&src&quot;);
>
> if (img && img.indexOf(&quot;//&quot;) === 0) img =
> &quot;https:&quot; + img;
>
> if (img) img += &quot;@Referer=&quot; + this.host + &quot;/&quot;;
>
> var link = pd(it, &quot;a&&href&quot;, this.host);
>
> d.push({
>
> title: pdfh(it, &quot;.title&&Text&quot;),
>
> desc: pdfh(it, &quot;.duration&&Text&quot;),
>
> pic_url: img,
>
> url: link + \_rule, // ✅ 直接拼接
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> return d;
>
> },
>
> 分类: function() { return this.主页(); },
>
> 搜索: function(key) { /\* 同主页，url 换搜索接口 \*/ }
>
> };
>
> ⚠️ **警告:** \_getVideoRule()
> 每次调用都会生成一段新字符串。在循环体内应提前调用一次赋给变量（var
> \_rule = this.\_getVideoRule()），然后循环内直接用
> \_rule，避免重复生成。

**17.2 多分类隔离写法（图文/视频/小说混合站）**

特征：一个站点同时包含视频、图片、小说等多种内容类型，fyclass
返回的路径带有不同前缀（vod/art-type），主页函数用正则判断分流，每种类型调用对应的解析逻辑。

> // 静态分类 class_url 里埋入路径前缀作为类型标识
>
> 静态分类: {
>
> &quot;type&quot;: &quot;主页&quot;, &quot;url&quot;:
> &quot;fyclass&quot;,
>
> &quot;class_name&quot;:
> &quot;今日热门&自拍视频&精品图片&秘爱小说&quot;,
>
> &quot;class_url&quot;:
> &quot;label-daytop-pg-&vod-type-id-1-pg-&art-type-id-1-pg-&art-type-id-2-pg-&quot;
>
> },
>
> 主页: function() {
>
> var d = \[\];
>
> var cid = (MY_URL === &quot;fyclass&quot;) ?
> &quot;label-daytop-pg-&quot; : MY_URL;
>
> var fetchUrl = this.host + &quot;/&quot; + cid + MY_PAGE +
> &quot;.html&quot;;
>
> var html = fetch(fetchUrl, { headers: { &quot;User-Agent&quot;:
> this.UA } });
>
> if (/vod\|label/.test(cid)) {
>
> // A. 视频版块：lazyRule 解析
>
> var items = pdfa(html, &quot;body&&#listcontent\>.citem&quot;);
>
> for (var i = 0; i \< items.length; i++) {
>
> var img = pd(items\[i\], &quot;img&&data-original&quot;) \|\|
> pd(items\[i\], &quot;img&&src&quot;);
>
> d.push({
>
> title: pdfh(items\[i\], &quot;.citemtitle&&title&quot;),
>
> pic_url: img,
>
> url: \$(img).lazyRule(this.源码.videoLogic, this.host, img),
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> } else if (/art-type-id-2/.test(cid)) {
>
> // B. 小说版块：唤起内置阅读器
>
> var items = pdfa(html, &quot;body&&.list-group-item&quot;);
>
> for (var i = 0; i \< items.length; i++) {
>
> var link = pd(items\[i\], &quot;a&&href&quot;, this.host);
>
> d.push({
>
> title: pdfh(items\[i\], &quot;.media-heading&&Text&quot;),
>
> // ✅ 用 hiker://empty#readTheme##autoPage# 唤起阅读器
>
> url: \$(&quot;hiker://empty#readTheme##autoPage#&quot;).rule(
>
> this.源码.novelLogic, this.host, link),
>
> col_type: &quot;text_1&quot;
>
> });
>
> }
>
> } else if (/art-type-id-1/.test(cid)) {
>
> // C. 图片版块：pics:// 协议
>
> var items = pdfa(html, &quot;body&&.list-group-item&quot;);
>
> for (var i = 0; i \< items.length; i++) {
>
> var link = pd(items\[i\], &quot;a&&href&quot;, this.host);
>
> d.push({
>
> title: pdfh(items\[i\], &quot;.media-heading&&Text&quot;),
>
> url: \$(link).lazyRule(this.源码.picLogic, this.host, link),
>
> col_type: &quot;text_1&quot;
>
> });
>
> }
>
> }
>
> return d;
>
> },
>
> // 把各类型的解析函数封装在 源码 子对象中，便于传参
>
> 源码: {
>
> videoLogic: function(H, Link) {
>
> var id = Link.split(&quot;/&quot;).pop().split(&quot;.&quot;)\[0\];
>
> return &quot;https://cdn.example.com/&quot; + id + &quot;/&quot; +
> id + &quot;.mp4&quot;
>
> \+ &quot;;{User-Agent@Mozilla/5.0&&Referer@&quot; + H +
> &quot;/}&quot;;
>
> },
>
> novelLogic: function(H, Link) {
>
> var d = \[\];
>
> var html = request(Link, { headers: { &quot;User-Agent&quot;:
> &quot;pc&quot;, &quot;Referer&quot;: H+&quot;/&quot; } });
>
> var raw = pdfh(html, &quot;#content&&Html&quot;) \|\| &quot;&quot;;
>
> var text =
> raw.replace(/\<br\\s\*\\/?\>/gi,&quot;\\n&quot;).replace(/\<\[\^\>\]+\>/g,&quot;&quot;);
>
> var clean = &quot;&quot;;
>
> text.split(&quot;\\n&quot;).forEach(function(ln) {
>
> var t = ln.trim();
>
> if (t.length \> 3) clean += &quot;\\u3000\\u3000&quot; + t +
> &quot;\\n\\n&quot;;
>
> });
>
> d.push({ title: clean, col_type: &quot;rich_text&quot;, extra: {
> textSize: 19 } });
>
> setResult(d);
>
> },
>
> picLogic: function(H, Link) {
>
> var html = request(Link, { headers: { &quot;Referer&quot;:
> H+&quot;/&quot; } });
>
> var imgs = pdfa(html, &quot;#gallery&&img&quot;);
>
> var urls = \[\];
>
> for (var i = 0; i \< imgs.length; i++) {
>
> var src = pd(imgs\[i\], &quot;img&&src&quot;, H);
>
> if (src) urls.push(src + &quot;@Referer=&quot; + H + &quot;/&quot;);
>
> }
>
> return &quot;pics://&quot; + urls.join(&quot;&&quot;);
>
> }
>
> }
>
> 💡 **源码子对象的作用:** 把不同类型的解析逻辑封装在 源码:{}
> 子对象中，主页函数通过 this.源码.videoLogic 引用。传给 lazyRule
> 时作为函数参数传入，避免了 this 指向问题，同时实现代码复用。

**17.3 小说站点（CZBooks/集书阁型）**

特征：矩阵分类（分类 + 榜单 + 排序三行联动）+ 二级返回章节列表 +
解析函数处理正文排版。两行互斥：选了榜单就清分类，选了分类就清榜单。

> // 三行矩阵联动：行0=分类，行1=榜单，行2=排序
>
> // 行0与行1互斥：点击行1(榜单)时重置行0(分类)，反之亦然
>
> // 行2(排序)独立叠加，不互斥
>
> renderRow(0, catT, catI, d, \_host); // 分类行
>
> renderRow(1, chartT, chartI, d, \_host); // 榜单行（与分类互斥）
>
> renderRow(2, sortT, sortI, d, \_host); // 排序行（独立）
>
> // lazyRule 内互斥逻辑：
>
> if (rIdx == 1 && val != &quot;none&quot;) {
>
> putMyVar(h + &quot;cindex0&quot;, &quot;0&quot;); putMyVar(h +
> &quot;curl0&quot;, &quot;none&quot;); // 清分类
>
> } else if (rIdx == 0) {
>
> putMyVar(h + &quot;cindex1&quot;, &quot;0&quot;); putMyVar(h +
> &quot;curl1&quot;, &quot;none&quot;); // 清榜单
>
> }
>
> // URL 缝合：榜单优先于分类
>
> var c0 = getMyVar(\_host + &quot;curl0&quot;, &quot;/c/default&quot;);
>
> var c1 = getMyVar(\_host + &quot;curl1&quot;, &quot;none&quot;);
>
> var c2 = getMyVar(\_host + &quot;curl2&quot;, &quot;/new&quot;);
>
> var path = (c1 !== &quot;none&quot;) ? c1 : c0;
>
> var \_url = \_host + path;
>
> if (c1 === &quot;none&quot; && c2 !== &quot;none&quot;) \_url += c2;
> // 叠加排序
>
> if (MY_PAGE \> 1) \_url = \_url.replace(/\\/\$/, &quot;&quot;) +
> &quot;/&quot; + MY_PAGE;
>
> // 二级：返回章节列表，URL 加 #isJiexi=1#readTheme##autoPage#
>
> 二级: function(url) {
>
> var \_host = this.host;
>
> var html = request(url, { headers: { &quot;User-Agent&quot;: this.\_ua
> } });
>
> juItem.set(&quot;site_cookie&quot;, fetchCookie(url)); // ✅ 持久化
> Cookie
>
> var list = pdfa(html,
> &quot;ul#chapter-list&&li&quot;).map(function(it) {
>
> if (it.indexOf(&quot;\<a&quot;) \> -1) {
>
> return {
>
> title: pdfh(it, &quot;a&&Text&quot;),
>
> url: pd(it, &quot;a&&href&quot;, \_host) +
> &quot;#isJiexi=1#readTheme##autoPage#&quot;
>
> };
>
> }
>
> }).filter(Boolean);
>
> return { vod_name: pdfh(html,&quot;h1&&Text&quot;),
> line:\[&quot;章节列表&quot;\], list:\[list\] };
>
> },
>
> // 解析：正文排版（聚阅核心：只能 return，禁用 setResult）
>
> 解析: function(url) {
>
> var \_realUrl = url.split(&quot;#&quot;)\[0\];
>
> var \_cookie = juItem.get(&quot;site_cookie&quot;, &quot;&quot;);
>
> var html = request(\_realUrl, {
>
> headers: { &quot;User-Agent&quot;: this.\_ua, &quot;Referer&quot;:
> \_realUrl, &quot;Cookie&quot;: \_cookie }
>
> });
>
> var d = \[\];
>
> var contentHtml = pdfh(html, &quot;body&&.content&&Html&quot;);
>
> if (contentHtml) {
>
> var text =
> contentHtml.replace(/\\u00A9\[\^\<\]\*/g,&quot;&quot;).replace(/\<br\\s\*\\/?\>/gi,&quot;\\n&quot;).replace(/\<\[\^\>\]+\>/g,&quot;&quot;);
>
> var clean = \[\];
>
> text.split(&quot;\\n&quot;).forEach(function(ln) {
>
> if (ln.trim().length \> 0) clean.push(&quot;\\u3000\\u3000&quot; +
> ln.trim());
>
> });
>
> d.push({
>
> title: clean.join(&quot;\\n\\n&quot;),
>
> col_type: &quot;rich_text&quot;,
>
> extra: { next:
> pd(html,&quot;a:contains(下一章)&&href&quot;,this.host) +
> &quot;#isJiexi=1#readTheme##autoPage#&quot; }
>
> });
>
> }
>
> // ✅ 聚阅小说必须 return，禁止 setResult
>
> return &quot;pics://&quot; + d.map(function(i){ return i.url \|\|
> &quot;&quot;; }).join(&quot;&&quot;);
>
> }

**17.4 小说站点高级过滤写法（域名自愈 + 翻页路径处理）**

特征：域名不固定，预处理阶段自动获取并缓存；翻页路径规则特殊（list_1.html
→ list_1_2.html）；静态分类用 hiker://empty##fyclass 模式，MY_URL
解析出路径。

> // ── 域名自愈（带时间缓存，1小时内复用）──
>
> 预处理: function() {
>
> var cached = getVar(&quot;site_host&quot;, &quot;&quot;);
>
> var cachedTime = parseInt(getVar(&quot;site_host_time&quot;,
> &quot;0&quot;));
>
> if (cached && (Date.now() - cachedTime) \< 3600000) {
>
> this.host = cached; return cached;
>
> }
>
> var backupHost = &quot;https://backup.example.com&quot;;
>
> this.host = backupHost;
>
> try {
>
> var html = fetch(&quot;https://index.example.com/&quot;, { timeout:
> 3000 });
>
> var m = html.match(/https?:\\/\\/\[\^\\/\\&quot;\'\<\>\\s\]+/);
>
> if (m) this.host = m\[0\];
>
> } catch(e) {}
>
> putVar(&quot;site_host&quot;, this.host);
>
> putVar(&quot;site_host_time&quot;, Date.now() + &quot;&quot;);
>
> return this.host;
>
> },
>
> // ── 静态分类 hiker://empty##fyclass 模式 ──
>
> 静态分类: {
>
> &quot;type&quot;: &quot;主页&quot;, &quot;url&quot;:
> &quot;hiker://empty##fyclass&quot;,
>
> &quot;class_name&quot;: &quot;全部短篇&全部长篇&现代情色&quot;,
>
> &quot;class_url&quot;:
> &quot;list_1.html&list_2.html&list_15.html&quot;
>
> },
>
> // ── 主页：从 MY_URL 解析路径 + 特殊翻页规则 ──
>
> 主页: function() {
>
> var host = this.预处理();
>
> var path = &quot;list_1.html&quot;;
>
> if (MY_URL && MY_URL.indexOf(&quot;##&quot;) \> -1)
>
> path = MY_URL.split(&quot;##&quot;)\[1\] \|\| path;
>
> // 翻页：list_1.html → list_1_2.html（去掉 .html 加页码再加回来）
>
> if (MY_PAGE \> 1)
>
> path = path.replace(&quot;.html&quot;, &quot;\_&quot; + MY_PAGE +
> &quot;.html&quot;);
>
> var html = fetch(host + &quot;/&quot; + path);
>
> var d = \[\];
>
> pdfa(html, &quot;.ucontent&&li&quot;).forEach(function(item) {
>
> var title = pdfh(item, &quot;.title&&Text&quot;);
>
> var link = pdfh(item, &quot;a&&href&quot;);
>
> if (!link) return;
>
> if (link.indexOf(&quot;http&quot;) !== 0)
>
> link = (link.indexOf(&quot;/&quot;) === 0 ? host :
> host+&quot;/&quot;) + link;
>
> d.push({ title: title, url: link, col_type: &quot;text_1&quot; });
>
> });
>
> return d;
>
> }

**17.5 音频站点（WordPress 型）**

特征：WordPress 架构，中文路径翻页需 encodeURI；音频解析通过 API 获取
stream_url；返回协议需要加 #isMusic=true# 标记。

> // 静态分类：fyclass 替换路径（含中文，翻页时必须 encodeURI）
>
> 静态分类: {
>
> &quot;type&quot;: &quot;主页&quot;, &quot;url&quot;:
> &quot;fyclass&quot;,
>
> &quot;class_name&quot;: &quot;最新音频&有声中篇&有声短篇&quot;,
>
> &quot;class_url&quot;: &quot;/&/有声中篇/&/有声短篇/&quot;
>
> },
>
> // 翻页：WordPress 固定链接模式 /page/N/ + encodeURI
>
> \_抓取列表: function(url) {
>
> var realUrl = url;
>
> if (MY_PAGE \> 1)
>
> realUrl = url.replace(/\\/+\$/, &quot;&quot;) + &quot;/page/&quot; +
> MY_PAGE + &quot;/&quot;;
>
> realUrl = encodeURI(realUrl); // ✅ 中文路径必须编码
>
> var html = fetch(realUrl, { headers: { &quot;User-Agent&quot;: this.UA
> } });
>
> // \... 解析列表 \...
>
> },
>
> // 音频解析：请求 API 获取 stream_url
>
> \_audioRule: function() {
>
> return \$(&quot;&quot;).lazyRule(function() {
>
> var html = request(input);
>
> var id = pdfh(html, &quot;article,0&&data-play-id&quot;);
>
> var nonce = html.match(/nonce&quot;\\:&quot;(.\*?)&quot;/)\[1\];
>
> var apiUrl = &quot;https://example.com/api/play/play/&quot; + id +
> &quot;?type=post&quot;;
>
> var play = JSON.parse(fetch(apiUrl, {
>
> headers: {
>
> &quot;Referer&quot;: &quot;https://example.com/&quot;,
>
> &quot;X-WP-Nonce&quot;: nonce,
>
> &quot;X-Requested-With&quot;: &quot;XMLHttpRequest&quot;
>
> }
>
> })).stream_url;
>
> // 直播流不加标记，点播流加 #isMusic=true#
>
> if (/\\.m3u8\|live\|stream/.test(play)) return play;
>
> return play + &quot;#isMusic=true#&quot;;
>
> });
>
> },
>
> 主页: function() {
>
> var path = (MY_URL === &quot;fyclass&quot;) ? &quot;/&quot; : MY_URL;
>
> var audioLazy = this.\_audioRule();
>
> var items = pdfa(html, &quot;body&&article&quot;);
>
> for (var i = 0; i \< items.length; i++) {
>
> d.push({
>
> title: pdfh(items\[i\], &quot;h2&&Text&quot;),
>
> pic_url: pd(items\[i\], &quot;img&&data-src&quot;) \|\| pd(items\[i\],
> &quot;img&&src&quot;),
>
> url: pd(items\[i\], &quot;a&&href&quot;) + audioLazy, // ✅ 直接拼接
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> }
>
> 📌 **音频协议说明:** #isMusic=true#
> 强制让引擎识别为音频资源，触发音频播放器而不是视频播放器。如果是 m3u8
> 直播流（HLS），通常不加此标记直接播放。

**17.6 图片站点（Photos18 型）**

特征：fyclass 静态分类 + \_parseList 公共方法复用 + 二级详情 +
解析函数用正则提取所有图集图片，返回 pics:// 协议。

> // ── 公共列表解析方法（主页/搜索共用）──
>
> \_parseList: function(html) {
>
> var d = \[\];
>
> var self = this;
>
> if (html.indexOf(&quot;Just a moment&quot;) \> -1) {
>
> d.push({ title: &quot;⚠️ CF 拦截，请验证&quot;, url: self.host,
> col_type: &quot;text_1&quot; });
>
> return d;
>
> }
>
> var list = pdfa(html, &quot;.card-columns&&.card&quot;);
>
> for (var i = 0; i \< list.length; i++) {
>
> var it = list\[i\];
>
> var href = pdfh(it, &quot;a&&href&quot;);
>
> if (!href) continue;
>
> if (href.indexOf(&quot;http&quot;) !== 0) href = self.host + href;
>
> var img = pdfh(it, &quot;img&&data-src&quot;) \|\| pdfh(it,
> &quot;img&&src&quot;);
>
> if (img && img.indexOf(&quot;http&quot;) !== 0) img = self.host + img;
>
> d.push({
>
> title: pdfh(it, &quot;.card-body a&&Text&quot;) \|\|
> &quot;无标题&quot;,
>
> desc: pdfh(it, &quot;.badge&&Text&quot;),
>
> img: img ? img + &quot;@Referer=&quot; + self.host : &quot;&quot;,
>
> url: href,
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> return d;
>
> },
>
> 主页: function() {
>
> var url = this.host + &quot;/&quot; + MY_URL;
>
> if (MY_PAGE \> 1) url += (url.indexOf(&quot;?&quot;) \> -1 ?
> &quot;&&quot; : &quot;?&quot;) + &quot;page=&quot; + MY_PAGE;
>
> var html = request(url, { headers: { &quot;Referer&quot;:
> this.host+&quot;/&quot; } });
>
> return this.\_parseList(html); // ✅ 复用公共方法
>
> },
>
> 搜索: function(key) {
>
> var url = this.host + &quot;/q/&quot; + encodeURIComponent(key) +
> &quot;?category_id=&quot;;
>
> if (MY_PAGE \> 1) url += &quot;&page=&quot; + MY_PAGE;
>
> var html = request(url, { headers: { &quot;Referer&quot;:
> this.host+&quot;/&quot; } });
>
> return this.\_parseList(html); // ✅ 同样复用
>
> },
>
> // 解析：用正则提取 data-fancybox=&quot;gallery&quot; 的所有图片链接
>
> 解析: function(url) {
>
> var html = request(url, { headers: { &quot;Referer&quot;:
> this.host+&quot;/&quot; } });
>
> var pics = \[\];
>
> var regex =
> /\<a\[\^\>\]+href=&quot;(\[\^&quot;\]+)&quot;\[\^\>\]+data-fancybox=&quot;gallery&quot;/g;
>
> var m;
>
> while ((m = regex.exec(html)) !== null) {
>
> var imgUrl = m\[1\];
>
> if (imgUrl.indexOf(&quot;http&quot;) !== 0) imgUrl = this.host +
> imgUrl;
>
> pics.push(imgUrl + &quot;@Referer=&quot; + this.host);
>
> }
>
> if (pics.length === 0) return &quot;toast://未找到图片&quot;;
>
> return &quot;pics://&quot; + pics.join(&quot;&&quot;);
>
> }

**17.7 动漫/视频站（矩阵分类 + MACCMS 架构）**

特征：矩阵静态分类（url:&quot;0&quot;，全由主页函数管理）+ MACCMS 的
vod-show 路径 + SID 强关联多线路 + player_aaaa 播放解密。

> // 静态分类用矩阵模式
>
> 静态分类: { &quot;type&quot;: &quot;主页&quot;, &quot;url&quot;:
> &quot;0&quot;, &quot;class_name&quot;: &quot;矩阵版&quot;,
> &quot;class_url&quot;: &quot;0&quot; },
>
> // 三行矩阵：分类 + 类型 + 年份
>
> // dataClass\[0\] = 动漫分类，dataClass\[1\] = 类型，dataClass\[2\] =
> 年份
>
> // 三行各自独立（不互斥），URL 同时叠加三个维度
>
> // URL 缝合（vod-show 格式）
>
> var c0 = getMyVar(host + &quot;curl0&quot;, &quot;4&quot;); // 分类 ID
>
> var c1 = getMyVar(host + &quot;curl1&quot;, &quot;&quot;); //
> 类型（繁体字）
>
> var c2 = getMyVar(host + &quot;curl2&quot;, &quot;&quot;); // 年份
>
> var fetchUrl = host + &quot;/vod-show/&quot; + c0
>
> \+ &quot;\-\--&quot; + encodeURIComponent(c1)
>
> \+ &quot;\-\-\-\--&quot; + MY_PAGE
>
> \+ &quot;\-\--&quot; + c2 + &quot;/&quot;;
>
> // 二级：SID 强关联防线路错位（Law 16）
>
> 二级: function(url) {
>
> var host = this.host;
>
> var html = fetch(url, { headers: { &quot;User-Agent&quot;: this.UA }
> });
>
> var tabs = \[\], lists = \[\];
>
> var tabNodes = pdfa(html,
> &quot;body&&.module-tab-item\[data-sid\]&quot;);
>
> for (var a = 0; a \< tabNodes.length; a++) {
>
> var sid = tabNodes\[a\].match(/data-sid=&quot;(\\d+)&quot;/)\[1\];
>
> var epItems = pdfa(html, &quot;body&&#pane&quot; + sid +
> &quot;&&.module-play-list-link&quot;);
>
> if (epItems.length \> 0) {
>
> tabs.push(pdfh(tabNodes\[a\], &quot;span&&Text&quot;));
>
> var sub = \[\];
>
> for (var i = 0; i \< epItems.length; i++) {
>
> sub.push({
>
> title: pdfh(epItems\[i\], &quot;Text&quot;),
>
> url: pd(epItems\[i\], &quot;a&&href&quot;, host)
>
> });
>
> }
>
> lists.push(sub);
>
> }
>
> }
>
> return { vod_name: pdfh(html,&quot;h1&&Text&quot;), line: tabs, list:
> lists };
>
> },
>
> // 解析：player_aaaa 双重解密（见第九章）
>
> 解析: function(url) {
>
> var html = this.\_autoDeshell(request(url, { headers: {
> &quot;User-Agent&quot;: this.UA } }));
>
> var match = html.match(/var
> player_aaaa\\s\*=\\s\*(\\{\[\\s\\S\]\*?\\})(?=;\\s\*\<\\/script)/);
>
> if (!match) return url + &quot;#嗅探&quot;;
>
> try {
>
> var cfg = JSON.parse(match\[1\]);
>
> var v = cfg.url;
>
> if (cfg.encrypt == 2) v = unescape(this.\_base64Decode(v));
>
> else if (cfg.encrypt == 1) v = unescape(v);
>
> var slash = String.fromCharCode(92);
>
> return &quot;video://&quot; + v.split(slash).join(&quot;&quot;) +
> &quot;;{Referer@&quot; + this.host + &quot;/}&quot;;
>
> } catch(e) { return url + &quot;#嗅探&quot;; }
>
> }

**17.8 漫画站高级写法（多源切换 + 分页解析）**

特征：多条备用域名通过 juItem.set
存储当前选中的源；漫画章节图片可能跨多页（下一页），解析函数需循环抓取；图片加
Referer。

> // ── 多源切换（用 juItem.set 持久化选中源）──
>
> 主页: function() {
>
> var d = \[\];
>
> var sources = \[&quot;源一\|https://site1.com&quot;,
> &quot;源二\|https://site2.com&quot;,
> &quot;源三\|https://site3.com&quot;\];
>
> var curSrc = juItem.get(&quot;xdn.source&quot;, sources\[0\]);
>
> var host = curSrc.split(&quot;\|&quot;)\[1\];
>
> putMyVar(&quot;current_host&quot;, host);
>
> // 切换源的按钮
>
> d.push({
>
> title: curSrc.split(&quot;\|&quot;)\[0\],
>
> col_type: &quot;text_icon&quot;,
>
> url: \$(sources, 4).select(function() {
>
> input = input.replace(/🌸/g, &quot;&quot;);
>
> juItem.set(&quot;xdn.source&quot;, input);
>
> refreshPage(true);
>
> return &quot;toast://已切换: &quot; + input;
>
> })
>
> });
>
> var html = request(host + &quot;/&quot;, { timeout: 8000 });
>
> // \... 解析首页列表 \...
>
> return d;
>
> },
>
> // ── 漫画解析：自动跨页抓取图片 ──
>
> 解析: function(url) {
>
> var ua = this.UA;
>
> var html = request(url, { headers: { &quot;User-Agent&quot;: ua } });
>
> var pics = pdfa(html,
> &quot;.reader-chapter&&img&quot;).map(function(h) {
>
> return pdfh(h, &quot;img&&data-src&quot;) \|\| pdfh(h,
> &quot;img&&src&quot;);
>
> });
>
> // 如果有&quot;下一页&quot;，循环抓取后续页
>
> if (html.indexOf(&quot;下一页&quot;) \> -1) {
>
> for (var k = 2; ; k++) {
>
> var nextUrl = url.replace(&quot;.html&quot;, &quot;\_&quot; + k +
> &quot;.html&quot;);
>
> var nextHtml = request(nextUrl, { headers: { &quot;User-Agent&quot;:
> ua } });
>
> var nextPics = pdfa(nextHtml,
> &quot;.reader-chapter&&img&quot;).map(function(h) {
>
> return pdfh(h, &quot;img&&data-src&quot;) \|\| pdfh(h,
> &quot;img&&src&quot;);
>
> });
>
> pics = pics.concat(nextPics);
>
> if (nextHtml.indexOf(&quot;下一页&quot;) === -1) break;
>
> }
>
> }
>
> // 图片加 Referer，用 \@headers= 新格式
>
> var refHost = &quot;https://pic.cdn.example.com&quot;;
>
> return &quot;pics://&quot; + pics.map(function(p) {
>
> return p + &quot;@headers={\\&quot;Referer\\&quot;:\\&quot;&quot; +
> refHost + &quot;/\\&quot;}&quot;;
>
> }).join(&quot;&&quot;);
>
> }

**17.9 各类站点核心要点速查**

  ------------------------------------------------------------------------------------------------------------------------------
  **站点类型**                       **静态分类模式**            **核心解析协议**           **关键注意事项**
  ---------------------------------- --------------------------- -------------------------- ------------------------------------
  普通视频站                         fyclass（单维）             video:// 或 lazyRule m3u8  lazyRule 内硬编码 host/UA，不用 this
                                                                 匹配                       

  多分类隔离（图文/视频/小说混合）   fyclass（路径含类型前缀）   按 cid                     源码子对象封装各类型逻辑，传参传入
                                                                 正则分流，各类型独立处理   

  小说站（矩阵三行）                 url:&quot;0&quot;           解析函数 return rich_text  解析函数禁用 setResult，只能 return
                                     矩阵，行0行1互斥            内容                       

  小说站（域名自愈）                 hiker://empty##fyclass      text_1 列表 + 解析函数正文 路径翻页规则特殊，需单独处理

  WordPress 音频站                   fyclass + 中文路径          stream_url API +           中文路径翻页必须 encodeURI
                                                                 #isMusic=true#             

  图片站                             fyclass                     pics:// + 正则提取 gallery \_parseList 公共方法主页/搜索复用
                                                                 链接                       

  动漫/视频站（矩阵）                url:&quot;0&quot;           player_aaaa 双重解密       SID 强关联防线路错位（Law 16）
                                     矩阵三行叠加                                           

  漫画站（多源）                     juItem.set 持久化源选择        pics:// + 跨页图片循环抓取 图片 Referer 填 CDN 域名；@Referer=
                                                                                            或 \@headers={} 均可
  ------------------------------------------------------------------------------------------------------------------------------

> **第十八章：多模式矩阵站点实战总结**

本章基于 hanime1.me 多模式矩阵规则的完整开发过程提炼，涵盖 8
个在通用章节中未覆盖的高频踩坑点。

**18.1 getMyVar key 命名------绝对不能用 host 做前缀**

用 host 字符串拼前缀会导致 key 含 &quot;://&quot; 和
&quot;/&quot;，静默失败------存进去读不出来，永远返回默认值，规则表现为筛选点了没反应。

  ------------------------------------------------------------------------------------------------------
  **写法**                                             **key 实际值**             **结果**
  ---------------------------------------------------- -------------------------- ----------------------
  getMyVar(host+&quot;curl0&quot;,&quot;video&quot;)   https://site.comcurl0      ❌ 含 :// 和
                                                                                  /，静默失败

  getMyVar(&quot;hm_curl0&quot;,&quot;video&quot;)     hm_curl0                   ✅ 纯字母+下划线，正常

  getMyVar(host+&quot;mx_genre&quot;,&quot;&quot;)     https://site.commx_genre   ❌ 静默失败

  getMyVar(&quot;hm_mx_genre&quot;,&quot;&quot;)       hm_mx_genre                ✅ 正常
  ------------------------------------------------------------------------------------------------------

> ⚠️ **警告:** getMyVar/putMyVar 的 key
> 只能用纯字母、数字、下划线。建议用站点缩写做前缀，如 hm\_、cz\_、xo\_
> 等。（Law 51）

**18.2 lazyRule 传对象------最隐蔽的 Bug**

lazyRule 的参数只能传基础类型。传对象时会被序列化为 &quot;\[object
Object\]&quot;，fetch 的 headers
参数收到字符串，请求头完全失效，表现为视频解析失败返回 #嗅探，极难定位。

> // ❌ 错误：headers 对象传进去变成字符串 &quot;\[object Object\]&quot;
>
> var headers = { &quot;User-Agent&quot;: \_ua, &quot;Referer&quot;:
> host+&quot;/&quot; };
>
> url: \$(link).lazyRule(function(url, reqHeaders) {
>
> var html = fetch(url, { headers: reqHeaders }); // reqHeaders
> 是字符串！
>
> }, link, headers)
>
> // ✅ 正确：只传字符串，内部自己拼 headers
>
> url: \$(link).lazyRule(function(url, ua, host) {
>
> var html = fetch(url, { headers: { &quot;User-Agent&quot;: ua,
> &quot;Referer&quot;: host+&quot;/&quot; } });
>
> }, link, \_ua, \_host)

**18.3 URL 参数顺序------服务端可能有严格要求**

同样的参数内容，顺序不同可能导致服务端返回空。调试标准手段：浏览器手动操作一次，抓真实请求
URL 解码后和代码生成的逐字对比。

> // ❌ tags\[\] 放在 sort/date/duration 后面 → 返回空列表
>
> url = host + &quot;/search?genre=&quot; + genre
>
> \+ &quot;&sort=&quot; + sort + &quot;&date=&quot; + date +
> &quot;&tags\[\]=&quot; + tag;
>
> // ✅ 对照实测有效 URL：genre → tags\[\] → sort → date → duration
>
> url = host + &quot;/search?query=&type=&genre=&quot; + genre;
>
> for (var i = 0; i \< tags.length; i++) {
>
> url += &quot;&tags\[\]=&quot; + encodeURIComponent(tags\[i\]);
>
> }
>
> url += &quot;&sort=&quot; + sort + &quot;&date=&quot; + date +
> &quot;&duration=&quot; + duration;
>
> 📌 **空值参数也要传:** 接口有时要求参数键名完整，即使值为空也必须写出
> sort=&date=&duration=，缺少键名会导致返回空。（Law 54）

**18.4 多选参数 tags\[\] 的编码规则**

  -------------------------------------------------------------------------
  **格式**                **适用场景**         **说明**
  ----------------------- -------------------- ----------------------------
  &tags\[\]=value         第1页（推荐）        方括号本身不编码，只对值
                                               encodeURIComponent

  &tags%5B%5D=value       ---                  部分服务端不兼容，避免使用

  &tags%5B0%5D=value      第2页+（部分站点）   带下标，翻页时格式可能变化
  -------------------------------------------------------------------------

> // 第1页：tags\[\]= 不带下标
>
> for (var i = 0; i \< tags.length; i++) {
>
> url += &quot;&tags\[\]=&quot; + encodeURIComponent(tags\[i\]);
>
> }
>
> // 第2页+：tags\[0\]= 带下标（对照实测 URL）
>
> for (var i = 0; i \< tags.length; i++) {
>
> url += &quot;&tags%5B&quot; + i + &quot;%5D=&quot; +
> encodeURIComponent(tags\[i\]);
>
> }

**18.5 广告污染过滤**

部分站点搜索结果页混入赞助商广告，href
指向第三方域名，和真实卡片结构完全一样，pdfa 无法区分。

> for (var i = 0; i \< items.length; i++) {
>
> var link = pd(items\[i\], &quot;a&&href&quot;,
> host).split(&quot;#&quot;)\[0\];
>
> if (!link \|\| link.indexOf(&quot;/watch&quot;) == -1) continue;
>
> if (link.indexOf(host) == -1) continue; // ✅ Law
> 56：过滤第三方广告链接
>
> }

**18.6 多模式主页------命名空间设计规范**

多模式规则（视频/漫画/标签/角色\...）各模式的筛选状态用不同 key
存储，切换模式时 Row0 的 lazyRule 统一重置所有相关 key。

> // Row0 lazyRule：切换模式时重置所有子状态
>
> if (rIdx == 0) {
>
> putMyVar(h+&quot;cindex1&quot;,&quot;0&quot;);
> putMyVar(h+&quot;curl1&quot;,&quot;&quot;);
>
> putMyVar(h+&quot;cindex2&quot;,&quot;0&quot;);
> putMyVar(h+&quot;curl2&quot;,&quot;&quot;);
>
> var resetKeys =
> \[&quot;mx_genre&quot;,&quot;mx_date&quot;,&quot;mx_duration&quot;,&quot;mx_sort&quot;,
>
> &quot;mx_tags_sel_attr&quot;,&quot;mx_tags_sel_rel&quot;,&quot;mx_tags_sel_role&quot;\];
>
> for (var ri = 0; ri \< resetKeys.length; ri++) {
>
> putMyVar(h + resetKeys\[ri\], &quot;&quot;);
>
> putMyVar(h + resetKeys\[ri\] + &quot;\_idx&quot;, &quot;0&quot;);
>
> }
>
> putMyVar(h+&quot;sel_characters&quot;,&quot;&quot;);
> putMyVar(h+&quot;sel_groups&quot;,&quot;&quot;);
>
> }

**18.7 多选标签行------renderMulti 设计规范**

> var renderMulti = function(key, titleStr, valueStr, d, h) {
>
> var tArr = titleStr.split(&quot;&&quot;);
>
> var vArr = valueStr.split(&quot;&&quot;);
>
> var selStr = getMyVar(h + key, &quot;&quot;);
>
> var selArr = selStr ? selStr.split(&quot;,&quot;).filter(function(s){
> return s!==&quot;&quot;; }) : \[\];
>
> for (var i = 0; i \< tArr.length; i++) {
>
> var isSel = selArr.indexOf(vArr\[i\]) \> -1;
>
> d.push({
>
> title: isSel ? &quot;\\u2705 &quot; + tArr\[i\] : tArr\[i\],
>
> url: \$(&quot;#noLoading#&quot;).lazyRule(function(k, val, h) {
>
> var cur = getMyVar(h+k, &quot;&quot;);
>
> var arr = cur ? cur.split(&quot;,&quot;).filter(function(s){return
> s!==&quot;&quot;;}) : \[\];
>
> var idx = arr.indexOf(val);
>
> if (idx \> -1) arr.splice(idx, 1); else arr.push(val);
>
> putMyVar(h+k, arr.join(&quot;,&quot;));
>
> refreshPage(false); return &quot;hiker://empty&quot;;
>
> }, key, vArr\[i\], h),
>
> col_type: &quot;scroll_button&quot;
>
> });
>
> }
>
> // ✅ Law 57：多选行末尾必须加清空按钮
>
> d.push({
>
> title: &quot;\\uD83D\\uDDD1\\uFE0F \\u6E05\\u7A7A&quot;,
>
> url: \$(&quot;#noLoading#&quot;).lazyRule(function(k,h){
>
> putMyVar(h+k,&quot;&quot;); refreshPage(false); return
> &quot;hiker://empty&quot;;
>
> }, key, h),
>
> col_type: &quot;scroll_button&quot;
>
> });
>
> d.push({ col_type: &quot;blank_block&quot; });
>
> };
>
> // 合并多个分组的多选值 → 统一 \_allTags 数组
>
> var \_keys =
> \[&quot;hm_mx_tags_sel_attr&quot;,&quot;hm_mx_tags_sel_rel&quot;,&quot;hm_mx_tags_sel_role&quot;\];
>
> var \_allTags = \[\];
>
> for (var ki = 0; ki \< \_keys.length; ki++) {
>
> var \_s = getMyVar(\_keys\[ki\], &quot;&quot;);
>
> if (\_s) \_s.split(&quot;,&quot;).forEach(function(v){ if(v)
> \_allTags.push(v); });
>
> }

**18.8 进二级 vs 直接播放------判断规范**

  ------------------------------------------------------------------------------
  **情况**                     **url 写法**            **说明**
  ---------------------------- ----------------------- -------------------------
  单集视频，直接可播           \$(link).lazyRule(fn,   lazyRule 直接返回
                               link, ua, host)         video://

  多集系列（需展示集数列表）   url: link（进二级）     二级返回
                                                       line/list，各集再解析

  漫画/小说章节                url: link（进二级）     二级 list 的 url 加
                                                       #isJiexi=1
  ------------------------------------------------------------------------------

> // 按分类判断：里番/泡面番/预告有多集→进二级，其他→直接 lazyRule
>
> var needEpisode = (c1 == &quot;\\u88CF\\u756A&quot; \|\| c1 ==
> &quot;\\u6CE1\\u9EB5\\u756A&quot; \|\| c1 == &quot;previews&quot;);
>
> \_res.push({
>
> title: tit, pic_url: pic,
>
> url: needEpisode ? link : \$(link).lazyRule(function(url, ua, host) {
>
> var html = request(url, { headers:
> {&quot;User-Agent&quot;:ua,&quot;Referer&quot;:host+&quot;/&quot;} });
>
> var m1080 =
> html.match(/\<source\\s+src=&quot;(\[\^&quot;\]+)&quot;\[\^\>\]+size=&quot;1080&quot;/i);
>
> var m720 =
> html.match(/\<source\\s+src=&quot;(\[\^&quot;\]+)&quot;\[\^\>\]+size=&quot;720&quot;/i);
>
> var vSrc = m1080 ? m1080\[1\] : (m720 ? m720\[1\] : null);
>
> if (!vSrc) { var
> fb=html.match(/\<source\\s+src=&quot;(\[\^&quot;\]+)&quot;/i);
> vSrc=fb?fb\[1\]:null; }
>
> if (!vSrc) return url+&quot;#\\u5605\\u63A2&quot;;
>
> var slash = String.fromCharCode(92);
>
> return
> &quot;video://&quot;+vSrc.split(slash).join(&quot;&quot;)+&quot;;{User-Agent@&quot;+ua+&quot;&&Referer@&quot;+host+&quot;/}&quot;;
>
> }, link, \_ua, \_host),
>
> col_type: &quot;movie_2&quot;
>
> });

**18.9 标签 value 必须从源码 input 取------不能猜**

多选标签的 value 必须从站点 HTML 源码中 \<input
name=&quot;tags\[\]&quot; value=&quot;xxx&quot;\> 的 value
属性取，不能用页面显示的中文文字替代。

  ------------------------------------------------------------------------
  **显示文字**         **实际 value**       **错误写法后果**
  -------------------- -------------------- ------------------------------
  姐姐                 姐                   value:&quot;姐姐&quot; →
                                            接口返回空

  处女                 處女                 value:&quot;处女&quot; →
                                            接口返回空

  护士                 護士                 value:&quot;护士&quot; →
                                            接口返回空

  2D动画               2D動畫               value:&quot;2D动画&quot; →
                                            接口返回空

  巫女                 巫女                 ✅ 一致，不需要转换
  ------------------------------------------------------------------------

> // ✅ title 显示简体，value 用源码 input value 属性原值（繁体）
>
> renderMulti(&quot;mx_tags_sel&quot;,
>
> &quot;处女&护士&姐姐&2D动画&quot;, // title：给用户看的简体
>
> &quot;\\u8655\\u5973&\\u8B77\\u58EB&\\u59D0&2D\\u52D5\\u756畫&quot;,
> // value：源码繁体原值
>
> \_res, &quot;hm\_&quot;);
>
> ⚠️ **警告:** 开发前必须打开站点筛选面板，右键检查 input 元素，取 value
> 属性值，不能靠简繁转换猜测。（Law 58）

**18.10 本章新增铁律（Law 51-58）**

  ----------------------------------------------------------------------------------------------------
  **编号**   **铁律名称**       **核心要义**                       **关键口诀**
  ---------- ------------------ ---------------------------------- -----------------------------------
  Law 51     key 纯字母原则     key 只用纯字母+下划线，禁用 host   用站点缩写前缀 hm\_/cz\_/xo\_
                                拼接                               

  Law 52     lazyRule 禁传对象  只传字符串/数字/布尔，对象变       headers 在 lazyRule 内自己拼
                                \[object Object\]                  

  Law 53     参数顺序对照实测   顺序错误导致返回空，必须对照真实   浏览器抓包→解码→逐字对比
                                URL                                

  Law 54     空值参数显式传     空值也必须写                       不传 ≠ 空字符串
                                sort=&date=，缺键名服务端报错      

  Law 55     tags\[\]           方括号不编码，只编码值             &tags\[\]=encodeURIComponent(val)
             不编码括号                                            

  Law 56     广告链接过滤       link.indexOf(host)==-1 则跳过      防第三方广告混入

  Law 57     多选行加清空按钮   renderMulti 末尾必须有清空按钮     否则用户无法取消已选

  Law 58     value 从源码 input 必须取 input value 属性，不能猜    右键检查→value 属性
             取                                                    
  ----------------------------------------------------------------------------------------------------

> **第十九章：视频站实战总结（17AV 案例）**

本章基于 17av.one 规则开发过程提炼，新增 Law 59-60 及视频解析提速套路。

**19.1 fyclass 模式下 MY_URL 是路径，不是索引（Law 60）**

fyclass 模式下 MY_URL 的值是 class_url 里对应的路径字符串（如
/categories/all），不是分类的数字下标。parseInt() 对路径字符串返回
NaN，导致永远只请求第0个分类。

> // ❌ 致命错误：parseInt(&quot;/categories/all&quot;) =
> NaN，classIndex 永远是 0
>
> var classIndex = parseInt(MY_URL) \|\| 0;
>
> var baseUrl = host + classUrls\[classIndex\]; // 永远第0个分类
>
> // ✅ 正确：MY_URL 就是路径，直接拼接
>
> var path = (MY_URL === &quot;fyclass&quot; \|\| !MY_URL) ?
> &quot;/categories/all&quot; : MY_URL;
>
> var baseUrl = host + path;
>
> // 翻页
>
> if (MY_PAGE \> 1) {
>
> baseUrl += (baseUrl.indexOf(&quot;?&quot;) \> -1 ? &quot;&&quot; :
> &quot;?&quot;) + &quot;page=&quot; + MY_PAGE;
>
> }
>
> ⚠️ **警告:** fyclass 模式 MY_URL = class_url 里的路径值，直接用，禁止
> parseInt()。（Law 60）

**19.2 Cookie 禁入 video:// 协议头（Law 59）**

Cookie 字符串含有 ;、=、空格等特殊字符，拼进 video://
协议头会破坏解析，导致播放失败。Cookie 只在 request()
阶段用于获取播放地址，不传给播放器。

> // ❌ Cookie 拼进协议头，特殊字符破坏解析
>
> return &quot;video://&quot; + url + &quot;;{Cookie@&quot; + cookie +
> &quot;&&Referer@&quot; + host + &quot;}&quot;;
>
> // ✅ 播放器只传 Origin/Referer/UA，Cookie 只用于 request() 阶段
>
> var h = request(detailUrl, {
>
> headers: { &quot;Cookie&quot;: cookie, &quot;User-Agent&quot;: ua,
> &quot;Referer&quot;: host+&quot;/&quot; }
>
> });
>
> // \... 从 h 提取 finalPlayUrl \...
>
> return &quot;video://&quot; + finalPlayUrl
>
> \+ &quot;;{Origin@&quot; + host
>
> \+ &quot;&&Referer@&quot; + detailUrl
>
> \+ &quot;&&User-Agent@&quot; + ua + &quot;}&quot;;

**19.3 视频解析提速套路------hash 从 img 取，token 从详情页取**

很多视频站的图片 URL 里已经包含了视频
hash，不需要从详情页再找一遍。列表构建时缓存 img URL，lazyRule
里直接提取，只从详情页取必须实时获取的签名 token。

> // ── 列表构建时：缓存 img URL，供 lazyRule 使用 ──
>
> var img = pdfh(item, &quot;img&&data-src&quot;) \|\| pdfh(item,
> &quot;img&&src&quot;) \|\| &quot;&quot;;
>
> if (img) putVar(&quot;site_img\_&quot; + fullLink, img); // key =
> 站点缩写+链接
>
> // ── lazyRule 内：分工提取 ──
>
> // Step1: hash 从 img 取（不需要请求详情页）
>
> var \_imgUrl = getVar(&quot;site_img\_&quot; + input, &quot;&quot;);
>
> var \_hash = &quot;&quot;;
>
> if (\_imgUrl) {
>
> var \_hm = \_imgUrl.match(//videos/(\[a-f0-9\]{32,64})//);
>
> if (\_hm) \_hash = \_hm\[1\];
>
> }
>
> // Step2: 请求详情页，只取前 8000 字符，优先匹配完整 m3u8 地址
>
> var html = request(input, { headers: { \... } });
>
> var htmlShort = html.substring(0, 8000); // hash/token 通常在页面头部
>
> // 优先：直接找完整 m3u8（含 token）
>
> var m3u8Full =
> htmlShort.match(/https?:\\/\\/\[\^\\s&quot;\]+\\.m3u8\[\^\\s&quot;\]\*/);
>
> if (!m3u8Full) m3u8Full =
> html.match(/https?:\\/\\/\[\^\\s&quot;\]+\\.m3u8\[\^\\s&quot;\]\*/);
>
> if (m3u8Full) {
>
> finalPlayUrl = m3u8Full\[1\]; // 直接用，最准确
>
> } else if (\_hash) {
>
> // 从页面提取 h= token，拼接播放地址
>
> var tokenM = html.match(/\[?&\]h=(\[a-f0-9\]{8,})/);
>
> finalPlayUrl = &quot;https://cdn.example.com/videos/&quot; + \_hash +
> &quot;/g.m3u8&quot;
>
> \+ (tokenM ? &quot;?h=&quot; + tokenM\[1\] : &quot;&quot;);
>
> }

  ------------------------------------------------------------------------
  **信息**             **来源**                **是否需要请求详情页**
  -------------------- ----------------------- ---------------------------
  视频 hash（32/64位） img URL 正则提取        ❌ 不需要（列表页已有）

  签名 token（h=xxx）  详情页实时提取          ✅ 需要（每次不同）

  完整 m3u8 地址       详情页直接匹配          ✅ 需要，但只取前 N 字符
  ------------------------------------------------------------------------

> 💡 **调试方法:** 先在列表构建时 log 出前3条 href 和 img，观察 img URL
> 是否含 hash；再在 lazyRule 里 log 出 input（详情页 URL）和最终 m3u8
> 地址，确认格式后删除 log。

**19.4 新增铁律（Law 59-60）**

  ------------------------------------------------------------------------------------------
  **编号**   **铁律名称**       **核心要义**             **关键口诀**
  ---------- ------------------ ------------------------ -----------------------------------
  Law 59     Cookie 禁入协议头  Cookie 只用于            特殊字符破坏协议头解析
                                request()，不拼进        
                                video://                 

  Law 60     fyclass MY_URL     MY_URL = class_url       parseInt(path)=NaN，永远第0个分类
             是路径             路径值，直接用，禁止     
                                parseInt()               
  ------------------------------------------------------------------------------------------

> **第二十章：API 接口型源------纯 JSON 接口写法**

与 HTML 抓取型源不同，API 型源通过 POST/GET 请求 JSON
接口获取数据，速度更快，结构更稳定。以熊猫 APP 类型为例，整理核心写法。

**20.1 整体架构特征**

  -----------------------------------------------------------------------
  **特征**              **HTML 抓取型**          **API 接口型**
  --------------------- ------------------------ ------------------------
  数据获取              fetch HTML + pdfa/pdfh   fetch/post JSON +
                        解析                     JSON.parse

  域名切换              改 host 属性             改 静态分类.url 里的 API
                                                 地址

  播放解析              lazyRule 请求播放页提取  从封面 URL 推导 / 从 API
                        m3u8                     字段提取

  稳定性                站点改版即失效           接口变化较少，更稳定

  速度                  慢（需抓页面）           快（直接 JSON）
  -----------------------------------------------------------------------

**20.2 API 地址藏在静态分类 URL 里**

把 API 地址嵌入 静态分类.url 的 \## 分隔段中，通过
MY_URL.split(&quot;##&quot;) 拆出，换域名只需改 url 字段，不改代码逻辑。

> 静态分类: {
>
> type: &quot;主页&quot;,
>
> // 格式：hiker://empty##API地址##fyAll（三段，##分隔）
>
> url:
> &quot;hiker://empty##https://api.example.com/forward##fyAll&quot;,
>
> class_name: &quot;传媒&91&精东&麻豆&quot;,
>
> class_url: &quot;1&6&7&8&quot;, // 分类 ID
>
> area_name: &quot;视频&自拍&欧美&quot;,
>
> area_url: &quot;2&14&16&quot;, // 类型 ID
>
> },
>
> 主页: function() {
>
> var parts = MY_URL.split(&quot;##&quot;);
>
> var apiUrl = parts\[1\]; // API 地址
>
> var typeId = parts\[2\]; // 当前分类 ID（fyAll 时为默认值）
>
> // \...
>
> }

**20.3 POST 请求 JSON 接口**

> // ✅ 新版海阔可用 ES6
> 安全子集（const/let/箭头函数）；聚阅/旧版引擎须用 ES5（var）。见 Law
> 34 更新（第三十一章）
>
> var json = fetch(apiUrl, {
>
> headers: { &quot;content-type&quot;: &quot;application/json&quot; },
>
> body: {
>
> &quot;command&quot;: &quot;WEB_GET_INFO&quot;,
>
> &quot;pageNumber&quot;: MY_PAGE,
>
> &quot;RecordsPage&quot;: 20,
>
> &quot;typeId&quot;: typeId,
>
> &quot;typeMid&quot;: 1,
>
> &quot;languageType&quot;: &quot;CN&quot;,
>
> &quot;content&quot;: &quot;&quot;
>
> },
>
> method: &quot;POST&quot;
>
> });
>
> var list = JSON.parse(json).data.resultList;
>
> ⚠️ **警告:** body 传对象时引擎自动序列化为 JSON，不需要
> JSON.stringify()。headers 里的 content-type
> 是给服务端看的编码声明，不是请求体格式。

**20.4 视频直链从封面 URL 推导（最快方案）**

很多 API 型源的 CDN 路径有规律：封面图和 m3u8
在同一目录下，只是文件名不同。可以直接替换文件名得到播放地址，完全跳过
lazyRule 请求播放页。

> // 封面图路径：https://cdn.example.com/2026/abc123/1.jpg
>
> // 播放地址： https://cdn.example.com/2026/abc123/playlist.m3u8
>
> for (var i = 0; i \< list.length; i++) {
>
> var item = list\[i\];
>
> d.push({
>
> title: item.vod_name,
>
> pic_url: item.vod_pic,
>
> // ✅ 从封面 URL 直接推导播放地址，无需任何网络请求
>
> url: item.vod_pic.replace(&quot;/1.jpg&quot;,
> &quot;/playlist.m3u8&quot;),
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> 💡 **如何发现这个规律:** 打开一个视频，用浏览器开发者工具 Network
> 面板过滤 .m3u8，看 CDN
> 路径是否和封面图路径同目录。如果是，直接替换文件名即可，这是最快的播放方案。

**20.5 图片类 lazyRule------从 API 字段提取图片列表**

图片型内容（type=2）的图片 URL 藏在 API
返回的某个字段里，通常是一段含多个 URL
的字符串，用正则提取后过滤广告图。

> // ✅ 提前生成 lazyRule 字符串，循环内复用（不在循环里每次调用）
>
> var lazy = \$(&quot;&quot;).lazyRule(function(apiUrl) {
>
> // input = 图片 ID（list\[i\].id）
>
> var json = fetch(apiUrl, {
>
> body: { &quot;command&quot;: &quot;WEB_GET_INFO_DETAIL&quot;,
> &quot;id&quot;: input, &quot;languageType&quot;: &quot;CN&quot; },
>
> method: &quot;POST&quot;
>
> });
>
> var artUrl = JSON.parse(json).data.result.art_url;
>
> // art_url 是含多个图片地址的字符串，正则提取所有 jpg URL
>
> var pics = artUrl.match(/https?\[\^s&quot;\]+.jpg/g) \|\| \[\];
>
> // 过滤广告图（根据实测找出广告域名/路径特征）
>
> pics = pics.filter(function(u) { return u.indexOf(&quot;adcdn&quot;)
> === -1; });
>
> if (pics.length === 0) return &quot;toast://未找到图片&quot;;
>
> return &quot;pics://&quot; + pics.join(&quot;&&quot;);
>
> }, apiUrl); // ✅ apiUrl 通过参数传入，不用 this
>
> for (var i = 0; i \< list.length; i++) {
>
> d.push({
>
> title: list\[i\].art_name,
>
> pic_url: list\[i\].art_pic,
>
> url: list\[i\].id + lazy, // ✅ id 直接拼接 lazy 字符串
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }

**20.6 用分类 ID 区分内容类型**

同一个接口，typeId 不同代表不同内容类型（视频/图片），主页函数根据 ID
范围判断，走不同的渲染逻辑。

> // ✅ 用 \|\| 逻辑或，不用 \| 按位或（\|
> 是位运算，语义上是错的，凑巧能跑）
>
> var isImg = (typeId == 4 \|\| typeId \> 29);
>
> if (!isImg) {
>
> // 视频：从封面推导 m3u8
>
> for (var i = 0; i \< list.length; i++) {
>
> d.push({
>
> title: list\[i\].vod_name,
>
> url: list\[i\].vod_pic.replace(&quot;/1.jpg&quot;,
> &quot;/playlist.m3u8&quot;),
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> } else {
>
> // 图片：lazyRule POST API 取图片列表
>
> var lazy = \$(&quot;&quot;).lazyRule(function(apiUrl) { /\* \... \*/
> }, apiUrl);
>
> for (var i = 0; i \< list.length; i++) {
>
> d.push({ title: list\[i\].art_name, url: list\[i\].id + lazy });
>
> }
>
> }

**20.7 完整 ES5 规范模板**

> var parse = {
>
> 作者: null,
>
> 版本: &quot;1.0&quot;,
>
> host: &quot;&quot;, // API 地址藏在静态分类 url 里，host 可留空
>
> 页码: { 主页: 1 },
>
> 静态分类: {
>
> type: &quot;主页&quot;,
>
> url: &quot;hiker://empty##https://api.example.com/data##fyAll&quot;,
>
> class_name: &quot;传媒&视频&图片&quot;,
>
> class_url: &quot;1&2&4&quot;
>
> },
>
> 主页: function() {
>
> var d = \[\];
>
> var parts = MY_URL.split(&quot;##&quot;);
>
> var apiUrl = parts\[1\];
>
> var typeId = parseInt(parts\[2\]) \|\| 1;
>
> var isImg = (typeId == 4 \|\| typeId \> 29);
>
> var json = fetch(apiUrl, {
>
> body: { &quot;command&quot;: &quot;WEB_GET_INFO&quot;,
> &quot;pageNumber&quot;: MY_PAGE,
>
> &quot;RecordsPage&quot;: 20, &quot;typeId&quot;: typeId,
>
> &quot;typeMid&quot;: isImg ? 2 : 1, &quot;languageType&quot;:
> &quot;CN&quot; },
>
> method: &quot;POST&quot;
>
> });
>
> var list = JSON.parse(json).data.resultList \|\| \[\];
>
> if (!isImg) {
>
> for (var i = 0; i \< list.length; i++) {
>
> d.push({
>
> title: list\[i\].vod_name,
>
> pic_url: list\[i\].vod_pic,
>
> url: list\[i\].vod_pic.replace(&quot;/1.jpg&quot;,
> &quot;/playlist.m3u8&quot;),
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> } else {
>
> var lazy = \$(&quot;&quot;).lazyRule(function(api) {
>
> var res = fetch(api, {
>
> body: { &quot;command&quot;: &quot;WEB_GET_INFO_DETAIL&quot;,
> &quot;id&quot;: input,
>
> &quot;type_Mid&quot;: &quot;2&quot;, &quot;languageType&quot;:
> &quot;CN&quot; },
>
> method: &quot;POST&quot;
>
> });
>
> var artUrl = JSON.parse(res).data.result.art_url \|\| &quot;&quot;;
>
> var pics = (artUrl.match(/https?\[\^s&quot;\]+.jpg/g) \|\| \[\])
>
> .filter(function(u){ return u.indexOf(&quot;adcdn&quot;) === -1; });
>
> if (pics.length === 0) return &quot;toast://未找到图片&quot;;
>
> return &quot;pics://&quot; + pics.join(&quot;&&quot;);
>
> }, apiUrl);
>
> for (var i = 0; i \< list.length; i++) {
>
> d.push({
>
> title: list\[i\].art_name,
>
> pic_url: list\[i\].art_pic,
>
> url: list\[i\].id + lazy,
>
> col_type: &quot;movie_2&quot;
>
> });
>
> }
>
> }
>
> return d;
>
> }
>
> };

*文档版本 V3.6 · 2026年3月 · 新增第二十章 API 接口型源写法 ·
基于海阔视界底层*

> **第二十一章：hikerPop 原生 UI 弹窗库**

hikerPop 是海阔视界/聚阅的第三方原生 Android UI 弹窗组件库，作者
LoyDgIk。通过 Java 反射调用 App
内部组件，在规则里弹出精美的交互弹窗，极大提升用户体验。运行要求 Android
8.0（API 26）及以上。

> ⚠️ 警告：hikerPop 使用了 const / let / 箭头函数等 ES6+
> 语法，是作为独立依赖库加载的，不在 parse 对象内运行，因此不受 ES5
> 限制。在你的规则里调用时仍需用 var。

**21.1 引入方式**

将 hikerPop 作为依赖页面导入规则后，在规则代码中使用 \$.require 引入：

> // 在规则顶部引入
>
> var pop = \$.require(&quot;hiker://page/hikerPop&quot;);
>
> // 之后即可调用所有弹窗函数
>
> pop.selectBottom({ title: &quot;选择线路&quot;, options:
> \[&quot;线路1&quot;, &quot;线路2&quot;\], click: function(v, i) {
> log(v); } });

**21.2 弹窗函数速查总表**

**选择类弹窗：**

-   selectCenter({options, click, title, columns, position}) →
    居中网格选择弹窗

-   selectBottom({options, click, title, columns, height}) →
    底部网格选择弹窗

-   selectCenterMark({options, click, title, position}) →
    居中带勾选标记的列表

-   selectBottomMark({options, click, title, position}) →
    底部带勾选标记的列表

-   selectCenterIcon({iconList, click, title, columns}) →
    居中图标选择弹窗（iconList 含 title/url/icon）

-   selectBottomRes({options, click, title, extraInputBox}) →
    底部列表，支持搜索框扩展（ResExtraInputBox）

-   selectBottomResIcon({iconList, click, allowDrag, drag}) →
    底部图标列表，支持拖拽排序

-   collectBottom({list, click, title}) → 底部收藏样式列表（list 含
    title/pic_url/tag/info/desc）

-   FlexMenuBottom({sections, click, title}) → 底部分组标签菜单（配合
    FlexSection 使用）

**输入类弹窗：**

-   inputAutoRow({title, hint, confirm, cancel, defaultValue}) →
    单行输入框弹窗

-   inputTwoRow({title, titleHint, urlHint, confirm}) → 双行输入框弹窗

-   inputConfirm({title, content, hint, confirm, textarea}) →
    带说明文字的输入确认弹窗

-   inputConfirmSync({title, hint, defaultValue}) →
    同步输入弹窗（阻塞线程，直接返回输入字符串）

**确认/多选/滑块类：**

-   confirm({title, content, confirm, cancel, okTitle, cancelTitle}) →
    确认/取消弹窗

-   confirmSync({title, content, okTitle, cancelTitle}) →
    同步确认弹窗（返回 true/false）

-   multiChoice({title, options, checkedIndexs, onChoice}) →
    多选弹窗（AlertDialog 样式）

-   seekCenter({title, max, pos, onChange, confirm}) → 滑块进度选择弹窗

**其他工具：**

-   loading(title) → 显示加载中弹窗，返回 pop 对象，调用 pop.dismiss()
    关闭

-   copyBottom(title, content) → 底部内容复制弹窗

-   infoBottom({title, options, click}) → 底部信息展示列表

-   toastMeg(text, type) → 增强 Toast（type: LC=长居中 SC=短居中
    LB=长底部 SB=短底部）

-   playVideos(playList, pos) → 直接调用播放器播放视频列表（playList 含
    title/url 等字段）

-   scrollSmooth(idOrPos, isScroll) → 滚动主列表到指定项（传 id 字符串或
    position 数字）

-   runOnNewThread(func) → 在新线程异步执行函数

-   runOnUIThread(func) → 在 UI 线程执行函数（操作界面必须用这个）

-   getClipTopData() → 获取剪贴板内容，返回字符串

-   sendDanmaku({text, size, color, type, time}) → 向当前播放器发送弹幕

-   showRhinoUI(uiObj, {type, width, height}) →
    显示自定义布局弹窗（type: center 或 bottom）

**21.3 FlexMenuBottom 分组标签菜单（详解）**

FlexMenuBottom
是功能最强大的弹窗，适合多维度切换场景（如同时选择画质+线路）。每个
FlexSection 是一个分组，分组内包含多个按钮标签。

> var pop = \$.require(&quot;hiker://page/hikerPop&quot;);
>
> var FlexSection = pop.FlexMenuBottom.FlexSection;
>
> pop.FlexMenuBottom({
>
> title: &quot;播放设置&quot;,
>
> sections: \[
>
> new FlexSection(&quot;画质选择&quot;, \[&quot;1080P&quot;,
> &quot;720P&quot;, &quot;480P&quot;\]),
>
> new FlexSection(&quot;播放线路&quot;, \[&quot;线路A&quot;,
> &quot;线路B&quot;, &quot;线路C&quot;\]),
>
> \],
>
> click: function(button, sectionIndex, buttonIndex) {
>
> // button.title = 按钮文字
>
> log(&quot;点击分组&quot; + sectionIndex + &quot;按钮&quot; +
> buttonIndex + &quot;: &quot; + button.title);
>
> },
>
> longClick: function(button, sectionIndex, buttonIndex) {
>
> log(&quot;长按: &quot; + button.title);
>
> },
>
> height: 0.6 // 弹窗高度占屏幕比例，默认 0.75
>
> });

**21.4 同步弹窗（在 lazyRule 中使用）**

confirmSync 和 inputConfirmSync
会阻塞当前线程直到用户操作完成，非常适合在 lazyRule 的 .js:
代码块中做交互式选择。

> // 在二级页面的 url 的 lazyRule 中使用同步弹窗选择线路
>
> url: videoUrl + &quot;@lazyRule=.js:&quot; +
>
> &quot;var pop = \$.require(\'hiker://page/hikerPop\');&quot; +
>
> &quot;var ok =
> pop.confirmSync({title:\'提示\',content:\'需要付费，是否继续？\'});&quot;
> +
>
> &quot;if (!ok) return \'toast://已取消\';&quot; +
>
> &quot;return input;&quot;

**21.5 带搜索框的底部列表（ResExtraInputBox）**

selectBottomRes 支持附加一个搜索/输入框到弹窗内，通过 ResExtraInputBox
实现：

> var pop = \$.require(&quot;hiker://page/hikerPop&quot;);
>
> var ResExtraInputBox = pop.ResExtraInputBox; // 注意从 exports 取
>
> var searchBox = new ResExtraInputBox({
>
> hint: &quot;搜索集数\...&quot;,
>
> title: &quot;跳转&quot;,
>
> click: function(inputText, manage) {
>
> // inputText = 用户输入内容
>
> // manage.scrollToPosition(Number(inputText) - 1, true);
>
> log(&quot;跳转到: &quot; + inputText);
>
> }
>
> });
>
> pop.selectBottomRes({
>
> title: &quot;选择集数&quot;,
>
> options: \[&quot;第1集&quot;, &quot;第2集&quot;, &quot;第3集&quot;\],
>
> columns: 4,
>
> extraInputBox: searchBox,
>
> click: function(value, index, manage) {
>
> log(&quot;选择: &quot; + value);
>
> }
>
> });

**21.6 playVideos 直接调用播放器**

无需跳转页面，直接在弹窗回调中启动播放器，适合多线路切换：

> var pop = \$.require(&quot;hiker://page/hikerPop&quot;);
>
> // playList 格式
>
> var playList = \[
>
> { title: &quot;线路1&quot;, url:
> &quot;https://example.com/video1.m3u8&quot;, header: &quot;Referer:
> https://example.com/&quot; },
>
> { title: &quot;线路2&quot;, url:
> &quot;https://example.com/video2.m3u8&quot; },
>
> \];
>
> // 直接播放第0条
>
> pop.playVideos(playList, 0);
>
> // 配合选择弹窗使用
>
> pop.selectCenter({
>
> title: &quot;选择线路&quot;,
>
> options: playList.map(function(v) { return v.title; }),
>
> click: function(value, index) {
>
> pop.playVideos(playList, index);
>
> }
>
> });

**21.7 注意事项**

> ⚠️ 节流保护：所有弹窗函数内置 200ms
> 节流，短时间内重复点击会被忽略。如需调整，在调用前执行
> pop.setNextThrottle(500) 设置下次节流时长（毫秒）。
>
> ⚠️ UI 线程：直接操作界面的代码（如修改按钮文字）必须在 runOnUIThread
> 内执行，否则崩溃。
>
> ⚠️ 嗅觉浏览器适配：hikerPop 会自动检测并适配嗅觉浏览器环境，copyBottom
> 在嗅觉浏览器下返回 null。
>
> 📌 Android 版本要求：hikerPop 需要 Android 8.0（API
> 26）及以上，低版本会直接抛错。
>
> **第二十二章：RhinoUI 原生布局渲染库**

RhinoUI 是海阔视界/聚阅的高级 UI 框架，通过 E4X XML 语法在规则 JS
环境中直接描述 Android 原生布局，动态创建并操作 View 控件。配合 hikerPop
的 showRhinoUI() 可弹出任意自定义界面，是目前最灵活的规则 UI 方案。

> ⚠️ RhinoUI 使用 E4X XML 语法（XML 作为 JS 一等公民），布局 XML
> 不能被代码格式化工具处理，否则会乱码。规则文件请关闭自动格式化。
>
> ⚠️ 所有 UI 操作必须在 UI 线程执行，使用 RhinoUI.runOnUI(fn)
> 包裹，否则崩溃。

**22.1 引入与基本调用**

> const RhinoUI = \$.require(&quot;UI&quot;); // 引入 RhinoUI 依赖页面
>
> // render() 渲染布局，返回 idMap（key=XML中id属性，value=Wrapper对象）
>
> let ui = RhinoUI.render(
>
> \<vertical bg=&quot;#FFFFFF&quot; padding=&quot;16&quot;\>
>
> \<text id=&quot;tvTitle&quot; text=&quot;Hello&quot;
> textSize=&quot;18sp&quot; textStyle=&quot;bold&quot;/\>
>
> \<button id=&quot;btn&quot; text=&quot;点我&quot; margin=&quot;0
> 10&quot;/\>
>
> \</vertical\>
>
> );
>
> ui.tvTitle.text(&quot;修改标题&quot;); // 通过 id 访问并修改
>
> ui.btn.click(() =\> toast(&quot;点击了&quot;));
>
> // 配合 hikerPop 显示为底部弹窗（必须在 UI 线程执行）
>
> RhinoUI.runOnUI(() =\> {
>
> const pop =
> \$.require(&quot;hiker://page/hikerPop.js?rule=hikerPop&quot;);
>
> let popup = pop.showRhinoUI(ui, { type: &quot;bottom&quot;, height:
> 400 });
>
> ui.btn.click(() =\> popup.dismiss());
>
> });

**22.2 支持的控件标签**

**布局容器：**

-   \<vertical\> / \<linear\> → 垂直线性布局（LinearLayout）

-   \<horizontal\> → 水平线性布局

-   \<frame\> → 帧布局（FrameLayout），子控件可叠加

-   \<scroll\> / \<scrollview\> → 垂直滚动容器

-   \<hscroll\> → 水平滚动容器，自动隐藏滚动条

-   \<card\> → 卡片容器，支持 cardElevation / cardCornerRadius 属性

-   \<drawer\> / \<drawerlayout\> → 抽屉布局，配合
    layout_gravity=&quot;start&quot; 实现侧边栏

-   \<toolbar\> → 顶部工具栏，支持 title / titleTextColor 属性

-   \<grad\> / \<gradient\> → 纯色/渐变背景容器，支持 color / radius /
    stroke 属性

**基础控件：**

-   \<text\> → 文本（TextView），支持
    text/textSize/textColor/textStyle/maxLines/ellipsize/typeface

-   \<btn\> / \<button\> → 按钮（Button），继承 text 全部属性

-   \<input\> / \<edit\> → 输入框（EditText），支持
    hint/password/inputType/digits/hintColor

-   \<img\> / \<image\> → 图片（ImageView），支持
    src/scaleType/circle/radius/tint，自动使用 Glide 加载

-   \<fab\> → 悬浮按钮（FloatingActionButton）

-   \<check\> / \<checkbox\> → 复选框，支持 checked 属性

-   \<switch\> → 开关，支持
    thumbTint/trackTint（格式：未选色/选中色，用/分隔）

-   \<radio\> / \<radiogroup\> → 单选按钮 / 单选组

-   \<spinner\> → 下拉菜单，entries=&quot;A\|B\|C&quot; 快速设置选项

-   \<progressbar\> → 进度条，支持 max/progress/indeterminate 属性

-   \<seekbar\> → 拖动条（SeekBar），on(&quot;change&quot;, fn) 监听拖动

-   \<webview\> → 内嵌网页，url/src 属性直接加载 URL

-   \<list\> / \<recyclerview\> →
    列表（RecyclerView），内嵌一个子元素作为 item 模板，span 设置列数

-   \<viewpager\> / \<viewpager2\> →
    左右滑动分页（ViewPager2），内嵌一个子元素作为页面模板

-   \<viewswitcher\> / \<switcher\> → 视图切换器

-   \<canvas\> → 自定义画布，on(&quot;draw&quot;, canvas) 获取绘制回调

-   \<blank\> / \<space\> → 空白占位控件

-   \<divider\> / \<line\> → 分割线（默认 1px 高 / #E0E0E0 颜色 /
    宽度填满）

**22.3 通用属性速查**

**布局属性：**

-   w / width → 宽度：&quot;match_parent&quot; /
    &quot;wrap_content&quot; / &quot;\*&quot; / &quot;-1&quot; /
    &quot;200dp&quot;

-   h / height → 高度，同上

-   margin → 外边距，1/2/4个值（左上右下），如 &quot;8dp&quot; /
    &quot;8dp 4dp&quot; / &quot;左 上 右 下&quot;

-   padding → 内边距，格式同 margin

-   layout_weight / weight → 线性布局权重（配合 w=&quot;0&quot; 或
    h=&quot;0&quot; 使用）

-   layout_gravity → 在父容器中的对齐：center / start / end / bottom 等

-   gravity → 内容对齐（对 LinearLayout / TextView 有效）

**外观属性：**

-   bg / background → 背景色（#RRGGBB）/ ?attr/资源 / \@drawable/资源名

-   radius / cornerRadius → 全圆角，如 &quot;8dp&quot;

-   corners → 四角独立圆角（左上 右上 右下 左下），如 &quot;16dp 16dp 0
    0&quot;

-   stroke → 边框，&quot;宽度 颜色&quot;，如 &quot;1dp #CCCCCC&quot;

-   ripple → 点击水波纹，&quot;true&quot; 或直接写颜色
    &quot;#33000000&quot;

-   alpha → 透明度 0.0\~1.0

-   elevation → 阴影高度（Android 5.0+）

-   rotation → 旋转角度（度）

-   visibility → 可见性：&quot;visible&quot; / &quot;invisible&quot; /
    &quot;gone&quot;

**22.4 Wrapper 对象方法速查**

**通用方法（所有控件）：**

-   .attr(key, value) → 设置属性（等价于 XML 属性）；传入对象可批量设置

-   .attr(key) → 读取已设置的属性值

-   .click(fn) → 设置点击监听

-   .longClick(fn) → 设置长按监听

-   .on(event, fn) → 通用事件监听（\'click\' / \'long_click\' 等）

-   .visibility(v) → 设置可见性：&quot;visible&quot; /
    &quot;invisible&quot; / &quot;gone&quot;

-   .width(w) / .height(h) → 快捷设置尺寸

-   .margin(m) / .padding(p) → 快捷设置边距

-   .gravity(g) → 快捷设置对齐

-   .addView(viewOrXml) → 动态添加子控件（支持 E4X XML / 原生 View /
    Wrapper 对象）

-   .removeAllViews() → 清空所有子控件

-   .post(fn) → 在 View 消息队列中执行（UI 线程安全的异步操作）

-   .raw() → 获取底层 Android 原生 View 对象

**TextWrapper 特有（text/button/input）：**

-   .text(val) → 设置/获取文字内容（无参数为 getter）

-   .textColor(colorStr) → 设置文字颜色

-   .error(msg) → 设置输入框错误提示（msg=null 清除）

-   .setSelection(start, end) → 设置光标/选区位置

**InputWrapper 特有事件（on 方法）：**

-   .on(&quot;change&quot;, fn) → 文字变化后回调，fn(text)，最常用

-   .on(&quot;before_text_change&quot;, fn) → 文字变化前回调

-   .on(&quot;on_text_change&quot;, fn) → 文字变化中回调

-   .off(event) → 移除监听；&quot;all&quot; 移除全部

**CheckWrapper 特有（checkbox/switch/radio）：**

-   .checked(val) → 设置/获取选中状态（无参数为 getter）

-   .on(&quot;change&quot;, fn) → 选中状态变化回调，fn(isChecked)

**ImageWrapper 特有：**

-   .loadUrl(url) → 加载图片（自动 Glide，支持 HTTP/hiker://@drawable/）

-   .setTint(colorStr) → 设置图片着色滤镜

**RecyclerWrapper（list）特有：**

-   .setDataSource(arr) → 设置数据源并触发渲染

-   .setSpanCount(n) → 设置列数（1=列表，n\>1=网格）

-   .addItem(item) → 末尾追加一条数据并刷新

-   .insertItem(index, item) → 指定位置插入数据

-   .removeItem(index) → 删除指定位置数据

-   .updateItem(index, item) → 更新指定位置数据

-   .notifyDataSetChanged() → 全量刷新列表

-   .on(&quot;item_bind&quot;, fn) → item 绑定回调，fn(itemWrapper,
    itemData, position)

-   .on(&quot;item_click&quot;, fn) → 点击回调，fn(itemData, position,
    itemWrapper, listWrapper)

-   .on(&quot;item_long_click&quot;, fn) → 长按回调，fn(event, itemData,
    position)；event.consumed=true 消费事件

**PagerWrapper（viewpager）特有：**

-   .setDataSource(arr) → 设置页面数据源

-   .setCurrentItem(index, smooth) → 跳转到指定页，smooth=false 禁用动画

-   .notifyDataSetChanged() → 全量刷新

-   .on(&quot;item_bind&quot;, fn) → 页面绑定回调，fn(pageWrapper,
    pageData, position)

-   .on(&quot;change&quot;, fn) → 页面切换回调，fn(position)

**22.5 模板插值（{{key}} 语法）**

在 item 模板的 text 属性中使用 {{key}}
占位符，数据绑定时自动替换，无需手动 item_bind 赋值：

> let ui = RhinoUI.render(
>
> \<vertical\>
>
> \<list id=&quot;list&quot;\>
>
> \<vertical padding=&quot;12&quot;\>
>
> \<!\-- 自动从数据对象中取 name / age 字段 \--\>
>
> \<text text=&quot;{{name}}&quot; textSize=&quot;16sp&quot;
> textStyle=&quot;bold&quot;/\>
>
> \<text text=&quot;年龄: {{age}} 岁&quot; textSize=&quot;13sp&quot;
> textColor=&quot;#666&quot;/\>
>
> \<!\-- {{this}} 代表 item 本身，适合字符串数组 \--\>
>
> \</vertical\>
>
> \</list\>
>
> \</vertical\>
>
> );
>
> ui.list.setDataSource(\[{name:&quot;张三&quot;, age:25},
> {name:&quot;李四&quot;, age:30}\]);
>
> // 无需 item_bind，自动渲染

**22.6 三种显示方式**

-   RhinoUI.render(xml) → 只渲染不显示，配合 hikerPop.showRhinoUI()
    最灵活（推荐）

-   RhinoUI.showPage(xml, isFull) → 全屏 Dialog，isFull=true
    时真正全屏无状态栏，ui.dialog.dismiss() 关闭

-   RhinoUI.showDialog(xml, options) → AlertDialog 样式，支持
    title/positive/negative/neutral 按钮配置

> // showRhinoUI 配置项（最常用方式）
>
> let popup = hikerPop.showRhinoUI(ui, {
>
> type: &quot;bottom&quot;, // &quot;center&quot; 居中 或
> &quot;bottom&quot; 底部
>
> height: 400, // dp，底部弹窗高度
>
> maxHeight: 500, // dp，最大高度（bottom 类型）
>
> borderRadius: 16, // 圆角 dp
>
> dismissOnTouchOutside: true,
>
> dismissOnBackPressed: true,
>
> onShow: () =\> {}, // 显示后回调
>
> onDismiss: () =\> {} // 关闭后回调
>
> });
>
> popup.dismiss(); // 手动关闭
>
> // getPercentageHeight 计算屏幕百分比高度（dp）
>
> let h = RhinoUI.getPercentageHeight(0.75); // 屏幕高度的 75%（dp）

**22.7 fromStr 动态构建布局**

当布局需要插入 JS 变量（颜色、文字等）时，用模板字符串拼接后再通过
fromStr 转为 E4X 对象：

> var C = { bg: &quot;#1E1E1E&quot;, text: &quot;#E0E0E0&quot;, btn:
> &quot;#333333&quot; };
>
> var layoutXml = RhinoUI.fromStr(\`
>
> \<vertical bg=&quot;\${C.bg}&quot; corners=&quot;16dp 16dp 0 0&quot;
> w=&quot;match_parent&quot;\>
>
> \<text id=&quot;tvTitle&quot; textColor=&quot;\${C.text}&quot;
> textSize=&quot;16sp&quot; textStyle=&quot;bold&quot;/\>
>
> \<list id=&quot;rvList&quot; w=&quot;match_parent&quot;
> h=&quot;0&quot; weight=&quot;1&quot;/\>
>
> \</vertical\>
>
> \`);
>
> var itemXml = RhinoUI.fromStr(\`
>
> \<frame w=&quot;match_parent&quot; h=&quot;wrap_content&quot;
> padding=&quot;5dp&quot;\>
>
> \<text id=&quot;tvItem&quot; w=&quot;match_parent&quot;
> h=&quot;40dp&quot; gravity=&quot;center&quot;
>
> textSize=&quot;13sp&quot; radius=&quot;8dp&quot;
> bg=&quot;\${C.btn}&quot; textColor=&quot;\${C.text}&quot;/\>
>
> \</frame\>
>
> \`);
>
> var ui = RhinoUI.render(layoutXml);
>
> ui.rvList.setTemplate(itemXml); // 手动设置模板（fromStr
> 方式必须手动调用）
>
> ui.rvList.setDataSource(dataArr);

**22.8 自定义组件注册（registerWidget）**

将常用组合控件封装为自定义标签，在 XML 中像原生标签一样使用：

> RhinoUI.registerWidget(&quot;my-card&quot;, {
>
> template: (
>
> \<vertical padding=&quot;12&quot; bg=&quot;#FFFFFF&quot;
> radius=&quot;8dp&quot;\>
>
> \<text id=&quot;\_title&quot; textSize=&quot;16sp&quot;
> textStyle=&quot;bold&quot;/\>
>
> \<text id=&quot;\_sub&quot; textSize=&quot;12sp&quot;
> textColor=&quot;#999&quot; marginTop=&quot;4dp&quot;/\>
>
> \</vertical\>
>
> ),
>
> attrHandler: function(wrapper, attrName, value) {
>
> if (attrName === &quot;title&quot;) wrapper.\_title.text(value);
>
> if (attrName === &quot;sub&quot;) wrapper.\_sub.text(value);
>
> },
>
> methods: {
>
> getTitle: function() { return this.\_title.text(); }
>
> }
>
> });
>
> // 使用自定义标签
>
> let ui = RhinoUI.render(
>
> \<vertical\>
>
> \<my-card id=&quot;card1&quot; title=&quot;仙逆&quot;
> sub=&quot;125集已完结&quot;/\>
>
> \</vertical\>
>
> );
>
> toast(ui.card1.getTitle()); // 调用自定义方法
>
> **第二十三章：KVS 播放器 Hash 解密算法（x1hub 实战）**

KernelVideoSharing（KVS）是一套广泛使用的成人视频站建站程序，其播放器对视频直链进行了
Hash 重排加密保护。本章记录逆向还原过程与完整 ES5 实现，算法源自 yt-dlp
项目移植。

> 📌 适用站点：使用 KVS 程序搭建、embed 页含 flashvars.license_code 和
> video_url: \'function/0/\...\' 字段的站点，如 x1hub.com 等。

**23.1 加密原理**

**KVS 播放器的视频直链保护分两层：**

-   video_url 字段：格式为
    \'function/0/https://cdn\.../hash路径/video.mp4?embed=true\'，hash
    是经过重排的字符串

-   license_code 字段：格式为 \'\$361701411933162\'，去掉 \$
    后的数字用于生成 token 数组

-   token 数组：用于指导 hash 字符的还原顺序（swap 操作）

-   最终 URL：还原后的 hash 路径 + ?rnd=毫秒时间戳（去掉 embed=true）

**23.2 算法步骤详解**

**Step 1：从 license_code 生成 token 数组**

> // license_code 示例: \'\$361701411933162\'
>
> var lc = licCode.replace(/\\\$/g, \'\'); // \'361701411933162\'
>
> var lcVals = lc.split(\'\').map(Number); //
> \[3,6,1,7,0,1,4,1,1,9,3,3,1,6,2\]
>
> // 把 0 替换成 1，防止除数为0
>
> var modLc = lc.replace(/0/g, \'1\');
>
> var center = Math.floor(modLc.length / 2);
>
> // 前后半段相减，乘以4，截取前 center+1 位
>
> var front = parseInt(modLc.substring(0, center + 1));
>
> var back = parseInt(modLc.substring(center));
>
> var modStr = (4 \* Math.abs(front - back) + \'\').substring(0,
> center + 1);
>
> // 双重循环生成 token 数组（长度约32）
>
> var token = \[\];
>
> for (var ti = 0; ti \< modStr.length; ti++) {
>
> var cur = parseInt(modStr.charAt(ti));
>
> for (var off = 0; off \< 4; off++) {
>
> if (ti + off \< lcVals.length) {
>
> token.push((lcVals\[ti + off\] + cur) % 10);
>
> }
>
> }
>
> }

**Step 2：用 token 还原 hash（字符重排）**

> // 从 video_url 提取路径，找到长度 \>= 32 的 hash 段
>
> var url = rawUrl.replace(\'function/0/\', \'\');
>
> var parts = url.split(\'/\');
>
> var hashIdx = parts.findIndex(p =\> p.length \>= 32); // ES5用for循环
>
> var HL = 32;
>
> var hash = parts\[hashIdx\].substring(0, HL);
>
> // 构建下标数组 \[0,1,2,\...,31\]
>
> var indices = \[\];
>
> for (var ii = 0; ii \< HL; ii++) indices.push(ii);
>
> // 从尾到头做 swap（关键：从后往前，和加密时相反）
>
> var accum = 0;
>
> for (var src = HL - 1; src \>= 0; src\--) {
>
> accum += token\[src\] \|\| 0;
>
> var dest = (src + accum) % HL;
>
> var tmp = indices\[src\];
>
> indices\[src\] = indices\[dest\];
>
> indices\[dest\] = tmp;
>
> }
>
> // 用还原后的 indices 重组 hash 字符
>
> var newHash = \'\';
>
> for (var ni = 0; ni \< HL; ni++) {
>
> newHash += hash.charAt(indices\[ni\]);
>
> }
>
> parts\[hashIdx\] = newHash + parts\[hashIdx\].substring(HL);

**Step 3：拼接最终 URL**

> // 去掉 embed=true，rnd 用毫秒时间戳（服务器接受任意毫秒时间戳）
>
> var finalUrl = parts.join(\'/\') + \'?rnd=\' + new Date().getTime();
>
> return \'video://\' + finalUrl + \';{User-Agent@\' + ua +
> \'&&Referer@\' + host + \'/}\';

**23.3 完整 ES5 实现（可直接复用）**

以下是可直接用于海阔/聚阅规则的完整 lazyRule 实现：

> \_getVideoRule: function() {
>
> var \_host = this.host;
>
> var \_ua = this.UA;
>
> return \$(\'\').lazyRule(function(host, ua) {
>
> var \_embedUrl = input;
>
> if (\_embedUrl.indexOf(\'/videos/\') \> -1) {
>
> var \_vid = \_embedUrl.match(/\\/videos\\/(\\d+)/);
>
> if (\_vid) \_embedUrl = host + \'/embed/\' + \_vid\[1\] + \'/\';
>
> }
>
> var \_html = request(\_embedUrl, {
>
> headers: { \'User-Agent\': ua, \'Referer\': host + \'/\' }
>
> });
>
> if (!\_html \|\| \_html.length \< 200) return \_embedUrl + \'#嗅探\';
>
> // 提取 flashvars 两个关键字段
>
> var \_mUrl =
> \_html.match(/video_url\\s\*\[\':\]\\s\*\[\'&quot;\](\[\^\'&quot;\]+)\[\'&quot;\]/);
>
> var \_mLic =
> \_html.match(/license_code\\s\*\[\':\]\\s\*\[\'&quot;\](\[\^\'&quot;\]+)\[\'&quot;\]/);
>
> if (!\_mUrl \|\| !\_mLic) return \_embedUrl + \'#嗅探\';
>
> var \_rawUrl = \_mUrl\[1\];
>
> var \_licCode = \_mLic\[1\];
>
> // Step1: 生成 token 数组
>
> var \_lc = \_licCode.replace(/\\\$/g, \'\');
>
> var \_lcVals = \[\];
>
> for (var \_i = 0; \_i \< \_lc.length; \_i++)
> \_lcVals.push(parseInt(\_lc.charAt(\_i)));
>
> var \_modLc = \_lc.replace(/0/g, \'1\');
>
> var \_center = Math.floor(\_modLc.length / 2);
>
> var \_front = parseInt(\_modLc.substring(0, \_center + 1));
>
> var \_back = parseInt(\_modLc.substring(\_center));
>
> var \_modStr = (4 \* Math.abs(\_front - \_back) + \'\').substring(0,
> \_center + 1);
>
> var \_token = \[\];
>
> for (var \_ti = 0; \_ti \< \_modStr.length; \_ti++) {
>
> var \_cur = parseInt(\_modStr.charAt(\_ti));
>
> for (var \_off = 0; \_off \< 4; \_off++) {
>
> if (\_ti + \_off \< \_lcVals.length)
>
> \_token.push((\_lcVals\[\_ti + \_off\] + \_cur) % 10);
>
> }
>
> }
>
> // Step2: 还原 hash
>
> var \_isFunc = \_rawUrl.indexOf(\'function/0/\') === 0;
>
> var \_url = \_isFunc ? \_rawUrl.substring(11) : \_rawUrl;
>
> \_url = \_url.split(String.fromCharCode(92)).join(\'\');
>
> var \_qIdx = \_url.indexOf(\'?\');
>
> var \_base = \_qIdx \> -1 ? \_url.substring(0, \_qIdx) : \_url;
>
> if (\_isFunc) {
>
> var \_parts = \_base.split(\'/\');
>
> var \_hi = -1;
>
> for (var \_pi = 0; \_pi \< \_parts.length; \_pi++) {
>
> if (\_parts\[\_pi\].length \>= 32) { \_hi = \_pi; break; }
>
> }
>
> if (\_hi \> -1) {
>
> var \_HL = 32;
>
> var \_hash = \_parts\[\_hi\].substring(0, \_HL);
>
> var \_suf = \_parts\[\_hi\].substring(\_HL);
>
> var \_idx = \[\];
>
> for (var \_ii = 0; \_ii \< \_HL; \_ii++) \_idx.push(\_ii);
>
> var \_acc = 0;
>
> for (var \_src = \_HL - 1; \_src \>= 0; \_src\--) {
>
> \_acc += \_token\[\_src\] \|\| 0;
>
> var \_dst = (\_src + \_acc) % \_HL;
>
> var \_t = \_idx\[\_src\]; \_idx\[\_src\] = \_idx\[\_dst\];
> \_idx\[\_dst\] = \_t;
>
> }
>
> var \_nh = \'\';
>
> for (var \_ni = 0; \_ni \< \_HL; \_ni++) \_nh +=
> \_hash.charAt(\_idx\[\_ni\]);
>
> \_parts\[\_hi\] = \_nh + \_suf;
>
> \_base = \_parts.join(\'/\');
>
> }
>
> }
>
> // Step3: 拼接最终 URL
>
> var \_final = \_base + \'?rnd=\' + new Date().getTime();
>
> log(\'\[KVS\] \' + \_final.substring(0, 100));
>
> return \'video://\' + \_final
>
> \+ \';{User-Agent@\' + ua + \'&&Referer@\' + host + \'/}\';
>
> }, \_host, \_ua);
>
> },

**23.4 关键注意事项**

> 📌 rnd 用 Date.now() 毫秒时间戳即可，服务器只校验 hash 是否正确，rnd
> 任意毫秒值均可通过。
>
> 📌 swap 方向必须从尾到头（src = HL-1 →
> 0），这是还原操作，与加密时方向相反。
>
> 📌 license_code 中的 \$ 前缀必须去掉再处理，否则 parseInt 会返回 NaN。
>
> ⚠️ embed 页通常无 CF 保护，用普通 request 即可，无需
> fetchCodeByWebView，速度更快。
>
> ⚠️ 此算法适用于 KVS 程序站点，非 KVS 站点（如使用 MACCMS
> 的站点）无需此算法。
>
> **第二十四章：内嵌视频算法总结与 VidHide 实战**

内嵌视频（iframe
嵌套播放器）是目前成人视频站最常见的播放方式。本章汇总所有常见算法类型，并重点记录
VidHide 播放器的逆向实战过程（代表站：kbjus.com / playrecord.biz）。

**24.1 内嵌视频算法类型总览**

-   直接 m3u8/mp4 直链 --- 无加密，正则直接匹配

-   KVS Hash 重排（第二十三章） --- 特征：video_url:
    \'function/0/\...\' + license_code；代表站：x1hub.com

-   VidHide split 数组拼接（本章） --- 特征：eval(function(p,a,c,k\...))
    混淆 JS + split(\'\|\')
    字符串数组末尾固定结构；代表站：playrecord.biz / vidhide.com

-   MACCMS player_aaaa 加密（第九章） --- 特征：player_aaaa 配置块 +
    encrypt:1/2；encrypt=1 URL编码，encrypt=2 Base64+URL双重编码

-   AES/XOR 图片/视频加密（第二十一章） ---
    特征：图片灰色占位或视频黑屏，站点 worker.js 含
    CryptoJS.AES.decrypt；代表站：91short.com

-   iframe 二次请求 --- 特征：视频页源码含 \<iframe
    src=\'https://外部域名/embed/xxx\'\>，真实地址在 iframe 页里；需两步
    fetch

-   WebView 嗅探兜底 --- 以上方法均失败时，用 fetchCodeByWebView 或
    #嗅探 触发 App 内置嗅探器

> 📌 识别技巧：先看站点 Network 面板，找视频请求 URL 格式；再看 embed 页
> JS 特征（eval混淆、player_aaaa、flashvars）来判断使用哪种算法。

**24.2 iframe 二次请求通用写法**

视频页嵌套外部 iframe 时，需要两步请求才能拿到真实播放地址：

> // Step1：请求视频页，提取 iframe src
>
> var \_html = fetchPC(input, { headers: { \'Referer\': host + \'/\' }
> });
>
> // 方式A：从 hidden input 的 base64 value 解码（更可靠）
>
> // \<input type=\'hidden\' id=\'links\'
> value=\'aHR0cHM6Ly9wbGF5cmVjb3JkLmJpei9lbWJlZC8\...\'\>
>
> var \_b64m =
> \_html.match(/id=\[&quot;\'\]links\[&quot;\'\]\[\^\>\]\*value=\[&quot;\'\](\[A-Za-z0-9+\\/=\]+)\[&quot;\'\]/);
>
> if (\_b64m) {
>
> var \_bytes = android.util.Base64.decode(\_b64m\[1\],
> android.util.Base64.DEFAULT);
>
> \_iframeSrc = new java.lang.String(\_bytes, \'UTF-8\');
>
> }
>
> // 方式B：直接正则匹配 iframe src
>
> if (!\_iframeSrc) {
>
> var \_im =
> \_html.match(/\<iframe\[\^\>\]\*src=\[&quot;\'\](https?:\\/\\/\[\^&quot;\'\]+)\[&quot;\'\]/);
>
> if (\_im) \_iframeSrc = \_im\[1\];
>
> }
>
> // Step2：请求 embed 页提取播放地址
>
> var \_ih = fetchPC(\_iframeSrc, { headers: { \'Referer\': host + \'/\'
> } });
>
> var \_m =
> \_ih.match(/(https?:\[\^\\s&quot;\'\<\>\\\\\]+\\.m3u8\[\^\\s&quot;\'\<\>\\\\\]\*)/);
>
> if (\_m) return \'video://\' + \_m\[1\] + \';{User-Agent@\' + ua +
> \'&&Referer@\' + \_iframeSrc + \'}\';
>
> ⚠️ atob() 在 Rhino JS 引擎里未定义，base64 解码必须用
> android.util.Base64.decode()。

**24.3 VidHide 播放器算法（playrecord.biz / vidhide.com）**

VidHide 是一个广泛使用的视频托管播放器，embed 页使用
eval(function(p,a,c,k,\...)) 混淆 JS。通过分析解混淆后的字符串
split(\'\|\') 数组，可以从末尾固定位置提取播放地址的组成部分。

**split 数组末尾固定结构：**

-   arr\[-1\] = key （base64字符串，含大写字母，如
    VoFAMLOu95r4YO7Rc8KKWA）

-   arr\[-2\] = token （纯小写字母，如 hjkrhuihghfvu）

-   arr\[-3\] = 时间戳 （10位Unix时间戳，如 1774045296）

-   arr\[-4\] = key1 （另一个base64，hls2备用地址用）

-   arr\[-5\] = CDN子域 （如 acek）

-   arr\[-6\] = \'cdn\'

-   arr\[-7\] = hashid （5位数字，如 07671）

-   arr\[-8\] = 文件名 （小写字母+下划线，如 rzagv3vtqm2t\_）

-   arr\[-9\] = \'urlset\'

-   arr\[-10\] = token2 （大写base64，hls2地址用）

-   arr\[-11\] = 时间戳2

**最终 stream URL 拼接格式：**

> //
> https://cdn域名/stream/arr\[-1\]/arr\[-2\]/arr\[-3\]/file_id/master.m3u8
>
> // cdn域名直接从 iframeSrc 提取（如 playrecord.biz）
>
> // file_id 从 embed 页 \$.cookie(\'file_id\', \'xxx\') 提取
>
> // 示例：
>
> //
> https://playrecord.biz/stream/VoFAMLOu95r4YO7Rc8KKWA/hjkrhuihghfvu/1774045296/38359663/master.m3u8

**24.4 完整 ES5 实现（可直接复用）**

> \_getVideoRule: function() {
>
> var \_host = this.host;
>
> var \_ua = this.UA;
>
> return \$(\'\').lazyRule(function(host, ua) {
>
> // Step1: 视频页 → 提取 embed URL
>
> var \_html = fetchPC(input, { headers: { \'Referer\': host + \'/\' }
> });
>
> var \_iframeSrc = \'\';
>
> var \_b64m =
> \_html.match(/id=\[&quot;\'\]links\[&quot;\'\]\[\^\>\]\*value=\[&quot;\'\](\[A-Za-z0-9+\\/=\]+)\[&quot;\'\]/);
>
> if (\_b64m) {
>
> try {
>
> var \_bytes = android.util.Base64.decode(\_b64m\[1\],
> android.util.Base64.DEFAULT);
>
> \_iframeSrc = new java.lang.String(\_bytes, \'UTF-8\');
>
> } catch(e) {}
>
> }
>
> if (!\_iframeSrc) {
>
> var \_im =
> \_html.match(/\<iframe\[\^\>\]\*src=\[&quot;\'\](https?:\\/\\/\[\^&quot;\'\]+)\[&quot;\'\]/);
>
> if (\_im) \_iframeSrc = \_im\[1\];
>
> }
>
> if (!\_iframeSrc) return input + \'#嗅探\';
>
> // Step2: embed 页 → 提取播放地址
>
> var \_ih = fetchPC(\_iframeSrc, { headers: { \'Referer\': host + \'/\'
> } });
>
> var \_sl = String.fromCharCode(92);
>
> // 优先：直接匹配完整 m3u8
>
> var \_r1 =
> \_ih.match(/(https?:\\/\\/\[\^&quot;\'\\s\\\\\]+\\.m3u8\[\^&quot;\'\\s\\\\\]\*)/);
>
> if (\_r1) return \'video://\' + \_r1\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> // VidHide：从 split 数组末尾拼接 stream URL
>
> var \_arrm =
> \_ih.match(/\'(\[\^\'\]{500,})\'\\.split\\(\'\\\|\'\\)\\)\\)/);
>
> if (\_arrm) {
>
> var \_arr = \_arrm\[1\].split(\'\|\');
>
> var \_len = \_arr.length;
>
> var \_key = \_arr\[\_len - 1\];
>
> var \_token = \_arr\[\_len - 2\];
>
> var \_ts = \_arr\[\_len - 3\];
>
> var \_fidm =
> \_ih.match(/file_id\[\'&quot;\],\\s\*\[\'&quot;\](\\d{5,10})\[\'&quot;\]/);
>
> var \_fid = \_fidm ? \_fidm\[1\] : \'\';
>
> var \_cdn = \_iframeSrc.match(/https?:\\/\\/(\[\^\\/\]+)/)\[1\];
>
> if (\_key && \_token && \_ts && \_fid) {
>
> var \_m3u8 = \'https://\' + \_cdn + \'/stream/\' +
>
> \_key + \'/\' + \_token + \'/\' + \_ts + \'/\' + \_fid +
> \'/master.m3u8\';
>
> return \'video://\' + \_m3u8 +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> }
>
> }
>
> // 兜底 mp4
>
> var \_r3 =
> \_ih.match(/(https?:\[\^\\s&quot;\'\<\>\\\\\]+\\.mp4\[\^\\s&quot;\'\<\>\\\\\]\*)/);
>
> if (\_r3) return \'video://\' + \_r3\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> return \_iframeSrc + \'#嗅探\';
>
> }, \_host, \_ua);
>
> },

**24.5 注意事项**

> 📌 split 数组长度各站不同，但末尾相对位置固定：arr\[-1\]=key,
> arr\[-2\]=token, arr\[-3\]=时间戳。
>
> 📌 CDN域名直接从 iframeSrc
> 提取，不同视频可能用不同域名（playrecord.biz / recordplay.biz
> 等），不要硬编码。
>
> 📌 file_id 在 embed 页 JS 里以 \$.cookie(\'file_id\', \'38359663\')
> 形式出现，正则：/file_id\[\'&quot;\],\\s\*\[\'&quot;\](\\d{5,10})\[\'&quot;\]/
>
> ⚠️ fetchPC 比 fetch 更稳定，服务器对移动 UA
> 可能返回不同内容（甚至不返回 iframe）。
>
> ⚠️ pdfh 无法解析 iframe 标签的 src 属性（iframe
> 无闭合标签），必须用正则或 base64 解码方式提取。
>
> **第二十五章：VidHide Dean Edwards Unpacker 解密（进阶）**

第二十四章介绍了通过 split 数组末尾提取 stream URL 的方案，适用于
playrecord.biz 的标准格式。但 VidHide 有多种 CDN
后端（podcastguestfinder.sbs、seamistboutiquecreations.cfd
等），这些后端使用 hls3 格式而非 stream 格式，需要通过 Dean Edwards
Unpacker 解码 eval 混淆代码来提取真实地址。

**25.1 两种 VidHide URL 格式对比**

**VidHide embed 页使用 eval(function(p,a,c,k,e,d){\...}) 混淆，解码后 o
对象包含两个播放地址字段：**

-   o.hls4 / o.1j → stream
    格式（旧）：https://cdn域名/stream/key/token/ts/fid/master.m3u8

-   o.hls3 / o.1a → hls3
    格式（新）：https://host.cfd/fid/hls3/01/hashid/filename\_,l,n,h,.urlset/master.txt

-   两种格式都需要先通过 Unpacker 解码 eval 混淆代码，再从解码结果里提取
    URL

**25.2 Dean Edwards Unpacker 核心算法**

eval 混淆格式：eval(function(p,a,c,k,e,d){\...})(模板字符串, 进制, 词数,
词典字符串.split(\'\|\'))

**解码步骤：**

> // 1. 定位 eval 代码块，提取模板字符串
>
> var \_evalIdx = html.indexOf(\'eval(function(p,a,c,k,e,d)\');
>
> var \_pidx = html.indexOf(&quot;}(\'&quot;, \_evalIdx); // 找 \'}(\'
> 分隔符
>
> var \_tmpl = html.substring(\_pidx + 3, \...); // 模板字符串
>
> // 2. 提取参数：base进制, count词数, keywords词典
>
> var \_pm =
> after.match(/\',\\s\*(\\d+)\\s\*,\\s\*(\\d+)\\s\*,\\s\*\'(\[\^\'\]+)\'\\s\*\\.split/);
>
> var \_base = parseInt(\_pm\[1\]); // 36 或 62
>
> var \_cnt = parseInt(\_pm\[2\]); // 词典大小
>
> var \_kws = \_pm\[3\].split(\'\|\'); // 词典数组
>
> // 3. 替换模板中每个 token（36进制时）
>
> \_code = \_tmpl.replace(/\\b(\\w+)\\b/g, function(m) {
>
> var n = parseInt(m, \_base);
>
> return (!isNaN(n) && n \< \_cnt && \_kws\[n\]) ? \_kws\[n\] : m;
>
> });
>
> // 4. 从解码结果里提取播放地址（按优先级）
>
> // A. 绝对 stream URL
>
> var \_su =
> \_code.match(/(https?:\\/\\/\[\^&quot;\'\\s\]+\\/stream\\/\[\^&quot;\'\\s\]+master\\.m3u8)/);
>
> // B. 相对路径（o对象里是相对路径时）
>
> var \_rp =
> \_code.match(/&quot;(\\/\[\^&quot;\]+\\/master\\.(?:m3u8\|txt)\[\^&quot;\]\*)&quot;/);
>
> if (\_rp) return embedHost + \_rp\[1\]; // 拼上 embed 域名
>
> // C. 绝对 hls3/hls2 CDN URL
>
> var \_hu =
> \_code.match(/(https?:\\/\\/\[\^&quot;\'\\s\]+(?:hls3\|hls2)\[\^&quot;\'\\s\]+\\.(?:m3u8\|txt)\[\^&quot;\'\\s\]\*)/);
>
> 📌 关键细节：embed 域名（embedHost）用于拼接相对路径。有时 vidhide.com
> 的 iframe 会重定向到 playrecord.biz，需要从 embed
> 页内容里检测真实域名。
>
> 📌 master.txt 和 master.m3u8 都是有效的 HLS
> 播放列表，海阔/聚阅播放器都能识别，不需要额外处理。
>
> ⚠️ 62进制（base=62）时用 \'0-9a-zA-Z\' 字符集，不能直接用 parseInt(m,
> 62)，需要自己实现转换。

**25.3 完整通用解析函数（兼容两种格式）**

以下实现同时支持 stream 格式和 hls3 格式，适用于所有 VidHide/PlayRecord
embed 页：

> \_getVideoRule: function() {
>
> var \_host = this.host;
>
> var \_ua = this.UA;
>
> return \$(\'\').lazyRule(function(host, ua) {
>
> var \_html = fetch(input, { headers: { \'Referer\': host + \'/\',
> \'User-Agent\': ua } });
>
> if (!\_html \|\| \_html.length \< 200) return input + \'#嗅探\';
>
> // Step1: 提取 embed URL（base64解码 → iframe正则）
>
> var \_iframeSrc = \'\';
>
> var \_b64m =
> \_html.match(/id=\[&quot;\'\]links\[&quot;\'\]\[\^\>\]\*value=\[&quot;\'\](\[A-Za-z0-9+\\/=\]+)\[&quot;\'\]/);
>
> if (\_b64m) {
>
> try {
>
> var \_b = android.util.Base64.decode(\_b64m\[1\],
> android.util.Base64.DEFAULT);
>
> \_iframeSrc = new java.lang.String(\_b, \'UTF-8\');
>
> } catch(e) {}
>
> }
>
> if (!\_iframeSrc) {
>
> var \_im =
> \_html.match(/iframe\[\^\>\]+src=\[&quot;\'\](https?:\\/\\/\[\^&quot;\'\\s\>\]+\\/embed\\/\[\^&quot;\'\\s\>\]+)\[&quot;\'\]/i);
>
> if (\_im) \_iframeSrc = \_im\[1\];
>
> }
>
> if (!\_iframeSrc) return input + \'#嗅探\';
>
> // Step2: 请求 embed 页
>
> var \_ih = fetchPC(\_iframeSrc, { headers: { \'Referer\': host + \'/\'
> } });
>
> if (!\_ih \|\| \_ih.length \< 100) return \_iframeSrc + \'#嗅探\';
>
> var \_sl = String.fromCharCode(92);
>
> // 获取真实 embed 域名（处理 vidhide→playrecord 跳转）
>
> var \_embedHost = \_iframeSrc.match(/https?:\\/\\/\[\^\\/\]+/)\[0\];
>
> var \_rhm =
> \_ih.match(/src=\[&quot;\'\](https?:\\/\\/\[\^&quot;\'\\/\]\*(?:playrecord\|recordplay)\[\^&quot;\'\\/\]\*)\[\\/&quot;\'\]/i);
>
> if (\_rhm) \_embedHost = \_rhm\[1\];
>
> // ① 直接匹配完整 stream m3u8
>
> var \_rs =
> \_ih.match(/(https?:\\/\\/\[\^&quot;\'\\s\\\\\]+\\/stream\\/\[\^&quot;\'\\s\\\\\]+\\/master\\.m3u8)/);
>
> if (\_rs) return \'video://\' + \_rs\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> // ② Dean Edwards Unpacker 解码 eval 混淆
>
> var \_evalIdx = \_ih.indexOf(\'eval(function(p,a,c,k,e,d)\');
>
> if (\_evalIdx \> -1) {
>
> var \_pidx = \_ih.indexOf(&quot;}(\'&quot;, \_evalIdx);
>
> if (\_pidx \> -1) {
>
> // 提取模板字符串（处理转义）
>
> var \_ti = \_pidx + 3, \_te = -1, \_esc = false;
>
> for (var \_ci = \_ti; \_ci \< \_ih.length; \_ci++) {
>
> if (\_esc) { \_esc = false; continue; }
>
> if (\_ih.charAt(\_ci) === \_sl) { \_esc = true; continue; }
>
> if (\_ih.charAt(\_ci) === &quot;\'&quot;) { \_te = \_ci; break; }
>
> }
>
> if (\_te \> \_ti) {
>
> var \_tmpl = \_ih.substring(\_ti, \_te);
>
> var \_after = \_ih.substring(\_te, Math.min(\_te + 10000,
> \_ih.length));
>
> var \_pm =
> \_after.match(/\',\\s\*(\\d+)\\s\*,\\s\*(\\d+)\\s\*,\\s\*\'(\[\^\'\]+)\'\\s\*\\.split/);
>
> if (\_pm) {
>
> var \_base = parseInt(\_pm\[1\]);
>
> var \_cnt = parseInt(\_pm\[2\]);
>
> var \_kws = \_pm\[3\].split(\'\|\');
>
> var \_code = \_tmpl.replace(/\\b(\\w+)\\b/g, function(tok) {
>
> var n = parseInt(tok, \_base);
>
> return (!isNaN(n) && n \>= 0 && n \< \_cnt && \_kws\[n\]) ? \_kws\[n\]
> : tok;
>
> });
>
> // A. 绝对 stream URL
>
> var \_su =
> \_code.match(/(https?:\\/\\/\[\^&quot;\'\\s\]+\\/stream\\/\[\^&quot;\'\\s\]+master\\.m3u8)/);
>
> if (\_su) return \'video://\' + \_su\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> // B. 相对路径（拼 embedHost）
>
> var \_rp =
> \_code.match(/&quot;(\\/\[\^&quot;\]+\\/master\\.(?:m3u8\|txt)\[\^&quot;\]\*)&quot;/);
>
> if (\_rp) return \'video://\' + \_embedHost +
> \_rp\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> // C. 绝对 hls3/hls2
>
> var \_hu =
> \_code.match(/(https?:\\/\\/\[\^&quot;\'\\s\]+(?:hls3\|hls2)\[\^&quot;\'\\s\]+\\.(?:m3u8\|txt)\[\^&quot;\'\\s\]\*)/);
>
> if (\_hu) return \'video://\' + \_hu\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> // D. 任意 m3u8
>
> var \_mu =
> \_code.match(/(https?:\\/\\/\[\^&quot;\'\\s\]+\\.m3u8\[\^&quot;\'\\s\]\*)/);
>
> if (\_mu) return \'video://\' + \_mu\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> }
>
> }
>
> }
>
> }
>
> // ③ 通用兜底
>
> var \_r1 =
> \_ih.match(/(https?:\\/\\/\[\^&quot;\'\\s\\\\\]+\\.m3u8\[\^&quot;\'\\s\\\\\]\*)/);
>
> if (\_r1) return \'video://\' + \_r1\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> var \_r3 =
> \_ih.match(/(https?:\[\^\\s&quot;\'\<\>\\\\\]+\\.mp4\[\^\\s&quot;\'\<\>\\\\\]\*)/);
>
> if (\_r3) return \'video://\' + \_r3\[1\].split(\_sl).join(\'\') +
>
> \';{User-Agent@\' + ua + \'&&Referer@\' + \_iframeSrc + \'}\';
>
> return \_iframeSrc + \'#嗅探\';
>
> }, \_host, \_ua);
>
> },

**25.4 注意事项**

> 📌 此方案已在 sexbjcam.com（host: playrecord.biz）和 kbjus.com
> 上验证可用，两种格式均能正确解析。
>
> 📌 embedHost 的作用：o 对象里的 hls3 URL 有时是相对路径（如
> /fid/hls3/\...），需要拼上 embed 页的域名才能播放。
>
> ⚠️ 不同视频可能使用不同
> CDN（playrecord.biz、podcastguestfinder.sbs、seamistboutiquecreations.cfd
> 等），域名会变但 URL 结构固定，Unpacker 方案不受域名影响。
>
> ⚠️ 第二十四章的 split 数组末尾方案（arr\[-1\]=key）对 hls3
> 格式无效，应优先使用本章的 Unpacker 方案，split 方案作为兜底。
>
> **第二十六章：\$ 工具函数完全参考**

\$
是海阔视界/聚阅内置的核心工具命名空间，提供动态规则生成、模块化开发、交互弹窗等功能。本章内容整合自官方《海阔视界工具文档》，与手册其他章节形成互补------其他章节侧重规则结构与实战铁律，本章专注于
\$ 命名空间下所有可用方法的完整语法与参数说明。

**26.1 模块化开发（\$.exports + \$.require）**

\$.exports 和 \$.require
是海阔视界的模块系统，用于将规则代码拆分为多个依赖页面，实现逻辑复用。

  ----------------------------------------------------------------------------------------------------------------------
  **方法**             **说明**                               **注意事项**
  -------------------- -------------------------------------- ----------------------------------------------------------
  \$.exports           导出模块，默认值为                     赋值必须在页面顶层执行，不能延迟。
                       {}。在依赖页面中赋值，供其他页面通过   
                       \$.require() 引入。                    

  \$.require(path,     导入模块。先执行路径文件代码，再返回   调用 \$.exports
  param, headers,      \$.exports。若路径以 .json 结尾，自动  中的函数时，无法直接使用函数外部变量，必须通过传参传入。
  time)                JSON.parse 后赋给 \$.exports。         

  \$.importRequire()   将模块引入到指定的 scope 环境中运行。  较少使用，适合沙箱隔离场景。
  ----------------------------------------------------------------------------------------------------------------------

依赖页面（hiker://page/utils）：

> // 依赖页面中定义并导出
>
> \$.exports.formatTitle = function(title) {
>
> return title.replace(/\\s+/g, \'\').trim();
>
> };
>
> \$.exports.buildUrl = function(host, path) {
>
> return host + path;
>
> };

主规则中引入使用：

> var utils = \$.require(\'hiker://page/utils\');
>
> var title = utils.formatTitle(rawTitle); // ✅ 正确
>
> // ❌ 错误：utils 内部函数不能访问外部的 host 变量
>
> // 必须通过参数传入：utils.buildUrl(host, path)
>
> 📌 \$.require 适合封装 \_base64Decode、\_autoDeshell
> 等工具函数，在多个规则之间复用，避免每个规则都粘贴一遍。

**26.2 动态规则生成（核心方法）**

\$ 最核心的能力是生成动态 URL
字符串，驱动页面跳转、懒加载解析、图片解密。以下四个方法构成了海阔视界规则的骨干。

> 📌 **`$` 工具相关章节地图（V3.66）：**
>
> | 章节 | 焦点 | 深度 |
> |---|---|---|
> | **本章（26）** | 4 大方法基础参考 + 用法示例 | 入门必读 |
> | **第 54 章** | `$.lazyRule` 参数传递深度、闭包陷阱、组合写法 | 进阶 |
> | **第 45.10 节** | `$.toString()` 字符串化模式（**不推荐**，壳子遗留）+ 替代方案 | 旧规则清洗 |
> | **第 26.6 / 26.7 节** | `registerTask` / `banner()` 引擎原生轮播（非 `$` 工具，本章末尾附录） | 轮播功能 |

  -------------------------------------------------------------------------------------------
  **方法**              **用途**                       **典型场景**
  --------------------- ------------------------------ --------------------------------------
  \$().rule(func,       生成用于新开页面的 URL         CF
  \...args)             字符串。func                   过盾沙箱页、小说正文页、自定义设置页
                        在新页面中执行，通常用         
                        setResult() 设置内容。         

  \$().lazyRule(func,   生成懒加载解析                 列表页构建 url
  \...args)             URL。用户点击后才执行          字段时，延迟提取真实视频地址
                        func，返回值作为真实 URL（如   
                        video://）。                   

  \$().image(func,      生成图片 URL。func 中 input    防盗链封面、Base64 解码图片、AES
  \...args)             为图片标识，必须返回           加密图片解密
                        InputStream，引擎自动渲染。    

  \$().x5Lazy() /       生成网页资源嗅探 URL，分别使用 无法直接解析的视频页，嗅探兜底
  \$().webLazy()        X5 内核和 Webkit               
                        内核加载页面并提取视频地址。   
  -------------------------------------------------------------------------------------------

▶ \$().lazyRule 详解

> 📌 Law 2（作用域隔离）：lazyRule
> 内部是独立作用域，外部变量一律无法访问。所有需要的值必须通过参数显式传入------包括
> host、UA、任何局部变量。
>
> ⚠️ lazyRule
> 参数只能传基础类型（字符串、数字、布尔）。传入对象时会被序列化为
> \"\[object Object\]\"，导致请求头失效（Law 52）。
>
> // ✅ 正确：只传字符串，内部自己拼 headers
>
> url: \$(\'\').lazyRule(function(link, ua, host) {
>
> var html = fetch(link, {
>
> headers: { \'User-Agent\': ua, \'Referer\': host + \'/\' }
>
> });
>
> var m = html.match(/https?:\\/\\/\[\^\\s\'\"\]+\\.m3u8/);
>
> if (m) return \'video://\' + m\[0\] + \';{Referer@\' + host + \'/}\';
>
> return link + \'#嗅探\';
>
> }, detailUrl, parse.UA, parse.host)
>
> // ❌ 错误：传对象，headers 变成字符串 \"\[object Object\]\"
>
> var headers = { \'User-Agent\': ua };
>
> url: \$(\'\').lazyRule(function(link, reqHeaders) {
>
> fetch(link, { headers: reqHeaders }); // reqHeaders 是字符串！
>
> }, link, headers)

▶ \$().image 详解（封面解密标准方案）

> ⚠️ **再次修订（V3.66 二修）：** `$().image()` 与 `$().lazyRule()` 同理，**有两种模式**，`input` 含义不同。早先 V3.66 一修把 input 一律说成 `InputStream` 过于绝对——实战规则（xchina、笔盒、禁漫、ACG、jinman 等）两种模式都在用。

**`$().image()` 双模式速查**

| 模式 | 写法 | 回调里 `input` 是什么 | 适用场景 |
|---|---|---|---|
| **独立形式** | `$(picUrl).image(fn, ...args)`<br>放在 `img` / `pic_url` 字段 | **引擎下载完成的 `InputStream`** | 图片字节加密（XOR / AES / Base64 嵌套） |
| **拼接形式** | `picUrl + '@Referer=...' + $('').image(fn, ...args)`<br>整条字符串作为 `pic_url` | **拼在前面的字符串前缀**（含 URL + headers 尾巴） | 防盗链 Cookie 注入、自定义 headers 下载 |

```javascript
// ── 模式一：独立形式 ───────────────────────────────────────
// input = 引擎已下载完成的 InputStream，需要 toBytes/parseInputStream 处理
//
// 范例：笔盒（XOR + Base64 解密）
let image = $('').image(() => {
    const FileUtil = com.example.hikerview.utils.FileUtil;
    let data = FileUtil.toBytes(input);              // input 是 InputStream
    for (let i = 0; i < 4096; i++) data[i] = data[i] ^ 18;
    return FileUtil.toInputStream(
        android.util.Base64.decode(data, 1).slice(15)
    );
});

// 范例：禁漫天堂（AES-CBC 解密 → 走 pics:// 拼接）
var tlazy = $('').image(function() {
    var CryptoUtil = $.require('hiker://assets/crypto-java.js');
    var key = CryptoUtil.Data.parseUTF8('f5d965df75336270');
    var iv  = CryptoUtil.Data.parseUTF8('97b60394abc2fbe1');
    var dec = CryptoUtil.AES.decrypt(
        CryptoUtil.Data.parseInputStream(input),     // ← input 是 InputStream
        key, { mode: 'AES/CBC/PKCS7Padding', iv: iv }
    );
    return dec.toInputStream();
});
// 使用：拼到 pics:// 每张图后面
'pics://' + pics.map(function(p) { return p + tlazy; }).join('&&')

// ── 模式二：拼接形式（防盗链 + Cookie，xchina 实战）───────
// input = 拼在前面的字符串，是 "imgUrl@Referer=host/" 这种带后缀的完整前缀
var imgProxy = $().image(function(ua_s, ck_s) {
    var FileUtil = com.example.hikerview.utils.FileUtil;
    var real = String(input || '').split('@')[0];    // ← input 是字符串！切 @ 拿真实 URL
    var h    = { 'User-Agent': ua_s, 'Cookie': ck_s };
    var dm   = real.match(/^(https?:\/\/[^\/]+)/);
    if (dm) h['Referer'] = dm[1] + '/';
    try { return FileUtil.toInputStream(FileUtil.readBytes(real, h)); }
    catch(e) { return null; }
}, ua, cookie);
// 使用：图片 URL + headers 尾巴 + imgProxy 拼到一起
pics.push(imgUrl + '@Referer=' + host + '/' + imgProxy);
```

> ⚠️ **三条铁律（修正版）：**
> 1. 选模式看代码结构：**整段字符串放 `pic_url` → 拼接形式 input 是字符串前缀**；**裸 `$(url).image(fn)` 放 `img` 字段 → 独立形式 input 是 InputStream**
> 2. 拼接形式约定俗成的格式：`imgUrl + '@Referer=...' + $('').image(fn)` —— 回调里 `String(input).split('@')[0]` 拿真实 URL
> 3. `$().image()` 每次调用生成新字符串 ID，**循环内严禁重复调用** —— 必须提前 `var imgProxy = $().image(...)` 调用一次，循环内 `pics.push(url + imgProxy)`

> 📌 `$().image(fn, ...args)` 与 `$(url).image({image:fn})` 的区别：前者是函数式写法（func 通过位置参数传值），后者是对象式写法（手册第 10 章详解），两者功能等同，选一种风格保持一致即可。完整三种返回类型（URL/InputStream/null）和实战案例见 **10.5 节**；带 Cookie 防盗链案例见 **41.4 节**。

**26.3 \$().b64()------Base64 编解码辅助**

\$().b64() 对字符串进行 Base64 编解码，主要用于解决 lazyRule
等方法无法嵌套使用时的传值问题。当需要把一段含特殊字符的字符串安全地传入
lazyRule 时，可以先 b64 编码，内部再解码。

> // 场景：把含特殊字符的 JSON 字符串安全传入 lazyRule
>
> var encoded = \$(\'\').b64(JSON.stringify(configObj)); // 编码
>
> url: \$(\'\').lazyRule(function(enc) {
>
> var cfg = JSON.parse(\$(\'\').b64(enc, true)); // 第二参数 true = 解码
>
> // 使用 cfg\...
>
> }, encoded)

**26.4 辅助工具方法**

  ---------------------------------------------------------------------------------------------------------------
  **方法**              **说明**                                                   **示例**
  --------------------- ---------------------------------------------------------- ------------------------------
  \$.type(param)        比 typeof 更强大的类型判断，可识别 Array、null、Date 等。  \$.type(\[\]) ===
                                                                                   \'array\'（typeof \[\] 返回
                                                                                   \'object\'）

  \$.dateFormat(date,   日期格式化。text 支持 yyyy/MM/dd/HH/mm/ss 占位符。         \$.dateFormat(new Date(),
  text)                                                                            \'yyyy-MM-dd HH:mm\')

  \$.extend             扩展 \$                                                    \$.extend.myHelper =
                        工具（添加静态属性或方法），仅在当前规则生命周期内有效。   function(){\...}
  ---------------------------------------------------------------------------------------------------------------

**26.5 交互类方法（弹窗）**

以下方法触发系统级交互弹窗，通常用在按钮的 url 字段中，通过 \$().rule 或
\$().lazyRule 包裹调用。注意：这些是 \$
基础层的轻量弹窗，功能比第二十一章 hikerPop 简单，但无需额外依赖。

  ------------------------------------------------------------------------------------------
  **方法**                **说明**                          **典型用法**
  ----------------------- --------------------------------- --------------------------------
  \$().input(title, hint, 弹出单行输入框。用户确认后执行    搜索框、自定义 Cookie 输入
  func)                   func，input 为用户输入内容。      

  \$().confirm(title,     弹出确认/取消框。confirm/cancel   删除操作二次确认、清空缓存确认
  content, confirm,       为对应回调函数。                  
  cancel)                                                   

  \$(options,             弹出下拉/网格选择框。options      多线路切换、分辨率选择、源切换
  columns).select(func)   为字符串数组，columns             
                          控制列数，func 中 input           
                          为选中值。                        
  ------------------------------------------------------------------------------------------

> // 示例：用 \$().select 实现源切换
>
> d.push({
>
> title: \'🔄 切换源\',
>
> col_type: \'text_1\',
>
> url: \$(\[\'源一\|https://site1.com\', \'源二\|https://site2.com\'\],
> 1)
>
> .select(function() {
>
> // input = 用户选中的字符串，如 \'源一\|https://site1.com\'
>
> input = input.replace(/🌸/g, \'\'); // 去除已选标记
>
> juItem.set(\'current_source\', input);
>
> refreshPage(true);
>
> return \'toast://已切换: \' + input.split(\'\|\')\[0\];
>
> })
>
> });

**26.6 rule / lazyRule / image / x5Lazy 选型速查**

  ------------------------------------------------------------------------------------------------------------
  **方法**          **执行时机**                       **返回值要求**              **适用场景**
  ----------------- ---------------------------------- --------------------------- ---------------------------
  \$().rule()       用户点击后，新开页面时执行         内部调用 setResult(d)       自定义内容页、CF
                                                                                   沙箱、小说正文

  \$().lazyRule()   用户点击后立即执行，返回值作为新   返回 URL                    列表页懒加载解析视频/图集
                    URL                                字符串（video://、pics://   
                                                       等）                        

  \$().image()      图片加载时执行（异步）             必须返回 InputStream        防盗链封面、Base64/AES
                                                                                   图片解密

  \$().x5Lazy()     X5 WebView 加载并嗅探              自动提取页面内视频地址      JS 渲染页面，无法直接 fetch
                                                                                   解析时
  ------------------------------------------------------------------------------------------------------------

**26.7 本章相关铁律速查**

  ---------------------------------------------------------------------------------------
  **编号**   **铁律名称**         **核心要义**                   **关键口诀**
  ---------- -------------------- ------------------------------ ------------------------
  Law 2      作用域隔离           lazyRule/rule/image            显式传参：fn(a,b,c),
                                  内通过参数传值，禁用 this      val1, val2, val3

  Law 26     传参透传             \$().lazyRule 显式传参，不能用 不能用 this，必须传参
                                  this                           

  Law 28     字节流图片           图片走 FileUtil.readBytes +    \$().image({image:fn})
                                  toInputStream                  或 \$().image(fn)

  Law 34     ES5 全量降级         lazyRule/rule/image 内禁用     全量 var + function(){}
                                  let/const/=\>/?.等             

  Law 52     lazyRule 禁传对象    只传字符串/数字/布尔，对象变   headers 在 lazyRule
                                  \[object Object\]              内自己拼
  ---------------------------------------------------------------------------------------

> 📌 本章知识来源：整合自语雀《海阔视界工具文档》（\$.exports /
> \$.require / \$.type / \$().rule / \$().lazyRule / \$().image
> 等官方说明），与手册其他章节的实战代码形成互补。

## 26.6 registerTask / unRegisterTask——定时任务（轮播图）

`registerTask` 在页面内注册一个定时执行的后台任务，常用于实现轮播图自动切换。

```javascript
// ── 注册定时任务 ────────────────────────────────────────────
registerTask(
    taskId,          // 任务唯一标识（字符串），同名任务会覆盖旧任务
    intervalMs,      // 执行间隔（毫秒），如 5000 = 每5秒执行一次
    $.toString(function(obj, id) {
        // ⚠️ 独立沙箱：外部变量全部不可用，必须通过参数传入
        var data = obj.data;
        var i    = Number(getMyVar('banneri', '0'));
        i++;
        if (i > data.length - 1) i = 0;
        var item = data[i];
        try {
            updateItem(id + 'bar', {       // 更新轮播卡片内容
                title: item.name,
                img:   item.img,
                url:   item.url
            });
        } catch (e) {
            unRegisterTask(id);            // 页面销毁后 updateItem 会抛异常，此时注销任务
        }
        putMyVar('banneri', i);
    }, { data: bannerData }, taskId)       // 额外参数依次跟在回调后面传入
);

// ── 注销定时任务 ────────────────────────────────────────────
unRegisterTask(taskId);
```

**完整轮播图封装范式（R星精选实战）：**

```javascript
banner: function(title, start, arr, data, cfg) {
    var id   = title + 'lunbo';
    var rnum = Math.floor(Math.random() * data.length);
    putMyVar('rnum', rnum);

    // 渲染第一张（必须设 extra.id，后续 updateItem 靠它定位卡片）
    arr.push({
        col_type: cfg.col_type || 'card_pic_1',
        img:   data[rnum].img,
        title: data[rnum].name,
        url:   data[rnum].url,
        extra: { id: id + 'bar' }
    });

    if (!start) { unRegisterTask(id); return; }

    registerTask(id, cfg.time || 5000, $.toString(function(obj, id) {
        var data = obj.data;
        var rum  = getMyVar('rnum');
        var i    = Number(getMyVar('banneri', '0'));
        if (rum != '') { i = Number(rum) + 1; clearMyVar('rnum'); }
        else i++;
        if (i > data.length - 1) i = 0;
        var item = data[i];
        try {
            updateItem(id + 'bar', { title: item.name, img: item.img, url: item.url });
        } catch (e) {
            unRegisterTask(id);   // 页面已销毁，停止任务
        }
        putMyVar('banneri', i);
    }, { data: data }, id));
}
```

**注意事项：**

| 规则 | 说明 |
|---|---|
| 沙箱隔离 | 回调是独立沙箱，外部变量全部无效，必须用参数传入 |
| 必须 try-catch | 页面销毁后任务仍在跑，`updateItem` 会抛异常，不处理会持续报错 |
| 同名覆盖 | 同一 `taskId` 重复注册会覆盖旧任务，不叠加 |
| 主动注销 | 翻页或切 tab 时建议主动调用 `unRegisterTask`，避免后台泄漏 |
| `extra.id` 必须设 | 轮播卡片必须有 `extra: { id: 'xxx' }`，`updateItem` 才能找到它 |

## 26.7 `banner()` 引擎原生轮播函数

上面 26.6 节是用 `registerTask` 手动封装的轮播。引擎同时提供了一个**原生 `banner()` 函数**，直接调用即可，不需要手动维护定时器：

```javascript
// 原生 banner() 函数签名
banner(start, d, dataList, cfg)

// 参数说明：
// start    Boolean  true=启动自动轮播定时器；false=只渲染第一张不自动切换
// d        Array    push 目标数组（主页的 d）
// dataList Array    轮播数据，每项格式：{ title, img, url }
// cfg      Object   配置项（可选）

// ── 最常见写法 ──────────────────────────────────────────────
var blist = bannerItems.map(function(it) {
    return {
        title: it.name,
        img:   it.cover,
        url:   it.link
    };
});
banner(true, d, blist, {
    col_type: 'card_pic_1',  // 轮播卡片样式，默认 card_pic_1
    desc:     '0',           // '0' = 隐藏底部描述文字；不传或传文字则显示
    time:     5000           // 自动切换间隔（毫秒），默认 5000
});
```

**`cfg` 配置项说明：**

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `col_type` | string | `'card_pic_1'` | 轮播卡片的 col_type，常用 `card_pic_1`（全宽）|
| `desc` | string | — | 卡片底部描述文字；传 `'0'` 隐藏描述区域 |
| `time` | number | `5000` | 自动切换间隔（毫秒） |

**原生 `banner()` 与手动封装版的区别：**

| 维度 | 原生 `banner(start, d, list, cfg)` | 手动封装（`registerTask`）|
|---|---|---|
| 调用方式 | 直接调用，引擎内置 | 自己写定时器逻辑 |
| 第一参数 | `Boolean`（是否启动定时器）| `String`（任务 ID 前缀）|
| 数据字段 | `title / img / url` | 自定义（可含 `extra` 等）|
| 自定义程度 | 低，只控制样式和间隔 | 高，可在切换时更新任何字段 |
| 适用场景 | 简单轮播图，数据格式统一 | 需要 `extra`/`longClick` 等复杂附加数据 |

> ⚠️ 两者第一个参数**含义完全不同**——原生版是 `Boolean`，手动封装版是 `String`。从其他规则复制代码时务必确认用的是哪个版本，否则会静默失效（传字符串给原生版会被当作 `true`，传布尔值给封装版会作为 taskId 出错）。

---

> **第二十七章：Next.js 站点实战------加密视频解析与分类独立页**

本章基于 rou.video（Next.js SSR + 客户端渲染）规则开发过程提炼，记录了
\$(url).lazyRule 触发机制、Rhino 环境下 Base64
解密、多字节字节流操作等实战踩坑，以及分类独立标签页的标准写法。

**27.1 \$(url).lazyRule 触发机制------字符串拼接不触发**

lazyRule 必须用 \$(detailUrl).lazyRule(fn, \...args)
形式，引擎才会识别并在点击时触发。直接把 lazyRule 字符串拼接到 URL
后面，引擎会把整体当普通 URL 处理，直接跳转网页。

> ⚠️ 致命错误：url + \$(\'\').lazyRule() 字符串拼接，引擎不触发
> lazyRule，直接跳网页。（Law 61）
>
> // ❌ 错误：字符串拼接，lazyRule 不触发
>
> url: host + \'/v/\' + id + \$(\'\').lazyRule(function(ua) {\...}, ua)
>
> // ✅ 正确：\$(detailUrl).lazyRule()，input = detailUrl
>
> var detailUrl = host + \'/v/\' + id;
>
> url: \$(detailUrl).lazyRule(function(ua, host) {
>
> // input 就是 detailUrl
>
> var html = fetch(input, { headers: { \'User-Agent\': ua } });
>
> // \...
>
> }, \_ua, \_host)

**27.2 Rhino 无 atob------用 android.util.Base64 替换**

Rhino JS 引擎（ES5）没有浏览器环境的 atob()/btoa() 函数，调用会报
ReferenceError: \"atob\" 未定义。必须用 Android 原生 Base64 类替换。

> ⚠️ Rhino 引擎不支持 atob()/btoa()，调用直接报 ReferenceError。（Law
> 62）
>
> // ❌ 错误：Rhino 没有 atob
>
> var decoded = atob(base64Str);
>
> // ✅ 正确：android.util.Base64 + java.lang.String
>
> var raw = android.util.Base64.decode(base64Str,
> android.util.Base64.DEFAULT);
>
> var decoded = new java.lang.String(raw, \'UTF-8\') + \'\'; // + \'\'
> 强转 JS 字符串
>
> 📌 同理，btoa() 用 android.util.Base64.encodeToString(bytes,
> android.util.Base64.NO_WRAP) 替换。

**27.3 多字节字符解密------必须按字节操作，不能按字符 split**

站点 JS 的凯撒解密逻辑是：先 atob()
得到二进制字符串（每字符=1字节），再对每个字节的 charCode 减去偏移量
k。用 android.util.Base64 解码后得到的是 byte\[\]，如果先转字符串再
.split(\'\') 切割，多字节 UTF-8 字符会被拆散，导致解密结果乱码（出现 ￣
等乱码字符）。正确做法是直接对 byte\[\] 按字节减 k，再整体转字符串。

> ⚠️ 对多字节编码字符串按字符 split(\'\') 再操作
> charCode，会拆散多字节序列导致乱码。必须在字节层面操作。（Law 63）
>
> // ❌ 错误：先转字符串再 split，多字节字符被拆散
>
> var str = new java.lang.String(bytes, \'UTF-8\') + \'\';
>
> var dec = str.split(\'\').map(function(c) {
>
> return String.fromCharCode(c.charCodeAt(0) - k);
>
> }).join(\'\');
>
> // ✅ 正确：直接在 byte\[\] 上操作，每字节减 k，再整体转字符串
>
> var k = \_ev.k & 0xff;
>
> for (var i = 0; i \< bytes.length; i++) {
>
> bytes\[i\] = ((bytes\[i\] & 0xff) - k + 256) & 0xff;
>
> }
>
> var dec = new java.lang.String(bytes, \'UTF-8\') + \'\';
>
> 📌 & 0xff 把 Java 有符号 byte（-128\~127）转为无符号值，+ 256 再 &
> 0xff 防止减法结果为负数溢出。

**27.4 Next.js \_\_NEXT_DATA\_\_ 加密字段解析完整方案**

部分 Next.js 站点为防止直接爬取视频地址，在 \_\_NEXT_DATA\_\_ 的
pageProps 中不直接暴露视频 URL，而是放一个加密字段（如 ev.d +
ev.k）。解密逻辑藏在 JS bundle 中，需逆向分析后在 lazyRule 内复现。

以 rou.video 为例，解密链路如下：

> // 1. fetch 拿 SSR HTML（\_\_NEXT_DATA\_\_ 是服务端渲染，fetch
> 即可，无需 WebView）
>
> var html = fetch(input, { headers: { \'User-Agent\': ua, \'Referer\':
> host + \'/\' } });
>
> // 2. 提取 \_\_NEXT_DATA\_\_
>
> var dm = html.match(/\<script id=\"\_\_NEXT_DATA\_\_\"
> type=\"application\\/json\"\>(\[\\s\\S\]\*?)\<\\/script\>/);
>
> var pp = JSON.parse(dm\[1\]).props.pageProps;
>
> // 3. 取加密字段 ev.d（base64）和 ev.k（偏移量）
>
> var ev = pp.ev; // { d: \'base64\...\', k: 36 }
>
> // 4. Base64 解码得到 byte\[\]
>
> var bytes = android.util.Base64.decode(ev.d,
> android.util.Base64.DEFAULT);
>
> // 5. 按字节减 k（凯撒解密）
>
> var k = ev.k & 0xff;
>
> for (var i = 0; i \< bytes.length; i++) {
>
> bytes\[i\] = ((bytes\[i\] & 0xff) - k + 256) & 0xff;
>
> }
>
> // 6. 转字符串并 JSON.parse
>
> var obj = JSON.parse(new java.lang.String(bytes, \'UTF-8\') + \'\');
>
> // 7. 从 URL 字段推导真实视频地址
>
> // videoUrl 是封面图（index.jpg），替换扩展名得到 m3u8
>
> var src = obj.videoUrl.replace(\'index.jpg\', \'index.m3u8\');
>
> return \'video://\' + src + \';{User-Agent@\' + ua + \'&&Referer@\' +
> host + \'/}\';
>
> 📌 \_\_NEXT_DATA\_\_ 是 Next.js SSR 注入的 JSON，用普通 fetch
> 就能拿到，不需要 fetchCodeByWebView。需要 WebView
> 的是客户端渲染（CSR）页面------没有 \_\_NEXT_DATA\_\_ 或数据在 JS
> 执行后才有。

**27.5 从 URL 结构推导视频地址------扩展名替换**

部分站点的封面图 URL 和视频 m3u8 URL 只有文件名不同，路径和 token
参数完全一样。这种情况可以直接替换扩展名，完全跳过 lazyRule
内的额外请求，是最快的解析方案。

> // 封面:
> https://cdn.example.com/hls/xxxid/xxxid-720/index.jpg?exp=\...&auth=\...
>
> // 视频:
> https://cdn.example.com/hls/xxxid/xxxid-720/index.m3u8?exp=\...&auth=\...
>
> var src = obj.videoUrl.replace(\'index.jpg\', \'index.m3u8\');
>
> // ✅ auth/exp token 不变，直接可用
>
> 📌 发现这个规律的方法：打开浏览器开发者工具 Network 面板，过滤
> .m3u8，看 CDN
> 路径是否和封面图同目录。如果是，直接替换文件名，无需额外请求（参见第二十章
> 20.4 节）。

**27.6 分类独立标签页------\$(\'hiker://empty##fypage\').rule()
翻页方案**

当分类数量多、结构复杂，不适合用 fyclass
静态分类时，用独立标签页方案：主页展示分类列表，每个 tag
点击后新开独立页面，页面内部用 MY_PAGE 翻页。

> // 分类按钮
>
> d.push({
>
> title: cat.name,
>
> col_type: \'scroll_button\',
>
> url: \$(\'hiker://empty##fypage\').rule(function(tid, host, ua) {
>
> // MY_PAGE 自动注入，支持翻页
>
> var d = \[\];
>
> var url = host + \'/t/\' + encodeURIComponent(tid);
>
> if (MY_PAGE \> 1) url += \'?page=\' + MY_PAGE;
>
> var html = fetch(url, { headers: { \'User-Agent\': ua } });
>
> // \... 解析列表 \...
>
> setResult(d); // ✅ \$().rule 内用 setResult，不 return
>
> }, cat.id, host, ua)
>
> });
>
> ⚠️ \$().rule() 内必须用 setResult(d)，不能 return d。\$().lazyRule()
> 才是 return 字符串。（Law 47 延伸）

**27.7 本章新增铁律（Law 61-63）**

  ------------------------------------------------------------------------------
  **编号**   **铁律名称**         **核心要义**             **关键口诀**
  ---------- -------------------- ------------------------ ---------------------
  Law 61     lazyRule 触发方式    必须用                   拼接 =
                                  \$(url).lazyRule()       跳网页，\$(url) =
                                  形式，字符串拼接不触发   触发

  Law 62     禁用 atob/btoa       Rhino 无 atob/btoa，用   atob →
                                  android.util.Base64      Base64.decode；btoa →
                                                           encodeToString

  Law 63     字节层面解密         多字节编码字符串必须在   & 0xff
                                  byte\[\] 上操作，不能    转无符号，字节减
                                  split 后操作 charCode    k，整体转字符串
  ------------------------------------------------------------------------------

> 📌 本章内容来自 rou.video（Next.js SSR）规则开发实战，适用于所有使用
> \_\_NEXT_DATA\_\_ 加密字段的 Next.js 站点。

**27.8 `__PAGE__PARAMS__` Hex-XOR 加密视频站通用破解模式**

大量使用 Video.js + WASM 播放器的成人视频站采用同一套加密方案：把页面配置（含嵌入播放链接）编码成一段 hex 字符串塞进页面变量，后 N 位是 XOR key，前面是加密数据，循环 XOR 后 JSON.parse 即得播放地址。

**27.8.1 特征识别**

满足以下任意两条即可认定适用本模式：

| 特征 | 检测方法 |
|---|---|
| 页面内有 `__PAGE__PARAMS__` 或 `__APP_CONFIG__` 赋值 | 页面源码搜索 `__PAGE_` |
| 页面底部加载 `common-v12.js`（或类似版本命名） | 源码搜索 `common-v` |
| 页面加载 `video.min.js` + WASM 文件 | 源码搜索 `.wasm` |
| embed URL 自带 token 参数（如 `?token=xxx`） | Network 面板观察播放请求 |

**27.8.2 破解流程**

```
1. 请求详情页 HTML
2. 正则提取 __PAGE__PARAMS__ 的 hex 字符串值
3. 后 32 位 hex = XOR key（不同站可能是 16/32/64 位，从 JS 里确认）
4. 前面部分 = 加密数据 hex
5. 按字节 hex decode + 循环 XOR → JSON.parse
6. 取 pageConfig.player.embedUrl（已含完整 token）
7. 返回 video:// + embedUrl + #嗅探;{Header}
```

**27.8.3 标准解析代码模板**

```javascript
解析: function(url) {
    var host = this.host;
    var ua   = MOBILE_UA;
    var html = fetch(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });

    // ── Step1：提取 hex 字符串 ──────────────────────────────
    var paramsMatch = html.match(/__PAGE__PARAMS__\s*=\s*"([^"]+)"/);
    if (!paramsMatch) return 'toast://未找到加密参数';
    var hexStr = paramsMatch[1];

    // ── Step2：分离 key 和加密数据 ─────────────────────────
    // key 长度需从站点 JS 确认，常见值：16 / 32 / 64（hex 字符数）
    var keyLen  = 32;
    var key     = hexStr.slice(-keyLen);         // 后 32 位
    var dataHex = hexStr.slice(0, -keyLen);      // 前面全部

    // ── Step3：hex decode + XOR 循环解密 ───────────────────
    var result = '';
    for (var i = 0; i < dataHex.length; i += 2) {
        var byteVal = parseInt(dataHex.substr(i, 2), 16);
        var keyChar = key.charCodeAt((i / 2) % key.length);
        result += String.fromCharCode(byteVal ^ keyChar);
    }

    // ── Step4：解析 JSON，取播放地址 ────────────────────────
    var pageConfig = JSON.parse(result);
    var embedUrl   = pageConfig.player.embedUrl;
    // 部分站点路径不同，备选：pageConfig.video.url / pageConfig.embed_url

    // ── Step5：返回（video:// + #嗅探 组合，不能只用 #嗅探）
    return 'video://' + embedUrl
        + '#嗅探;{User-Agent@' + ua
        + '&&Referer@' + host + '/}';
},
```

**27.8.4 关键注意事项**

| 注意点 | 说明 |
|---|---|
| key 长度要从 JS 确认 | 不同站可能是 16/32/64 hex 字符，在 `common-vXX.js` 里搜索 `slice(-` 即可找到 |
| 变量名可能不同 | `__PAGE__PARAMS__` / `__APP_CONFIG__` / `__INIT_DATA__`，按实际正则匹配 |
| JSON 路径要按站点确认 | `player.embedUrl` 是最常见路径，也可能是 `video.url` 或 `data.embed` |
| 必须用 `video://` + `#嗅探` 组合 | 单独用 `#嗅探` 不带 `video://` 前缀会导致协议识别失败 |
| embedUrl 已含完整 token | 不需要额外请求 token 接口，直接用即可 |
| 中文分类名可直接拼 URL | 此类站点 `class_url` 里的中文分类名是有效 URL 路径，无需编码 |
| Referer 必须带 | 播放请求通常校验 Referer，填主域名根路径 |

**27.8.5 适配其他同类站点**

遇到新站点时的排查步骤：

```javascript
// 1. 先打印 hexStr 确认格式
log('hexStr length: ' + hexStr.length);
log('hexStr sample: ' + hexStr.slice(0, 40));

// 2. 尝试不同 key 长度，看解密结果是否为合法 JSON
var keyLens = [16, 32, 64, 128];
keyLens.forEach(function(kl) {
    try {
        var k = hexStr.slice(-kl);
        var d = hexStr.slice(0, -kl);
        var r = '';
        for (var i = 0; i < Math.min(d.length, 40); i += 2) {
            r += String.fromCharCode(parseInt(d.substr(i, 2), 16) ^ k.charCodeAt((i/2) % k.length));
        }
        log('keyLen=' + kl + ' result prefix: ' + r);
    } catch(e) {}
});

// 3. 确认 key 长度后，log 完整解密结果找正确 JSON 路径
log(JSON.stringify(Object.keys(pageConfig)));
log(JSON.stringify(Object.keys(pageConfig.player || {})));
```

> 📌 **Law 63 延伸**：hex 字符串每两位是一个字节，循环步长必须是 `i += 2`，key 索引用 `(i/2) % key.length`（字节索引，不是字符索引）。两处搞混会导致解密出乱码但不报错，属于静默 Bug。

---

> **第二十八章：静态分类多维筛选补充------URL 模板四种写法**

第二章已介绍静态分类的五维占位符和矩阵方案的核心逻辑。本章作为补充，整理四种常见
URL 模板写法和注意事项，供快速查阅。

**28.1 四种 URL 模板写法**

根据站点 URL 结构不同，静态分类的 url 字段有以下四种常见形态：

▶ 写法一：标准 MACCMS 路径型

> \"静态分类\": {
>
> \"type\": \"主页\",
>
> \"url\":
> \"https://example.com/vod-show/fyclass-fyarea-fysort\-\--fyyear\-\--fypage\-\--.html\",
>
> \"class_name\": \"电影&电视剧&综艺&动漫\", \"class_url\": \"1&2&3&4\",
>
> \"area_name\": \"全部&大陆&香港&台湾&美国&韩国&日本\",
>
> \"area_url\": \"&大陆&香港&台湾&美国&韩国&日本\",
>
> \"year_name\": \"全部&2026&2025&2024&2023\",
>
> \"year_url\": \"&2026&2025&2024&2023\",
>
> \"sort_name\": \"时间&人气&评分\", \"sort_url\": \"time&hits&score\"
>
> }

▶ 写法二：纯路径拼接型

> \"静态分类\": {
>
> \"type\": \"主页\",
>
> \"url\": \"https://example.com/list/fyclass/fyarea/fyyear\",
>
> \"class_name\": \"电影&电视剧&综艺&动漫\", \"class_url\": \"1&2&3&4\",
>
> \"area_name\": \"全部&大陆&香港&台湾\", \"area_url\":
> \"all&mainland&hongkong&taiwan\",
>
> \"year_name\": \"全部&2026&2025&2024\", \"year_url\":
> \"all&2026&2025&2024\"
>
> }

▶ 写法三：单维仅分页型

> \"静态分类\": {
>
> \"type\": \"主页\",
>
> \"url\": \"https://example.com/type/fyclass/page/fypage\",
>
> \"class_name\": \"推荐&最新&热门&排行\",
>
> \"class_url\": \"recommend&latest&hot&rank\"
>
> }

▶ 写法四：查询参数型（API 站点）

> \"静态分类\": {
>
> \"type\": \"主页\",
>
> \"url\":
> \"https://example.com/api?type=fyclass&area=fyarea&year=fyyear&sort=fysort&page=fypage\",
>
> \"class_name\": \"电影&电视剧&综艺&动漫\", \"class_url\": \"1&2&3&4\",
>
> \"area_name\": \"全部&大陆&香港&台湾\", \"area_url\":
> \"&mainland&hongkong&taiwan\",
>
> \"year_name\": \"全部&2026&2025&2024\", \"year_url\":
> \"&2026&2025&2024\",
>
> \"sort_name\": \"最新&最热\", \"sort_url\": \"new&hot\"
>
> }

**28.2 使用静态分类的主页函数写法**

启用静态分类后，引擎自动将占位符替换为用户选择的值，并把完整 URL 注入
MY_URL。主页函数只需直接用 MY_URL 请求即可，无需手动拼接分类参数。

> var parse = {
>
> \"页码\": { \"主页\": 1 },
>
> \"静态分类\": {
>
> \"type\": \"主页\",
>
> \"url\": \"https://example.com/list/fyclass_fypage.html\",
>
> \"class_name\": \"电影&电视剧&动漫\", \"class_url\": \"1&2&3\"
>
> },
>
> \"主页\": function() {
>
> var \_ua = this.UA;
>
> var \_url = MY_URL; // ✅ 引擎已自动替换所有占位符
>
> var \_html = fetch(\_url, { headers: { \"User-Agent\": \_ua } });
>
> var \_d = \[\];
>
> var \_items = pdfa(\_html, \"body&&.list-item\");
>
> for (var i = 0; i \< \_items.length; i++) {
>
> var \_t = \_items\[i\];
>
> \_d.push({
>
> title: pdfh(\_t, \"a&&Text\"),
>
> url: pd(\_t, \"a&&href\", this.host),
>
> pic_url: pd(\_t, \"img&&src\"),
>
> col_type: \"movie_3\"
>
> });
>
> }
>
> return \_d;
>
> }
>
> };
>
> 📌 静态分类的 type 必须为 \"主页\"，聚阅引擎通过 type
> 识别入口类型。fypage 由引擎自动管理，开发者只需在 url
> 模板中放置占位符即可，无需在函数内处理页码。
>
> ⚠️ name 与 url 数量必须严格一一对应（& 分隔），\"全部\" 选项对应的 url
> 留空字符串。数量不匹配会导致越界取到 undefined（Law 49 / Law 50）。
>
> **第二十九章：聚阅测试仓核心调用机制**

聚阅测试仓（聚阅）是阔界小程序的专用测试环境，所有源接口（JS
代码）都在壳子内解析执行。本章说明各入口函数的调用流程和数据结构规范，帮助开发者理解壳子如何驱动
parse 对象。

**29.1 一级（主页）调用流程**

> 用户打开小程序
>
> → yiji() 被调用
>
> → 读取当前选中的源接口信息 jkdata
>
> → 渲染顶部菜单栏（切源/频道/搜索/收藏/管理）
>
> → 调用 getYiData(\'主页\', jkdata, d) 加载主页内容
>
> → 内部执行 parse\[\'主页\'\]() 获取列表数据
>
> 📌 jkdata 包含接口的 name、url（代码文件路径）、type、group、id
> 等信息。getObjCode(jkdata, \'yi\') 解析出 parse
> 对象。静态分类由壳子自动渲染，开发者只需定义好字段即可。

**29.2 二级（详情页）调用流程**

> 用户点击列表项
>
> → erji() 被调用
>
> → 从 MY_PARAMS 获取附加信息（含 jkdata）
>
> → 执行 parse\[\'二级\'\](MY_URL) 获取详情数据
>
> → 渲染封面、简介、线路、选集

二级函数完整返回结构（手册第七章基础上的补充字段）：

> return {
>
> // ── 封面信息 ────────────────────────────────────────
>
> img: \'封面图片URL\', // 必填
>
> desc: \'简介文字\', // 可选
>
> detailurl: \'点击封面跳转URL\', // 可选
>
> detailObj: {}, // 可选，完整封面对象，优先级最高
>
> // ── 线路与选集（核心）──────────────────────────────
>
> line: \[\'线路1\', \'线路2\'\], // 线路名称数组
>
> list: \[ // 与 line 一一对应的二维数组
>
> \[ {title:\'第01集\', url:\'链接1\'}, {title:\'第02集\',
> url:\'链接2\'} \],
>
> \[ {title:\'第01集\', url:\'链接1\'}, {title:\'第02集\',
> url:\'链接2\'} \]
>
> \],
>
> // ── 高级控制字段 ────────────────────────────────────
>
> type: \'video\', // video/comic/novel/audio/aggregate
>
> noShow: { 封面:false, 简介:false, 排序:false, 选集:false },
>
> moreitems: \[\], // 选集上方扩展项
>
> extenditems: \[\], // 选集下方扩展项（图文详情常用）
>
> extra: {}, // 选集扩展对象
>
> novel: true, // 是否为小说模式
>
> // ── 分页支持 ────────────────────────────────────────
>
> pageparse: function(url) {}, // 分页动态解析函数
>
> listparse: function(lineid, linename) {} // 列表动态解析函数
>
> };
>
> ⚠️ line 与 list 必须严格一一对应，list
> 是二维数组（外层按线路，内层按集数）。line.length !== list.length
> 会导致线路错位（Law 51）。

**29.3 搜索 / 解析 / 最新 / 频道 / 换源调用流程**

  -------------------------------------------------------------------------------------------
  **入口**        **调用方式**                          **说明**
  --------------- ------------------------------------- ----------------------------------------------
  搜索            parse\[\'搜索\'\](name, page)         用户输入关键词后触发，返回列表数组；page 同时也可用 MY_PAGE

  解析            parse\[\'解析\'\](url)                用户点击选集后触发，返回播放链接字符串

  最新            parse\[\'最新\'\](surl, parse)        书架"最新"触发，返回字符串（如"更新至第100集"）显示在书架副标题

  频道            parse\[\'频道\'\]()                   \"频道\"菜单触发，返回频道数据或执行频道函数

  换源            壳子重新加载接口代码                  用户切换数据源后，壳子重新执行一级/二级
  -------------------------------------------------------------------------------------------

解析函数返回格式：

> // 视频模式：返回播放链接字符串
>
> return \'video://\' + url + \';{User-Agent@\' + ua + \'&&Referer@\' +
> host + \'/}\';
>
> // 小说模式：返回章节内容数组
>
> return \[{ title: \'第一章\', content: \'正文内容\...\' }\];
>
> // 嗅探兜底
>
> return url + \'#嗅探\';

**29.3.1 `最新` 函数——书架更新提示钩子**

`最新` 是一个书架专用钩子，引擎在刷新书架时调用，结果显示在书架卡片的副标题位置（如"更新至第100集"）。**必须 `return` 字符串，不能用 `setResult`。**

```javascript
// ── 正确写法 ──────────────────────────────────────────────────
"最新": function(surl, parse) {
    // surl  = 当前内容的详情页 URL（即二级页面的 MY_URL）
    // parse = 当前规则的 parse 对象（可调用其上的方法）
    var html = request(surl, { timeout: 8000 });
    var latest = pdfh(html, '.update-info&&Text');
    return '更新至：' + latest;   // ✅ return 字符串
},

// ❌ 错误写法：setResult 在最新函数里不会输出任何内容
"最新": function(surl, parse) {
    var news = pdfh(html, '.update&&Text');
    setResult(news);   // ❌ 静默失败，书架副标题显示空白
}
```

**参数说明：**

| 参数 | 说明 |
|---|---|
| `surl` | 当前内容的详情页 URL，与二级函数收到的 `MY_URL` 相同 |
| `parse` | 当前规则的 parse 对象，可调用 `parse.post()` 等工具方法 |

> 📌 `最新` 函数不是每次打开规则都触发，只在用户主动刷新书架、或书架后台定时检查时触发。返回值只能是字符串，引擎直接拿来显示，不会解析 HTML。

**29.3.2 搜索函数 `page` 第二参数**

引擎调用搜索函数时会同时传入两个参数：关键词 `name` 和当前页码 `page`。两者都可以直接用，`page` 和 `MY_PAGE` 值相同，用哪个都行：

```javascript
// ── 两种写法等价 ───────────────────────────────────────────────
// 写法一：只用 name，翻页靠 MY_PAGE
"搜索": function(name) {
    var url = host + '/search/' + MY_PAGE + '?q=' + encodeStr(name, 'GBK');
    // ...
},

// 写法二：name + page 双参数，更明确
"搜索": function(name, page) {
    var url = host + '/search/' + page + '?q=' + encodeStr(name, 'GBK');
    // ...
},
```

> 📌 第一章骨架里搜索函数只写了 `function(name)`，容易误导认为只有一个参数。实际上引擎注入的是 `(name, page)` 双参数，GBK 站点翻页搜索用 `page` 参数比 `MY_PAGE` 更清晰。

**29.3.2a 搜索状态机模式——搜索函数只做变量写入，主页接管渲染**

当站点的搜索结果页结构与普通列表页完全一致时，可以不在搜索函数里渲染，而是把搜索关键词写入变量，由主页函数检测后接管渲染。这样搜索结果天然支持翻页，且与主页共用同一套列表渲染逻辑：

```javascript
// ── 搜索函数：只写变量，不渲染，返回空数组 ─────────────────────
"搜索": function(key) {
    putMyVar('keyword', key);
    refreshPage(false);
    return [];
},

// ── 主页函数：最开头检测 keyword ──────────────────────────────
主页: function() {
    var d = this.d;
    var kw = getMyVar('keyword', '');

    // 搜索态：按关键词请求并渲染
    if (kw) {
        if (MY_PAGE == 1) {
            // 顶部加"← 返回"按钮，点击清空搜索态
            d.push({
                title: '← 返回',
                col_type: 'scroll_button',
                url: $('#noLoading#').lazyRule(function() {
                    putMyVar('keyword', '');
                    refreshPage(false);
                    return 'hiker://empty';
                })
            });
        }
        var html = fetch(host + '/search?q=' + encodeURIComponent(kw) + '&page=' + MY_PAGE);
        return this._parseList(html, d);
    }

    // 正常态：渲染首页
    // ...
},
```

**优势：**
- 搜索结果页和普通列表页共用 `_parseList`，零重复代码
- 翻页自动生效（`MY_PAGE` 在主页函数里正常递增）
- 搜索态和普通态切换靠变量，不需要额外页面跳转

> ⚠️ 此模式要求搜索结果页和普通列表页 HTML 结构一致。如果搜索结果页结构不同，仍需在搜索函数里独立渲染。

**29.3.3 二级 `list` 省略 `line` 的单线路写法**

Law 65 说 `line` 与 `list` 必须一一对应，`list` 是二维数组。但这是**多线路时**的规则。只有一条线路时，可以**省略 `line`，`list` 直接传一维数组**：

```javascript
// ── 多线路写法（必须二维数组 + line）─────────────────────────
return {
    img: img, desc: desc,
    line: ['线路1', '线路2'],
    list: [
        [{ title: '第01集', url: 'url1' }, ...],   // 线路1选集
        [{ title: '第01集', url: 'url2' }, ...],   // 线路2选集
    ]
};

// ── 单线路写法（省略 line，list 直接一维数组）────────────────
return {
    img: img, desc: desc,
    list: [
        { title: '第01集', url: 'url1' },
        { title: '第02集', url: 'url2' },
    ]
    // 不传 line，引擎自动识别为单线路
};
```

适用场景：漫画章节、有声书集数、小说章节——这类内容天然只有一个播放/阅读序列，不需要多线路。

> ⚠️ 省略 `line` 时 `list` 必须是**纯一维数组**（元素是 `{title, url}` 对象）。如果 `list` 是二维数组但没有 `line`，引擎行为未定义，可能出现线路错位。两种写法不能混用。

**29.3.4 有声/听书站标题清洗正则链**

有声站书名经常附带主播、系列、状态等杂质，需要多步正则清理。以下是实战提炼的通用清洗链：

```javascript
title: pdfh(data, 'a&&title')
    // 去除全角括号内容：书名（完结）→ 书名
    .replace(/\（[^）]*\）/g, '')
    // 去除主播分隔符后的内容：书名｜主播名 → 书名
    .replace(/\｜.*/, '')
    .replace(/\|.*/, '')
    // 去除空格后的内容（英文括号前）：书名 (完本) → 书名
    .replace(/\s.*/, '')
    // 去除书名号：【完结】→ ''
    .replace(/【.*?】/g, '')
    // 去除竖线变体
    .replace(/\丨/g, '')
    // 去除斜杠后内容
    .replace(/\/.*/, '')
    // 去除连字符后内容
    .replace(/-.*/, '')
    .trim()

// ── 更简洁的链式合并版本 ────────────────────────────────────
title: pdfh(data, 'a&&title')
    .replace(/（.*|｜.*|\|.*|\(.*|【.*?】|丨|\/.*|-.*|\s.*/, '')
    .trim()
```

> 📌 有声站书名清洗原则：只保留主书名，去除括号内的状态词（完结/完本/更新中）、主播名、系列名、分隔符。正则顺序有影响——先去括号内容，再去分隔符后内容，最后 trim。

**29.4 新增铁律（Law 64-66）**

  -----------------------------------------------------------------------------
  **编号**   **铁律名称**       **核心要义**                 **关键口诀**
  ---------- ------------------ ---------------------------- ------------------
  Law 64     静态分类 type 固定 静态分类.type 必须为         type 写错 =
                                \"主页\"，壳子通过 type      壳子不识别
                                识别入口                     

  Law 65     line/list 一一对应 line 与 list                 line.length ===
                                长度必须相等，list           list.length
                                是二维数组                   

  Law 66     页码字段联动       页码对象定义起始页，fypage   页码: { 主页: 1 }
                                由引擎自动递增               
  -----------------------------------------------------------------------------

> 📌
> 本章内容来源：聚阅测试仓（壳子）核心机制分析，适用于所有基于聚阅壳子开发的接口规则。
>
> **第三十章：base64 封面图处理------正确选型与 batchFetch 并发方案**

本章修正第二十六章关于 \$.image()
的部分描述，并总结封面图处理的正确选型原则。核心结论：引擎原生支持
data:image 字符串，无需 Java 解码；\$.image()
只用于需要额外处理（XOR/AES解密）的场景。

**30.1 引擎对 pic_url 的支持格式**

聚阅/海阔视界引擎的 pic_url
字段支持以下三种格式，直接赋值即可渲染，不需要任何额外处理：

  ---------------------------------------------------------------------------------------------------
  **格式**                     **示例**                            **说明**
  ---------------------------- ----------------------------------- ----------------------------------
  普通 HTTP URL                https://cdn.example.com/cover.jpg   最常见，直接请求图片

  data:image 字符串            data:image/webp;base64,UklGR\...    引擎原生支持，直接渲染，无需解码

  data:image（反斜杠分隔符）   data:image\\webp;base64,UklGR\...   引擎同样支持，replace(/\\\\/g,
                                                                   \'/\') 修正后更稳
  ---------------------------------------------------------------------------------------------------

> 📌 站点 txt 文件内容是 data:image\\webp;base64,\... 格式时，直接 fetch
> 取到字符串，replace(/\\\\/g, \'/\') 修正反斜杠后赋给 pic_url
> 即可。引擎会自动解码渲染，不需要 Java Base64 解码。

**30.2 \$.image() 的正确使用场景**

\$.image() 的 func 是图片加载完成后的处理管道，input 是引擎下载该 URL
后得到的
InputStream。只有在需要对图片字节流做额外变换时才用它，直接赋值能解决的一律不用。

  -----------------------------------------------------------------------
  **场景**                **是否需要       **方案**
                          \$.image()**     
  ----------------------- ---------------- ------------------------------
  普通图片 URL（HTTP）    ❌ 不需要        直接赋给 pic_url

  data:image base64       ❌ 不需要        replace(/\\\\/g, \'/\') 后赋给
  字符串                                   pic_url

  图片前 N 字节 XOR 加密  ✅ 需要          \$.image() +
                                           FileUtil.toBytes + 手动 XOR

  图片内容 AES 加密       ✅ 需要          \$.image() +
                                           CryptoUtil.AES.decrypt

  防盗链（需带 Referer）  ❌ 不需要        pic_url + \@Referer=xxx 或
                                           \@headers={} 格式
  -----------------------------------------------------------------------

> ⚠️ 不要把 data:image base64 字符串误当作「需要解密」的场景去用
> \$.image()。\$.image()
> 只处理「图片字节流需要变换」的情况，否则只会让代码复杂化并引入不必要的
> bug。

**30.3 batchFetch 并发方案------解决串行 fetch 资源占用高**

每张封面单独同步
fetch，一页24张卡片就是24次串行网络请求，严重阻塞列表构建。改用
batchFetch
并发拉取，引擎自动分批（最多16个/批），速度大幅提升，不阻塞主线程。

> // ❌ 旧方案：串行 fetch，24张=24次阻塞，资源占用高
>
> for (var i = 0; i \< items.length; i++) {
>
> var b64 = fetch(txtUrl, { headers: { \'Referer\': host + \'/\' } });
> // 阻塞
>
> \_b64Map\[vid\] = b64.replace(/\\\\/g, \'/\').trim();
>
> }
>
> // ✅ 新方案：batchFetch 并发，自动分批，不阻塞
>
> var \_tasks = \[\];
>
> for (var ti = 0; ti \< \_txtUrls.length; ti++) {
>
> \_tasks.push({ url: \_txtUrls\[ti\].url, options: { headers: {
> \'Referer\': host + \'/\' } } });
>
> }
>
> var \_results = batchFetch(\_tasks); // 最多16个/批，超出自动分批串行
>
> for (var ri = 0; ri \< \_results.length; ri++) {
>
> var b64 = \_results\[ri\];
>
> if (typeof b64 === &quot;string&quot; && b64.length \> 50) {
>
> \_b64Map\[\_txtUrls\[ri\].vid\] = b64.replace(/\\\\/g, \'/\').trim();
>
> }
>
> }
>
> // 构建卡片时直接取结果
>
> d.push({
>
> pic_url: \_b64Map\[\_vid\] \|\| \'\',
>
> // \...
>
> });
>
> 📌 batchFetch 并发上限为 16（手册第三章 3.3 节）。传入超过 16 个 URL
> 时自动分批串行执行。对于一页24张封面，实际是第一批16个并发 +
> 第二批8个并发，比串行快约 8-10 倍。

**30.4 完整封面处理流程（batchFetch 方案模板）**

> \_parseList: function(html, cfg) {
>
> var \_host = this.host;
>
> var \_tupath = (cfg && cfg.tupath) ? cfg.tupath : \'\';
>
> // Step1：解析封面路径映射
>
> var \_coverMap = {};
>
> var \_fcm = html.match(/fetchMultipleCover\\(\\\[(\[\^\\\]\]+)\\\]/);
>
> if (\_fcm) {
>
> var \_pairs = \_fcm\[1\].match(/\'(\[\^\'\]+)\'/g) \|\| \[\];
>
> for (var j = 0; j \< \_pairs.length; j++) {
>
> var \_pair = \_pairs\[j\].replace(/\'/g, \'\').split(\'\|\');
>
> if (\_pair.length === 2) {
>
> var \_vm = \_pair\[0\].match(/-(\\d+)\$/);
>
> if (\_vm) \_coverMap\[\_vm\[1\]\] = \_pair\[1\];
>
> }
>
> }
>
> }
>
> // Step2：收集所有 txt URL
>
> var \_txtUrls = \[\];
>
> var \_list = pdfa(html, \'body&&.cell\');
>
> for (var i = 0; i \< \_list.length; i++) {
>
> var \_vidm = \_list\[i\].match(/\\/(\\d+)\\.htm\$/);
>
> var \_vid = \_vidm ? \_vidm\[1\] : \'\';
>
> if (\_vid && \_tupath && \_coverMap\[\_vid\]) {
>
> \_txtUrls.push({ vid: \_vid, url: \_tupath + \'cover_64_2/\' +
> \_coverMap\[\_vid\] + \'.txt\' });
>
> }
>
> }
>
> // Step3：batchFetch 并发拉取
>
> var \_b64Map = {};
>
> if (\_txtUrls.length \> 0) {
>
> var \_tasks = \_txtUrls.map(function(item) {
>
> return { url: item.url, options: { headers: { \'Referer\': \_host +
> \'/\' } } };
>
> });
>
> var \_results = batchFetch(\_tasks);
>
> for (var ri = 0; ri \< \_results.length; ri++) {
>
> var \_b64 = \_results\[ri\];
>
> if (\_b64 && \_b64.length \> 50) {
>
> \_b64Map\[\_txtUrls\[ri\].vid\] = \_b64.replace(/\\\\/g,
> \'/\').trim();
>
> }
>
> }
>
> }
>
> // Step4：构建卡片，pic_url 直接赋 base64 字符串
>
> var d = \[\];
>
> for (var i = 0; i \< \_list.length; i++) {
>
> // \... 标题/时长解析 \...
>
> d.push({
>
> title: \_title,
>
> pic_url: (\_vid && \_b64Map\[\_vid\]) ? \_b64Map\[\_vid\] : \'\', //
> ✅ 直接赋值
>
> url: \$(detailUrl).lazyRule(fn, \_host, \_ua), // ✅ \$(url).lazyRule
>
> col_type: \'movie_3\'
>
> });
>
> }
>
> return d;
>
> },
>
> **第三十一章：新版海阔视界 ES6 兼容性实测报告**

新版海阔视界引擎声称支持约 90% 的 ES6
写法。本章记录实测验证结果（基于动漫站规则开发测试），明确哪些特性可用、哪些仍需回避，供后续规则开发参考。

**31.1 兼容性速查表**

  ---------------------------------------------------------------------------
  **ES6 特性**             **支持状态**   **说明 / 替代方案**
  ------------------------ -------------- -----------------------------------
  const / let              ✅ 支持        可完全替代 var

  箭头函数 () =\> {}       ✅ 支持        回调、map/filter
                                          等链式操作均可用。完整函数体 item
                                          =\> { } 和单参数简写 item =\> expr
                                          均支持，实测验证

  模板字符串 \`\${}\`      ✅ 支持        字符串拼接推荐使用

  .filter().map() 链式     ✅ 支持        数组处理可替代 for 循环

  对象属性简写 { x }       ✅ 支持        { title, url } 等简写正常

  方法简写 fn() {}         ❌ 不支持      必须写成 fn: function() {}

  展开运算符 \...          ❌ 不支持      数组合并用 .concat()，对象合并用
                                          Object.assign()

  参数解构 ({ name, id })  ❌ 不支持      改为 item =\> { const name =
                                          item.name }

  对象解构 const { a } =   ⚠️ 不稳定      建议逐行赋值：const a = obj.a
  obj                                     

  可选链 ?.                ⚠️ 不稳定      用 && 替代：obj && obj.prop

  空值合并 ??              ⚠️ 不稳定      用 \|\| 替代

  async / await            ❌ 不适用      引擎单线程模型，无实际并发效果
  ---------------------------------------------------------------------------

**31.2 推荐写法模板（ES6 安全子集）**

基于实测结果，以下是在新版引擎中既简洁又安全的写法组合：

> // ✅ 变量：const/let
>
> const host = this.host;
>
> let d = \[\];
>
> // ✅ 箭头函数 + 模板字符串
>
> const url = MY_PAGE \> 1 ? \`\${host}/list/\${MY_PAGE}.html\` :
> \`\${host}/list/\`;
>
> // ✅ 数组链式处理
>
> const cards = pdfa(html, \'.item&&li\')
>
> .filter(it =\> pdfh(it, \'a&&Text\') && pd(it, \'a&&href\'))
>
> .map(it =\> ({
>
> title: pdfh(it, \'a&&Text\'),
>
> url: pd(it, \'a&&href\'),
>
> col_type: \'movie_3\'
>
> }));
>
> // ✅ 数组合并：concat 而非 \...
>
> return d.concat(cards);
>
> // ❌ 不用方法简写
>
> // 主页() { } ← 报错
>
> // ✅ 用冒号函数
>
> 主页: function() { }
>
> // ❌ 不用参数解构
>
> // cats.forEach(({ name, id }) =\> { }) ← 报错
>
> // ✅ 先取属性
>
> cats.forEach(cat =\> {
>
> const name = cat.name;
>
> const id = cat.id;
>
> });

**31.3 lazyRule / rule 内部可以用完整 ES6**

lazyRule 和 rule 的 func 参数是独立编译的字符串，引擎对其 ES6
支持更完整。在 func
内部可以放心使用箭头函数、模板字符串、const/let、链式数组方法。

> 📌 parse 对象方法层（外层）受引擎限制较多；lazyRule/rule 的 func
> 内部（内层）ES6 支持更好。外层保守、内层可激进。
>
> ⚠️ 无论内层外层，展开运算符 \... 和方法简写 fn(){}
> 都不支持，一律避免。

**31.4 原 Law 34 更新**

原 Law 34「ES5 全量降级------禁用 let/const/=\>/?./ 全量 var +
function{}」在新版引擎已过时，更新如下：

  ------------------------------------------------------------------------------
  **编号**     **铁律名称**         **新内容**
  ------------ -------------------- --------------------------------------------
  Law          ES6 安全子集         const/let/箭头函数/模板字符串/链式数组方法
  34（更新）                        可用；方法简写/展开运算符/参数解构/?./??
                                    禁用；方法必须写成 key: function() {}
                                    形式。箭头函数完整体 item =\> { } 与简写
                                    item =\> expr 均实测可用。⚠️ window/document
                                    等引擎内置名禁止用 let/const
                                    重声明，必须保持 var。

  ------------------------------------------------------------------------------

> 📌
> 本章结论基于新版海阔视界引擎实测（2026年），旧版引擎或聚阅低版本仍需遵守原
> Law 34 全量 ES5。建议开发时标注目标引擎版本。
>
> **⚡ Law 68（开发规范）：所有规则统一使用 ES6
> 安全子集开发。即：const/let 替代 var，箭头函数替代回调
> function，字符串保持 + 拼接，方法写成 key: function() {}
> 形式，window/document 等引擎内置名保持 var
> 声明。禁止使用方法简写、展开运算符、模板字符串（可选）、可选链 ?.
> 、空值合并 ?? 。**

**31.5 Rhino 引擎 ES6 支持详表（基于 mozilla/rhino 官方仓库）**

来源：github.com/mozilla/rhino，纯 Java 实现的 JavaScript 引擎，最新版本
1.9.0（2025-12）。海阔视界/聚阅内置 Rhino
版本决定实际可用特性，以下为官方各版本演进。

**31.5.1 版本演进路线**

  --------------------------------------------------------------------------------------------------------------
  **版本**   **发布时间**   **ES6 关键变化**
  ---------- -------------- ------------------------------------------------------------------------------------
  1.7.13     2020-09        function\* 生成器、for-of 遍历 Java Iterable

  1.7.14     2022-01        Promise、BigInt、模板字面量、\*\* 指数运算符、shorthand
                            属性、Object.values/entries/fromEntries、globalThis

  1.7.15     2024-05        rest 参数、Symbol.species、属性排序修正

  1.8.0      2025-01        默认语言级别改为 ES6、super、Reflect、Proxy

  1.9.0      2025-12        解构增强、展开增强、正则命名捕获组/后行断言/Unicode模式、Promise.withResolvers/try
  --------------------------------------------------------------------------------------------------------------

**31.5.2 特性支持状态（基于 1.8.0+）**

  -----------------------------------------------------------------------
  ✅ 完整支持（1.8.0+）               ❌ / ⚠️ 不支持或未确认
  ----------------------------------- -----------------------------------
  ✅ let / const 块级作用域           ❌ class 语法（不稳定）

  ✅ 箭头函数（含 lexical this）      ❌ async / await

  ✅ 模板字面量 / 标签模板            ❌ import / export（ES Modules）

  ✅ 解构赋值（数组/对象/参数）       ⚠️ 可选链 ?. （未确认）

  ✅ 默认参数 / rest 参数             ⚠️ 空值合并 ?? （未确认）

  ✅ 展开语法 \... （数组/可迭代）    

  ✅ for\...of 循环                   

  ✅ Promise / BigInt                 

  ✅ Reflect / Proxy / super          

  ✅ 计算属性名 / 简写属性/方法       
  -----------------------------------------------------------------------

**31.5.3 image/lazyRule 回调的箭头函数陷阱**

虽然 Rhino 1.8.0+ 完整支持箭头函数，但在 \$.image() 和 \$.lazyRule()
回调中，框架通过 input 变量注入数据，箭头函数会导致 input
取不到（作用域问题），必须使用 function(){}。

**⚠️ \$.image() 和 \$.lazyRule() 回调必须用
function(){}，不能用箭头函数，否则 input 为 undefined。**

> // ❌ 错误：箭头函数导致 input 取不到
>
> return \$(\'\').image(() =\> { /\* input 是 undefined \*/ });
>
> // ✅ 正确：function 回调
>
> return \$(\'\').image(function() { /\* input 正常注入 \*/ });

**31.5.4 开发建议**

📌 海阔内置 Rhino
版本可能滞后于官方最新版。以下特性已实测可靠可用：const/let、箭头函数（非回调场景）、模板字面量、解构、展开、for\...of、Promise。

**⚠️ class / async-await / import-export / ?. / ??
等特性不确定，规则内禁止使用，统一按不可用处理。**

**Law 68 补充：image/lazyRule 回调固定使用
function(){}；其余位置（forEach、map、filter 等）可自由使用箭头函数。**

> **第三十二章：分类方案选型------rule() 沙箱限制与矩阵方案适用边界**

本章补充第六章（矩阵方案）和第二十七章（独立标签页方案）的适用边界说明。核心结论：rule()
沙箱内的 http 链接永远跳浏览器，有二级的视频站必须用矩阵方案。

> 📌 **本章与第 67 章的分工:**
> - **第 32 章（本章）= 沙箱限制深度规则**：解释"为什么必须矩阵"——`rule()` 沙箱内 http 永远跳浏览器（Law 67）、嵌套沙箱变量传递、`$().rule()` vs `$().lazyRule()` 沙箱差异、双域名坑等 10 节深度内容
> - **第 67 章 = 清洗决策简明表**：旧规则改造时该选静态分类 / 矩阵 / 拆多规则的快速判断（5 节小表）
>
> 新建规则先读本章理解约束 → 旧规则清洗可直接翻第 67 章决策树。两章硬过滤层均为"有无二级"（C1 修订后已对齐）。

**32.1 rule() 沙箱的硬限制**

\$(\'hiker://empty##fypage\').rule(fn)
新开的页面是独立沙箱，引擎对沙箱内卡片 url 的处理规则与主页不同：

  ---------------------------------------------------------------------------------
  **卡片 url 类型**       **沙箱内行为**   **说明**
  ----------------------- ---------------- ----------------------------------------
  hiker:// 协议           ✅ 正常处理      hiker://empty、hiker://page/
                                           等内部协议正常

  pics:// 协议            ✅ 正常处理      图集/漫画直接渲染

  video:// 协议           ✅ 正常处理      直接播放

  http/https URL          ❌ 跳浏览器      引擎不知道走哪个函数，直接打开外部链接

  lazyRule 返回 http      ❌ 跳浏览器      lazyRule 返回 http url 同样跳浏览器
  ---------------------------------------------------------------------------------

> ⚠️ rule() 沙箱内的卡片 url 如果是 http
> 链接，无论如何包装都会跳浏览器。没有任何方法让沙箱内的 http url
> 走规则的二级函数。（Law 67）

**32.2 两种分类方案的适用场景**

  ---------------------------------------------------------------------------
  **方案**        **适用场景**                   **不适用场景**
  --------------- ------------------------------ ----------------------------
  独立标签页      漫画/图集（pics://）           有二级函数的视频站
  \$().rule(fn)   视频直链（video://） Next.js   需要封面/简介/选集的详情页
                  等无二级的站点                 

  矩阵方案        所有视频站（有二级）           基本无限制，通用性最强
  getMyVar +      动漫/影视等需要详情页的站点    
  主页刷新        任何需要走规则二级函数的场景   
  ---------------------------------------------------------------------------

**32.3 有二级的视频站标准分类方案（矩阵）**

有二级函数的站点，分类必须用矩阵方案------分类按钮在主页内渲染，用
getMyVar 记录选中状态，点按钮刷新主页，主页根据选中状态请求对应分类
URL，卡片直接给详情页地址让引擎走二级。

> // 页码必须开启翻页
>
> 页码: { \'主页\': true },
>
> 主页: function() {
>
> const d = \[\];
>
> const KEY = \'site_cindex\'; // 纯字母前缀（Law 51）
>
> const cur = getMyVar(KEY, \'0\');
>
> // 第一页渲染分类按钮行
>
> if (MY_PAGE == 1) {
>
> cats.forEach((cat, i) =\> {
>
> const isSel = (i + \'\' === cur);
>
> d.push({
>
> title: isSel ? \`❆ \${cat.name} ❆\` : cat.name,
>
> col_type: \'scroll_button\',
>
> url: \$(\`#noLoading#\`).lazyRule((k, idx) =\> {
>
> putMyVar(k, idx);
>
> refreshPage(false);
>
> return \'hiker://empty\';
>
> }, KEY, i + \'\')
>
> });
>
> });
>
> d.push({ col_type: \'blank_block\' });
>
> }
>
> // 根据选中分类请求列表
>
> const cat = cats\[parseInt(getMyVar(KEY, \'0\'))\] \|\| cats\[0\];
>
> const url = MY_PAGE \> 1
>
> ? \`\${host}/type\${cat.id}/\${MY_PAGE}.html\`
>
> : \`\${host}/type\${cat.id}\`;
>
> const html = fetch(url, { headers: { \'User-Agent\': ua } });
>
> // 卡片直接给详情页 url，引擎自动走二级函数
>
> return d.concat(parseCards(html));
>
> },
>
> 📌 矩阵方案的卡片 url
> 在主页上下文中，引擎认识当前规则的二级函数，点击会正常进入二级页面渲染封面/简介/选集。这是唯一能让分类列表正确走二级的方案。

**32.4 新增铁律（Law 67）**

  -------------------------------------------------------------------------------------------------
  **编号**   **铁律名称**         **核心要义**                                   **关键口诀**
  ---------- -------------------- ---------------------------------------------- ------------------
  Law 67     沙箱 http 跳浏览器   rule() 沙箱内 http url                         有二级 =
                                  永远跳浏览器，有二级的站点禁用独立标签页方案   矩阵方案；无二级 =
                                                                                 可用沙箱

  -------------------------------------------------------------------------------------------------

> 📌 本章结论来自动漫站（dk95.com）开发实测，经历了 rule() 沙箱 →
> rulePage → 矩阵方案的完整踩坑路径。

**32.5 rule() / lazyRule 沙箱变量传递完整规则（R星精选实战提炼）**

每一层 `rule()` / `lazyRule()` 都是独立沙箱，**外部作用域的变量完全不可见**，参数传递是唯一合法的数据通道。

```javascript
// ❌ 错误：沙箱内直接引用外部变量
var myHost = 'https://example.com';
$('hiker://empty').rule(function() {
    fetch(myHost + '/api');  // myHost 是 undefined！
});

// ✅ 正确：通过参数传入
var myHost = 'https://example.com';
$('hiker://empty').rule(function(host) {
    fetch(host + '/api');    // host = 'https://example.com' ✅
}, myHost);

// ── 多参数 ────────────────────────────────────────────────────
$('hiker://empty?page=fypage').rule(function(host, token, cfg) {
    var data = fetch(host + '/api', { headers: { token: token } });
}, myHost, myToken, myConfig);

// ── lazyRule 参数不自动穿透到内层 rule ──────────────────────
url: $('#noLoading#').lazyRule(function(host, id) {
    return $('hiker://empty').rule(function(h, i) {
        // host/id 不会自动进来，必须再次显式传入 h, i
        fetch(h + '/data/' + i);
    }, host, id);   // ← 必须再传一次
}, myHost, myId);
```

**沙箱内跨函数调用的正确方式：**

```javascript
// getMyVar / juItem.get / storage0.getMyVar 在沙箱内可用
// 这是除参数以外另一种合法的跨沙箱读数据方式
$('hiker://empty').rule(function() {
    var host  = juItem.get('host', '');           // ✅ 持久存储可读
    var sort  = getMyVar('prefix_sort', 'time');  // ✅ 会话变量可读
    var cache = storage0.getMyVar('fenlei');       // ✅ 大数据缓存可读
});

// $.require("jiekou") 是沙箱内调用宿主规则对象的标准桥梁
$('hiker://empty').rule(function(jkdata) {
    var rule = $.require('jiekou').parse(jkdata);
    rule.images(url);     // 调用宿主规则的图片解密方法
    rule.post('api/path');// 调用宿主规则的网络请求方法
}, jkdata);
```

> 📌 **Law 109 延伸**：`$(x).lazyRule(fn)` 中的 `x` 是 `input`，不是 `fn` 的第一个参数。如果回调需要用到 `x` 的值，必须再显式传入一次：`$(x).lazyRule(fn, x)`。

**32.6 `$().rule()` 用 `MY_URL`，`$().lazyRule()` 用 `input`——最常踩的沙箱混淆坑**

两者都能开沙箱，但内部获取"当前 URL"的方式完全不同：

| | `$().rule()` | `$().lazyRule()` |
|---|---|---|
| 当前 URL 变量 | `MY_URL`（引擎注入） | `input`（`$()` 括号里的值）|
| 触发时机 | 用户点击卡片，进入新页面 | 用户点击卡片，在原页面执行，返回协议字符串 |
| 返回值 | 无（用 `setResult(d)` 渲染）| 必须 return 字符串（URL/协议）|
| 典型用途 | 混合内容页（图文视频混排）| 纯解析（返回 `pics://` / `video://`）|

```javascript
// ── $().rule() 回调里用 MY_URL ────────────────────────────
url: $(link).rule(function() {
    var html = fetch(MY_URL);   // ✅ MY_URL = link（引擎注入）
    // fetch(input) 在 rule() 里会报错或取到空值
    var d = [];
    // ...渲染多组件混排...
    setResult(d);               // rule() 必须用 setResult
});

// ── $().lazyRule() 回调里用 input ────────────────────────
url: $(link).lazyRule(function() {
    var html = fetch(input);    // ✅ input = link（$()括号里的值）
    // fetch(MY_URL) 在 lazyRule() 里取不到正确值
    var pics = [];
    // ...提取图片...
    return 'pics://' + pics.join('&&');  // lazyRule 必须 return
});
```

**混合内容（视频+图片+文字）必须走 `$().rule()`：**

```javascript
// ❌ 错误：lazyRule 只能 return 一个协议字符串，无法渲染多组件混排
url: $(link).lazyRule(function() {
    // 这里无法同时返回视频 + 图片 + 文字...
    return 'video://' + xxx;  // 只能返回一种
});

// ✅ 正确：混合内容用 rule() + setResult 渲染
url: $(link).rule(function() {
    var html = fetch(MY_URL);
    var d = [];
    d.push({ title: 标题,   col_type: 'text_center_1' });
    d.push({ title: '▶播放', col_type: 'scroll_button', url: 'video://' + xxx });
    d.push({ col_type: 'pic_1_full', pic_url: imgUrl });
    d.push({ title: 正文,   col_type: 'text_1' });
    setResult(d);  // 多组件混排，setResult 一次性渲染
});
```

> 📌 判断标准：详情页只有一种媒体（纯图集 / 纯视频）→ `lazyRule` 返回协议字符串；详情页有多种组件混排 → `rule()` + `setResult`。

**32.7 `$().rule()` 沙箱翻页——必须在 URL 里写 `?page=fypage`**

`$().rule()` 沙箱开的是独立页面，`MY_PAGE` 不会自动从外部页面继承，**必须在 `$()` 的 URL 参数里显式写 `?page=fypage`**，引擎才会把当前翻页值注入沙箱内的 `MY_PAGE`：

```javascript
// ❌ 错误：URL 里没有 fypage，沙箱内 MY_PAGE 永远是 1，翻页不生效
url: $('hiker://empty').rule(function(host, ua) {
    var html = fetch(host + '/list?page=' + MY_PAGE);  // MY_PAGE 始终 = 1
    // ...
}, host, ua);

// ✅ 正确：URL 里写 ?page=fypage，引擎才会注入翻页值
url: $('hiker://empty?page=fypage').rule(function(host, ua) {
    var html = fetch(host + '/list?page=' + MY_PAGE);  // MY_PAGE = 实际页码
    // ...
}, host, ua);

// 带主题标识时，fypage 放在 # 分隔的 URL 片段里
url: $('hiker://empty?page=fypage&#gameTheme#').rule(function(host, ua) { ... }, host, ua);

// 也可以用 ## 语法（两者等价）
url: $('hiker://empty##fypage').rule(function(host, ua) { ... }, host, ua);
```

> ⚠️ **Law 125 补充**：子标签页、分类沙箱页等需要翻页的 `$().rule()` 沙箱，必须在 `$()` URL 里包含 `fypage`，否则沙箱内 `MY_PAGE` 固定为 1，点下一页无效但不报错，是静默 Bug。

**32.8 `pd()` 第三参数在沙箱内导致双域名坑**

`pd(html, rule, baseUrl)` 的第三参数 `baseUrl` 会把相对路径补全为绝对 URL。但如果已经传了 `baseUrl`，在沙箱内就不能再 `host + link` 拼接，否则会得到 `host + host + path` 的双域名错误：

```javascript
// 假设 HTML 里 href="/gallery/123"
// pd 第三参数传了 host，返回绝对 URL：https://example.com/gallery/123

// ❌ 错误：沙箱内又拼了一次 host → 双域名
var link = pd(item, 'a&&href', host);   // = "https://example.com/gallery/123"
d.push({ url: host + link });           // = "https://example.comhttps://example.com/gallery/123"

// ✅ 正确：pd 已经补全，直接用 link，沙箱内不再拼 host
var link = pd(item, 'a&&href', host);   // = "https://example.com/gallery/123"
d.push({ url: link });                  // ✅

// 或者：不用 pd 第三参数，自己在沙箱内判断拼接
var href = pdfh(item, 'a&&href');       // = "/gallery/123"（相对路径）
var link = /^https?/.test(href) ? href : host + href;  // 安全拼接
d.push({ url: link });
```

> 📌 **判断是否会双域名**：看 `pd()` 有没有传第三参数，有传就已经是绝对 URL，直接用；没传则是原始值（可能是相对路径），需要自己拼 `host`。两种方式选一种，不要混用。

**32.9 多级嵌套沙箱的参数传递链**

三层或更多层沙箱嵌套时（如：话题列表 → 用户主页 → 动态详情），**每一层的所有依赖都必须从上一层显式传入**，不能假设任何变量能穿透沙箱：

```javascript
// ── 外层沙箱（话题列表，一层）────────────────────────────────
// 外部取好所有需要传入的值
var tok   = juItem.get('token', '');
var devId = juItem.get('deviceId', '');
var host  = this.host;
var imgDomain = juItem.get('imgDomain', '');

url: $('hiker://empty?page=fypage').rule(function(host, imgDomain, tok, devId) {
    // 一层沙箱内渲染话题列表
    var d = [];
    topics.forEach(function(t) {
        d.push({
            url: $('hiker://empty?page=fypage').rule(function(host, imgDomain, tok, devId, tid) {
                // ── 二层沙箱（用户主页）────────────────────────
                var d2 = [];
                // ...
                d2.forEach(function(item) {
                    item.url = $('hiker://empty').rule(function(host, tok, devId, did) {
                        // ── 三层沙箱（动态详情）────────────────
                        // tok、devId、host 全部从参数取
                    }, host, tok, devId, item.id);
                });
                setResult(d2);
            }, host, imgDomain, tok, devId, t.id)
            // ↑ 每一层都把所有依赖显式传入下一层
        });
    });
    setResult(d);
}, host, imgDomain, tok, devId)  // ← 外部取值，一次性传入
```

**嵌套层数越多，越容易漏传参数。推荐用命名对象打包所有依赖：**

```javascript
// 统一打包，避免参数列表过长
var ctx = {
    host:      this.host,
    tok:       juItem.get('token', ''),
    devId:     juItem.get('deviceId', ''),
    imgDomain: juItem.get('imgDomain', ''),
    ua:        this.UA
};

url: $('hiker://empty').rule(function(ctx) {
    // 沙箱内通过 ctx.tok、ctx.host 等访问
    // 向下传时直接传整个 ctx
    item.url = $('hiker://empty').rule(function(ctx, id) {
        // ctx 整体传入
    }, ctx, item.id);
    setResult(d);
}, ctx)
```

> ⚠️ `juItem` 在部分版本的沙箱内不可用，统一在沙箱**外**调用 `juItem.get` 取值后传入，不要在沙箱内调用。

**32.10 Law 129：嵌套沙箱 host 必须逐层透传——严禁跨层引用外层变量**

沙箱的序列化机制分两种，两者在不同引擎版本下均有出现：

| 模式 | 行为 | 可靠性 |
|---|---|---|
| 闭包模式 | 沙箱回调能访问外层函数变量 | ❌ 不稳定，引擎版本相关 |
| 字符串模式 | 回调被 `toString()` 后 `eval` 执行，所有外部变量不可见 | ✅ 必须兼容 |

**不能假设引擎走闭包模式。** 所有沙箱需要的值必须从直接外层沙箱的**形参**获取，通过**实参**逐层传入下一层：

```javascript
// ── 四级嵌套（社区话题卡片）正确写法 ─────────────────────────

// 外层：主页函数取值
var host = this.host;
var tok  = juItem.get('token', '');

// 一级沙箱：参数名 hh
url: $('hiker://empty?page=fypage').rule(function(hh, imd, t2, d2, tid) {
    // ✅ 二级沙箱传 hh（一级沙箱的形参），不传外层 host
    url: $('hiker://empty?page=fypage').rule(function(hhh, imdd, t3, d3, uid) {
        // ✅ 三级沙箱传 hhh（二级沙箱的形参），不传 hh 或 host
        url: $('hiker://empty').rule(function(hhh2, imdd2, t4, d4, did) {
            // ✅ 这里 hhh2 = host，保证有值
            var detail = fetch(hhh2 + '/api/dynamic/' + did, {
                headers: { token: t4, deviceId: d4 }
            });
        }, hhh, imdd, t3, d3, item.dynamicId);  // 用 hhh，不用 host
    }, hh, imd, t2, d2, user.userId);  // 用 hh，不用 host
}, host, imgDomain, tok, devId, topic.id);  // 外层 host 只传一次

// ❌ 错误：各层都直接引用外层 host
url: $('hiker://empty').rule(function(hhh) {
    url: $('hiker://empty').rule(function(hhh2) {
        fetch(host + '/api/...')  // host 在字符串模式下 ReferenceError
    }, host, ...)
}, host, ...)
```

**口诀：一层沙箱一个名（hh → hhh → hhh2），传参只用上一层的形参名，不跨层。**

**各模块嵌套层级与修复对照：**

| 模块 | 嵌套层级 | 传参链路 |
|---|---|---|
| 社区话题卡片 | 四级 | `host → hh → hhh → hhh2` |
| 社区广场列表 | 四级 | 同上 |
| 精选用户主页 | 三级 | `host → hh → hhh` |
| 频道/写真站台更多 | 二级 | `host → hh` |

**第三十三章：API 接口型规则实战技巧（熊猫APP实战提炼）**

**33.1 POST body 直接传对象**

海阔/聚阅的 fetch 内部会自动处理对象 body，无需手动
JSON.stringify，直接传 JS 对象即可。这是 API 型规则最简洁的 POST 写法。

> // ✅ 直接传对象，无需 JSON.stringify
>
> let json = fetch(url, {
>
> body: { \"command\": \"GET_LIST\", \"pageNumber\": page, \"typeId\":
> fl },
>
> method: \'POST\'
>
> });

**33.2 fyAll 占位符 + \## 多段传参**

fyAll 会把所有筛选维度（class/area/year/sort）统一替换为同一个值，适合
API 只有一个分类 ID 参数的站点。配合 \## 分隔符在 url 字段中同时携带 API
地址和分类 ID，主页函数用 split(\"##\") 分别取出。

> // 静态分类写法
>
> 静态分类: {
>
> type: \"主页\",
>
> url: \"hiker://empty##https://api.example.com/forward##fyAll\",
>
> class_name: \"分类A&分类B\", class_url: \"1&2\",
>
> // 主页函数取值
>
> let apiUrl = MY_URL.split(\"##\")\[1\]; //
> https://api.example.com/forward
>
> let typeId = MY_URL.split(\"##\")\[2\]; // 1 或 2 等分类ID

**33.3 分类 ID 动态 type 判断**

用分类 ID 范围区分内容类型（如视频 vs 图集），一行三元表达式替代多个
if/else。

> let type = fl == 4 \| fl \> 29 ? 2 : 1; // ID=4 或 ID\>29
> 为图集(2)，其余为视频(1)

**33.4 lazyRule 传 API 地址参数**

lazyRule 内部无法访问外部变量，把 API 地址通过参数传入，input
留给列表项的唯一 ID，职责清晰。

> let lazy = \$(\'\').lazyRule((apiUrl) =\> {
>
> // input = 列表项 id，apiUrl = 外部传入的接口地址
>
> let json = fetch(apiUrl, { body: { \"id\": input }, method: \'POST\'
> });
>
> return \"pics://\" + JSON.parse(json).pics.join(\"&&\");
>
> }, apiUrl); // ← 把 apiUrl 作为参数传入

**33.5 封面图推导播放地址**

当封面图与播放地址同域名同路径时，直接替换文件名得到
m3u8，省去二级请求，效率最高。

> url: list\[i\].vod_pic.replace(\"/1.jpg\", \"/playlist.m3u8\")

**33.6 随机颜色函数**

用于 detail1/detail2
等标题的随机颜色渲染，每次调用返回一个随机十六进制颜色值。

> let Color = function() {
>
> return \'#\' + (\'00000\' + (Math.random() \* 0x1000000 \<\<
> 0).toString(16)).substr(-6);
>
> };

📌 本章技巧来自熊猫APP规则（2025年）实战提炼，适用于所有 POST JSON
接口型站点。

> **第三十四章：JS 混淆播放器解析\-\-\-\-\--Dean Edwards Packer
> 解包实战**

本章来自 91短视频类站点实战，总结双层 Packer
混淆播放器的完整解析流程，以及三个高价值开发技巧：多选择器降级容错、toast://
调试返回值、时间戳分段 token。

**34.1 Dean Edwards Packer 通用解包函数**

p,a,c,k,e,d 混淆是前端最常见的 JS
压缩方式，通过字典替换还原变量名。规则里需要手动在 lazyRule
内实现解包，不能用外部库。

> // ✅ 通用 Packer 解包，支持任意 a 进制
>
> function unpack(code) {
>
> let pm =
> code.match(/\\(\'(\[\\s\\S\]\*?)\',(\\d+),(\\d+),\'(\[\\s\\S\]\*?)\'\\.split\\(\'\\\|\'\\)/);
>
> if (!pm) return code;
>
> let p = pm\[1\], a = parseInt(pm\[2\]), c = parseInt(pm\[3\]), k =
> pm\[4\].split(\'\|\');
>
> function e(c) {
>
> return (c \< a ? \'\' : e(parseInt(c/a))) +
>
> ((c = c%a) \> 35 ? String.fromCharCode(c+29) : c.toString(36));
>
> }
>
> let d = {};
>
> while (c\--) { let key = e(c); d\[key\] = k\[c\] \|\| key; }
>
> return p.replace(/\\b\\w+\\b/g, w =\> d\[w\] !== undefined ? d\[w\] :
> w);
>
> }

**⚠️ lazyRule 内不能 require 外部库，解包函数必须内联定义在 lazyRule
内部。**

**34.2 双层 Packer 播放解析流程**

部分站点播放地址经过两层混淆：第一层 embed 页解包拿 token，第二层
embed_play.js 解包拿 m3u8。流程固定，可作为模板复用。

> // Step1：请求视频详情页，提取 videoId
>
> let html = request(input, { headers: { \'User-Agent\': ua } });
>
> let vidMatch = html.match(/videoId\\s\*=\\s\*\[\'\"\](
> \\d+)\[\'\"\]/);
>
> if (!vidMatch) return \'toast://未找到videoId\';
>
> // Step2：请求 embed 页，第一层解包取 token
>
> let embedUrl = home + \'/index/embed?id=\' + vidMatch\[1\];
>
> let embedHtml = request(embedUrl, { headers: { \'Referer\': input }
> });
>
> let layer1 = unpack(embedHtml);
>
> let tkMatch = layer1.match(/encodeURIComponent\\(\"(\[\^\"\]+)\"\\)/);
>
> if (!tkMatch) return \'toast://token失败\';
>
> // Step3：构造时间戳 token（每30分钟变化）
>
> let t = parseInt(Date.now() / 1000 / 1800);
>
> let playUrl = home + \'/index/embed_play.js?u=\' +
> encodeURIComponent(tkMatch\[1\]) + \'&t=\' + t;
>
> // Step4：请求 embed_play.js，第二层解包取 m3u8
>
> let resp = fetch(playUrl, { headers: { \'Referer\': embedUrl } });
>
> let layer2 = unpack(resp);
>
> let m3u8 =
> layer2.match(/https?:\\/\\/\[\^\\s\"\'\<\>\\\\\]+\\.m3u8\[\^\\s\"\'\<\>\\\\\]\*/i);
>
> if (m3u8) return m3u8\[0\].replace(/&amp;/g, \'&\');

**34.3 三个高价值开发技巧**

**技巧1：多选择器降级容错**

站点改版时列表结构可能变化，提前写好备用选择器，按顺序降级尝试，避免全量失效。

> // ✅ 三级降级，任一命中即停
>
> let items = pdfa(html, \'body&&.video-items li\');
>
> if (items.length === 0) items = pdfa(html, \'body&&li.video-item\');
>
> if (items.length === 0) items = pdfa(html, \'body&&.list li\');

**技巧2：toast:// 作为调试返回值**

解析失败时返回 toast://
弹出提示，用户能看到具体失败原因，比黑屏或空播放器更容易定位问题。

> // ✅ 各步骤返回不同提示，快速定位失败节点
>
> if (!vidMatch) return \'toast://未找到videoId\';
>
> if (!tkMatch) return \'toast://token提取失败\';
>
> if (!m3u8) return \'toast://未找到m3u8地址\';

**技巧3：时间戳分段 token（防盗链）**

部分站点用时间分段值作为请求验证参数，每 N 分钟变化一次。用 Date.now()
整除计算当前分段，无需额外请求。

> // 每30分钟变化一次的 t 参数
>
> let t = parseInt(Date.now() / 1000 / 1800); // 1800秒=30分钟
>
> // 每小时变化：/ 1000 / 3600，每10分钟变化：/ 1000 / 600

**34.4 新增铁律**

  ---------------------------------------------------------------------------------------------------------
  编号     名称               正确做法                                    错误现象
  -------- ------------------ ------------------------------------------- ---------------------------------
  Law 69   Packer解包内联     lazyRule内直接定义unpack函数，不能require   require外部库在lazyRule内不可用

  Law 70   toast://调试返回   解析各步骤失败返回toast://具体原因          黑屏无提示，难以定位问题

  Law 71   多选择器降级       按优先级列出3个备用选择器依次尝试           单一选择器，站点改版即全量失效
  ---------------------------------------------------------------------------------------------------------

> **第三十五章：多站点实战总结（禁漫/7mmtv/Pornhub/XVideos/MissAV）**

**35.1 scroll_button 不支持 HTML------Law 78**

**⚠️ Law 78：scroll_button 标题内禁止使用 fontcolor()/bold() 等 HTML
方法，会直接显示原始标签字符串。选中状态只能靠 backgroundColor +
纯文本符号区分。**

> // ❌ 错误：scroll_button 不渲染 HTML
>
> title: i === bigIdx ? \'\"\"\"\"\' +
> big.title.fontcolor(\"#FFFFFF\").bold() : big.title
>
> // ✅ 正确：纯文本符号 + backgroundColor
>
> title: i === bigIdx ? \"▶ \" + big.title : big.title,
>
> extra: { backgroundColor: i === bigIdx ? \"#FF6B6B\" : \"\" }

**35.2 CF 过盾两段式方案------Law 72**

访问 CF 站点先 fetch 带缓存 Cookie 尝试，失败则 fetchCodeByWebView
自动过盾，过盾后保存 Cookie 供下次复用。

**⚠️ Law 72：fetchCodeByWebView 的 UA 必须放在 headers 内；check
用纯文本关键词（不含引号）；javaScriptEnabled: true 必须加。参数名是
check 不是 checkJs。**

> html = fetchCodeByWebView(url, {
>
> headers: { \"User-Agent\": \_ua },
>
> check: \"页面特征文本\", // 纯文本，不含引号，不是 checkJs
>
> timeout: 25000,
>
> javaScriptEnabled: true // CF 验证必须
>
> });

**35.3 Packer 站点正则变量提取------Law 73**

7mmtv 播放列表在 Dean Edwards Packer
混淆块内。解包后用捕获组取数值，不要 eval 拼接变量声明。

> let src = (packedMatch ? unpack(packedMatch\[0\]) : \"\") + html;
>
> let \_hcdeedg252 = src.match(/hcdeedg252=(\\d+)/);
>
> let hcdeedg252 = parseInt(\_hcdeedg252\[1\]); // 直接用数值

**35.4 Pornhub 图片与播放地址------Law 74/75**

**⚠️ Law 74：Pornhub 列表图片优先用 default_thumb（ei.phncdn.com
稳定域名），thumb 字段是 pix-cdn 带鉴权时效参数会过期变空白。**

**⚠️ Law 75：Pornhub m3u8 地址 em-h.phncdn.com 需替换为
im-h.phncdn.com，截掉 ? 后面鉴权参数才能播放。**

> let img = v.default_thumb \|\| v.thumb \|\| \"\";
>
> url = url.replace(\"em-h.phncdn.com\", \"im-h.phncdn.com\");
>
> let q = url.indexOf(\"?\"); if (q \> -1) url = url.substring(0, q);

**35.5 MissAV surrit UUID 直取------Law 76**

**⚠️ Law 76：MissAV 原规则用 checkJs 参数名错误（应为 check），且两次
WebView 请求容易超时。正确做法：surrit UUID 直接写在 missav 页面 HTML
里，正则取出无需 WebView。**

> let uuidM =
> html.match(/surrit\\.com\\/(\[a-f0-9\]{8}-\[a-f0-9\]{4}-\[a-f0-9\]{4}-\[a-f0-9\]{4}-\[a-f0-9\]{12})/);
>
> let baseUrl = \"https://surrit.com/\" + uuidM\[1\] + \"/\";
>
> let m3u8 = request(baseUrl + \"video.m3u8\", { headers: { \"Referer\":
> baseUrl } });

**35.6 搜索函数禁用 MY_PAGE------Law 77**

**⚠️ Law 77：搜索函数内禁止使用 MY_PAGE，否则报错\"MY_PAGE
未定义\"。搜索直接拼 URL，不加翻页参数。**

> 搜索: function(key) {
>
> // ❌ let url = this.\_buildUrl(\...) // \_buildUrl 内用了 MY_PAGE
>
> // ✅
>
> let url = this.host + \"/search?q=\" + encodeURIComponent(key);
>
> return this.\_parse(fetch(url));
>
> },
>
> **第三十七章：多标签 categoryList
> 写法------与矩阵方案、静态分类的全面对比**

本章基于 Pornhub 规则（作者 R）的 categoryList
架构，深入分析其多标签写法，并与手册第六章（矩阵方案）、第二章（静态分类）做系统对比，帮助开发者在三种方案中准确选型。

**37.1 categoryList 核心架构解析**

**categoryList 是一个 JSON
数组，每项代表一个顶级标签页，包含四个字段：**

> // 每项的完整结构
>
> {
>
> \"title\": \"视频\", // 显示在顶部 scroll_button 的文字
>
> \"path\": \"\", // 无 sub 时直接用此路径；有 sub 时忽略
>
> \"type\": \"video\", // 内容类型标识，驱动 switch(type) 分发
>
> \"sub\": \[ // 子分类数组，空数组表示无子分类
>
> { \"title\": \"色情片\", \"path\": \"/video\" },
>
> { \"title\": \"🇨🇳中文\", \"path\": \"/language/chinese\" }
>
> \]
>
> }

六种内容类型（type）及其对应的渲染函数：

-   video → videoType(url, html) 普通视频列表

-   category → categoryType(html) 分类入口（大类 + 小类网格）

-   pornstar → pornstarType(url, html) 明星/演员卡片

-   channel → channelType(url, html) 频道列表

-   playlist → playlistType(url, html) 片单列表

-   login → loginType(html) 登录/我的页面，再按 url 特征二次分流

**37.2 两级标签的渲染逻辑**

第一级（顶栏）：遍历整个 categoryList，每项渲染一个
scroll_button，点击记录 Apo.category 并清空子分类状态，刷新主页。

第二级（子栏）：只有当前选中的 category 有 sub 时才渲染，遍历
currentCate.sub，记录 Apo.subCate，同样刷新主页。

> // 一级标签渲染
>
> categoryList.forEach((cate, index) =\> {
>
> parse.d.push({
>
> title: parseInt(parse.data.category) === index
>
> ? \'\"\"\"\"\' + cate.title.fontcolor(\"#FFFFFF\").bold()
>
> : cate.title,
>
> url: \$(parse.empty + \'#noLoading#\').lazyRule((index) =\> {
>
> putMyVar(\"Apo.category\", index.toString())
>
> putMyVar(\"Apo.subCate\", \"0\") // ✅ 切换一级时重置二级
>
> clearMyVar(\"url\")
>
> refreshPage(true)
>
> return \"hiker://empty\"
>
> }, index),
>
> col_type: \'scroll_button\',
>
> extra: {
>
> backgroundColor: parseInt(parse.data.category) === index
>
> ? parse.getRangeColors() : \'\'
>
> }
>
> })
>
> })
>
> // 二级标签：仅当 currentCate.sub.length \> 0 时渲染
>
> if (currentCate.sub.length \> 0) {
>
> parse.d.push({ col_type: \'blank_block\' })
>
> currentCate.sub.forEach((cate, index) =\> {
>
> parse.d.push({
>
> title: parseInt(parse.data.subCate) === index
>
> ? \'\"\"\"\"\' + cate.title.fontcolor(\"#FFFFFF\").bold()
>
> : cate.title,
>
> // \... 同一级，记录 Apo.subCate
>
> })
>
> })
>
> }

**37.3 switch(type) 分发------URL 判断二次路由**

switch 的第一层按 type 分流，但 login 类型内部还有 url
特征判断，实现了三层路由：

> switch (type) {
>
> case \'video\': parse.videoType(url, html); break
>
> case \'pornstar\':
>
> // url 含 search 时走视频列表，否则走明星卡片
>
> url.includes(\'search\') ? parse.videoType(url, html) :
> parse.pornstarType(url, html)
>
> break
>
> case \'login\':
>
> // url 特征二次分流：片单/登录/订阅 走不同函数
>
> if (url.includes(\'playlists\')) parse.playlistType(url, html)
>
> else if (url.includes(\'login\')) parse.loginType(html)
>
> else if (url.includes(\'subscriptions\'))parse.pornstarType(url, html)
>
> else parse.videoType(url, html)
>
> break
>
> // \...
>
> }
>
> setResult(parse.d) // ✅ switch 后统一调用（Law 80）
>
> 📌 login 类型的二次分流是关键：同一个「我的」入口，根据当前 url
> 的路径特征决定走登录页、片单页还是订阅页，一个 type
> 字段覆盖了多种实际内容类型。

**37.4 \$.require 模块化调用机制**

该规则大量使用 \$.require(\"jiekou\") 跨规则调用，共 8 处：

> // 视频卡片点击 → 调用 videoParse
>
> url: vurl + \$(\'#noHistory#\').rule(() =\> {
>
> const parse = \$.require(\"jiekou\").parse()
>
> parse.videoParse(MY_URL)
>
> setResult(parse.d)
>
> }),
>
> // 明星/频道/片单卡片点击 → 调用 yijiParse
>
> url: \"hiker://empty#\" + vurl + \$(\"/videos##fypage\").rule(() =\> {
>
> const parse = \$.require(\"jiekou\").parse()
>
> parse.yijiParse(MY_URL.split(\'##\')\[0\])
>
> setResult(parse.d)
>
> }),
>
> ⚠️ \$.require(\"jiekou\") 中的 \"jiekou\"
> 是规则在聚阅里保存的接口名称，不是文件名。如果规则没有保存为对应接口名，会报\"Module
> not found\"错误。
>
> ⚠️ 解决方案：把规则保存成接口名 \"jiekou\"，或者把调用改成
> \$.require(MY_RULE.find_rule)（但 MY_RULE.find_rule 在 rule()
> 沙箱里会返回整个代码字符串而非名称，同样失效）。最可靠的方案是把
> videoParse 逻辑内联进 rule() 里，完全不依赖 \$.require。

**37.5 三种方案全面对比**

三种方案对比：

**对比维度 \| 静态分类（第二章） \| 矩阵方案（第六章） \| categoryList
多标签（本章）**

-   【分类数据位置】 静态: 静态分类字段（class_name/url） \| 矩阵:
    主页函数内 dataClass 数组 \| categoryList: 主页函数内 categoryList
    数组

-   【分类维度】 静态: 1-4 维（fyclass/area/year/sort） \| 矩阵: 最多 9
    行独立筛选行 \| categoryList: 1级主标签 + 1级子标签，共2层

-   【内容类型分流】 静态: 无，所有分类走同一解析逻辑 \| 矩阵:
    无，所有分类走同一解析逻辑 \| categoryList: ✅ type 字段 + switch
    分发，不同标签走完全不同的解析函数

-   【URL 构建】 静态: 引擎自动替换占位符 \| 矩阵: 主页函数自己读
    cindex/curl 拼接 \| categoryList: 主页函数读 category/subCate 取
    path 拼接

-   【翻页】 静态: fypage 引擎自动管理 \| 矩阵: 主页函数自己处理 MY_PAGE
    \| categoryList: putMyVar(\'nextPage\') 手动追踪下一页 URL

-   【引擎依赖】 静态: 强依赖（占位符替换） \| 矩阵: 弱依赖（只用
    MY_PAGE） \| categoryList: 弱依赖（只用 MY_PAGE）

-   【代码复杂度】 静态: 低，配置式 \| 矩阵: 中，循环+渲染函数 \|
    categoryList: 高，多函数分工，需要 \$.require 或内联

-   【适用场景】 静态: URL 规律固定、分类维度少的普通视频站 \| 矩阵:
    分类极多、多维筛选、URL 不规律的复杂站 \| categoryList:
    内容类型多样（视频/明星/频道/片单/登录）的大型站

-   【主要优点】 静态: 零代码、配置即用、引擎全自动 \| 矩阵:
    灵活、支持多维叠加筛选 \| categoryList:
    类型分发清晰、各类型独立渲染、扩展性强

-   【主要缺点】 静态: 只能走同一解析函数，不能按分类切换内容类型 \|
    矩阵: 各分类仍走同一解析函数，不能按分类切换内容类型 \|
    categoryList: 代码量大，\$.require 跨规则调用有坑

**37.6 categoryList 方案的核心优势**

**相比静态分类和矩阵方案，categoryList 的最大价值在于：**

-   内容类型完全隔离：视频/明星/频道/片单各走独立的渲染函数，互不干扰。静态分类和矩阵方案所有分类都走同一个解析逻辑，无法做到这一点

-   登录态感知：login type 内部根据 url
    特征二次分流，登录前显示登录按钮，登录后显示个人内容（点赞/收藏/片单/订阅），其他方案无法实现

-   子分类动态展开：只有选中有 sub
    的主分类时才渲染子分类行，其他主分类不显示子分类，比矩阵方案的固定多行更灵活

-   可扩展性强：增加新内容类型只需在 categoryList 加一项、在 switch
    加一个 case、新增一个渲染函数，结构清晰

-   search 自动兼容：各 type 分支都有 url.includes(\'search\')
    判断，搜索结果统一走 videoType，无需单独处理

**37.7 categoryList 方案的局限与注意事项**

-   \$.require 依赖陷阱：rule() 沙箱内用 \$.require 调用
    videoParse/yijiParse，必须把规则保存为对应接口名才能找到。单规则直接跑会报
    Module not found（Law 67 延伸）

-   翻页非标准：用 putMyVar(\'nextPage\') 手动追踪下一页 URL，不依赖
    fypage。MY_PAGE \> 1 时直接用 nextPage，没有 nextPage
    则显示\'没有下一页\'，逻辑简单但不支持跳页

-   parse.data 每次刷新重新取值：data 对象在规则加载时执行
    getMyVar，主页刷新时规则重新加载，所以 category/subCate
    始终是最新值，这是正确的

-   parse.d 必须在入口清空：d 定义在顶层，主页/搜索入口必须先执行
    parse.d = \[\]（Law 79），否则刷新时数据累积

-   不适合有二级函数的普通视频站：如果站点只有一种内容类型（视频列表），用
    categoryList 是过度设计，静态分类或矩阵方案更简洁

**37.8 选型决策树**

> 问题1：站点有多种内容类型（视频/明星/频道/片单/登录）吗？
>
> → 是：用 categoryList + switch(type) 方案（本章）
>
> → 否：继续问题2
>
> 问题2：分类超过4维，或 URL 结构不规律，无法用占位符表达吗？
>
> → 是：用矩阵方案（第六章）
>
> → 否：继续问题3
>
> 问题3：分类数量少（每行 \< 20 个），URL 有固定规律吗？
>
> → 是：用静态分类（第二章）--- 最简单，零代码
>
> → 否：用矩阵方案（第六章）

**37.9 \$.require 内联方案------彻底解决 Module not found**

**当无法使用 \$.require 时，把 videoParse 逻辑直接内联进 rule()
是最可靠的方案：**

> // ✅ 完全内联，不依赖 \$.require，不依赖接口名
>
> url: vurl + \$(\'#noHistory#\').rule(() =\> {
>
> var ck =
> fetchPC(\'hiker://files/rules/parse/Cookie/pornhub_cookie.txt\') \|\|
> \'\';
>
> var ua = \'Mozilla/5.0 \...\';
>
> var html = fetch(MY_URL, { headers: { cookie: ck, \'User-Agent\': ua }
> });
>
> var d = \[\];
>
> // 标题
>
> d.push({ title: \'\"\"\"\"\' +
> pdfh(html,\'body&&h1&&Text\').fontcolor(\'\').small(),
>
> url: MY_URL, col_type: \'text_1\', extra: { lineVisible: false } });
>
> // flashvars 解析
>
> var playerObjList = {}, flashvars;
>
> try {
>
> var js = parseDomForHtml(html,
> \'\[id=mobileContainer\]&&script&&Html\');
>
> eval(js.replace(/var flashvars\_.\*?=/, \'var flashvars =\'));
>
> } catch(e) {}
>
> var urls = \[\], names = \[\];
>
> if (typeof flashvars !== \'undefined\' && flashvars &&
> flashvars.mediaDefinitions) {
>
> flashvars.mediaDefinitions
>
> .sort((a,b) =\> b.quality - a.quality)
>
> .forEach(item =\> {
>
> if (typeof item.quality === \'string\' && item.videoUrl) {
>
> urls.push(item.videoUrl.replace(/&amp;/g,\'&\'));
>
> names.push(item.quality + \'P\');
>
> }
>
> });
>
> }
>
> // 封面 + playlist
>
> d.push({ img: parseDom(html,\'#videoPlayerPlaceholder&&img&&src\')
> \|\| \'\',
>
> url: JSON.stringify({urls,names}), col_type: \'pic_1_full\' });
>
> setResult(d);
>
> }),
>
> ⚠️ Law 83（新增）：\$.require 在 rule()/lazyRule()
> 沙箱内调用时，接口名必须和规则在聚阅里保存的名称完全一致，不是文件名。无法确认接口名时，优先把逻辑内联进
> rule() 里，彻底消除依赖。

**37.10 本章新增铁律（Law 83）**

**Law 83 \$.require 接口名必须与聚阅保存名一致**

> ✅ 正确做法：规则保存为接口名 \'jiekou\'，代码里写
> \$.require(\'jiekou\')；或把逻辑内联进 rule() 彻底不用 \$.require
>
> ❌ 错误后果：接口名不匹配 → Module not found；用文件名（如
> 1754149556905）→ 同样找不到

**37.11 三种卡片跳转模式------不走规则二级函数**

categoryList 规则里所有卡片都不走规则的「二级函数」，而是用 \$().rule()
新开沙箱页面，在页面内部自己
setResult。这是该架构与手册标准骨架的最大差异之一。

**三种卡片跳转模式对比：**

> // ── 模式1：视频卡片 → 直接调 videoParse 渲染播放页 ──────────
>
> // videoType() 里的卡片
>
> url: vurl + \$(\'#noHistory#\').rule(() =\> {
>
> const parse = \$.require(\'jiekou\').parse()
>
> parse.videoParse(MY_URL) // 渲染标题+封面+清晰度选择+相关视频
>
> setResult(parse.d)
>
> }),
>
> // ── 模式2：明星/频道卡片 → 新开列表页，带 fypage 翻页 ────────
>
> // pornstarType / channelType 里的卡片
>
> url: \"hiker://empty#\" + vurl + \$(\"/videos##fypage\").rule(() =\> {
>
> const parse = \$.require(\'jiekou\').parse()
>
> parse.yijiParse(MY_URL.split(\'##\')\[0\]) // 渲染视频列表
>
> setResult(parse.d)
>
> }),
>
> // ── 模式3：分类/片单卡片 → 新开列表页，带 fypage 翻页 ─────────
>
> // categoryType / playlistType 里的卡片
>
> url: \"hiker://empty#\" + vurl + \$(\'##fypage\').rule(() =\> {
>
> const parse = \$.require(\'jiekou\').parse()
>
> parse.yijiParse(MY_URL.split(\'##\')\[0\])
>
> setResult(parse.d)
>
> }),

**三种模式的关键区别：**

-   模式1（videoParse）：\$(\'#noHistory#\').rule() --- #noHistory#
    不记录历史；无 hiker://empty# 前缀，直接在当前页覆盖渲染；调
    videoParse 展示播放页内容

-   模式2（yijiParse + /videos##fypage）：hiker://empty#
    前缀新开独立页面；/videos##fypage 表示路径补充 + 支持 fypage
    翻页；MY_URL.split(\'##\')\[0\] 取出真实 URL

-   模式3（yijiParse + ##fypage）：和模式2 相同，区别是 \##
    前无路径补充，直接用 vurl 本身作为列表 URL

> 📌 为什么不走规则二级函数？因为「二级函数」返回 {line, list}
> 格式，只能渲染选集列表。这里的明星/频道/分类点进去是「另一个视频列表页」而不是「选集页」，必须用
> \$().rule() + setResult 自己渲染，不能用二级函数。
>
> 📌 hiker://empty# 前缀的作用：告诉引擎新开一个独立页面，页面 URL 是 \#
> 后面的内容。没有这个前缀时（如模式1），rule()
> 的内容直接在触发页面覆盖渲染。
>
> ⚠️ 这三种模式都依赖 \$.require(\'jiekou\')，如果规则没有保存为接口名
> \'jiekou\'，全部报错。解决方案：把 videoParse 和 yijiParse
> 逻辑分别内联进对应的 rule() 里，或把规则保存为接口名（Law 83）。

**37.12 yijiParse 的翻页机制------手动追踪 nextPage**

yijiParse 没有用 fypage 占位符翻页，而是手动追踪下一页 URL：

> yijiParse: (url) =\> {
>
> // 翻页：从 getMyVar 取上次保存的下一页 URL
>
> let nextPage = getMyVar(\'YnextPage\', \'\')
>
> if (nextPage && MY_PAGE \> 1) {
>
> url = url.includes(\'random\') ? url : nextPage
>
> } else if (MY_PAGE \> 1) {
>
> url = \'没有下一页哦😑\' // 没有 nextPage 时硬终止
>
> }
>
> var html = fetch(url, { headers: { \... } })
>
> // 请求完成后，从页面提取下一页 URL 存起来
>
> try {
>
> var next = parse.url + pdfh(html,
> \'body&&.pagination&&.page_next&&a&&href\')
>
> putMyVar(\'YnextPage\', next)
>
> } catch {
>
> clearMyVar(\'YnextPage\') // 没有下一页按钮，清空
>
> }
>
> }

**与 fypage 方案的对比：**

-   fypage 方案：引擎用 MY_PAGE 自动计算页码，URL 模板固定（如
    /page/N/），适合 URL 规律的站点

-   nextPage 方案：从页面 HTML 里提取「下一页」按钮的 href，适合 URL
    不规律、每页下一页 URL 动态变化的站点（如 Pornhub 的 ?page=N
    带参数翻页）

-   nextPage 的缺点：不能跳页，只能顺序翻；第一次进入必须
    MY_PAGE=1，否则 nextPage 为空直接报没有下一页

> **第三十八章：if/else if 分流模式深度总结**

通过实际开发，总结出 if/else if 在规则开发中的四种核心应用场景。

**1. 分类入口分流（最常用）**

根据 MY_URL 的值决定走哪个渲染函数：

> 主页: function() {
>
> var cls = MY_URL;
>
> if (cls === \'tags\') {
>
> return this.\_renderTagCloud(); // 热门标签页
>
> } else if (cls === \'index\') {
>
> return this.\_renderList(\'/\'); // 最新精选
>
> } else if (cls === \'hots\') {
>
> return this.\_renderList(\'/hots.html\'); // 美图精选
>
> } else if (cls.indexOf(\'/tag/\') === 0) {
>
> return this.\_renderList(this.host + cls); // 标签列表页
>
> } else {
>
> return this.\_renderList(this.host + \'/group/\' + cls + \'.html\');
> // 普通分类
>
> }
>
> }

**2. URL 类型识别**

根据 URL 特征决定请求方式和参数：

> function getRequestUrl(url) {
>
> if (url.indexOf(\'/all-tags\') !== -1) {
>
> return url; // 全部分类页，直接请求
>
> } else if (url.indexOf(\'/tags/\') !== -1) {
>
> return url + (MY_PAGE \> 1 ? \'/\' + MY_PAGE : \'\'); //
> 标签列表页，加页码
>
> } else if (url.indexOf(\'/file/\') !== -1) {
>
> return url; // 详情页，直接请求
>
> } else {
>
> return host + \'/group/\' + url + \'.html\'; // 普通分类，补全路径
>
> }
>
> }

**3. 内容类型分流（320影视模式）**

同一入口下，根据内容类型走不同解析逻辑：

> if (/vod\|label/.test(cid)) {
>
> // 视频版块：用 lazyRule 解析
>
> return this.\_parseVideo(html);
>
> } else if (/art-type-id-2/.test(cid)) {
>
> // 小说版块：用 rule 唤起阅读器
>
> return this.\_parseNovel(html);
>
> } else if (/art-type-id-1/.test(cid)) {
>
> // 图片版块：返回 pics://
>
> return this.\_parsePics(html);
>
> }

**4. 选择器降级**

多个选择器依次尝试，取第一个成功的：

> var items = pdfa(html, \'ul.update_area_lists&&li\');
>
> if (items.length === 0) items = pdfa(html,
> \'#content&&article.node-teaser\');
>
> if (items.length === 0) items = pdfa(html,
> \'li:has(a\[href\*=\"/pic/\"\])\');
>
> if (items.length === 0) items = pdfa(html, \'a\');

**为什么 if/else if 如此重要？**

> 1\. 引擎调用机制：点击分类时，引擎把 class_url 的值赋给
> MY_URL，然后调用主页函数。必须在主页函数里用 if/else if 判断 MY_URL
> 来分流。
>
> 2\. URL 多样性：一个站点可能有多种 URL
> 格式（/newest、/tags/xxx、/group/xxx、/all-tags），只有用 if/else if
> 才能统一处理。
>
> 3\. 内容类型混合：像
> 320影视那样，同一入口下有视频、小说、图片三种内容，必须根据 URL
> 特征分流。
>
> 4\. 降级容错：选择器可能失效，用 if/else if 逐步降级，保证规则稳定性。
>
> **核心口诀：**MY_URL 分入口，if/else if 走不同路 \|
> 选择器降级保底，备选链条不能输 \| URL 特征先识别，请求参数再构造 \|
> 类型分流按 cid，各走各的逻辑树
>
> **核心定位：**if/else if 不是简单的条件判断，而是整个规则的路由系统。
>
> **第三十九章：if/else if 同页面多功能模块拼接------Tab 切换模式**

if/else if 最强大的地方在于在同页面内拼接不同功能模块，实现类似"Tab
切换"的效果。以下是三种典型模式：

**模式1：顶部导航 + 内容区域（320影视模式）**

> 主页: function() {
>
> var d = \[\];
>
> var cid = MY_URL;
>
> // ===== 第一部分：顶部分类按钮 =====
>
> if (MY_PAGE == 1) {
>
> var navs = \[\'热门标签\', \'最新精选\', \'美图精选\'\];
>
> for (var i = 0; i \< navs.length; i++) {
>
> d.push({
>
> title: navs\[i\],
>
> url: \$(\'#noLoading#\').lazyRule(function(mode) {
>
> putMyVar(\'mode\', mode);
>
> refreshPage(false);
>
> }, navs\[i\]),
>
> col_type: \'scroll_button\'
>
> });
>
> }
>
> d.push({ col_type: \'blank_block\' });
>
> }
>
> // ===== 第二部分：根据当前模式显示不同内容 =====
>
> var mode = getMyVar(\'mode\', \'index\');
>
> if (mode === \'tags\') {
>
> // 功能A：显示标签云
>
> var tags = this.\_getTags();
>
> for (var i = 0; i \< tags.length; i++) {
>
> d.push({ title: tags\[i\], col_type: \'text_1\' });
>
> }
>
> } else if (mode === \'taglist\') {
>
> // 功能B：显示标签下的图片列表
>
> var tag = getMyVar(\'tag\', \'\');
>
> d.push({ title: \'🔖 \' + tag, col_type: \'text_center_1\' });
>
> // \... 加载图片列表
>
> } else {
>
> // 功能C：显示普通分类图片列表
>
> // \... 加载图片列表
>
> }
>
> return d;
>
> }

**模式2：搜索框 + 结果区域**

> 主页: function() {
>
> var d = \[\];
>
> // ===== 第一部分：搜索框（始终显示）=====
>
> if (MY_PAGE == 1) {
>
> d.push({ title: \"🔍\", col_type: \"input\", desc: \"搜你想要\...\",
>
> url: \$(\'hiker://search?s=\' + input + \'&rule=\' +
> MY_RULE.title).rule()
>
> });
>
> }
>
> // ===== 第二部分：搜索结果或默认内容 =====
>
> var keyword = getVar(\'keyword\', \'\');
>
> if (keyword) {
>
> // 显示搜索结果
>
> var html = fetch(this.host + \'/search?q=\' + keyword);
>
> var items = pdfa(html, \'.result-item\');
>
> for (var i = 0; i \< items.length; i++) {
>
> d.push(this.\_parseCard(items\[i\]));
>
> }
>
> } else {
>
> // 显示默认推荐列表
>
> var html = fetch(this.host + \'/\');
>
> var items = pdfa(html, \'.recommend-item\');
>
> for (var i = 0; i \< items.length; i++) {
>
> d.push(this.\_parseCard(items\[i\]));
>
> }
>
> }
>
> return d;
>
> }

**模式3：多级筛选（分类 + 地区 + 年份）**

> 主页: function() {
>
> var d = \[\];
>
> var cat = getMyVar(\'cat\', \'0\');
>
> var area = getMyVar(\'area\', \'0\');
>
> var year = getMyVar(\'year\', \'0\');
>
> // ===== 第一行：分类筛选 =====
>
> if (MY_PAGE == 1) {
>
> var cats = \[\'全部\', \'电影\', \'电视剧\', \'动漫\'\];
>
> for (var i = 0; i \< cats.length; i++) {
>
> d.push({
>
> title: i == cat ? \'❆ \' + cats\[i\] + \' ❆\' : cats\[i\],
>
> url: \$(\'#noLoading#\').lazyRule(function(idx) {
>
> putMyVar(\'cat\', idx); refreshPage(false);
>
> }, i),
>
> col_type: \'scroll_button\'
>
> });
>
> }
>
> d.push({ col_type: \'blank_block\' });
>
> // ===== 第二行：地区筛选 =====
>
> var areas = \[\'全部\', \'大陆\', \'香港\', \'台湾\', \'日本\',
> \'韩国\'\];
>
> for (var i = 0; i \< areas.length; i++) {
>
> d.push({
>
> title: i == area ? \'❆ \' + areas\[i\] + \' ❆\' : areas\[i\],
>
> url: \$(\'#noLoading#\').lazyRule(function(idx) {
>
> putMyVar(\'area\', idx); refreshPage(false);
>
> }, i),
>
> col_type: \'scroll_button\'
>
> });
>
> }
>
> d.push({ col_type: \'blank_block\' });
>
> // ===== 第三行：年份筛选 =====
>
> var years = \[\'全部\', \'2026\', \'2025\', \'2024\'\];
>
> for (var i = 0; i \< years.length; i++) {
>
> d.push({
>
> title: i == year ? \'❆ \' + years\[i\] + \' ❆\' : years\[i\],
>
> url: \$(\'#noLoading#\').lazyRule(function(idx) {
>
> putMyVar(\'year\', idx); refreshPage(false);
>
> }, i),
>
> col_type: \'scroll_button\'
>
> });
>
> }
>
> d.push({ col_type: \'blank_block\' });
>
> }
>
> // ===== 第四部分：根据三个筛选条件请求内容 =====
>
> var url = this.host + \'/list\';
>
> if (cat != \'0\') url += \'/cat/\' + cat;
>
> if (area != \'0\') url += \'/area/\' + area;
>
> if (year != \'0\') url += \'/year/\' + year;
>
> if (MY_PAGE \> 1) url += \'/page/\' + MY_PAGE;
>
> var html = fetch(url);
>
> var items = pdfa(html, \'.list-item\');
>
> for (var i = 0; i \< items.length; i++) {
>
> d.push(this.\_parseCard(items\[i\]));
>
> }
>
> return d;
>
> }

**核心原理**

> 1\. 状态存储：用 putMyVar/getMyVar 保存当前处于哪个"功能模块"
>
> 2\. 刷新机制：点击按钮时更新状态，调用 refreshPage(false) 刷新页面
>
> 3\. 条件渲染：页面刷新后，根据状态变量决定显示哪个功能模块的内容
>
> 4\. 模块拼接：在同一个主页函数里，用 if/else if
> 把不同功能模块的渲染逻辑拼接在一起

**优势总结**

> · 无跳转：所有切换都在当前页面完成，用户感知不到页面刷新
>
> · 状态持久：putMyVar 保存的状态在翻页时依然有效
>
> · 模块化：每个功能模块的代码独立，易于维护
>
> · 灵活组合：可以自由组合搜索框、筛选器、内容列表等多个模块
>
> **总结：**if/else if
> 在同页面拼接不同功能的强大之处------无跳转、状态持久、模块化、灵活组合，是规则开发中实现复杂交互的核心武器。
>
> **第四十章：tlazy 内联图集规则------替换 lazyRule 的轻量方案（tuzac
> 实战）**

本章记录将 \_getPicsLazyRule（外部 lazyRule 函数）切换为内联 tlazy
写法的实战过程，以及两种方案的核心差异。

**背景：两种图集加载方案**

> · 方案A（\_getPicsLazyRule）：封装为独立函数，支持多页分页 +
> batchFetch 并发，逻辑完整但代码量大
>
> · 方案B（tlazy 内联）：用 \$(\'\').rule() 直接内联在 \_renderList
> 里，轻量、调试方便

切换原因：tlazy 方案在 \_renderList
里更直观，避免闭包参数传递的复杂性；\_getPicsLazyRule 仍保留在
\_renderAllTags 中使用。

**切换后的 \_renderList 核心写法**

> // tlazy：内联图集规则，直接拼接到 url
>
> var tlazy = \$(\'\').rule(() =\> {
>
> var d = \[\]
>
> var html = fetch(MY_URL)
>
> var htmlpage = pdfh(html, \'#pager&&a,-1&&href\').split(\'=\')\[1\]
>
> var surl = pdfh(html, \'#auto-play&&data\').replace(/\\D/g, \'\');
>
> var url = MY_URL.replace(/\\/\[\^/\]+\$/, \'/\' + surl);
>
> var map = (html, arr) =\> {
>
> parseDomForArray(html, \'.file-detail&&img\').map(item =\> {
>
> arr.push(parseDom(item, \'img&&src\'));
>
> });
>
> }
>
> var htmlUrl = \[\]
>
> for (var i = 2; i \<= htmlpage; i++) {
>
> htmlUrl.push({ url: url + \'/?at=\' + i })
>
> }
>
> var htmlArr = batchFetch(htmlUrl);
>
> var pics = \[\];
>
> map(html, pics);
>
> htmlArr.map(item =\> map(item, pics));
>
> for (let k = 0; k \< pics.length; k++) {
>
> d.push({ pic_url: pics\[k\], url: \'pics://\' + pics\[k\],
>
> title: \'\', col_type: \'pic_3\' });
>
> }
>
> setResult(d)
>
> })
>
> // 列表项 url 直接拼接 tlazy
>
> \_d.push({
>
> title: \_title,
>
> pic_url: \_pic ? this.\_addReferer(\_pic) : \'\',
>
> url: \_link + tlazy, // ← 切换点：原为 this.\_getPicsLazyRule(\_link)
>
> col_type: \'movie_3\'
>
> });

**两种方案对比**

\_getPicsLazyRule vs tlazy 核心差异：

> · 作用域：\_getPicsLazyRule 通过闭包参数传入 host/UA；tlazy 用 MY_URL
> 直接在规则内部 fetch，无需外部参数
>
> · 分页处理：\_getPicsLazyRule
> 解析末页链接提取最大页码，容错更强；tlazy 从 #pager&&a,-1&&href
> 提取总页数，更简洁
>
> · 图片选择器：\_getPicsLazyRule 用 .image-loading-box 降级到
> .file-detail&&img；tlazy 直接用 .file-detail&&img
>
> · 去重：\_getPicsLazyRule 有 seen 对象去重；tlazy
> 无去重（站点图片一般不重复）
>
> · 保留位置：\_getPicsLazyRule 仍在 \_renderAllTags
> 中使用（标签页图集）；tlazy 用于 \_renderList 主列表

**注意：保留 \_getPicsLazyRule 的原因**

> **重要：**\_renderAllTags 里的标签页列表仍调用
> \_getPicsLazyRule，因为标签页需要传入 host/UA 参数，且通过 eval
> 重建函数上下文（\_getRuleFn.call(\_ctx,
> \_link)）。两种方案并存，各司其职。

**同时修复的代码问题**

原代码 \_getPicsLazyRule 内有一行乱码需清理：

> // ❌ 原始乱码行（应删除）
>
> } catch (e) {} ;、v，
>
> // ✅ 修复后
>
> } catch (e) {}
>
> **铁律：**rule() 内联写法（tlazy）适合逻辑简单、参数少的场景；lazyRule
> 封装函数适合需要多参数、多分支、跨函数复用的场景。根据复杂度选型，不必强求统一。
# 第四十一章：工具对象封装模式——多媒体类型解析函数复用

## 41.1 背景与问题

规则里有视频、图集、有声三种内容类型，每种解析逻辑不同。如果每个卡片都内联一套完整的 lazyRule 代码，会导致：

- 代码重复量极大
- `renderList` 函数臃肿难以维护
- 修改解析逻辑时需要改多处

**解决方案：** 把各类型的解析逻辑封装到 `工具` 对象里，作为具名函数，在 `lazyRule` 里传函数引用。

## 41.2 工具对象标准结构

```js
var parse = {
    host: 'https://example.com',
    UA:   'Mozilla/5.0 ...',

    工具: {
        // 视频解析：正则提取 m3u8，带 Cookie
        videoLogic: function(host, ua) {
            var ck  = juItem.get('site_cookie', '');
            var html = request(input, { headers: { 'User-Agent': ua, 'Referer': host + '/', 'Cookie': ck } });
            var m = String(html || '').match(/https?:[^\s"'<>\\]+\.m3u8[^\s"'<>\\]*/i);
            if (m) {
                var sl  = String.fromCharCode(92);
                var url = m[0].split(sl).join('');
                return 'video://' + url + ';{User-Agent@' + ua + '&&Referer@' + host + '/&&Cookie@' + ck + '}';
            }
            return input + '#嗅探';
        },

        // 图集解析：$.image() 带 Cookie 防盗链，返回 pics://
        picLogic: function(host, ua) {
            var ck  = juItem.get('site_cookie', '');
            var html = fetch(input, { headers: { 'User-Agent': ua, 'Referer': host + '/', 'Cookie': ck } });

            // $.image() 带 Cookie：处理需要登录认证的防盗链图片（Law 28 延伸）
            var imgProxy = $().image(function(ua_s, ck_s) {
                var FU   = com.example.hikerview.utils.FileUtil;
                var real = String(input || '').split('@')[0];
                var h    = { 'User-Agent': ua_s, 'Cookie': ck_s };
                var dm   = real.match(/^(https?:\/\/[^\/]+)/);
                if (dm) h['Referer'] = dm[1] + '/';
                try { return FU.toInputStream(FU.readBytes(real, h)); } catch(e) { return null; }
            }, ua, ck);

            var pics = [];
            var items = pdfa(html, 'body&&.photo-item');
            for (var i = 0; i < items.length; i++) {
                var m = (pdfh(items[i], '.img&&style') || '').match(/url\('([^']+)'\)/);
                if (m) {
                    var iurl = m[1];
                    var dm   = iurl.match(/^(https?:\/\/[^\/]+)/);
                    pics.push(iurl + (dm ? '@Referer=' + dm[1] + '/' : '') + imgProxy);
                }
            }
            return pics.length > 0 ? 'pics://' + pics.join('&&') : 'toast://未提取到图片';
        },

        // 有声解析：提取 audio src，返回音频协议
        audioLogic: function(host, ua) {
            var ck   = juItem.get('site_cookie', '');
            var html = request(input, { headers: { 'User-Agent': ua, 'Referer': host + '/', 'Cookie': ck } });
            var src  = pdfh(html, 'audio source&&src') || '';
            if (!src) {
                var m = String(html || '').match(/https?:[^"'\s]+\.mp3[^"'\s]*/i);
                if (m) src = m[0];
            }
            if (src) return String(src).replace(/\\/g, '') + ';{Referer@' + host + '/}#isMusic=true#';
            return 'toast://未发现音频';
        },

        // 统一列表渲染：按 URL 特征自动分流三种内容类型
        renderList: function(html, host, ua) {
            var res   = [];
            var items = pdfa(html, 'body&&.item');
            for (var j = 0; j < items.length; j++) {
                try {
                    var item = items[j];
                    var link = pd(item, 'a&&href', host);
                    if (!link) continue;

                    var style = pdfh(item, '.img&&style') || '';
                    var pic   = (style.match(/url\('([^']+)'\)/) || [])[1];
                    if (pic) pic += '@Referer=' + host + '/';

                    var title = pdfh(item, '.title&&Text');

                    // ✅ 按 URL 特征分流，传函数引用复用解析逻辑
                    if (String(link).indexOf('/video/') > -1) {
                        var dur = pdfh(item, '.duration&&Text') || '';
                        res.push({
                            title: title, desc: '🎬 ' + dur, pic_url: pic,
                            url: $(link).lazyRule(parse.工具.videoLogic, host, ua),
                            col_type: 'movie_2'
                        });
                    } else if (String(link).indexOf('/photo/') > -1) {
                        var cnt = pdfh(item, '.count&&Text') || '';
                        res.push({
                            title: title, desc: '📸 ' + cnt, pic_url: pic,
                            url: $(link).lazyRule(parse.工具.picLogic, host, ua),
                            col_type: 'movie_3'
                        });
                    } else if (String(link).indexOf('/fiction/') > -1) {
                        res.push({
                            title: '🎧 ' + title, desc: '有声', pic_url: pic,
                            url: $(link).lazyRule(parse.工具.audioLogic, host, ua),
                            col_type: 'movie_2'
                        });
                    }
                } catch(e) {}
            }
            return res;
        }
    }
};
```

## 41.3 调用方式

```js
// ✅ 传函数引用，不是字符串，不是内联代码
url: $(link).lazyRule(parse.工具.videoLogic, host, ua)

// ❌ 不要这样写（内联重复逻辑）
url: $(link).lazyRule(function(host, ua) {
    // 和 videoLogic 里一模一样的代码...
}, host, ua)
```

## 41.4 $.image() 带 Cookie 的防盗链图片（Law 28 延伸）

手册第十章介绍了 `$.image()` 的基本用法。当站点图片需要**登录态（Cookie）** 才能访问时，Cookie 也要通过参数传入 `image()` 的 func：

```js
// ✅ Cookie 作为参数传入，lazyRule 内部读取持久化存储
var imgProxy = $().image(function(ua_s, ck_s) {
    var FU   = com.example.hikerview.utils.FileUtil;
    var real = String(input || '').split('@')[0];  // 去掉 @Referer= 后缀
    var h    = { 'User-Agent': ua_s, 'Cookie': ck_s };
    var dm   = real.match(/^(https?:\/\/[^\/]+)/);
    if (dm) h['Referer'] = dm[1] + '/';           // 自动从图片 URL 提取域名作为 Referer
    try {
        return FU.toInputStream(FU.readBytes(real, h));
    } catch(e) { return null; }
}, ua, ck);

// 图片 URL 后拼接 imgProxy 即可
pics.push(imgUrl + '@Referer=' + host + '/' + imgProxy);
```

> ⚠️ **注意**：`$().image()` 里的 `input` 是图片 URL（引擎下载前传入的 URL），不是详情页 URL。`FU.readBytes(real, h)` 用指定 headers 下载图片字节，`toInputStream` 转为流返回引擎渲染。

---

# 第四十二章：cate:// 虚拟协议——静态分类与矩阵子分类二级联动

## 42.1 问题背景

静态分类（第二章）：顶部 tab 零代码，但所有分类走同一解析逻辑，无法在进入某个大类后动态渲染**该大类专属的子分类按钮行**。

矩阵方案（第六章）：支持子分类，但全部逻辑都在主页函数里，分类按钮要手动维护。

**`cate://` 方案** 融合两者优势：静态分类做顶部大类 tab，进入后主页函数按 `cate://` 标识动态渲染子分类。

## 42.2 静态分类配置

```js
静态分类: {
    type: '主页',
    url: 'fyclass',
    class_name: '全部🎬&中文AV&日本AV&📸套图&💃模特',
    // ✅ 真实路径 和 cate:// 虚拟协议 混用
    class_url: '/videos/1.html&cate://中文AV&cate://日本AV&/photos/1.html&/models.html'
},
```

- 有实际路径的分类（如 `/videos/1.html`）：主页直接请求该路径
- `cate://大类名`：主页识别后查询子分类数据库，渲染子分类按钮行

## 42.3 子分类数据库

```js
_subCategories: {
    '中文AV': [
        { name: '麻豆传媒', url: '/videos/series-xxx1.html' },
        { name: '糖心Vlog', url: '/videos/series-xxx2.html' },
        { name: '蜜桃传媒', url: '/videos/series-xxx3.html' },
        // ...
    ],
    '日本AV': [
        { name: '有码AV', url: '/videos/series-yyy1.html' },
        { name: '无码AV', url: '/videos/series-yyy2.html' },
    ],
    // ...
},

// 大类默认列表页（点进大类时默认展示全部）
_mainCateUrlMap: {
    '中文AV': '/videos/series-all-cn.html',
    '日本AV': '/videos/series-all-jp.html',
},
```

## 42.4 主页函数核心逻辑

```js
主页: function() {
    var d      = [];
    var host   = this.host;
    var ua     = this.UA;
    var curUrl = typeof MY_URL !== 'undefined' ? MY_URL : '';

    // ── Step1：切换大类时清空子分类状态（状态互斥清除）──────────────
    var lastClass = getMyVar('site_last_class', '');
    if (lastClass !== curUrl) {
        putMyVar('site_sub_path', '');   // 清空子分类路径
        putMyVar('site_sub_idx', '0');   // 重置子分类选中
        putMyVar('site_last_class', curUrl);
    }

    // ── Step2：解析当前路径 ──────────────────────────────────────────
    var subPath    = getMyVar('site_sub_path', '');
    var isMainCate = false;
    var mainName   = '';
    var path       = subPath || curUrl;

    if (String(path).indexOf('cate://') === 0) {
        // 初次进入大类（未选子分类）
        isMainCate = true;
        mainName   = path.split('//')[1];
        path       = this._mainCateUrlMap[mainName] || path;
    } else if (String(curUrl).indexOf('cate://') === 0 && subPath !== '') {
        // 已选子分类，curUrl 仍是 cate://xxx，path 已是子分类路径
        isMainCate = true;
        mainName   = curUrl.split('//')[1];
    }

    // ── Step3：请求列表页 ────────────────────────────────────────────
    var fetchUrl = this._buildUrl(path, MY_PAGE);
    var html     = fetch(fetchUrl, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });

    // ── Step4：第一页渲染子分类按钮行 ───────────────────────────────
    if (MY_PAGE == 1 && isMainCate && this._subCategories[mainName]) {
        var subs    = [{ name: '全部', url: this._mainCateUrlMap[mainName] }]
                      .concat(this._subCategories[mainName]);
        var curIdx  = parseInt(getMyVar('site_sub_idx', '0'));

        for (var s = 0; s < subs.length; s++) {
            var isSel = (s === curIdx);
            d.push({
                title:    isSel ? '❆ ' + subs[s].name + ' ❆' : subs[s].name,
                col_type: 'scroll_button',
                url: $('#noLoading#').lazyRule(function(idx, subUrl) {
                    putMyVar('site_sub_idx',  idx + '');
                    putMyVar('site_sub_path', subUrl);
                    refreshPage(false);
                    return 'hiker://empty';
                }, s, subs[s].url)
            });
        }
        d.push({ col_type: 'blank_block' });
    }

    // ── Step5：渲染内容列表 ──────────────────────────────────────────
    return d.concat(this.工具.renderList(html, host, ua));
},
```

## 42.5 状态互斥清除——防止子分类残留（Law 84）

```js
// ❌ 不清除时的 bug：
// 用户在"中文AV"里选了"麻豆传媒"，putMyVar('site_sub_path', '/videos/series-xxx')
// 切到"日本AV"后，site_sub_path 仍是麻豆的路径 → 日本AV页面显示麻豆内容

// ✅ 切换大类时强制清除子分类状态
var lastClass = getMyVar('site_last_class', '');
if (lastClass !== curUrl) {
    putMyVar('site_sub_path', '');
    putMyVar('site_sub_idx', '0');
    putMyVar('site_last_class', curUrl);
}
```

> 📌 **Law 84（新增）**：静态分类 + 矩阵子分类联动时，必须用 `lastClass !== curUrl` 检测大类切换，在切换瞬间清空所有子分类状态变量，防止跨分类残留。

## 42.6 选型决策

| 场景 | 推荐方案 |
|---|---|
| 分类少、URL 规律、单内容类型 | 静态分类（第二章）|
| 分类多、多维筛选、单内容类型 | 矩阵方案（第六章）|
| 有大类+子分类两级结构 | **cate:// 虚拟协议融合方案（本章）**|
| 内容类型多样（视频/图集/有声/模特）| categoryList（第三十七章）|

---

# 第四十三章：模特/演员二级页——noShow + extenditems 完全自定义

## 43.1 场景说明

模特/演员详情页不是普通的「选集列表」页，它包含：

- 头像大图
- 姓名 + 简介
- AI 评价文字
- 「查看全部视频」入口（带翻页）
- 「查看全部套图」入口（带翻页）
- 收录作品缩略列表

用引擎默认的二级格式（`line + list`）无法满足。需要用 `noShow` 隐藏默认组件，用 `extenditems` 完全自定义页面内容。

## 43.2 完全自定义二级页模板

```js
二级: function(url) {
    var d    = [];
    var host = this.host;
    var ua   = this.UA;
    var html = fetch(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });

    if (url.indexOf('/model/') > -1 || url.indexOf('/actor/') > -1) {
        // 头像
        var avatar = pdfh(html, '.avatar&&img&&src') || '';
        if (avatar) d.push({ pic_url: avatar + '@Referer=' + host + '/', url: avatar, col_type: 'pic_1_full' });

        // 姓名
        var name = pdfh(html, '.info&&.name&&Text') || '';
        if (name) d.push({ title: '💎 ' + name, col_type: 'text_center_1', extra: { textSize: 22, isBold: true } });

        // 简介
        var bio = pdfh(html, '.info&&.bio&&Text') || '';
        if (bio && bio.length > 5) {
            d.push({ title: bio, col_type: 'rich_text', extra: { textSize: 15 } });
            d.push({ col_type: 'line' });
        }

        // 「查看全部视频」入口——用 $().rule() 新开列表页，带翻页
        var vLink = pd(html, 'a:contains(全部视频)&&href', host) || '';
        if (vLink) {
            d.push({
                title:    '🎬 查看全部视频 >>',
                col_type: 'text_center_1',
                url: $(vLink + '##fypage').rule(function(lnk, h, u) {
                    var pg  = typeof MY_PAGE !== 'undefined' ? MY_PAGE : 1;
                    var ck  = juItem.get('site_cookie', '');
                    var base = lnk.replace(/\/\d+\.html$/, '').replace('.html', '');
                    var pageUrl = pg <= 1 ? lnk : base + '/' + pg + '.html';
                    var rHtml = fetch(pageUrl, { headers: { 'User-Agent': u, 'Referer': h + '/', 'Cookie': ck } });
                    // 解析视频列表...
                    var rs = [];
                    var its = pdfa(rHtml, 'body&&.item');
                    for (var i = 0; i < its.length; i++) {
                        var lk = pd(its[i], 'a&&href', h);
                        if (!lk || lk.indexOf('/video/') === -1) continue;
                        rs.push({
                            title:    pdfh(its[i], '.title&&Text'),
                            pic_url:  pd(its[i], 'img&&src', h) + '@Referer=' + h + '/',
                            url:      $(lk).lazyRule(parse.工具.videoLogic, h, u),
                            col_type: 'movie_2'
                        });
                    }
                    setResult(rs);
                }, vLink, host, ua)
            });
        }

        d.push({ col_type: 'line' });
        d.push({ title: '📚 收录作品', col_type: 'text_center_1', extra: { isBold: true } });
        d = d.concat(this.工具.renderList(html, host, ua));

        // ✅ noShow 隐藏引擎默认组件，extenditems 完全自定义
        return {
            vod_name: name,
            vod_pic:  avatar,
            noShow:   { 简介: true, 选集: true, 排序: true },
            list:     [[]],       // 必须给空二维数组，否则引擎报错
            extenditems: d
        };
    }

    return { detail1: '详情加载中', list: [[]] };
},
```

## 43.3 noShow 字段速查

```js
noShow: {
    封面: false,   // 是否隐藏封面图区域
    简介: true,    // 隐藏引擎默认简介区域（用 extenditems 自定义）
    排序: true,    // 隐藏选集排序按钮
    选集: true     // 隐藏选集列表区域（用 extenditems 代替）
}
```

> ⚠️ 使用 `extenditems` 时，`list` 字段必须给 `[[]]`（空的二维数组），否则引擎会报结构错误。

## 43.4 三种二级页结构对比

| 结构 | 适用场景 | 关键字段 |
|---|---|---|
| 标准结构 | 有选集的视频/动漫 | `line + list` |
| 图文二级 | 图集、文章 | `extenditems` |
| 完全自定义 | 模特/演员/作者主页 | `noShow + extenditems + list:[[]]` |

---

## 43.5 本章新增铁律

| 编号 | 铁律名称 | 核心要义 | 口诀 |
|---|---|---|---|
| Law 84 | 子分类状态互斥清除 | 切换顶部大类时必须清空所有子分类 getMyVar，防止跨分类残留 | lastClass !== curUrl → 清空 sub 相关变量 |
| Law 85 | $.image() Cookie 传参 | 需要登录态的防盗链图片，Cookie 要作为参数传入 $.image() 的 func | $.image() 不能访问外部变量，Cookie 必须参数传入 |
| Law 86 | noShow + extenditems 配套 | 使用 extenditems 完全自定义二级页时，list 必须给 `[[]]`，noShow 按需隐藏默认组件 | extenditems 自定义，list:[[]] 保底，noShow 隐组件 |
| Law 87 | typeof 防御注入变量 | MY_URL / MY_PAGE 等引擎注入变量在某些环境可能未定义，用 typeof 判断兜底 | `typeof MY_URL !== 'undefined' ? MY_URL : ''` |

---

# 第四十四章：1024吃瓜网规则开发难点总结

## 44.1 章节背景

1024吃瓜网是典型的高对抗性成人内容站点，具有域名频繁轮换、图片/视频双重加密、API 结构多变三大核心难点。本章系统梳理开发过程中遇到的问题与解决方案，作为同类型站点的参考范本。

---

## 44.2 规则架构说明

> 📌 本章聚焦难点，44.2 节略（架构与通用规则相同，见第四十六章）。

## 44.3 双重加密体系

站点对**图片**和**视频**分别采用不同的 AES 加密策略。

### 图片加密（固定 key/iv）

```javascript
// 使用 crypto-java.js，key/iv 在规则内固定写死
var IMG_KEY = 'xxxxxxxxxxxxxxxx';
var IMG_IV  = 'xxxxxxxxxxxxxxxx';
// CryptoUtil.decrypt(encData, key, iv) → 明文 URL
```

### 视频加密（动态 key/iv）

```javascript
// key/iv 从页面 data-config 属性中动态提取，每个视频不同
var config = JSON.parse(pdfh(html, '.dplayer&&data-config').replace(/&quot;/g, '"'));
var api  = config['data-api'];
var _key = config['data-key'];   // 动态，不可硬编码
var _iv  = config['data-iv'];    // 动态，不可硬编码
```

> ⚠️ **key/iv 的动态/静态属性决定了能否复用解密逻辑**。开发前必须先抓包确认，切勿假设二者相同。

---

## 44.4 播放器配置提取

### data-config 解析要点

| 难点 | 解决方案 |
|---|---|
| `data-config` 嵌套在 DPlayer 容器中 | 正则或 `pdfh` 匹配 `.dplayer&&data-config` |
| JSON 中含 HTML 转义字符 | 先 `.replace(/&quot;/g, '"')` 再 `JSON.parse()` |
| 一篇文章含多个播放器 | `pdfa(html, "body&&.dplayer")` 遍历，循环处理 |

### 多视频容器遍历模板

```javascript
var players = pdfa(html, 'body&&.dplayer');
for (var i = 0; i < players.length; i++) {
    var rawConfig = pdfh(players[i], 'self&&data-config') || '';
    if (!rawConfig) continue;
    var config = JSON.parse(rawConfig.replace(/&quot;/g, '"'));
    // 生成各自独立的 lazyRule...
}
```

---

## 44.5 API 响应结构多变

同一站点的视频 API 可能返回三种结构，需按优先级降级处理：

```javascript
// 优先级：video_url → url → 解密 data
var obj  = JSON.parse(resp);
var mUrl = '';

if (obj.video_url && obj.video_url.indexOf('http') === 0) {
    mUrl = obj.video_url;               // 情况1：直接可用
} else if (obj.url && obj.url.indexOf('http') === 0) {
    mUrl = obj.url;                     // 情况2：url 字段
} else if (obj.data) {
    mUrl = decrypt(obj.data, key, iv);  // 情况3：需解密
}
```

> 📌 **兜底机制**：解密仍失败时，尝试从原始页面 HTML 正则匹配 `.m3u8` 链接，确保播放成功率。

---

## 44.6 聚阅/海阔引擎限制——与本站的交叉影响

| 引擎限制 | 对本站的影响 | 解决方案 |
|---|---|---|
| 同步请求阻塞 | 多个视频 API 串行请求导致页面卡顿 | 懒加载（点击时才请求 API） |
| `lazyRule` 参数顺序严格 | 顺序错乱 → `enc invalid` 报错 | 严格按 `(apiUrl, key, iv, host, ua)` 顺序传参 |
| `this` 在 `lazyRule` 内丢失 | 无法访问 `this.UA` 等实例属性 | 提前 `var self = this`，通过参数传入 |
| 无 `atob`/`btoa` | Base64 解码报错 | 用 `android.util.Base64`（Law 62） |
| `CryptoUtil` 非标准 API | 与 CryptoJS 用法不同 | 用 `Data.parseUTF8() + .toText()` |

---

## 44.7 懒加载实现——正确的参数传递方式

参数顺序错乱是本站开发中最高频的坑点，必须严格对照。

```javascript
// ✅ 正确写法：lazyRule 函数的形参顺序 = 传入参数顺序
var lazyUrl = $(apiUrl).lazyRule(function(apiUrl, key, iv, host, ua) {
    // 在此发起 API 请求并解密
    var resp = fetch(apiUrl, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });
    // ... 解密返回 m3u8 URL
}, apiUrl, _key, _iv, host, self.UA);
//  ↑第1实参→第1形参  ↑第2  ↑第3  ↑第4  ↑第5

// ❌ 错误示例：实参与形参顺序不一致
var lazyUrl = $(apiUrl).lazyRule(function(key, apiUrl, iv, ua, host) {
    // key 收到的实际是 apiUrl 的值 → 解密必然失败
}, apiUrl, _key, _iv, host, self.UA);
```

> 📌 **调试技巧**：在 lazyRule 内 `log('key=' + key + ' iv=' + iv)` 打印收到的参数，与预期值对比，快速定位顺序问题。

---

## 44.8 多视频支持

| 难点 | 解决方案 |
|---|---|
| 一篇文章多个 DPlayer | `pdfa(html, "body&&.dplayer")` 遍历 |
| 每个视频有独立 API + key/iv | 循环内各自提取 config，生成独立 lazyRule |
| 线路名称动态化 | 单视频显示"播放"，多视频显示"视频列表 (N个)" |

---

## 44.9 Cookie 与防盗链处理

| 场景 | 方案 |
|---|---|
| CF 验证 Cookie | `fetchCookie` 获取后 `juItem.set` 持久化 |
| 图片防盗链 | 图片 URL 拼接 `@Referer=host/` |
| 视频 Headers | `video://url;{User-Agent@xxx&&Referer@xxx}` |

---

## 44.12 核心开发经验总结

1. **先跑通基础流程，再优化性能**——能播放的版本永远比完美的方案更重要。
2. **保留能正常播放的版本**，在其基础上逐步迭代，避免"破坏式重构"。
3. **`lazyRule` 参数顺序是最大坑点**，必须严格对照函数签名，上线前逐参打 log 验证。
4. **日志是调试利器**，关键节点（参数收到的值、API 响应结构、解密结果）务必打出来，上线时可保留非敏感日志。
5. **兜底机制很重要**：解密失败 → 正则抓页面 m3u8 → 最终兜底显示错误提示，三层保障播放成功率。
6. **页面特征测试优于长度测试**：任何涉及"请求是否成功"的判断，必须用业务内容特征而非 `html.length`。

---

> 📌 本章所涉及的铁律：Law 62（无 atob/btoa）、Law 63（多字节 UTF-8 字节级解密）、Law 67（sandbox HTTP URL 开浏览器）均在前章已详细说明，本章不重复展开，以交叉引用为准。


---

# 第四十五章：壳子内置工具函数完全参考（dy2020 框架）

本章基于实战规则代码（007吃瓜、51暗网、海角等站点）提炼，记录聚阅壳子（dy2020框架）提供的内置工具函数。这些函数由壳子通过 `rc()` 远程加载的公共库提供，**不属于海阔/聚阅引擎原生 API**。

> ⚠️ **Law 88（新增）：禁止在规则中使用 `rc()` / `gfd()` / `fc()` 远程加载调用。**
> 这三个函数用于从 Gitee/GitHub 远程拉取公共库代码，每次执行都会发起网络请求，存在以下致命问题：
> - **网络依赖**：Gitee/GitHub 被墙或访问慢时，规则直接卡死
> - **不可控**：远程库随时可能变更或失效，导致规则无法运行
> - **性能差**：每次主页调用都额外消耗网络请求
>
> **正确做法**：将所需工具函数直接内联到规则代码中，或使用手册前述章节的标准 API 实现相同功能。

---

## 45.1 rc() / gfd() / fc() ——远程库加载（禁止使用）

```javascript
// ❌ 禁止：远程拉取公共库
rc((rc('https://gitee.com/mistywater/hiker_info/raw/master/ghproxy.js'), gfd()) 
   + 'https://raw.githubusercontent.com/mistywater/hiker/main/f', 24);

// rc(url, cacheHours)  → 加载远程 JS 并 eval，cacheHours 为缓存小时数
// gfd()               → 返回 GitHub 代理前缀（来自 ghproxy.js）
// fc(url)             → 获取 JSON 配置文件中的代理前缀
```

**替代方案**：把所需功能（`getHtml`、`classTop` 等）直接实现在规则内，或使用手册各章的标准写法。

---

## 45.2 getHtml() ——带 CF 缓存的 fetch 封装

壳子提供的 `getHtml` 是对 `fetch`/`fetchCodeByWebView` 的封装，集成了 Cookie 管理和 CF 过盾：

```javascript
// 壳子写法（需要 rc 加载公共库）
var html = getHtml(MY_URL);
var html = getHtml(MY_URL, '', '', 1);  // 第4参数=1 表示需要 WebView 渲染

// ✅ 手册标准替代写法（第十二章）
var ck = juItem.get('site_cookie', '');
var html = fetch(MY_URL, { headers: { 'User-Agent': parse.UA, 'Cookie': ck } });
if (html.indexOf('视频卡片class') === -1) {
    // CF 过盾逻辑（详见第十二章）
}
```

---

## 45.3 cpage() ——分类页码解析

`cpage()` 从 `MY_URL` 里解析当前分类路径 `_c` 和页码 `page`：

```javascript
// 壳子写法
eval(cpage(''));
// 执行后注入：_c（分类路径）、page（当前页码）

// ✅ 手册标准替代写法
var _c = '';
var page = MY_PAGE;
if (MY_URL && MY_URL !== 'fyclass') {
    _c = MY_URL;  // MY_URL 就是 class_url 里的值（Law 60）
}
```

---

## 45.4 classTop() ——分类标签行渲染

`classTop()` 渲染顶部 `scroll_button` 分类行，对应手册第六章的 `renderRow`：

```javascript
// 壳子写法
var dataClass = [{
    title: '首页&最新热瓜&每日精选',
    id: '&htzx&mrjx'
}];
dataClass.forEach((item, index) => {
    classTop(index, item, host, d);
});

// ✅ 手册标准替代写法（第六章 renderRow）
var renderRow = function(rowIdx, titleStr, idStr, d, h) {
    var tArr = titleStr.split('&');
    var iArr = idStr.split('&');
    var curIdx = getMyVar(h + 'cindex' + rowIdx, '0');
    for (var i = 0; i < tArr.length; i++) {
        var isSel = (i == curIdx);
        d.push({
            title: isSel ? '❆ ' + tArr[i] + ' ❆' : tArr[i],
            url: $('#noLoading#').lazyRule(function(rIdx, idx, val, h) {
                putMyVar(h + 'cindex' + rIdx, idx + '');
                putMyVar(h + 'curl' + rIdx, val);
                if (rIdx == 0) {
                    for (var n = 1; n < 10; n++) {
                        putMyVar(h + 'cindex' + n, '0');
                        putMyVar(h + 'curl' + n, '');
                    }
                }
                refreshPage(false);
                return 'hiker://empty';
            }, rowIdx, i, iArr[i], h),
            col_type: 'scroll_button'
        });
    }
    d.push({ col_type: 'blank_block' });
};
```

---

## 45.5 pageAdd() / pageMoveto() ——翻页与记忆定位

```javascript
// pageAdd(page, host)  → 记录当前页码，供 pageMoveto 恢复用
// pageMoveto(host, page, type, pages, chchePath) → 生成 extra 对象，点击卡片后跳回上次位置

// ✅ 手册标准替代写法（简化版，不用位置记忆）
// 直接依赖 MY_PAGE 翻页即可，无需手动 pageAdd
```

---

## 45.6 getdTemp() / writeFile() ——页面缓存机制

壳子用 `hiker://files/_cache/` 缓存页面列表数据，避免重复网络请求：

```javascript
// 壳子写法
let _chchePath = `hiker://files/_cache/juyue/${jkdata.name}_${safePath(MY_URL)}.txt`;
dTemp = getdTemp(dTemp, d, _chchePath);
if (dTemp.length != 0) return dTemp;
// ... 构建 d ...
if (d.length != 0) writeFile(_chchePath, JSON.stringify(d));
return dTemp.concat(d);

// ✅ 手册标准替代写法（用 juItem.set 简化缓存）
var _cacheKey = 'page_cache_' + MY_PAGE;
var _cached = juItem.get(_cacheKey, '');
if (_cached) {
    try { return JSON.parse(_cached); } catch(e) {}
}
// ... 构建 d ...
juItem.set(_cacheKey, JSON.stringify(d));
return d;
```

> ⚠️ `hiker://files/` 路径写文件需要引擎版本支持，不如 `juItem.set` 通用。缓存策略应根据内容更新频率决定是否启用。

---

## 45.7 safePath() ——URL 转安全文件名

`safePath()` 把 URL 中的特殊字符替换为下划线，生成合法文件名：

```javascript
// 壳子写法
safePath('https://example.com/page/2/') 
// → 'https___example_com_page_2_'

// ✅ 手册标准替代（无需此函数，直接用 juItem.set，key 用 URL 的 hash）
var _key = 'cache_' + MY_PAGE + '_' + (_c || 'home');
```

---

## 45.8 getFastestDomain() ——多域名竞速选优

`getFastestDomain()` 并发探测多个备用域名，返回响应最快的：

```javascript
// 壳子写法（配合 fetchCodeByWebView 获取域名列表）
let urls = pdfa(html, '#list-wrap&&.btnLink').map(item => pdfh(item, 'Text'));
host = getFastestDomain(urls);
host = host ? 'https://' + host.slice(0, -1) : 'https://backup.example.com';

// ✅ 手册标准替代写法（第十三章域名自愈 + batchFetch 测速）
var candidates = ['https://site1.com', 'https://site2.com', 'https://site3.com'];
var tasks = candidates.map(function(u) { return { url: u + '/', options: { timeout: 3000 } }; });
var results = batchFetch(tasks);
var host = candidates[0];  // 默认备用
for (var ri = 0; ri < results.length; ri++) {
    if (results[ri] && results[ri].indexOf('特征关键词') > -1) {
        host = candidates[ri];
        break;
    }
}
juItem.set('current_host', host);
```

---

## 45.9 imgDec() ——AES 图片解密 lazyRule 生成

`imgDec(key, iv, 'AES')` 生成一段 `@js=` 图片解密字符串，拼接到图片 URL 后触发 AES 解密：

```javascript
// 壳子写法
img: 'https://cdn.example.com/path' + '@js=' + imgDec(key, iv, 'AES')

// ✅ 手册标准替代写法（第十章 $.image() + CryptoUtil，Law 28）
var tlazy = $('').image(function(k, iv_s) {
    try {
        var CryptoUtil = $.require('hiker://assets/crypto-java.js');
        var keyBytes = CryptoUtil.Data.parseUTF8(k);
        var ivBytes  = CryptoUtil.Data.parseUTF8(iv_s);
        var rawData  = CryptoUtil.Data.parseInputStream(input);
        var dec = CryptoUtil.AES.decrypt(rawData, keyBytes, {
            mode: 'AES/CBC/PKCS7Padding', iv: ivBytes
        });
        return dec.toInputStream();
    } catch(e) { log('imgDec fail: ' + e); return input; }
}, key, iv);

// 使用（提前调用一次，循环内复用）
d.push({ pic_url: imgUrl + tlazy });
```

---

## 45.10 $.toString() ——函数字符串化存储模式

> 📌 `$` 工具的标准用法见**第 26 章 `$ 工具函数完全参考`**；本节专讲壳子规则里残留的 `$.toString()` 字符串化反模式及其手册标准替代。

壳子规则用 `$.toString(() => { ... })` 把代码块序列化成字符串存在 `parse` 属性中，后续用 `eval()` 执行：

```javascript
// 壳子写法
parse.list = $.toString(() => {
    list = pdfa(html, 'article');
    list.forEach((item) => { d.push({...}); });
});

// 在主页函数中执行
eval(parse.list);

// ✅ 手册标准替代：直接定义为函数方法
_parseList: function(html, host, ua) {
    var d = [];
    var items = pdfa(html, 'article');
    for (var i = 0; i < items.length; i++) {
        d.push({ ... });
    }
    return d;
},

// 在主页函数中调用
var d = this._parseList(html, this.host, this.UA);
```

> 📌 `$.toString()` + `eval()` 模式的问题：作用域不透明，依赖外部变量（`html`、`d`、`host` 等）隐式注入，调试困难，且 `eval` 在引擎沙箱里性能差。**推荐改用明确参数的函数调用**。

---

## 45.11 String 扩展方法——壳子注入的链式 API

壳子公共库给 String 原型注入了一批链式方法，用于快速格式化标题：

| 方法 | 说明 | 等价写法 |
|---|---|---|
| `.color('fff')` | 文字着色（fontcolor 别名）| `str.fontcolor('#fff')` |
| `.colorR(333)` | 随机色 + 文字（333=透明度变体）| 接近 `str.fontcolor(randomColor())` |
| `.sbR(n)` | 加粗 + 随机大小（n 控制字号范围）| `'<big>' + str + '</big>'` |
| `.big(n)` | HTML big 标签包裹 | `'<big>' + str + '</big>'` |
| `strong(str, color)` | 加粗着色 | `str.bold().fontcolor('#' + color)` |
| `strongR(str, color)` | 富文本格式化（含 blockquote 处理）| 复杂 HTML 格式化 |

> ⚠️ 这些方法来自壳子公共库，不是引擎原生 API，在不使用 `rc()` 的规则里无法使用。替代方案：直接用 `fontcolor()`、`bold()`，或用 `rich_text` col_type 自定义 HTML。

---

## 45.12 getRandomColor() ——壳子版随机颜色

壳子版 `getRandomColor(type)` 参数控制颜色风格：

```javascript
// 壳子写法（type=2 表示高饱和度色彩）
extra: { backgroundColor: getRandomColor(2) }

// ✅ 手册标准替代（第三十七章"学习追加"节的改进版）
var getRangeColors = function() {
    var h = Math.floor(Math.random() * 360),
        s = Math.floor(Math.random() * 50 + 50) / 100,
        l = Math.floor(Math.random() * 50 + 50) / 100,
        c = (1 - Math.abs(2 * l - 1)) * s,
        x = c * (1 - Math.abs((h / 60) % 2 - 1)),
        m = l - c/2,
        [r1,g1,b1] = h<60?[c,x,0]:h<120?[x,c,0]:h<180?[0,c,x]:h<240?[0,x,c]:h<300?[x,0,c]:[c,0,x],
        r=Math.floor((r1+m)*255), g=Math.floor((g1+m)*255), b=Math.floor((b1+m)*255);
    return '#' + ((1<<24) + (r<<16) + (g<<8) + b).toString(16).slice(1);
};
```

---

# 第四十六章：WordPress 主题型博客站规则开发（吃瓜/暗网站型）

本章基于 007吃瓜（WordPress + Mirages主题）和 51暗网同类型站点规则提炼，总结此类站点的完整开发模式。

**站点特征**：WordPress + 自定义主题、图文混合内容（视频 + 图片 + 文字 + 评论）、图片 AES 加密、域名频繁更换。

> 📌 **吃瓜/图集类站点章节地图：**
>
> | 章 | 焦点 |
> |---|---|
> | **46** | WordPress 型完整骨架（本章，最全） |
> | **47** | Elementor 主题型 |
> | **50** | 图集站（pics:// 协议为主） |
> | **53** | 吃瓜源/聚合源**隐性陷阱** |
> | **58** | 吃瓜站清洗 + 远程调用替换对照表 |
> | **59** | 高级适配（域名自愈双保障、AES 图片解密、标签云） |
> | **65** | 吃瓜站 API 数据提取标准流程 |
> | **66** | 吃瓜站广告过滤词池 + 过滤函数 |
>
> 开发新规则建议顺序：46 骨架 → 50 图集 → 65 API → 66 过滤 → 59 高级 → 53 陷阱清单。

---

## 46.1 整体架构模式

```
parse 对象
├── host()         → 域名自愈函数（读缓存 → fetchCodeByWebView 发现 → 写缓存）
├── 主页()         → 分类渲染 + 列表加载，图片 AES 解密
├── list           → $.toString 字符串化的列表解析逻辑（改为函数后见下）
├── listTags       → 标签云渲染
├── listArchives   → 归档列表渲染
├── jiexi          → 详情页解析（视频 + 图文 + 评论）
└── 搜索()         → 搜索功能
```

---

## 46.2 域名发现与缓存（WordPress 型）

WordPress 型站点通常在跳转页 `#list-wrap&&.btnLink` 里列出所有备用域名：

```javascript
host: function(pathHost) {
    // Step1：读取持久化缓存
    var cached = juItem.get('site_host', '');
    if (cached) return cached;
    
    // Step2：从跳转页获取域名列表
    var landingHtml = fetchCodeByWebView('https://备用跳转页.com/', { timeout: 8000 });
    var host = '';
    if (landingHtml) {
        var urls = pdfa(landingHtml, '#list-wrap&&.btnLink').map(function(item) {
            return pdfh(item, 'Text');
        });
        // Step3：batchFetch 并发测速（替代 getFastestDomain）
        var tasks = urls.map(function(u) {
            return { url: 'https://' + u, options: { timeout: 3000 } };
        });
        var results = batchFetch(tasks);
        for (var i = 0; i < results.length; i++) {
            if (results[i] && results[i].indexOf('post-card') > -1) {
                host = 'https://' + urls[i].replace(/\/$/, '');
                break;
            }
        }
    }
    if (!host) host = 'https://备用兜底域名.com';
    juItem.set('site_host', host);
    // 设置1小时缓存有效期
    juItem.set('site_host_time', Date.now() + '');
    return host;
},
```

> ⚠️ **域名缓存必须加时间戳校验**：`juItem.set` 存的域名不会自动过期，需手动比较时间戳（1小时 = 3600000ms），否则域名更换后仍用旧缓存。

---

## 46.3 列表解析——图片 AES 解密标准写法

WordPress 站点图片 URL 通常经过 AES 加密存在 `data-*` 属性中：

```javascript
// 实战中的两种图片 URL 提取方式
// 方式A：从 z-image-loader-url 或 data-bg 属性取
var bgUrl = pd(item, 'img&&z-image-loader-url', host) || pd(item, '.lazy-bg&&data-bg', host);

// 方式B：从 script 标签 JS 内容中提取（51暗网）
var bgUrl = pdfh(item, 'script&&Html').split("'")[1];

// AES 解密图片（替代壳子的 imgDec，见第十章）
var key = 'f5d965df75336270';
var iv  = '97b60394abc2fbe1';
var tlazy = $('').image(function(k, iv_s) {
    var CryptoUtil = $.require('hiker://assets/crypto-java.js');
    var dec = CryptoUtil.AES.decrypt(
        CryptoUtil.Data.parseInputStream(input),
        CryptoUtil.Data.parseUTF8(k),
        { mode: 'AES/CBC/PKCS7Padding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
    );
    return dec.toInputStream();
}, key, iv);

// 列表卡片
d.push({
    title: title,
    desc: desc,
    pic_url: bgUrl + tlazy,  // 直接拼接解密 lazyRule
    col_type: 'pic_1_card',
    // ✅ 不走二级/rule沙箱：lazyRule 写入目标 URL，刷新当前页渲染详情
    url: $('#noLoading#').lazyRule(function(hk, detailUrl) {
        putMyVar(hk + '_view', detailUrl);
        putMyVar(hk + '_view_page', '1');
        refreshPage(false);
        return 'hiker://empty';
    }, host.replace(/https?:\/\//, '').replace(/[\/:.]/g, '_'), url)
});
```

---

## 46.3.1 主页三态路由（配合 46.4 翻页方案）

主页函数读取 MyVar 状态，决定渲染哪种内容：

```javascript
主页: function() {
    var host = this._getHost();
    var hk   = host.replace(/https?:\/\//, '').replace(/[\/:.]/g, '_');
    var viewUrl = getMyVar(hk + '_view', '');

    // ── 详情模式（有 _view 值）───────────────────────────────
    if (viewUrl) {
        var curPage = parseInt(getMyVar(hk + '_view_page', '1')) || 1;
        var fetchUrl = curPage === 1 ? viewUrl
            : viewUrl.replace(/\/?$/, '') + '/' + curPage;
        var html = fetch(fetchUrl, { headers: { 'User-Agent': this.UA, 'Referer': host + '/' } });
        if (!html || html.length < 100) {
            return [{ title: '加载失败', col_type: 'text_center_1', url: 'hiker://empty' }];
        }
        // 返回按钮
        var d = [{ title: '← 返回列表', col_type: 'text_1',
            url: $('#noLoading#').lazyRule(function(hk) {
                clearMyVar(hk + '_view');
                clearMyVar(hk + '_view_page');
                refreshPage(false);
                return 'hiker://empty';
            }, hk), extra: { lineVisible: false }
        }];
        // 渲染详情（含翻页按钮，见 46.4）
        return d.concat(this._renderDetail(html, viewUrl, host, hk, this._key, this._iv));
    }

    // ── 列表模式（无 _view 值）───────────────────────────────
    var cls = (MY_URL && MY_URL !== 'fyclass') ? MY_URL : '/';
    var url = host + cls.replace(/\/?$/, '') + '/page/' + MY_PAGE + '/';
    var html = fetch(url, { headers: { 'User-Agent': this.UA } });
    if (!html || html.length < 100) return [];
    return this._parseList(html, host, this._key, this._iv);
},
```

---

## 46.4 详情页解析——视频 + 图文 + 评论混合（主页内渲染，不走二级）

WordPress 详情页不需要二级函数，由主页函数检测 MyVar 状态后直接渲染。这样翻页只需 `putMyVar` + `refreshPage`，不开新页面。

三类内容分别提取：

```javascript
_renderDetail: function(html, baseUrl, host, hk, key, iv) {
    // html    = 已请求好的页面内容
    // baseUrl = 文章第一页 URL（不含页码）
    // hk      = MyVar 键前缀（host 派生）
    var d = [];
    
    // ── 标题 ──────────────────────────────────────────
    d.push({
        title: pdfh(html, 'h1&&Text'),
        desc: pdfh(html, '.post-meta&&Text'),
        url: MY_URL, col_type: 'text_1',
        extra: { lineVisible: false }
    });
    
    // ── 视频提取（DPlayer 格式）────────────────────────
    // 方式A：直接从 .dplayer&&data-config 提取（无加密）
    var videoNodes = pdfa(html, 'body&&article&&.post-content>.dplayer');
    for (var vi = 0; vi < videoNodes.length; vi++) {
        try {
            var cfg = JSON.parse(pdfh(videoNodes[vi], '.dplayer&&data-config'));
            var vUrl = cfg.video ? cfg.video.url : cfg.url;
            if (vUrl) {
                d.push({
                    title: '点击播放视频',
                    url: vUrl,
                    col_type: 'text_center_1',
                    extra: { lineVisible: false }
                });
            }
        } catch(e) { log('视频解析失败: ' + e); }
    }
    
    // 方式B：动态 API 解密（1024吃瓜模式，见第四十四章）
    // var apiCfg = JSON.parse(pdfh(html, '.dplayer&&data-config').replace(/&quot;/g, '"'));
    // 从 apiCfg['data-api'] + apiCfg['data-key'] + apiCfg['data-iv'] 解密
    
    // ── 图文内容 ──────────────────────────────────────
    // 过滤掉无关内容（广告、备用地址等）
    var contentNodes = pdfa(
        pdfh(html, 'body&&Html').replace(/更多热门大瓜[\s\S]*/, ''),
        'body&&article&&.post-content>*'
    ).filter(function(x) {
        return /^<(p|hr|h2|h3|blockquote)>|dplayer/.test(x) && !/备用地址/.test(x);
    });
    
    for (var ci = 0; ci < contentNodes.length; ci++) {
        var it = contentNodes[ci];
        
        if (/dplayer/.test(it)) {
            // 跳过，视频已在上方单独处理
            continue;
        }
        
        if (it.indexOf('<hr>') > -1) {
            d.push({ col_type: 'line' });
            continue;
        }
        
        // 图片节点
        var imgNodes = pdfa(it, 'body&&img');
        var tlazy = $('').image(function(k, iv_s) {
            var CryptoUtil = $.require('hiker://assets/crypto-java.js');
            var dec = CryptoUtil.AES.decrypt(
                CryptoUtil.Data.parseInputStream(input),
                CryptoUtil.Data.parseUTF8(k),
                { mode: 'AES/CBC/PKCS7Padding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
            );
            return dec.toInputStream();
        }, key, iv);
        
        for (var ii = 0; ii < imgNodes.length; ii++) {
            var imgSrc = pdfh(imgNodes[ii], 'img&&data-src') || pdfh(imgNodes[ii], 'img&&src');
            if (imgSrc) {
                d.push({
                    title: '',
                    pic_url: imgSrc + tlazy,
                    url: 'pics://' + imgSrc,
                    col_type: 'pic_1_full'
                });
            }
        }
        
        // 站内链接跳转
        var linkHref = pdfh(it, 'a&&href');
        if (linkHref && /\d+$/.test(linkHref)) {
            d.push({
                title: pdfh(it, 'a&&Text'),
                url: $(linkHref.replace(/https?:\/\/.*?\//, host + '/')).rule(parse._jiexi, host, key, iv),
                col_type: 'text_1'
            });
            continue;
        }
        
        // 富文本段落
        var textContent = pdfh(it, 'body&&Text');
        if (textContent && textContent.trim().length > 3) {
            var cleaned = it
                .replace(/<\/?(br|p)>/g, '\n')
                .replace(/<h2>/g, '<big>')
                .replace(/<img[\s\S]*?>/g, '')
                .replace(/^[\n]+|[\n]+$/g, '');
            d.push({
                title: cleaned,
                col_type: 'rich_text'
            });
        }
    }
    
    // ── 页码按钮（顶部 + 底部）─────────────────────────
    // 提取总页数
    var pages = 1;
    var pageHtml = pdfh(html, 'body&&.page-links&&Html') || '';
    if (pageHtml) {
        var nums = [], re = /<a[^>]*>(\d+)<\/a>/g, pm;
        while ((pm = re.exec(pageHtml)) !== null) nums.push(parseInt(pm[1]));
        if (nums.length > 0) pages = Math.max.apply(null, nums);
    }
    var curPage = parseInt(getMyVar(hk + '_view_page', '1')) || 1;

    if (pages > 1) {
        d.push({ col_type: 'line' });
        for (var p = 1; p <= pages; p++) {
            var pUrl = p === 1 ? baseUrl : baseUrl.replace(/\/?$/, '') + '/' + p;
            d.push({
                title: p === curPage ? '❆ ' + p + ' ❆' : '' + p,
                col_type: 'scroll_button',
                // ✅ 翻页：更新 MyVar，刷新当前页，不开新页面
                url: $('#noLoading#').lazyRule(function(hk, pUrl, pNum) {
                    putMyVar(hk + '_view',      pUrl);
                    putMyVar(hk + '_view_page', pNum + '');
                    refreshPage(false);
                    return 'hiker://empty';
                }, hk, pUrl, p)
            });
        }
        d.push({ col_type: 'blank_block' });
    }

    // ── 评论区 ──────────────────────────────────────
    parse._renderComments(html, d, host);

    // ✅ 不调用 setResult：由主页函数的 return d 统一输出
    return d;
},
```

---

## 46.5 评论区渲染——标准模板

WordPress 博客评论区结构固定，包含父评论和子回复两层：

```javascript
_renderComments: function(html, d, host) {
    var list = pdfa(html, 'body&&.comment-separator+ol>li');
    
    d.push({
        title: '已有 ' + list.length + ' 条评论',
        url: 'hiker://empty',
        col_type: 'text_icon'
    });
    
    for (var i = 0; i < list.length; i++) {
        var item = list[i];
        var isNested = /comment-children/.test(item);
        
        // 父评论
        d.push({
            title: pdfh(item, '.comment-author&&Text'),
            desc:  pdfh(item, '.comment-meta&&Text'),
            url:   'hiker://empty',
            col_type: 'avatar'
        });
        d.push({
            title: pdfh(item, '.comment-content&&Text'),
            url:   'hiker://empty',
            col_type: 'text_1',
            extra: { lineVisible: false }
        });
        
        // 子回复（嵌套评论）
        if (isNested) {
            var replies = pdfa(item, 'body&&.comment-list>li');
            for (var ri = 0; ri < replies.length; ri++) {
                d.push({
                    title: '　　' + pdfh(replies[ri], '.comment-reply-author&&Text'),
                    desc:  pdfh(item, '.comment-meta&&Text'),
                    url:   'hiker://empty',
                    col_type: 'avatar'
                });
                d.push({
                    title: '　　' + (
                        pdfh(replies[ri], '.comment-reply-author&&Text') + 
                        '　' + pdfh(replies[ri], 'p--span&&Text')
                    ),
                    url:   'hiker://empty',
                    col_type: 'text_1'
                });
            }
            d.push({ col_type: 'line' });
        }
    }
},
```

---

## 46.6 标签云渲染——flex_button 彩色标签

WordPress 标签页通常在 `#archives-tags` 容器中，用 `flex_button` 渲染彩色标签云：

```javascript
_renderTagCloud: function(html, d, host) {
    var tags = pdfa(html, '#archives-tags&&.itags');
    for (var i = 0; i < tags.length; i++) {
        var tagName = pdfh(tags[i], 'a&&Text');
        var tagHref = pd(tags[i], 'a&&href');
        d.push({
            title: tagName,
            col_type: 'flex_button',
            // 标签点击 → 新开独立列表页（带翻页）
            url: $('hiker://empty##' + tagHref + '##fypage').rule(function(host, key, iv) {
                var d = [];
                setPageTitle(getPageTitle().split(' ')[0] + ' - ' + MY_PAGE);
                var tagUrl = MY_URL.split('##')[1] + MY_PAGE + '/';
                var html = fetch(tagUrl, { headers: { 'User-Agent': 'pc' } });
                // 解析列表...
                setResult(d);
            }, host, key, iv),
            extra: { backgroundColor: getRangeColors() }
        });
    }
    d.push({ col_type: 'line' });
},
```

---

## 46.7 归档列表渲染——archive 时间线

```javascript
_renderArchives: function(html, d, host, key, iv) {
    // 归档标题（如"2024年12月"）
    d.push({
        title: pdfh(html, '.archive-title&&h2&&Text'),
        col_type: 'rich_text',
        url: 'hiker://empty'
    });
    
    var items = pdfa(html, '.archive-title&&.brick');
    for (var i = 0; i < items.length; i++) {
        var it = items[i];
        d.push({
            title: pdfh(it, '.time&&Text') + '  ' + pdfh(it, 'a--span&&Text'),
            col_type: 'text_1',
            url: $(pd(it, 'a&&href')).rule(parse._jiexi, host, key, iv)
        });
    }
},
```

---

## 46.8 完整规则骨架（WordPress 型，无远程调用）

```javascript
var parse = {
    作者: 'dev',
    版本: '20260408.V1',
    
    // ── 配置区 ──────────────────────────────────────────
    _defaultHost: 'https://备用域名.com',
    _key: 'f5d965df75336270',
    _iv:  '97b60394abc2fbe1',
    UA: 'Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Mobile Safari/537.36',
    
    页码: { '主页': 1 },
    
    静态分类: {
        type: '主页',
        url: 'fyclass',
        class_name: '首页&分类A&分类B&热门标签&往期内容',
        class_url: '&cata&catb&tags&archives'
    },
    
    // ── 域名自愈 ─────────────────────────────────────────
    _getHost: function() {
        var cached = juItem.get('wp_host', '');
        var cachedTime = parseInt(juItem.get('wp_host_time', '0'));
        if (cached && (Date.now() - cachedTime) < 3600000) return cached;
        
        // 从公告页发现域名列表
        try {
            var html = fetchCodeByWebView('https://公告页.com/', {
                timeout: 8000, javaScriptEnabled: true
            });
            if (html) {
                var urls = pdfa(html, '#list-wrap&&.btnLink').map(function(item) {
                    return pdfh(item, 'Text');
                });
                var tasks = urls.map(function(u) {
                    return { url: 'https://' + u, options: { timeout: 3000 } };
                });
                var results = batchFetch(tasks);
                for (var i = 0; i < results.length; i++) {
                    if (results[i] && results[i].indexOf('post-card') > -1) {
                        var host = 'https://' + urls[i].replace(/\/$/, '');
                        juItem.set('wp_host', host);
                        juItem.set('wp_host_time', Date.now() + '');
                        return host;
                    }
                }
            }
        } catch(e) { log('域名发现失败: ' + e); }
        
        juItem.set('wp_host', this._defaultHost);
        juItem.set('wp_host_time', Date.now() + '');
        return this._defaultHost;
    },
    
    // ── 主页（三态合一：列表 / 详情 / 归档）────────────────
    // MyVar hk+'_view' 有值 → 详情模式，无值 → 列表模式
    主页: function() {
        var d = [];
        var host = this._getHost();
        var key  = this._key;
        var iv   = this._iv;
        var ua   = this.UA;
        var cls  = MY_URL && MY_URL !== 'fyclass' ? MY_URL : '';
        
        // 构建请求 URL
        var url;
        if (!cls) {
            url = host + '/page/' + MY_PAGE + '/';
        } else if (cls === 'tags') {
            url = host + '/tags/' + MY_PAGE + '/';
        } else if (cls === 'archives') {
            url = host + '/archives/page/' + MY_PAGE + '/';
        } else {
            url = host + '/category/' + cls + '/' + MY_PAGE + '/';
        }
        
        var html = fetch(url, { headers: { 'User-Agent': ua } });
        if (!html || html.indexOf('post-card') === -1) {
            // 域名失效，清除缓存重试
            juItem.set('wp_host', '');
            toast('域名可能已更换，请重新刷新~');
            return d;
        }
        
        // 按分类分流
        if (cls === 'tags') {
            this._renderTagCloud(html, d, host);
        } else if (cls === 'archives') {
            this._renderArchives(html, d, host, key, iv);
        } else {
            d = d.concat(this._parseList(html, host, key, iv, ua));
        }
        
        return d;
    },
    
    // ── 列表解析 ─────────────────────────────────────────
    _parseList: function(html, host, key, iv, ua) {
        var d = [];
        var tlazy = $('').image(function(k, iv_s) {
            var CryptoUtil = $.require('hiker://assets/crypto-java.js');
            var dec = CryptoUtil.AES.decrypt(
                CryptoUtil.Data.parseInputStream(input),
                CryptoUtil.Data.parseUTF8(k),
                { mode: 'AES/CBC/PKCS7Padding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
            );
            return dec.toInputStream();
        }, key, iv);
        
        var items = pdfa(html, 'div[role="main"]&&article:has(h2)').filter(function(x) {
            var t = pdfh(x, 'h2&&Text');
            return t !== '' && !/更新通知|^高端约炮$/.test(t);
        });
        
        for (var i = 0; i < items.length; i++) {
            var it = items[i];
            // 图片 URL 提取（两种站点格式兼容）
            var bgUrl = pd(it, 'img&&z-image-loader-url', host) 
                     || pd(it, '.lazy-bg&&data-bg', host)
                     || pdfh(it, 'script&&Html').split("'")[1]
                     || '';
            var title = pdfh(it, 'h2&&Text');
            var url   = pd(it, 'a&&href', host);
            var desc  = pdfh(it, '.post-card-info&&Text');
            
            if (!url) continue;
            
            d.push({
                title: title,
                desc:  desc,
                pic_url: bgUrl ? bgUrl + tlazy : '',
                col_type: 'pic_1_card',
                // ✅ 不走 rule 沙箱，lazyRule 刷新当前页
                url: $('#noLoading#').lazyRule(function(hk, u) {
                    putMyVar(hk + '_view', u);
                    putMyVar(hk + '_view_page', '1');
                    refreshPage(false);
                    return 'hiker://empty';
                }, host.replace(/https?:\/\//, '').replace(/[\/:.]/g, '_'), url)
            });
        }
        return d;
    },
    
    // ── 详情页渲染（主页函数内调用，见 46.4）───────────────
    // 不再用 rule() 沙箱，由主页函数检测 MyVar 状态后直接调用
    _renderDetail: function(html, baseUrl, host, hk, key, iv) {
        // ... 见 46.4 完整实现，含顶部/底部页码按钮
    },
    
    // ── 评论区（见 46.5）────────────────────────────────
    _renderComments: function(html, d, host) { /* 见 46.5 */ },
    
    // ── 标签云（见 46.6）────────────────────────────────
    _renderTagCloud: function(html, d, host) { /* 见 46.6 */ },
    
    // ── 归档列表（见 46.7）──────────────────────────────
    _renderArchives: function(html, d, host, key, iv) { /* 见 46.7 */ },
    
    // ── 搜索 ─────────────────────────────────────────────
    搜索: function(name) {
        var d = [];
        var host = this._getHost();
        var html = fetch(host + '/search/' + encodeURIComponent(name) + '/' + page + '/');
        return this._parseList(html, host, this._key, this._iv, this.UA);
    }
};
```

---

## 46.9 本章新增铁律

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 88 | 禁止远程调用 | 禁止 `rc()`/`gfd()`/`fc()` 远程加载，所有逻辑内联 | 远程库 = 不可控，内联 = 稳定 |
| Law 89 | 域名缓存加时间戳 | `juItem.set` 缓存域名必须同时存时间戳，1小时后重新发现 | 无时间戳 = 域名换了还用旧缓存 |
| Law 90 | $.toString+eval禁用 | 禁止 `$.toString()` + `eval()` 模式，改为明确参数的函数 | eval 调试难，函数调用清晰 |
| Law 91 | 内容过滤必须显式 | 过滤广告/无关内容用具体正则（如 `/更多热门大瓜/`），不依赖壳子 | 正则 = 可维护，壳子函数 = 不可移植 |

---

## 46.10 吃瓜站规则标准模板（WordPress 型完整骨架）

吃瓜类站点的核心架构与普通 WordPress 站的区别：**不走二级函数**，卡片 `url` 直接挂 `_getDetailRule` 返回的沙箱规则，详情页图文混排在 `$().rule()` 沙箱内用 `setResult` 渲染。

**架构总览：**

```
列表页（主页/搜索）
    ↓ _parseList（公共方法，主页和搜索共用）
    ↓ 过滤广告：_adRegex 黑名单 + class 标记
卡片 url = _getDetailRule() 返回的 $().rule() 沙箱
    ↓ 沙箱内 setResult 渲染图文混排
    ├─ 标题（h1 / .post-title）
    ├─ 视频（.dplayer → video:// + scroll_button + extra.js 点击注入）
    ├─ 图片（$.image() AES 解密 → pic_url）
    └─ 文字（.post-content>* 遍历 → 过滤广告关键词）
```

**关键配置项：**

```javascript
let parse = {
    host:     'https://example.com',
    _imgKey:  '从站点JS提取的AES密钥',   // 搜索 key/secret 关键词
    _imgIv:   '从站点JS提取的AES IV',    // 搜索 iv/offset 关键词
    _adRegex: /广告词1|广告词2|推广/i,   // 广告黑名单正则
    // ...
}
```

**`_parseList` 公共列表解析方法：**

```javascript
_parseList: function(html) {
    var d    = [];
    var host = this.host;
    var self = this;

    var items = pdfa(html, 'body&&article');
    items.forEach(function(item) {
        var link  = pd(item, 'a&&href');
        var title = pdfh(item, '.post-title&&Text') || pdfh(item, 'h2&&Text');
        var img   = pdfh(item, 'img&&src');

        // ── 过滤1：链接必须含 /archives/（内容页标识）
        if (!link || link.indexOf('/archives/') === -1) return;

        // ── 过滤2：广告 class 标记
        if (pdfh(item, 'article&&class').indexOf('post-card-ads') !== -1) return;

        // ── 过滤3：标题匹配广告黑名单正则
        if (self._adRegex && self._adRegex.test(title)) return;

        d.push({
            title:    title,
            img:      img,
            col_type: 'movie_3',
            url:      self._getDetailRule(link)
        });
    });
    return d;
},
```

**`_getDetailRule` 详情页沙箱方法：**

```javascript
_getDetailRule: function(url) {
    var key  = this._imgKey;
    var iv   = this._imgIv;
    var host = this.host;
    var adRe = this._adRegex;

    return $(url).rule(function(key, iv, host, adRe) {
        var d    = [];
        var html = fetch(input, { headers: { Referer: host + '/' } });

        // ── 1. 标题 ─────────────────────────────────────────
        var title = pdfh(html, 'h1&&Text') || pdfh(html, '.post-title&&Text');
        if (title) d.push({ title: title, col_type: 'text_center_1' });

        // ── 2. 视频（.dplayer 播放器）───────────────────────
        var dplayers = pdfa(html, 'body&&.dplayer');
        dplayers.forEach(function(dp, idx) {
            var vidUrl = pdfh(dp, '.dplayer&&data-src') || pdfh(dp, 'video&&src');
            if (!vidUrl) return;
            d.push({
                title:    '▶ 播放视频 ' + (idx + 1),
                col_type: 'scroll_button',
                url:      'video://' + vidUrl,
                extra: {
                    // JS 注入：定时查找播放按钮并点击
                    js: $.toString(function(n) {
                        var timer = setInterval(function() {
                            var btns = document.querySelectorAll('.dplayer-play-icon');
                            if (btns && btns.length > n) {
                                btns[n].click();
                                clearInterval(timer);
                            }
                        }, 1500);
                        setTimeout(function() { clearInterval(timer); }, 15000);
                    }, idx)
                }
            });
        });

        // ── 3. 图文正文遍历 ──────────────────────────────────
        var contentNodes = pdfa(html, '.post-content&&*');
        contentNodes.forEach(function(node) {
            var cls = pdfh(node, 'self&&class');

            // 跳过广告 class
            if (/article-ads-btn|content-tabs|tags/.test(cls)) return;

            var tag = pdfh(node, 'self&&tag') || '';

            if (tag === 'img' || pdfh(node, 'img&&src')) {
                // 图片：用 $.image() AES 解密
                var src = pdfh(node, 'img&&src');
                if (!src) return;
                d.push({
                    col_type: 'pic_1_full',
                    pic_url:  $('').image(function(k, ivs) {
                        var CryptoUtil = $.require('hiker://assets/crypto-java.js');
                        var dec = CryptoUtil.AES.decrypt(
                            CryptoUtil.Data.parseInputStream(input),
                            CryptoUtil.Data.parseUTF8(k),
                            { mode: 'AES/CBC/PKCS7Padding', iv: CryptoUtil.Data.parseUTF8(ivs) }
                        );
                        return dec.toInputStream();
                    }, key, iv) + src  // src 作为 $.image() 的触发 URL
                });
            } else {
                // 文字：过滤广告关键词
                var text = pdfh(node, 'Text').trim();
                if (!text) return;
                if (adRe && adRe.test(text)) return;
                d.push({ title: text, col_type: 'text_1' });
            }
        });

        setResult(d);
    }, key, iv, host, adRe);
},
```

**广告过滤最佳实践：**

| 过滤层 | 位置 | 方式 |
|---|---|---|
| 列表级 | `_parseList` | 链接路径校验（`/archives/`）+ class 标记 + `_adRegex` 标题匹配 |
| 节点级 | `_getDetailRule` 正文遍历 | 跳过广告 class（`article-ads-btn` 等）|
| 关键词级 | `_getDetailRule` 文字节点 | `_adRegex` 正则匹配文本内容 |

> 📌 `_adRegex` 应从实际站点广告文本提炼，每个站不同。通用起点：`/更多热门|点击查看|广告|推广|关注我们/i`。发现漏网广告随时往正则追加关键词。

**视频 extra.js 点击注入说明：**

```javascript
// setInterval 轮询是因为 WebView 渲染有延迟
// 找到按钮后立即 clearInterval 避免重复点击
// setTimeout 15秒兜底清理，防止定时器泄漏
// idx 参数控制多个播放器时点击正确的那个
```

> ⚠️ `extra.js` 里的选择器（`.dplayer-play-icon`）需按实际站点播放器 DOM 结构调整，DPlayer / ArtPlayer / video.js 各有不同的按钮类名。

---

## 46.11 柠檬皮站规则实战踩坑记录

本节基于 emonl.com（柠檬皮，图片/视频混合站）规则开发实战提炼，10条踩坑完整覆盖了混合内容站的典型问题。

**踩坑速查表：**

| # | 问题现象 | 根本原因 | 解决方案 |
|---|---|---|---|
| 1 | 聚阅空白页 | 未带 UA，服务器 403 | 所有 `request`/`fetch` 强制带 `User-Agent` |
| 2 | `pdfa("article")` 只返回1条 | 旧版聚阅 JSoup 嵌套解析 bug | 改用 `split('</article>')` + 正则提取（见 3.2.1 节）|
| 3 | 静态分类切换无效 | `class_url` 写真实路径，`&` 被引擎解析出错 | `class_url` 写短数字标识，`_getPath()` 映射真实路径（见 56.1 Law 120 补充）|
| 4 | `lazyRule` 内 `parse` 未定义 | 沙箱隔离，外部变量不可见 | 所有逻辑内联到回调里，或通过参数显式传入 |
| 5 | `$().rule()` 内 `input` 未定义 | `rule()` 用 `MY_URL`，`lazyRule()` 才用 `input` | `rule()` 回调里改用 `MY_URL`（见 32.6 节）|
| 6 | 精选/下载分类走了图片解析 | 列表阶段未区分内容类型 | 按 `_path` 特征分流：`/japan/`→视频，`/downloads/`→下载，`/tag/`→标签页 |
| 7 | 混合内容页只有图片没视频 | 视频被 Premium 付费墙遮住，未登录不返回 `iframe` | 改用 `video://详情页URL#嗅探` 让引擎自动嗅探 |
| 8 | 标签页封面空白 | 子标签列表没提取封面图 | 在 `rule()` 回调里加 `_imgs` 正则提取封面 |
| 9 | 标签页 `ua` 未定义 | `lazyRule` 回调参数漏传 | 所有 `lazyRule` 回调显式传 `ua` 参数 |
| 10 | 图片 403 空白 | CDN 防盗链校验 `Referer` | 所有图片 URL 拼接 `@Referer=https://www.emonl.com/` |

**架构设计（柠檬皮多类型分流模式）：**

```
主页函数
├─ _getPath()        → parseInt(MY_URL) 映射真实路径
├─ fetch 列表页 HTML（强制带 UA）
└─ 分流判断（按路径特征）
    ├─ /tag/（人气女神/柠檬图库/写真集）
    │   └─ _parseTagPage → 渲染标签云
    │       └─ 标签卡片 url → $().rule() 沙箱 → 子标签列表
    │           └─ 子标签 url → lazyRule → pics://（拼 @Referer=）
    ├─ /japan/ 或 /downloads/（视频+图片混合内容）
    │   └─ 列表卡片 url → $().rule() 沙箱（用 MY_URL）
    │       └─ setResult 渲染：标题 + 视频嗅探按钮 + 图片瀑布流
    └─ 普通图集（/other/）
        └─ 列表卡片 url → lazyRule（用 input）→ pics://
```

**柠檬皮七条铁律（实战提炼）：**

```javascript
// 1. UA 必须带——所有请求
var html = fetch(url, { headers: { 'User-Agent': MOBILE_UA } });

// 2. rule() 用 MY_URL，lazyRule() 用 input（见 32.6 节）

// 3. 混合内容必须走 rule() + setResult（见 32.6 节）

// 4. 纯图集走 lazyRule，返回 pics://
url: $(link).lazyRule(function(ua, referer) {
    var html = fetch(input, { headers: { 'User-Agent': ua } });
    var imgs = /* 提取图片 */;
    return 'pics://' + imgs.map(function(i) {
        return i + '@Referer=' + referer;  // 5. 图片必须带 Referer
    }).join('&&');
}, MOBILE_UA, 'https://www.emonl.com/')

// 6. var 代替 let/const（旧版聚阅不支持 ES6 声明）
var title = pdfh(html, 'h1&&Text');  // ✅
// let title = ...;  // ❌ 旧版聚阅会报错

// 7. 所有沙箱参数显式传（沙箱内外完全隔离）
$(link).lazyRule(function(ua, host, key, iv) {
    // 所有需要用到的外部变量都从参数取
}, this.UA, this.host, this._imgKey, this._imgIv)
```

---适用于所有使用 `window.$avdt` 嵌入播放信息、CDN 签名 m3u8 的视频站。


# 第四十七章：WordPress Elementor 图集站规则开发（海角爱 型）

本章基于 haijiaolove.xyz（WordPress + Elementor 页面生成器）规则提炼，总结此类用 Elementor 构建的图集展示站的开发模式。

**站点特征**：Elementor widget 布局、多层嵌套 section、图集 + 视频混合、谷歌搜索引流。

---

## 47.1 Elementor 站点 HTML 结构特点

Elementor 生成的 HTML 有固定的嵌套模式：

```html
<article>
  <section>  <!-- 第一个 section 通常是导航/TOC -->
    <div class="uael-toc-heading">...</div>
    <ul class="uael-toc-list">...</ul>
  </section>
  <section>  <!-- 后续 section 是内容区块 -->
    <div class="elementor-widget-container">
      <h3>...</h3>
      <div class="e-gallery-image" data-thumbnail="..."></div>
      <iframe src="..."></iframe>
    </div>
  </section>
</article>
```

---

## 47.2 图集列表页解析

```javascript
主页: function() {
    var d = [];
    var host = this.host;
    var ua   = this.UA;
    var html = fetch(host, { headers: { 'User-Agent': ua } });
    
    // Elementor 图集列表
    var items = pdfa(html, 'body&&.e-gallery-item');
    for (var i = 0; i < items.length; i++) {
        var item = items[i];
        var thumb = pd(item, 'body&&.e-gallery-image&&data-thumbnail');
        // 分类 URL 藏在 elementor-gallery-item__description 的 Text 里
        var categoryUrl = pdfh(item, 'body&&.elementor-gallery-item__description&&Text');
        
        d.push({
            title: '',
            desc:  '0',
            pic_url: thumb,
            col_type: 'card_pic_2',
            // 点击进入该分类的文章列表（带翻页）
            url: $('hiker://empty##fypage').rule(function(catUrl, host, ua) {
                var d = [];
                var pageUrl = catUrl + '?_page=' + MY_PAGE;
                var html = fetch(pageUrl, { headers: { 'User-Agent': ua } });
                
                // 文章卡片
                var cards = pdfa(html, 'body&&.pt-cv-content-item');
                for (var j = 0; j < cards.length; j++) {
                    var card = cards[j];
                    d.push({
                        title: pdfh(card, 'body&&h4&&Text'),
                        desc:  pdfh(card, 'body&&.pt-cv-ctf-value&&Text'),
                        pic_url: pd(card, 'body&&.pt-cv-thumb-wrapper&&img&&data-cvpsrc'),
                        col_type: 'movie_2',
                        url: $('hiker://empty##' + pd(card, 'body&&a&&href')).rule(function() {
                            // 进入 Elementor 详情页
                            var d = [];
                            var pageUrl = MY_URL.split('##')[1];
                            var html = fetch(pageUrl, { headers: { 'User-Agent': 'pc' } });
                            parse._parseElementorDetail(html, d);
                            setResult(d);
                        })
                    });
                }
                setResult(d);
            }, categoryUrl, host, ua)
        });
    }
    return d;
},
```

---

## 47.3 Elementor 详情页解析

```javascript
_parseElementorDetail: function(html, d) {
    var sections = pdfa(html, 'article&&section');
    for (var si = 0; si < sections.length; si++) {
        var section = sections[si];
        
        // 第一个 section：TOC 导航
        if (si === 0) {
            d.push({
                title: pdfh(section, '.uael-toc-heading&&Text'),
                col_type: 'rich_text'
            });
            var tocItems = pdfa(section, '.uael-toc-list&&li');
            for (var ti = 0; ti < tocItems.length; ti++) {
                d.push({
                    title: pdfh(tocItems[ti], 'a&&Text'),
                    url: 'hiker://empty',
                    col_type: 'text_icon'
                });
            }
            continue;
        }
        
        // 后续 section：内容区块
        var widgets = pdfa(section, 'body&&.elementor-widget-container');
        for (var wi = 0; wi < widgets.length; wi++) {
            var widget = widgets[wi];
            
            // h3 标题
            var h3s = pdfa(widget, 'body&&h3');
            for (var hi = 0; hi < h3s.length; hi++) {
                d.push({ title: pdfh(h3s[hi], 'body&&Text'), col_type: 'rich_text' });
            }
            
            // h4 小标题
            var h4s = pdfa(widget, 'body&&h4');
            for (var h4i = 0; h4i < h4s.length; h4i++) {
                d.push({ title: pdfh(h4s[h4i], 'body&&Text'), col_type: 'rich_text' });
            }
            
            // 图片（e-gallery）
            var galleryImgs = pdfa(widget, 'body&&.e-gallery-image');
            for (var gi = 0; gi < galleryImgs.length; gi++) {
                d.push({
                    pic_url: pd(galleryImgs[gi], '.e-gallery-image&&data-thumbnail'),
                    col_type: 'pic_2_card'
                });
            }
            
            // 视频 iframe
            var iframes = pdfa(widget, 'body&&iframe');
            if (iframes.length > 0) {
                d.push({ col_type: 'blank_block' });
                for (var ii = 0; ii < iframes.length; ii++) {
                    var iframeSrc = pdfh(iframes[ii], 'iframe&&src');
                    d.push({
                        title: '点击播放视频',
                        url: $(iframeSrc).lazyRule(function() {
                            return 'video://' + input;
                        }),
                        col_type: 'text_2'
                    });
                }
                d.push({ col_type: 'blank_block' });
            }
        }
    }
},
```

---

## 47.4 谷歌搜索引流——搜索函数

部分无自带搜索的站点借用 Google `site:` 搜索：

```javascript
搜索: function(name) {
    var d = [];
    // ⚠️ 谷歌对爬取有限制，频繁调用会被封，仅作参考
    var url = 'https://www.google.com/search?q=' + encodeURIComponent(name) 
            + '+site:' + 'example.com' 
            + '&start=' + (page - 1) * 10;
    var html = fetchPC(url);  // 必须用 PC UA
    
    if (/errors|captcha/.test(html)) {
        toast('搜索被限制，稍后再试！');
        return d;
    }
    
    var items = pdfa(html, 'body&&.MjjYud');
    for (var i = 0; i < items.length; i++) {
        var it = items[i];
        var link = pdfh(it, 'a&&href');
        if (!link) continue;
        d.push({
            title:   pdfh(it, 'h3&&Text'),
            desc:    pdfh(it, 'div[style="-webkit-line-clamp:2"]&&Text'),
            col_type: 'text_1',
            url: $(link).lazyRule(function() { return input; })
        });
    }
    return d;
},
```

> ⚠️ **谷歌搜索限制说明**：Google 会对频繁的非浏览器请求返回 CAPTCHA 页面（特征：`/errors/` 路径或含 `captcha` 字样），必须检测并提示用户。此方案不稳定，优先考虑站点自带搜索。

---

## 46.12 咔咔西三（Drupal 7 + Tuzac）实战踩坑记录

站点特征：Drupal 7 + Tuzac 发行版，图片明文直链无加密无防盗链，列表结构统一（`article.node-teaser`），全站排除 `.node-teaser-ad` 广告卡片。

**踩坑速查表：**

| # | 问题现象 | 根本原因 | 解决方案 |
|---|---|---|---|
| 1 | 全部分类页子标签点进去空白 | `$().rule()` URL 里没写 `?page=fypage`，`MY_PAGE` 未注入 | 改成 `$('hiker://empty?page=fypage##...')` （见 32.7 节）|
| 2 | 子标签沙箱请求 URL 双域名 | `pd()` 第三参数已补全绝对路径，沙箱内又拼了一次 `host` | 直接用 `pd()` 返回的绝对 URL，沙箱内不再拼 `host`（见 32.8 节）|
| 3 | 全部分类页状态机清空了子 URL | 主页函数每次执行时 `_path` 不含 `/all-tags` 就清 `_subUrl` | 全部分类页独立处理分支，不走通用状态机 |
| 4 | 子标签列表沙箱内选择器报错 | `_host` 变量在沙箱内未定义 | 沙箱内硬编码站点域名，或通过参数显式传入 |
| 5 | 搜索跳转未知链接 | `lazyRule` 回调漏传 `ua` 参数，`_ua` 未定义 | `$(_link).lazyRule(function(h, ua){...}, _host, _ua)` |
| 6 | 分类页中文路径被 `parseInt` 转 NaN | `class_url` 写了完整路径如 `/tags/AI美女`，引擎按索引注入 | `class_url` 写短数字标识 + `_getPath()` 映射，或确保路径以 `/` 开头让引擎原样注入 |

**架构设计（标签云分流模式）：**

```
主页函数
├─ _getPath() → 按 MY_URL 映射真实路径
├─ fetch 列表页 HTML
└─ 分流判断
    ├─ _path 含 /all-tags → 全部分类页
    │   └─ _parseTagCloud(html) → flex_button 标签云
    │       └─ 标签 url → $('hiker://empty?page=fypage##tagUrl').rule()
    │           └─ 沙箱内请求、解析、setResult（带翻页）
    └─ 其他路径 → 普通列表页
        └─ _parseList(html) → 卡片列表
            └─ 卡片 url → lazyRule → pics://
```

**Drupal 7 / Tuzac 站通用选择器速查：**

| 提取目标 | 选择器 | 备注 |
|---|---|---|
| 列表项 | `article.node-teaser` | 必须排除 `.node-teaser-ad` |
| 详情链接 | `a[itemprop='url']&&href` | Drupal 语义标记 |
| 标题 | `[itemprop='name']&&Text` | 降级：`img&&alt` |
| 缩略图 | `img&&data-src` | 降级：`img&&src`（懒加载站点优先 data-src）|
| 详情页大图 | 正则 `image.kkc3.com` | 明文直链，不需要解密 |
| 全部分类标签 | `.teaser-thumbnail&&a` | 标签云入口 |

**搜索翻页 URL 格式：**

```javascript
// Drupal 搜索翻页格式：路径里插入页码，不是 ?page=N 参数
搜索: function(keyword) {
    var d = this.d;
    // 第一页：/search?term=keyword
    // 第二页起：/search/1?term=keyword（注意从 1 开始，不是 2）
    var page    = MY_PAGE > 1 ? '/' + (MY_PAGE - 1) : '';
    var url     = this.host + '/search' + page + '?term=' + encodeURIComponent(keyword);
    var html    = fetch(url, { headers: { 'User-Agent': this.UA } });
    var items   = this._parseList(html);
    return items;
}
// Drupal 翻页规律：/search/0 = 第2页，/search/1 = 第3页，即 /search/(MY_PAGE-1)
```

> 📌 Drupal 搜索 URL 翻页规律与通常站点不同：第一页无路径参数，第二页起路径里的数字从 `0` 开始（即 `MY_PAGE - 1`）。这是 Drupal 的 Views 模块默认分页偏移量写法。

**全站无加密站点的简化处理：**

```javascript
// 无加密：图片直接拼接，不需要 $.image()，不需要 Referer
url: $(link).lazyRule(function(host, ua) {
    var html = fetch(input, { headers: { 'User-Agent': ua } });
    // 正则提取大图 URL（image.kkc3.com CDN）
    var imgs = [];
    var re   = /https?:\/\/image\.kkc3\.com\/[^"'\s]+/g;
    var m;
    while ((m = re.exec(html)) !== null) {
        if (imgs.indexOf(m[0]) === -1) imgs.push(m[0]);
    }
    if (!imgs.length) return 'toast://未找到图片';
    return 'pics://' + imgs.join('&&');  // 不加 @Referer=，直接用
}, this.host, this.UA)
```

---

本章基于 jiuse 系列站点（Vue 客户端渲染 + 部分 SSR）规则开发实战提炼。此类站点大量存在于成人视频领域，核心特征是 `window.$avdt` 嵌入播放配置 + CDN 签名 m3u8，直接 fetch 详情页即可取到播放地址，不需要 WebView。

---

## 47补.1 站点特征识别

| 特征 | 检测方法 |
|---|---|
| Vue 客户端渲染 + 部分 SSR | 首页源码中存在 `id="rd6"` / `id="rd7"` 区域（SSR 渲染），分类页可直接 fetch |
| `window.$avdt` 播放配置 | 详情页源码搜索 `$avdt` |
| CDN 签名 m3u8 | `$avdt.hls` 是带签名的相对路径，`$avdt.cdns` 是 CDN 域名数组 |
| 分类路径为 `/video/category/xxx` | 首页导航 href 正则扫描确认 |

---

## 47补.2 列表提取——两种方式

**方式一：首页 SSR 区域（rd6/rd7）**

首页的 `#rd6` / `#rd7` 区块是服务端渲染，`fetch` 即可拿到完整 HTML，无需 WebView：

```javascript
var html    = fetch(url);
var rd6     = (html.match(/id="rd6">([\s\S]*?)<script/) || [])[1] || '';
var rd7     = (html.match(/id="rd7">([\s\S]*?)<script/) || [])[1] || '';
var content = rd6 + rd7;
// 按 article card 分割成单个卡片
var cards   = content.split(/<article class="[^"]*card/);
```

**方式二：分类页正则提取（通用）**

`/video/category/xxx` 分类页源码可直接正则扫描：

```javascript
var html  = fetch(host + '/video/category/' + cate);
var links = (html.match(/href="(\/video\/(?:view|viewhd)\/[^"]+)"/g) || [])
                .map(function(s) { return s.replace(/href="|"/g, ''); });
var alts  = (html.match(/alt="([^"]+)"/g) || [])
                .map(function(s) { return s.replace(/alt="|"/g, ''); });
var imgs  = (html.match(/src="(https:\/\/[^"]+\.webp)"/g) || [])
                .map(function(s) { return s.replace(/src="|"/g, ''); });
var durs  = (html.match(/<span class="[^"]*duration[^"]*">([^<]*)<\/span>/g) || [])
                .map(function(s) { return s.replace(/<[^>]+>/g, '').trim(); });

links.forEach(function(link, i) {
    d.push({
        title:    alts[i] || '',
        desc:     durs[i] || '',
        img:      imgs[i] || '',
        url:      host + link,
        col_type: 'movie_3'
    });
});
```

> 📌 `viewhd` 路径和普通 `view` 路径结构完全相同，`$avdt` 提取逻辑通用，不需要分开处理。

---

## 47补.3 播放地址提取（核心）

`window.$avdt` 在详情页 HTML 源码中，直接 fetch 即可，无需 WebView：

```javascript
url: $(host + link).lazyRule(function(ua, host) {
    var html = fetch(input, {
        headers: { 'User-Agent': ua, 'Referer': host + '/' },
        timeout: 10000
    });
    if (!html || html.length < 500) return 'toast://页面加载失败';

    // ── Step1：提取 $avdt 对象 ─────────────────────────────
    var avdtMatch = html.match(/\$avdt\s*=\s*(\{[^;]+\})/);
    if (!avdtMatch) return 'toast://未找到视频配置';
    var avdtStr = avdtMatch[1];

    // ── Step2：提取 hls 路径（含转义斜杠，需还原）──────────
    var hlsMatch = avdtStr.match(/"hls"\s*:\s*"([^"]+)"/);
    if (!hlsMatch) return 'toast://未找到hls地址';
    var hlsPath = hlsMatch[1].replace(/\\\//g, '/');  // \/ → /

    // ── Step3：从 cdns 数组取第一个 CDN 域名 ───────────────
    var cdnsMatch = avdtStr.match(/"cdns"\s*:\s*\[([^\]]+)\]/);
    var m3u8;
    if (cdnsMatch) {
        var cdn = cdnsMatch[1].replace(/"/g, '').split(',')[0].trim();
        m3u8 = 'https://' + cdn + hlsPath;
    } else {
        m3u8 = host + hlsPath;  // 兜底：用站点主域名
    }

    // ── Step4：返回带 Referer 的 video:// + #嗅探 ──────────
    return 'video://' + m3u8
        + '#嗅探;{User-Agent@' + ua
        + '&&Referer@' + host + '/}';
}, ua, host)
```

---

## 47补.4 分类路径发现

当 `/t/标签名` 这类路径返回 404 时，需从首页导航提取实际分类路径：

```javascript
// 从首页 HTML 里扫描所有 /video/category/ 入口
var html  = fetch(host);
var cats  = [];
var regex = /href="(\/video\/category\/[^"]+)"/g;
var m;
while ((m = regex.exec(html)) !== null) {
    if (cats.indexOf(m[1]) === -1) cats.push(m[1]);
}
// cats 即为实际分类路径列表，填入静态分类 class_url
```

然后配置静态分类：

```javascript
静态分类: {
    type:       '主页',
    url:        'fyclass',
    class_name: '分类A&分类B&分类C',
    class_url:  '/video/category/aaa&/video/category/bbb&/video/category/ccc'
}
```

---

## 47补.5 关键注意事项

| 注意点 | 说明 |
|---|---|
| `$avdt` 直接 fetch 可取 | 不需要 WebView，详情页源码里就有 |
| `hls` 路径有转义斜杠 | `\/` 必须 `.replace(/\\\//g, '/')` 还原，否则 URL 拼接错误 |
| `cdns` 是 JSON 数组 | 取 `cdns[0]` 拼完整 URL，不能直接用 `hls` 原值（是相对路径） |
| 必须带 Referer | CDN 签名 URL 校验 Referer，不带会 403，格式：`Referer@host/` |
| `video://` + `#嗅探` 组合 | 不能只用 `#嗅探`，必须加 `video://` 前缀（Law 3）|
| 分类路径扫描优先 | 不要猜路径结构，从首页导航正则扫描出真实路径再填静态分类 |
| `html.length < 500` 检测 | 防止 CDN 节点返回空页/错误页时 `$avdt` 匹配失败进入误报 |

---

## 47补.6 关键修正：必须用 embed 页而非详情页

上面 47补.3 写的是从**详情页**取 `$avdt`，但实测部分 jiuse 型站点详情页是 CSR 渲染（JS 执行后才注入 `$avdt`），直接 `fetch` 拿不到。**正确做法是请求 embed 页**，embed 页是服务端渲染，`$avdt` 直接在源码里：

```javascript
// ── 从详情页 URL 取 vid ────────────────────────────────────
var vid      = link.split('/').pop();                        // 取路径最后一段
var embedUrl = 'https://jiuse.io/video/embed/' + vid;       // embed 页 URL

// ── 请求 embed 页（服务端渲染，$avdt 直接在源码里）─────────
var ehtml = fetch(embedUrl, {
    headers: { 'Referer': host + '/' },
    timeout: 10000
});
if (!ehtml || ehtml.length < 500) return 'toast://加载失败';

var avdtMatch = ehtml.match(/window\.\$avdt\s*=\s*(\{[^}]+\})/);
if (!avdtMatch) return 'toast://未找到视频配置';

var hlsPath = avdtMatch[1].match(/"hls"\s*:\s*"([^"]+)"/)[1].replace(/\\\//g, '/');
var cdn     = avdtMatch[1].match(/"cdns"\s*:\s*\["([^"]+)"/)[1];
var m3u8    = 'https://' + cdn + hlsPath;

// ── Referer 必须用 embedUrl，不是主站 host ─────────────────
return 'video://' + m3u8
    + ';{User-Agent@' + ua
    + '&&Referer@' + embedUrl + '}';
```

> ⚠️ `#嗅探` 在这里不需要加——m3u8 地址已经完整，直接用 `video://` 协议即可。Referer 必须填 **embed 页地址**而非主站根路径，否则 CDN token 校验失败 403。

## 47补.7 翻页规则

```javascript
// 第一页：/video/category/latest
// 第二页起：/video/category/latest/页码
var pageUrl = host + '/video/category/' + cate;
if (MY_PAGE > 1) pageUrl += '/' + MY_PAGE;
var html = fetch(pageUrl);
```

## 47补.8 列表卡片分割提取（防索引错位）

分别提取 links/alts/imgs 再按索引组合容易错位（因为不是每张卡片都有所有字段）。**正确做法是按 `<article>` 卡片分割，每张卡片独立提取**：

```javascript
var cards = html.split(/<article class="[^"]*card/);
cards.shift();  // 去掉分割前的无效头部

cards.forEach(function(card) {
    var link  = (card.match(/href="(\/video\/(?:view|viewhd)\/[^"]+)"/) || [])[1] || '';
    var title = (card.match(/(?:alt|title)="([^"]+)"/)                  || [])[1] || '';
    var img   = (card.match(/src="(https?:\/\/[^"]+\.(?:webp|jpg|png))"/) || [])[1] || '';
    var dur   = (card.match(/<span[^>]*duration[^>]*>([^<]+)</)          || [])[1] || '';
    if (!link) return;
    d.push({
        title:    title,
        desc:     dur.trim(),
        img:      img,
        url:      host + link,
        col_type: 'movie_3'
    });
});
```

## 47补.9 完整踩坑记录

| 坑 | 原因 | 解决方案 |
|---|---|---|
| 详情页找不到 `$avdt` | CSR 渲染，JS 执行后才注入 | 改用 embed 页（服务端渲染） |
| CDN 拼接后 404 | 详情页的 token 已过期 | embed 页获取最新 token |
| m3u8 返回 403 | Referer 填的是主站而非 embed 页 | Referer 改为 embed 页地址 |
| 卡片字段索引错位 | 分别提取 links/alts/imgs 再组合 | 按 `<article>` 分割，每卡独立提取 |
| 翻页第二页无内容 | URL 没加 `/页码` 后缀 | `MY_PAGE > 1` 时追加 `'/' + MY_PAGE` |
| live 直播流无法播放 | 缺 `#isLive=true#` 标记 | 判断 m3u8 地址是否含 `live`，是则加标记 |

---

# 第四十八章：综合工具函数内联库——完全脱离壳子的独立实现

本章将各章散落的工具函数整合为一份可直接内联到任何规则的"本地工具库"，彻底消除对 `rc()` 壳子的依赖。

---

## 48.1 完整内联工具库（复制到 parse 对象即可使用）

```javascript
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 聚阅/海阔视界 内联工具库 V1.0（无远程依赖）
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// ── 随机颜色（排除低亮度/低饱和度）────────────────────────
var _getRangeColors = function() {
    var h = Math.floor(Math.random() * 360),
        s = Math.floor(Math.random() * 50 + 50) / 100,
        l = Math.floor(Math.random() * 50 + 50) / 100,
        c = (1 - Math.abs(2 * l - 1)) * s,
        x = c * (1 - Math.abs((h / 60) % 2 - 1)),
        m = l - c/2,
        r1, g1, b1;
    if      (h < 60)  { r1=c; g1=x; b1=0; }
    else if (h < 120) { r1=x; g1=c; b1=0; }
    else if (h < 180) { r1=0; g1=c; b1=x; }
    else if (h < 240) { r1=0; g1=x; b1=c; }
    else if (h < 300) { r1=x; g1=0; b1=c; }
    else              { r1=c; g1=0; b1=x; }
    var r=Math.floor((r1+m)*255), g=Math.floor((g1+m)*255), b=Math.floor((b1+m)*255);
    return '#' + ((1<<24)+(r<<16)+(g<<8)+b).toString(16).slice(1);
};

// ── Base64 解码（双保险，Law 37）──────────────────────────
var _base64Decode = function(str) {
    if (!str) return '';
    str = str.replace(/-/g, '+').replace(/_/g, '/');
    while (str.length % 4) str += '=';
    try {
        var b = android.util.Base64.decode(str, android.util.Base64.DEFAULT);
        return new java.lang.String(b, 'UTF-8') + '';
    } catch(e) {
        try { return decodeURIComponent(escape(atob(str))); }
        catch(e2) { log('Base64 decode fail'); return ''; }
    }
};

// ── 自动脱壳（最多3层，Law 39）───────────────────────────
var _autoDeshell = function(html) {
    if (!html) return html;
    for (var i = 0; i < 3; i++) {
        if (html.indexOf('decodeURIComponent("') > -1) {
            var m = html.match(/decodeURIComponent\("([^"]+)"\)/);
            if (m) html = decodeURIComponent(m[1]);
        } else break;
    }
    return html;
};

// ── AES 图片解密 lazyRule 生成（Law 28，复用版）─────────
var _getAesImageLazy = function(key, iv) {
    return $('').image(function(k, iv_s) {
        try {
            var CryptoUtil = $.require('hiker://assets/crypto-java.js');
            var dec = CryptoUtil.AES.decrypt(
                CryptoUtil.Data.parseInputStream(input),
                CryptoUtil.Data.parseUTF8(k),
                { mode: 'AES/CBC/PKCS7Padding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
            );
            return dec.toInputStream();
        } catch(e) { log('AES img fail: ' + e); return input; }
    }, key, iv);
};

// ── URL 翻页构建（兼容 ? 和路径翻页）────────────────────
var _buildPageUrl = function(base, page) {
    if (page <= 1) return base;
    if (base.indexOf('?') > -1) return base + '&page=' + page;
    return base.replace(/\/?$/, '') + '/page/' + page + '/';
};

// ── 中文路径安全编码（Law 17）────────────────────────────
var _safeEncodeUrl = function(url) {
    // 只对非 ASCII 字符编码，保留 ://?=&#
    return url.replace(/[^\x00-\x7F]/g, function(c) {
        return encodeURIComponent(c);
    });
};

// ── 多域名竞速（替代 getFastestDomain）──────────────────
var _getFastestHost = function(candidates, keyword, timeout) {
    var tasks = candidates.map(function(u) {
        return { url: u, options: { timeout: timeout || 3000 } };
    });
    var results = batchFetch(tasks);
    for (var i = 0; i < results.length; i++) {
        if (results[i] && (keyword ? results[i].indexOf(keyword) > -1 : results[i].length > 100)) {
            return candidates[i];
        }
    }
    return candidates[0];  // 兜底返回第一个
};

// ── 滚动按钮选中样式（纯文本符号，Law 42）────────────────
var _selTitle = function(title, isSel) {
    return isSel ? '❆ ' + title + ' ❆' : title;
};

// ── Dean Edwards Packer 解包（第三十四章）────────────────
var _unpack = function(code) {
    var pm = code.match(/\('([\s\S]*?)',(\d+),(\d+),'([\s\S]*?)'\.split\('\|'\)/);
    if (!pm) return code;
    var p = pm[1], a = parseInt(pm[2]), c = parseInt(pm[3]), k = pm[4].split('|');
    function e(c) {
        return (c < a ? '' : e(parseInt(c/a))) +
               ((c = c%a) > 35 ? String.fromCharCode(c+29) : c.toString(36));
    }
    var dict = {};
    while (c--) { var key = e(c); dict[key] = k[c] || key; }
    return p.replace(/\b\w+\b/g, function(w) {
        return dict[w] !== undefined ? dict[w] : w;
    });
};
```

---

## 48.2 壳子 API → 手册标准 API 对照速查

| 壳子函数 | 说明 | 手册标准替代 |
|---|---|---|
| `rc(url, hours)` | 远程加载 JS 库 | 禁止，内联逻辑（Law 88）|
| `gfd()` | 获取 GitHub 代理 | 禁止使用 |
| `fc(url)` | 获取代理 JSON | 禁止使用 |
| `getHtml(url, ...)` | 带 CF 处理的 fetch | `fetch()` + 第十二章 CF 过盾 |
| `classTop(idx, item, host, d)` | 渲染分类按钮行 | `renderRow()`（第六章）|
| `cpage('')` | 解析分类和页码 | `MY_URL` 直接使用（Law 60）|
| `pageAdd(page, host)` | 记录翻页信息 | `putMyVar` 手动管理 |
| `pageMoveto(...)` | 生成位置记忆 extra | 不推荐，用标准翻页 |
| `getdTemp(...)` | 读取页面缓存 | `juItem.get` + `JSON.parse` |
| `writeFile(path, content)` | 写文件缓存 | `juItem.set` 持久化 |
| `getFastestDomain(urls)` | 多域名测速 | `batchFetch` + 特征检测（见 45.8）|
| `imgDec(key, iv, 'AES')` | AES 图片解密 lazyRule | `$('').image()` + CryptoUtil（第十章）|
| `safePath(url)` | URL 转文件名 | 不需要，改用 `juItem.set` key |
| `getRandomColor(type)` | 随机颜色 | `_getRangeColors()`（第三十七章）|
| `.color('fff')` | 字符串着色 | `.fontcolor('#fff')` |
| `.colorR(333)` | 随机着色 | `.fontcolor(randomColor())` |
| `.sbR(n)` | 加粗放大 | `'<big>' + str + '</big>'` |
| `strong(str, color)` | 加粗着色 | `str.bold().fontcolor('#' + color)` |
| `$.toString(() => {...})` | 函数字符串化 | 改为函数方法，显式传参 |
| `eval(parse.list)` | 执行字符串化代码 | 改为 `this._parseList(html, ...)` |

---

## 48.3 铁律总表更新（Law 88-91）

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 88 | 禁止远程调用 | 禁止 `rc()`/`gfd()`/`fc()`，所有逻辑必须内联 | 远程库 = 不可控，内联 = 稳定 |
| Law 89 | 域名缓存时间戳 | 域名 `juItem.set` 缓存必须同时记时间戳，超时重新发现 | 无时间戳 = 域名换了还用旧缓存 |
| Law 90 | 禁用 $.toString+eval | 禁止 `$.toString()`+`eval()` 代码字符串化，改为函数方法 | eval 调试难，函数调用清晰 |
| Law 91 | 内容过滤显式化 | 过滤广告/无关内容用具体正则，不依赖壳子内置过滤 | 正则可维护，壳子函数不可移植 |

---

*文档版本 V3.46 · 2026年4月 · 新增第四十五-四十八章（壳子工具函数参考、WordPress站型、Elementor站型、内联工具库）· 去除 rc()/gfd()/fc() 远程调用，统一内联实现 · 基于 007吃瓜/51暗网/海角爱实战规则提炼*

---

# 第四十九章：手册勘误与补充——基于实战验证的修正

本章记录手册中经实战验证发现的错误、不足和遗漏，作为对前述章节的正式修订。

---

## 49.1 严重错误修正

### 错误1：第十章 $.image() 的 input 参数说明错误

**手册原文描述（错误）**：`$.image()` 的 func 中，input 是图片 URL，需要先 fetch 获取加密数据。

**实际验证结论**：`$.image()` 回调中的 `input` 是引擎**已经下载好的 InputStream**，不是 URL 字符串，不需要也不能再手动 `fetch`。手动 fetch 会触发类型错误：`fetch argument 1 has type java.lang.String, got okio.RealBufferedSource`。

**错误写法**：
```javascript
// ❌ 错误：input 已经是 InputStream，不能当 URL 传给 fetch
var tlazy = $('').image(function(k, iv_s) {
    var raw = fetch(input, { headers: {...} });  // 报错！
    var CryptoUtil = $.require('hiker://assets/crypto-java.js');
    var dec = CryptoUtil.AES.decrypt(CryptoUtil.Data.parseInputStream(raw), ...);
});
```

**正确写法**：
```javascript
// ✅ 正确：input 直接就是 InputStream，可以直接传给 parseInputStream
var tlazy = $('').image(function(k, iv_s) {
    var CryptoUtil = $.require('hiker://assets/crypto-java.js');
    // input 直接可用，它是引擎下载图片后传入的 InputStream
    var textData  = CryptoUtil.Data.parseInputStream(input);
    var decrypted = CryptoUtil.AES.decrypt(textData,
        CryptoUtil.Data.parseUTF8(k),
        { mode: 'AES/CBC/NoPadding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
    );
    return decrypted.toInputStream();
}, key, iv);
```

> **⚠️ Law 92（新增）：`$.image()` 回调中的 `input` 是引擎下载完成后传入的 `InputStream`，不是 URL 字符串，禁止对 `input` 调用 `fetch()`。直接将 `input` 传给 `CryptoUtil.Data.parseInputStream()` 即可。**

---

### 错误2：padding 模式不能默认 PKCS7Padding

**手册原文描述（不完整）**：示例统一使用 `AES/CBC/PKCS7Padding`，给读者暗示这是通用选择。

**实际验证结论**：不同站点的 padding 模式不同。padding 模式写错会导致解密结果乱码或解密失败，且不会抛出明显错误，极难调试。必须从站点 JS 源码中确认。

| padding 模式 | 特征 | 常见于 |
|---|---|---|
| `PKCS7Padding` | 明文长度不是 16 的倍数时自动补齐 | 多数标准实现 |
| `NoPadding` | 明文长度必须是 16 的整数倍 | 部分自定义实现 |
| `PKCS5Padding` | 与 PKCS7 等价，Java 环境常见别名 | Java 后端 |

**如何从站点源码确认 padding 模式**：
```javascript
// 在站点 JS bundle 中搜索以下关键词：
// - "NoPadding"
// - "PKCS7"  / "PKCS7Padding"
// - "PKCS5"  / "PKCS5Padding"
// - CryptoJS.AES.decrypt(...)  → 查看第三个参数的 padding 字段

// 示例：站点源码含
// CryptoJS.AES.decrypt(data, key, { iv: iv, mode: CryptoJS.mode.CBC, padding: CryptoJS.pad.NoPadding })
// → 对应规则写法：mode: 'AES/CBC/NoPadding'
```

> **⚠️ Law 93（新增）：padding 模式必须从站点 JS 源码中确认，不能假设为 `PKCS7Padding`。常见站点使用 `NoPadding`，写错会导致解密失败且无明显报错。**

---

## 49.2 重要不足补充

### 补充1：$.image() input 参数的完整说明

`$.image()` 的执行流程如下，手册第十章未做完整说明：

```
1. 引擎遇到 pic_url 字段中拼接了 $.image() 生成的 lazyRule 字符串
2. 引擎自动下载该图片 URL 对应的字节流
3. 将下载结果封装为 InputStream，赋值给 input 变量
4. 执行 $.image() 的 func 回调，func 内可直接使用 input
5. func 必须返回 InputStream，引擎用于渲染图片
```

**关键约束总结**：
- `input` 类型：`InputStream`（`okio.RealBufferedSource`）
- `input` 来源：引擎自动下载，开发者无需手动 fetch
- `func` 返回值：必须是 `InputStream`（通过 `toInputStream()` 获得），返回 `null` 则图片不显示
- **`$.image()` 回调不能使用箭头函数**（Law 31.5.3），否则 `input` 为 `undefined`

**完整可用的图片防盗链 + AES 解密模板**：
```javascript
// ✅ 完整正确写法（无 AES 加密，仅防盗链）
var tlazy = $('').image(function(referer) {
    var FU = com.example.hikerview.utils.FileUtil;
    // 防盗链：用 FileUtil.readBytes 自定义 headers 下载
    // 注意：这里的 input 是已下载的 InputStream，
    // 如果需要自定义 headers，要用另一种方式（见下）
    return FU.toInputStream(FU.readBytes(
        // 此处不能传 input！input 已经是流了
        // 这种模式用于 pic_url 本身就是 URL 的情况
        // 需要在 pic_url 后拼 @Referer= 来处理防盗链
    ));
    return input;  // 无需解密时直接返回
}, referer);

// ✅ 图片 AES 解密（input 已是流）
var tlazy = $('').image(function(k, iv_s) {
    var CryptoUtil = $.require('hiker://assets/crypto-java.js');
    var dec = CryptoUtil.AES.decrypt(
        CryptoUtil.Data.parseInputStream(input),  // ✅ input 直接用
        CryptoUtil.Data.parseUTF8(k),
        { mode: 'AES/CBC/NoPadding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
    );
    return dec.toInputStream();
}, key, iv);
```

> 📌 **防盗链（无解密）直接用 `pic_url + '@Referer=xxx'` 或 `'@headers={...}'` 格式处理，不需要 `$.image()`。`$.image()` 只用于需要字节流变换（解密/XOR）的场景。**

---

### 补充2：eval 字符串代码模式——复用解密逻辑的正确姿势

当需要在 `lazyRule`/`rule` 回调内部复用解密代码时，由于 lazyRule 的作用域隔离，无法直接访问外部函数。正确做法是把代码存为字符串，内部用 `eval` 执行：

```javascript
var parse = {
    // 把 tlazy 生成代码存为字符串属性
    _imageLazyCode: 'var tlazy = $(\'\').image(function(k, iv_s) {\
        var CryptoUtil = $.require(\'hiker://assets/crypto-java.js\');\
        var dec = CryptoUtil.AES.decrypt(\
            CryptoUtil.Data.parseInputStream(input),\
            CryptoUtil.Data.parseUTF8(k),\
            { mode: \'AES/CBC/NoPadding\', iv: CryptoUtil.Data.parseUTF8(iv_s) }\
        );\
        return dec.toInputStream();\
    }, \'' + KEY + '\', \'' + IV + '\');',

    // 在需要 tlazy 的地方 eval 执行，得到 tlazy 变量
    _getPageRule: function() {
        return $('').lazyRule(function(lazyCode) {
            eval(lazyCode);  // 执行后 tlazy 变量可用
            var d = [];
            var pics = [];
            // ... 解析图片 URL ...
            for (var i = 0; i < pics.length; i++) {
                d.push({ pic_url: pics[i] + tlazy, col_type: 'pic_1_full' });
            }
            return 'pics://' + pics.map(function(p) { return p; }).join('&&');
        }, this._imageLazyCode);
    }
};
```

> ⚠️ **eval 字符串模式的注意事项**：字符串内的引号需要转义；字符串较长时可读性差；调试困难。**优先用参数传递 key/iv，在 lazyRule 内部直接定义解密逻辑**；只有当解密代码非常长且需要在多个 lazyRule 中复用时，才考虑此模式（Law 90 精神：优先函数方法，eval 作为最后手段）。

---

### 补充3：二级函数完整返回结构与 noShow 用法（补充第七章）

手册第七章给出了基础结构，以下补充完整字段说明和 `noShow + extenditems` 的实战模板。

**noShow 字段完整说明**：

```javascript
noShow: {
    封面: false,   // true = 隐藏封面图区域（默认显示）
    简介: true,    // true = 隐藏默认简介区域
    排序: true,    // true = 隐藏选集排序按钮
    选集: true     // true = 隐藏默认选集列表区域
}
// 注意：key 是中文字符串，不是英文
```

**extenditems 完全自定义二级页面模板**：

```javascript
二级: function(url) {
    var html  = fetch(url, { headers: { 'User-Agent': this.UA } });
    var host  = this.host;
    var extra = [];

    // ── 自定义内容区（图文混排示例）──────────────────────
    var nodes = pdfa(html, 'body&&.text-content&&*');
    for (var i = 0; i < nodes.length; i++) {
        var node = nodes[i];
        // 图片节点
        var imgUrl = '';
        try { imgUrl = pdfh(node, 'img&&z-image-loader-url') || pdfh(node, 'img&&src'); } catch(e) {}
        if (imgUrl) {
            imgUrl = imgUrl.replace(/`/g, '').trim();
            extra.push({
                title:    '',
                pic_url:  imgUrl + '@Referer=' + host + '/',
                url:      'pics://' + imgUrl,
                col_type: 'pic_1_full'
            });
            continue;
        }
        // 视频节点（DPlayer）
        var cfg = '';
        try { cfg = pdfh(node, 'self&&data-config'); } catch(e) {}
        if (cfg) {
            try {
                var cfgObj = JSON.parse(cfg.replace(/&quot;/g, '"'));
                var vUrl   = cfgObj.video && cfgObj.video.url ? cfgObj.video.url : '';
                if (vUrl) {
                    var sl = String.fromCharCode(92);
                    extra.push({
                        title:    '▶ 点击播放',
                        url:      'video://' + vUrl.split(sl).join('') + ';{Referer@' + host + '/}',
                        col_type: 'text_center_1'
                    });
                }
            } catch(e) {}
            continue;
        }
        // 文字节点
        var txt = '';
        try { txt = pdfh(node, 'Text'); } catch(e) {}
        if (txt && txt.trim().length > 3) {
            extra.push({ title: txt.trim(), col_type: 'rich_text' });
        }
    }

    return {
        vod_name:    pdfh(html, 'h1&&Text'),
        vod_pic:     '',
        // ⚠️ 使用 extenditems 时：
        // 1. list 必须给 [[]]（空二维数组），否则引擎报结构错误
        // 2. noShow 按需隐藏默认组件
        // 3. extenditems 放自定义内容，在选集区域下方渲染
        list:        [['']],
        noShow:      { 封面: true, 简介: true, 排序: true, 选集: true },
        extenditems: extra
    };
},
```

> **⚠️ Law 94（新增）：使用 `extenditems` 完全自定义二级页面时，`list` 字段必须给 `[['']]`（含一个空字符串的二维数组），否则引擎抛结构错误。`noShow` 按需隐藏不需要的默认组件，`extenditems` 中的内容在选集区域下方渲染。**

---

### 补充4：图文按 HTML 顺序穿插显示

手册只说了分别提取图片和文字，但实际上内容页通常图文穿插，需要按原始 HTML 中元素的出现顺序渲染，而不是先渲染所有图片再渲染所有文字。

**错误做法（先图后文，顺序错乱）**：
```javascript
// ❌ 先单独提取图片，再单独提取文字，顺序完全乱掉
var imgs  = pdfa(html, 'body&&.text-content&&img');
var paras = pdfa(html, 'body&&.text-content&&p');
for (var i = 0; i < imgs.length; i++) { d.push({...img...}); }
for (var j = 0; j < paras.length; j++) { d.push({...text...}); }
```

**正确做法（按 HTML 元素顺序遍历）**：
```javascript
// ✅ 遍历直接子节点，按出现顺序处理每个元素
var nodes = pdfa(html, 'body&&.text-content&&*');  // 取所有直接子节点
for (var i = 0; i < nodes.length; i++) {
    var node = nodes[i];
    // 先判断是否含图片
    var imgUrl = '';
    try { imgUrl = pdfh(node, 'img&&z-image-loader-url') || pdfh(node, 'img&&src'); } catch(e) {}
    if (imgUrl && imgUrl.indexOf('http') === 0) {
        d.push({ pic_url: imgUrl + '@Referer=' + host + '/', col_type: 'pic_1_full', url: 'pics://' + imgUrl });
        continue;
    }
    // 再判断是否有文字
    var txt = '';
    try { txt = pdfh(node, 'Text'); } catch(e) {}
    if (txt && txt.trim().length > 3) {
        d.push({ title: txt.trim(), col_type: 'rich_text' });
    }
}
```

> **⚠️ Law 95（新增）：详情页图文穿插渲染，必须用 `pdfa(html, '内容容器&&*')` 遍历所有直接子节点，在循环内按节点类型分流处理，保持原 HTML 顺序。不能分别提取图片和文字再拼接，否则顺序错乱。**

---

### 补充5：getResCode() vs fetch() 区别说明

手册中两个函数都出现但未做区分说明，实际使用场景不同：

| | `getResCode()` | `fetch(url, opts)` |
|---|---|---|
| **可用位置** | 仅限 `$().rule()` 沙箱内部 | 任意位置（主页、二级、lazyRule 等）|
| **获取内容** | 当前沙箱页面的 HTML 源码 | 指定 URL 的响应内容 |
| **URL 来源** | 自动从 `$('url').rule()` 传入的 url 取 | 必须手动指定 |
| **典型场景** | `rule()` 回调内获取详情页 HTML | 所有主动发起请求的场景 |

```javascript
// getResCode() 用法：只在 $().rule() 内部使用
url: $(detailUrl).rule(function() {
    var html = getResCode();  // ✅ 获取 detailUrl 对应的页面源码
    var d = [];
    // ... 解析 html ...
    setResult(d);
});

// fetch() 用法：主动请求任意 URL
var html = fetch(url, { headers: { 'User-Agent': ua } });
```

> 📌 在 `$().rule()` 内部，`getResCode()` 和 `fetch(MY_URL)` 效果相同，但 `getResCode()` 更简洁。在 `$().lazyRule()` 内部只能用 `fetch()`，没有 `getResCode()`。

---

### 补充6：二级函数分步调试最佳实践

手册缺少完整的调试流程说明，以下是实战验证的推荐步骤：

**第一步：单独验证详情页 URL 可访问**
```javascript
// 在主页函数中临时添加日志
log('详情页 URL: ' + detailUrl);
log('请求结果长度: ' + html.length);
log('页面特征: ' + (html.indexOf('特征关键词') > -1 ? '✅' : '❌'));
```

**第二步：验证图片 URL 提取正确**
```javascript
// log 出前3张图片的 URL
var imgs = pdfa(html, '图片选择器&&img');
for (var i = 0; i < Math.min(3, imgs.length); i++) {
    log('图片' + i + ': ' + pdfh(imgs[i], 'img&&z-image-loader-url'));
}
```

**第三步：验证解密后的 URL 可播放/可显示**
```javascript
// 在 $.image() 回调内 log 解密结果的前100字节
log('解密结果类型: ' + (dec ? dec.getClass().getName() : 'null'));
```

**第四步：整合测试**
建议先做不含解密的基础版本，确认列表和跳转正常后，再加入图片解密逻辑。

---

### 补充7：站点 JS 源码分析方法（找 key/iv/padding）

手册缺少如何定位加密配置的具体说明：

**方法1：搜索全局配置对象**
```javascript
// 常见位置：页面 HTML 的 <script> 标签内，或加载的 JS 文件中
// 搜索关键词：
// - __APP_CONFIG__
// - window.config
// - crypto / encrypt / decrypt
// - CryptoJS.AES
// - key: / iv: / secret:

// 示例站点源码（可能藏在混淆的 JS bundle 中）：
var __APP_CONFIG__ = {
    crypto: {
        key: 'abcdef1234567890',
        iv:  '1234567890abcdef',
        mode: 'NoPadding'     // ← 关键！
    }
};
```

**方法2：浏览器 DevTools 断点调试**
1. 打开 DevTools → Sources → 搜索 `AES.decrypt` 或 `CryptoJS`
2. 在解密函数处打断点
3. 刷新页面，断点触发时查看 key/iv 的实际值

**方法3：正则搜索 JS 文件中的密钥**
```javascript
// 在规则调试时，先抓一个 JS 文件并 log 搜索结果
var jsContent = fetch('https://site.com/js/app.bundle.js');
var keyMatch  = jsContent.match(/key\s*[:=]\s*['"]([a-zA-Z0-9]{16,32})['"]/);
var ivMatch   = jsContent.match(/iv\s*[:=]\s*['"]([a-zA-Z0-9]{16,32})['"]/);
log('key: ' + (keyMatch ? keyMatch[1] : '未找到'));
log('iv:  ' + (ivMatch  ? ivMatch[1]  : '未找到'));
```

> 📌 密钥长度参考：AES-128 = 16字节，AES-192 = 24字节，AES-256 = 32字节。找到的 key 字符串长度是加密强度的直接判断依据。

---

## 49.3 表述澄清

### 澄清1：MY_PAGE 注入条件的完整描述

> ⚠️ **本节修订（V3.66）：** 旧版手册多次说"`type:'主页'+url:'fypage'` 才注入 MY_PAGE"，实测**不严谨**。下面是修正后的完整规则。

**MY_PAGE 注入的真正条件**：

```javascript
静态分类: {
    type: '主页',   // ✅ 必须是字符串 '主页'，不能是数字 0 或 1
    url:  任意      // url 是否含 fypage 不影响 MY_PAGE 注入
                    // 三种合法写法：'fypage' / 'hiker://empty##fyclass##fypage' / '0'（矩阵方案）
},
页码: { "主页": 1 }  // 必须在 parse 顶层声明（部分版本若缺此声明会回退到不注入）
// 满足以上两条，MY_PAGE 即注入；BBYS / 矩阵方案都印证此规律
```

**fypage 与 MY_PAGE 是两件事（详见 2.3.1）：**
- `fypage` 是 URL 模板里的字符串占位符，引擎请求前替换为页码字符串
- `MY_PAGE` 是 JS 注入变量，与 url 是否含 fypage 无关
- 偏移量语法 `fypage@-1@*20@` 让 URL 中的占位符变 0/20/40，但 **MY_PAGE 始终是 1/2/3**

**type:0 / type:1 的实际后果（不是词法报错）：**
- 词法和语法分析能通过
- 运行期访问 `MY_PAGE` 得到 `undefined`
- 后续表达式 `MY_PAGE * 20`、`encodeURIComponent(MY_PAGE)` 会抛错
- 排查时直接 `log(typeof MY_PAGE)` 即可定位

**MY_PAGE 的类型**：注入后是数字（`number`），可以直接用于算术运算和比较，无需 `parseInt()`。但**字符串比较仍然合法**（`MY_PAGE == "1"` 也成立），BBYS 实战规则即用此模式。

### 澄清2：lazyRule 参数传递的完整规则

手册说"参数只能传基础类型"，补充具体说明：

```javascript
// ✅ 可以传：string、number、boolean
url: $(link).lazyRule(function(str, num, bool) { }, 'abc', 123, true)

// ❌ 不能传：object、array、function
// 传对象时会序列化为 '[object Object]'，传数组变为 '1,2,3' 字符串
var obj = { key: 'val' };
url: $(link).lazyRule(function(o) {
    log(typeof o);   // 'string'
    log(o);          // '[object Object]'
}, obj)              // ❌ obj 变成字符串

// ✅ 对象拆解成多个参数传递
url: $(link).lazyRule(function(k, v) { }, obj.key, obj.val)

// ✅ 或者 JSON.stringify 后传，内部 JSON.parse
url: $(link).lazyRule(function(s) {
    var obj = JSON.parse(s);
}, JSON.stringify(obj))
```

### 澄清3：parse 对象在 lazyRule/rule 中的可访问性

```javascript
// ❌ lazyRule 内部无法访问外层 parse 变量
url: $(link).lazyRule(function() {
    var host = parse.host;  // ❌ ReferenceError: parse is not defined
})

// ✅ 必须通过参数传入
var _host = this.host;
url: $(link).lazyRule(function(host) {
    // host 正常可用
}, _host)

// ⚠️ 特殊情况：$.toString() 字符串化的代码可以访问 parse，
// 因为 eval 执行时处于包含 parse 声明的同一作用域内
// 但这是 eval 特性，不是 lazyRule 特性，不可混淆
```

---

## 49.4 本章新增铁律汇总

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 92 | $.image() input 直接可用 | 回调中的 `input` 是引擎已下载的 `InputStream`，禁止对 `input` 调用 `fetch()` | `input` 是流不是 URL |
| Law 93 | padding 从源码确认 | 不能默认 `PKCS7Padding`，必须从站点 JS 中确认，写错无明显报错 | padding 写错 = 解密失败但无报错 |
| Law 94 | extenditems 配套 list | 使用 `extenditems` 时 `list` 必须给 `[['']]`，`noShow` 按需隐藏组件 | `extenditems + list:[['']] + noShow` 三件套 |
| Law 95 | 图文穿插按节点顺序 | 详情页图文混排须遍历所有子节点按序处理，不能分别提取再拼接 | `pdfa('内容&&*')` 遍历，循环内分类 |
| Law 96 | eval 字符串复用谨慎 | `eval` 字符串模式可用于复用解密代码，但优先用参数传递，`eval` 是最后手段 | 优先传参，eval 兜底 |

---

*手册勘误版本 V3.47 · 2026年4月 · 第四十九章：基于私房KTV规则开发实战的修正与补充*

---

# 第五十章：图集类站点开发总结——JunMeiTu 实战经验

本章基于 JunMeiTu（写真图集站）完整规则开发过程提炼，记录图集类站点的核心模式与手册通用规范的差异点。

---

## 50.1 图集类站点的核心架构

图集站与视频站的最大区别：内容的终点是 `pics://` 协议，而不是 `video://`。这个差异导致整个规则结构都有所不同：

| 维度 | 视频站 | 图集站 |
|---|---|---|
| 列表卡片 url | 详情页地址 → 进二级 | 直接 `$(url).lazyRule` 返回 `pics://` |
| 二级函数 | 必须，返回 `line/list` | 可选，仅资讯/街拍类内容需要 |
| 解析函数 | 必须，返回 `video://` | 不需要 |
| 翻页数据 | 引擎通过 `fypage` 处理 | 图集内部翻页需手动 `batchFetch` |

---

## 50.2 图集列表直接返回 pics://（不进二级）

纯图集类内容（写真集），点击卡片后直接看图，无需二级函数。列表项的 `url` 字段直接用 `lazyRule` 返回 `pics://`：

```javascript
// ✅ 图集列表标准写法：列表项直接 lazyRule 返回 pics://
var items = pdfa(html, 'body&&.gallery-item');
for (var i = 0; i < items.length; i++) {
    var it     = items[i];
    var link   = pd(it, 'a&&href', host);
    var title  = pdfh(it, '.title&&Text');
    var cover  = pd(it, 'img&&src', host);

    d.push({
        title:    title,
        pic_url:  cover + '@Referer=' + host + '/',
        col_type: 'movie_3',
        // ✅ 直接 lazyRule，不用二级函数
        url: $(link).lazyRule(function(h, ua) {
            // 获取图集详情页，批量抓取所有分页图片
            var html = fetch(input, { headers: { 'User-Agent': ua, 'Referer': h + '/' } });
            if (!html || html.length < 100) return 'toast://加载失败';

            var pics = [];
            // 提取图片 URL（见 50.3）
            // ...
            if (pics.length === 0) return 'toast://未找到图片';
            return 'pics://' + pics.join('&&');
        }, host, ua)
    });
}
```

---

## 50.3 pdfh 选择器不可靠时用正则——稳健图片提取

**场景**：部分站点图片容器的 HTML 结构较复杂，或 JSoup 对某些 class 名处理异常，`pdfh(html, '.pictures&&img&&src')` 会报错或返回空。

**原则**：`pdfh`/`pdfa` 优先，报错或空时降级到正则。两种方式都需要 `try...catch` 包裹。

```javascript
// ✅ 方式A：pdfa 选择器（优先）
var pics = [];
try {
    var imgNodes = pdfa(html, '.pictures&&img');
    for (var i = 0; i < imgNodes.length; i++) {
        var src = '';
        try { src = pdfh(imgNodes[i], 'img&&src'); } catch(e) {}
        if (!src) try { src = pdfh(imgNodes[i], 'img&&data-src'); } catch(e) {}
        if (src && src.indexOf('http') === 0) pics.push(src);
    }
} catch(e) { log('pdfa 图片提取失败，降级正则: ' + e); }

// ✅ 方式B：正则降级（pdfa 失败或返回空时）
if (pics.length === 0) {
    var re = /<img[^>]+src="(https?:\/\/[^"]+\.(jpg|jpeg|png|webp)[^"]*)"[^>]*>/gi;
    var m;
    while ((m = re.exec(html)) !== null) {
        pics.push(m[1]);
    }
}
```

> 📌 正则提取图片的通用模式：`/<img[^>]+src="(https?:\/\/[^"]+\.(jpg|jpeg|png|webp)[^"]*)"[^>]*>/gi`，用 `while + exec` 遍历所有匹配。注意正则对象不能复用（每次循环前重新赋值），否则 `lastIndex` 导致漏匹配。

> **⚠️ Law 97（新增）：`pdfh`/`pdfa` 是优先选择，但在图片提取场景下选择器可能因站点 HTML 结构异常而失败，必须有正则兜底。选择器和正则都要包在 `try...catch` 里。**

---

## 50.4 图集内部分页——batchFetch 批量抓取

图集站通常每页只显示部分图片，需要抓取所有分页才能得到完整图集。翻页 URL 规律通常是 `index-2.html`、`index-3.html` 这类非标准格式。

```javascript
// 标准图集分页批量抓取模式
url: $(link).lazyRule(function(h, ua) {
    var baseUrl = input;  // 如 https://site.com/set/001/index.html
    var html1 = fetch(baseUrl, { headers: { 'User-Agent': ua, 'Referer': h + '/' } });
    if (!html1 || html1.length < 100) return 'toast://加载失败';

    var pics = [];

    // Step1：提取第一页图片
    var addPics = function(html) {
        var re = /<img[^>]+src="(https?:\/\/[^"]+\.(jpg|jpeg|png|webp)[^"]*)"[^>]*>/gi;
        var m;
        while ((m = re.exec(html)) !== null) {
            var src = m[1];
            // 过滤广告图/Logo（根据实际站点路径特征）
            if (src.indexOf('/thumb/') > -1 || src.indexOf('/ad/') > -1) continue;
            pics.push(src + '@Referer=' + h + '/');
        }
    };
    addPics(html1);

    // Step2：从第一页提取总页数
    var totalPages = 1;
    var pageM = html1.match(/共\s*(\d+)\s*页/) || html1.match(/\/(\d+)\s*页/);
    if (pageM) totalPages = parseInt(pageM[1]);
    // 也可以从分页链接推断：找最大页码
    var maxPageM = html1.match(/index-(\d+)\.html/g);
    if (maxPageM) {
        var maxPage = 1;
        for (var mi = 0; mi < maxPageM.length; mi++) {
            var n = parseInt(maxPageM[mi].match(/\d+/)[0]);
            if (n > maxPage) maxPage = n;
        }
        totalPages = Math.max(totalPages, maxPage);
    }

    // Step3：batchFetch 并发抓取剩余页（最多16个/批，Law 3.3）
    if (totalPages > 1) {
        var tasks = [];
        // 翻页 URL 规律：index.html → index-2.html → index-3.html
        for (var p = 2; p <= Math.min(totalPages, 20); p++) {
            var pageUrl = baseUrl.replace(/index(\.html)?$/, 'index-' + p + '.html');
            tasks.push({ url: pageUrl, options: { headers: { 'User-Agent': ua, 'Referer': h + '/' } } });
        }
        var results = batchFetch(tasks);
        for (var ri = 0; ri < results.length; ri++) {
            if (results[ri] && results[ri].length > 100) addPics(results[ri]);
        }
    }

    if (pics.length === 0) return 'toast://未找到图片';
    return 'pics://' + pics.join('&&');
}, host, ua)
```

> 📌 `batchFetch` 最多16个并发（Law 3.3），图集页数超过16时自动分批串行，不需要额外处理。但抓取超过20页的图集时，考虑只抓前N页以控制等待时间。

---

## 50.5 资讯/街拍类内容——二级函数 + extenditems

资讯文章和街拍帖子是图文混排内容，需要用二级函数返回 `extenditems`，按 HTML 顺序渲染（Law 95）：

```javascript
二级: function(url) {
    var host = this.host;
    var ua   = this.UA;
    var html = fetch(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });
    if (!html || html.length < 100) {
        return { list: [['']], noShow: { 封面: true, 简介: true, 排序: true, 选集: true },
                 extenditems: [{ title: '加载失败', col_type: 'text_1', url: 'hiker://empty' }] };
    }

    var extra = [];

    // 标题
    var title = '';
    try { title = pdfh(html, 'h1&&Text') || pdfh(html, '.article-title&&Text'); } catch(e) {}
    if (title) extra.push({ title: title, col_type: 'text_1', url: url, extra: { lineVisible: false } });

    extra.push({ col_type: 'line' });

    // 按顺序遍历正文节点（Law 95）
    var nodes = [];
    try { nodes = pdfa(html, 'body&&.article-content&&*'); } catch(e) {}
    // 备用选择器
    if (nodes.length === 0) try { nodes = pdfa(html, 'body&&.content&&*'); } catch(e) {}

    for (var i = 0; i < nodes.length; i++) {
        var node = nodes[i];

        // 图片
        var imgSrc = '';
        try { imgSrc = pdfh(node, 'img&&src') || pdfh(node, 'img&&data-src'); } catch(e) {}
        if (imgSrc && imgSrc.indexOf('http') === 0) {
            extra.push({
                title:    '',
                pic_url:  imgSrc + '@Referer=' + host + '/',
                url:      'pics://' + imgSrc + '@Referer=' + host + '/',
                col_type: 'pic_1_full'
            });
            continue;
        }

        // 文字
        var txt = '';
        try { txt = pdfh(node, 'Text'); } catch(e) {}
        if (txt && txt.trim().length > 3) {
            extra.push({ title: txt.trim(), col_type: 'rich_text' });
        }
    }

    return {
        vod_name:    title,
        list:        [['']], // ✅ Law 94：必须给空二维数组
        noShow:      { 封面: true, 简介: true, 排序: true, 选集: true },
        extenditems: extra
    };
},
```

---

## 50.6 标签云 / 主题分类——独立新开页面

当标签云或主题分类需要独立的渲染逻辑（如彩色 `flex_button` + 翻页列表），适合用 `$().rule()` 新开沙箱页面，而不是依赖静态分类：

```javascript
// 标签云入口（在主页列表中）
d.push({
    title:    '🏷️ 所有标签',
    col_type: 'text_center_1',
    url: $('hiker://empty##fypage').rule(function(h, ua) {
        var d = [];
        var url = h + '/tags/' + (MY_PAGE > 1 ? 'page/' + MY_PAGE + '/' : '');
        var html = fetch(url, { headers: { 'User-Agent': ua } });
        if (!html || html.length < 100) { setResult(d); return; }

        var tags = pdfa(html, 'body&&.tag-item');
        for (var i = 0; i < tags.length; i++) {
            var tagLink  = pd(tags[i], 'a&&href', h);
            var tagName  = pdfh(tags[i], 'a&&Text');
            var tagCount = pdfh(tags[i], '.count&&Text') || '';
            d.push({
                title:    tagName + (tagCount ? ' (' + tagCount + ')' : ''),
                col_type: 'flex_button',
                // 点击标签 → 新开该标签的图集列表页（带翻页）
                url: $(tagLink + '##fypage').rule(function(th, tua) {
                    var d = [];
                    var tUrl = MY_URL.split('##')[0];
                    if (MY_PAGE > 1) tUrl = tUrl.replace(/\/?$/, '') + '/page/' + MY_PAGE + '/';
                    var tHtml = fetch(tUrl, { headers: { 'User-Agent': tua } });
                    // ... 解析图集列表 ...
                    setResult(d);
                }, h, ua),
                extra: { backgroundColor: getRangeColors() }
            });
        }
        setResult(d);
    }, host, ua)
});
```

> ⚠️ **标签云走 `rule()` 沙箱的限制（Law 67 延伸）**：沙箱内的卡片如果需要进二级（有 `二级` 函数的视频站），URL 会跳浏览器。图集站的卡片直接返回 `pics://` 或 `video://` 协议，不受此限制，所以图集站标签云适合用 `rule()` 方案。

---

## 50.7 空值防御——fetch 后的标准检查

图集站的图片和页面请求失败时，不抛出异常而是返回空字符串或极短内容，必须在每次 `fetch` 后做检查：

```javascript
// ✅ 标准空值检查模板
var html = fetch(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });

// 两个条件都要检查：
// 1. !html：fetch 返回 null 或 undefined
// 2. html.length < 100：返回了空页面或极短的错误页
if (!html || html.length < 100) {
    log('页面加载失败: ' + url);
    // 列表场景：返回空数组
    return d;
    // lazyRule 场景：返回 toast 提示
    // return 'toast://加载失败，请重试';
}

// ✅ 额外检查：验证页面含预期特征（比长度检查更准确）
if (html.indexOf('预期的CSS类名或关键词') === -1) {
    log('页面内容异常，可能被 CF 拦截或域名变更');
    return d;
}
```

> **⚠️ Law 98（新增）：每次 `fetch` 后必须做双重检查：`!html || html.length < 100`。对于关键页面（如详情页），进一步用页面特征关键词验证内容有效性，不能仅依赖长度。**

---

## 50.8 防盗链——图片 URL 后缀的完整写法

图集站几乎100%有防盗链，漏加 `Referer` 会导致图片显示为403或空白：

```javascript
// ✅ 三种防盗链写法（任选一种）

// 写法1：简短格式（推荐，适合单一 Referer）
var picUrl = imgSrc + '@Referer=' + host + '/';

// 写法2：完整 headers 格式（需要多个请求头时）
var picUrl = imgSrc + '@headers={"Referer":"' + host + '/","User-Agent":"' + ua + '"}';

// 写法3：在 $.image() 中用 FileUtil 带 headers 下载（需要解密时）
// 见第十章 / 第四十一章

// ⚠️ 注意：Referer 填图片所在页面的域名，不是图片 CDN 的域名
// 错误：@Referer=https://pic.cdn.example.com/
// 正确：@Referer=https://www.example.com/
```

---

## 50.9 图集站完整规则骨架

```javascript
var parse = {
    作者: 'dev',
    版本: '20260408.V1',
    host: 'https://www.example.com',
    UA:   'Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Mobile Safari/537.36',

    页码: { '主页': 1, '分类': 1, '搜索': 1 },

    静态分类: {
        type: '主页',
        url: 'fyclass',
        class_name: '推荐&写真&街拍&资讯',
        class_url:  '/&/photos/&/street/&/news/'
    },

    主页: function() {
        var d    = [];
        var host = this.host;
        var ua   = this.UA;
        var cls  = (MY_URL && MY_URL !== 'fyclass') ? MY_URL : '/';
        var url  = host + cls;
        if (MY_PAGE > 1) url = url.replace(/\/?$/, '') + '/page/' + MY_PAGE + '/';

        var html = fetch(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });
        if (!html || html.length < 100) return d;

        // 按路径分流（Law 38 精神：if/else if 路由）
        if (cls.indexOf('/news/') > -1 || cls.indexOf('/street/') > -1) {
            // 资讯/街拍：进二级
            return this._parseNewsListToSecond(html, host, ua);
        } else {
            // 写真/推荐：直接 lazyRule 返回 pics://
            return this._parsePhotoList(html, host, ua);
        }
    },

    // 写真列表解析（卡片直接返回 pics://）
    _parsePhotoList: function(html, host, ua) {
        var d     = [];
        var items = [];
        try { items = pdfa(html, 'body&&.photo-item'); } catch(e) {}
        for (var i = 0; i < items.length; i++) {
            var it    = items[i];
            var link  = pd(it, 'a&&href', host);
            var title = pdfh(it, '.title&&Text') || pdfh(it, 'img&&alt') || '';
            var cover = pd(it, 'img&&src', host) || pd(it, 'img&&data-src', host);
            if (!link) continue;
            d.push({
                title:    title,
                pic_url:  cover ? cover + '@Referer=' + host + '/' : '',
                col_type: 'movie_3',
                url: $(link).lazyRule(function(h, ua) {
                    var html = fetch(input, { headers: { 'User-Agent': ua, 'Referer': h + '/' } });
                    if (!html || html.length < 100) return 'toast://加载失败';
                    var pics = [];
                    // 选择器方式
                    try {
                        var imgs = pdfa(html, '.pictures&&img');
                        for (var i = 0; i < imgs.length; i++) {
                            var src = '';
                            try { src = pdfh(imgs[i], 'img&&src'); } catch(e) {}
                            if (!src) try { src = pdfh(imgs[i], 'img&&data-src'); } catch(e) {}
                            if (src && src.indexOf('http') === 0) pics.push(src + '@Referer=' + h + '/');
                        }
                    } catch(e) {}
                    // 正则降级（Law 97）
                    if (pics.length === 0) {
                        var re = /<img[^>]+src="(https?:\/\/[^"]+\.(jpg|jpeg|png|webp)[^"]*)"[^>]*>/gi;
                        var m;
                        while ((m = re.exec(html)) !== null) pics.push(m[1] + '@Referer=' + h + '/');
                    }
                    if (pics.length === 0) return 'toast://未找到图片';
                    return 'pics://' + pics.join('&&');
                }, host, ua)
            });
        }
        return d;
    },

    // 资讯/街拍列表解析（卡片进二级）
    _parseNewsListToSecond: function(html, host, ua) {
        var d = [];
        var items = [];
        try { items = pdfa(html, 'body&&.news-item'); } catch(e) {}
        for (var i = 0; i < items.length; i++) {
            var it    = items[i];
            var link  = pd(it, 'a&&href', host);
            var title = pdfh(it, '.title&&Text') || pdfh(it, 'h3&&Text') || '';
            var cover = pd(it, 'img&&src', host);
            if (!link) continue;
            d.push({
                title:    title,
                pic_url:  cover ? cover + '@Referer=' + host + '/' : '',
                col_type: 'movie_3',
                url:      link   // 直接给链接，引擎走二级函数
            });
        }
        return d;
    },

    // 二级（资讯/街拍详情）
    二级: function(url) {
        var host = this.host;
        var ua   = this.UA;
        var html = fetch(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });
        if (!html || html.length < 100) {
            return { list: [['']], noShow: { 封面: true, 简介: true, 排序: true, 选集: true },
                     extenditems: [{ title: '加载失败', col_type: 'text_1', url: 'hiker://empty' }] };
        }
        var extra = [];
        var title = '';
        try { title = pdfh(html, 'h1&&Text'); } catch(e) {}
        if (title) extra.push({ title: title, col_type: 'text_1', url: url, extra: { lineVisible: false } });
        extra.push({ col_type: 'line' });

        // 按 HTML 顺序遍历正文（Law 95）
        var nodes = [];
        try { nodes = pdfa(html, 'body&&.content&&*'); } catch(e) {}
        for (var i = 0; i < nodes.length; i++) {
            var imgSrc = '';
            try { imgSrc = pdfh(nodes[i], 'img&&src') || pdfh(nodes[i], 'img&&data-src'); } catch(e) {}
            if (imgSrc && imgSrc.indexOf('http') === 0) {
                extra.push({ title: '', pic_url: imgSrc + '@Referer=' + host + '/',
                             url: 'pics://' + imgSrc, col_type: 'pic_1_full' });
                continue;
            }
            var txt = '';
            try { txt = pdfh(nodes[i], 'Text'); } catch(e) {}
            if (txt && txt.trim().length > 3) extra.push({ title: txt.trim(), col_type: 'rich_text' });
        }
        return { vod_name: title, list: [['']], // ✅ Law 94
                 noShow: { 封面: true, 简介: true, 排序: true, 选集: true }, extenditems: extra };
    },

    搜索: function(kw) {
        var host = this.host;
        var ua   = this.UA;
        var url  = host + '/search/?q=' + encodeURIComponent(kw);
        if (MY_PAGE > 1) url += '&page=' + MY_PAGE;
        var html = fetch(url, { headers: { 'User-Agent': ua } });
        if (!html || html.length < 100) return [];
        return this._parsePhotoList(html, host, ua);
    },

    _base64Decode: function(str) {
        if (!str) return '';
        str = str.replace(/-/g, '+').replace(/_/g, '/');
        while (str.length % 4) str += '=';
        try {
            var b = android.util.Base64.decode(str, android.util.Base64.DEFAULT);
            return new java.lang.String(b, 'UTF-8') + '';
        } catch(e) { return ''; }
    },

    _autoDeshell: function(html) {
        if (!html) return html;
        for (var i = 0; i < 3; i++) {
            if (html.indexOf('decodeURIComponent("') > -1) {
                var m = html.match(/decodeURIComponent\("([^"]+)"\)/);
                if (m) html = decodeURIComponent(m[1]);
            } else break;
        }
        return html;
    }
};
```

---

## 50.10 本章新增铁律

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 97 | 图片选择器降级正则 | `pdfa`/`pdfh` 提取图片失败时，用正则 `/<img[^>]+src="(https?:\/\/[^"]+)"[^>]*>/gi` 兜底 | 选择器失败 → 正则接手 |
| Law 98 | fetch 双重空值检查 | 每次 `fetch` 后检查 `!html \|\| html.length < 100`，关键页面再验证内容特征 | 空串 + 短串 + 特征，三重保险 |

---

## 50.11 图集站 vs 视频站 vs 资讯站——三种模式对比

| 维度 | 图集站（写真） | 视频站 | 资讯站 |
|---|---|---|---|
| 列表卡片 url | `$(link).lazyRule` → `pics://` | `link`（进二级）或 `$(link).lazyRule` → `video://` | `link`（进二级）|
| 二级函数 | 不需要 | 必须（返回 `line/list`）| 必须（返回 `extenditems`）|
| 解析函数 | 不需要 | 按需（m3u8 解密等）| 不需要 |
| 内容终点 | `pics://` 协议 | `video://` 协议 | `extenditems` 富文本 |
| 翻页策略 | 图集内部 `batchFetch` 分页 | 引擎 `fypage` + 请求列表 | 引擎 `fypage` + 请求列表 |
| 核心 Law | Law 97/98 + `pics://` | Law 3/4/16/39 | Law 94/95 |

---

*手册补充版本 V3.47 · 第五十章：图集类站点开发总结（JunMeiTu 实战）*

---

# 第五十一章：API 接口调试与状态机模式——实战补充

本章补充实战中高频遇到、手册前述章节未充分覆盖的四个专题：POST 请求规范、HTML 内嵌 JSON 提取、状态机多视图模式、lazyRule 使用边界。

---

## 51.1 POST 请求数据提交规范

手册第三章（3.3）给出了 `fetch` 的基础用法，但对 JSON POST 的细节说明不足，以下是完整规范。

**三个必须同时满足的条件**：

| 项目 | 要求 | 错误后果 |
|---|---|---|
| `body` 格式 | 必须是字符串，用 `JSON.stringify()` 转换 | `param error`，服务端解析失败 |
| `Content-Type` | 必须设置为 `application/json;charset=UTF-8` | 响应为空或400 |
| 参数名 | 必须与 API 文档/源码完全一致 | `param error`，即使格式正确也失败 |

```javascript
// ✅ JSON POST 标准模板
var response = fetch(url, {
    headers: {
        'User-Agent': parse.UA,
        'Referer':    parse.host + '/',
        'Content-Type': 'application/json;charset=UTF-8'
    },
    body:   JSON.stringify({ id: postId, page: 1 }),  // ✅ 必须是字符串
    method: 'POST'
});
var json = JSON.parse(response || '{}');
```

> ⚠️ **手册第二十章 20.3 节描述"body 传对象时引擎自动序列化"，适用于表单提交（`application/x-www-form-urlencoded`）。对于 JSON API，body 必须手动 `JSON.stringify()`，不能依赖引擎自动处理。两种场景不要混用。**

**常见报错对照**：

```javascript
// ❌ 错误1：body 传对象（JSON API 场景）
body: { id: postId }    // 引擎当表单处理，服务端收到 "id=123" 而非 JSON

// ❌ 错误2：参数名拼写错误
body: JSON.stringify({ postId: id })  // 服务端期望 id，收到 postId → param error

// ❌ 错误3：缺少 Content-Type
// 服务端不知道如何解析 body，返回 400 或空响应

// ✅ 调试手段：用 Python 独立验证 API 格式
// import requests, json
// r = requests.post(url, headers={'Content-Type':'application/json'},
//                   data=json.dumps({"id":"688"}))  # 注意用 data= 不是 json=
// print(r.text)
```

> **⚠️ Law 99（新增）：JSON POST 请求中，`body` 必须是 `JSON.stringify()` 后的字符串，同时设置 `Content-Type: application/json;charset=UTF-8`。参数名必须从 API 源码中确认，不能猜测。**

---

## 51.2 数据来源识别与 HTML 内嵌 JSON 提取

现代 SPA 站点（Vue/React/Nuxt）常把数据内嵌在页面 HTML 的 `<script>` 标签中，不需要额外 API 请求。识别方式：

**三种常见模式**：

| 模式 | 特征 | 处理方法 |
|---|---|---|
| 纯 API | 数据在独立接口的 JSON 响应中 | `JSON.parse(fetch(apiUrl))` |
| HTML 内嵌 JSON | `window.__NUXT__`、`window.__INITIAL_STATE__`、`var data = {...}` | 正则提取后 `JSON.parse()` |
| API + 重定向 | API 返回 `redirectModelId` 等间接 ID | 先取真实 ID，再请求实际数据 |

```javascript
// ✅ 提取 window.__NUXT__ 数据
var nuxtM = html.match(/window\.__NUXT__\s*=\s*(\{[\s\S]*?\});\s*<\/script>/);
if (nuxtM) {
    var nuxtData = JSON.parse(nuxtM[1]);
    // 根据实际结构取值，如 nuxtData.data[0].post
}

// ✅ 提取内嵌的特定数组（更精确的正则）
var listM = html.match(/"videoList"\s*:\s*(\[[\s\S]*?\])\s*[,}]/);
if (listM) {
    var videoList = JSON.parse(listM[1]);
}

// ✅ API + 重定向处理
var apiJson = JSON.parse(fetch(apiUrl, { body: JSON.stringify({id: postId}), method: 'POST' }));
var realId  = (apiJson.data && apiJson.data.redirectModelId) ? apiJson.data.redirectModelId : postId;
var webUrl  = parse.host + '/model/' + realId;
var html    = fetch(webUrl);
```

> 📌 **识别技巧**：打开浏览器 DevTools → Sources，搜索 `__NUXT__` / `__INITIAL_STATE__` / `window.` 等关键词。找到后观察 JSON 结构，定位目标字段在哪一层。

---

## 51.3 状态机模式——单主页函数处理多视图

当一个规则需要在同一个主页函数内渲染多种完全不同的内容（首页推荐、分类列表、模特库、搜索结果、视频列表等），用 `getMyVar` 状态变量控制分发，比静态分类更灵活。

**手册第六章的矩阵方案**适合分类数量固定、URL 有规律的场景。**状态机模式**适合每种视图需要完全不同的渲染函数、甚至不同 API 的复杂场景。

```javascript
主页: function() {
    var d    = [];
    var host = this.host;
    var ua   = this.UA;
    var KEY  = 'site_state';  // 纯字母 key（Law 51）
    var curState = parseInt(getMyVar(KEY, '0'));

    if (MY_PAGE == 1) {
        // 渲染顶部导航按钮行
        var navs = ['推荐', '分类', '模特库', '热门榜'];
        for (var i = 0; i < navs.length; i++) {
            var isSel = (i === curState);
            d.push({
                title:    isSel ? '❆ ' + navs[i] + ' ❆' : navs[i],
                col_type: 'scroll_button',
                url: $('#noLoading#').lazyRule(function(k, idx) {
                    putMyVar(k, idx + '');
                    refreshPage(false);
                    return 'hiker://empty';
                }, KEY, i)
            });
        }
        d.push({ col_type: 'blank_block' });
    }

    // 状态分发
    switch (curState) {
        case 0: return d.concat(this._renderHome(host, ua));
        case 1: return d.concat(this._renderCategories(host, ua));
        case 2: return d.concat(this._renderModelList(host, ua));
        case 3: return d.concat(this._renderHotList(host, ua));
        default: return d.concat(this._renderHome(host, ua));
    }
},
```

**从 lazyRule 跳转到指定状态**（如点击一个模特卡片，进入该模特的视频列表视图）：

```javascript
// 在列表卡片的 url 里，点击后切换状态并传参
url: $('#noLoading#').lazyRule(function(k, stateKey, stateVal, paramKey, paramVal) {
    putMyVar(stateKey, stateVal);   // 设置目标状态
    putMyVar(paramKey, paramVal);   // 传递参数（如模特 ID）
    refreshPage(false);
    return 'hiker://empty';
}, '#noLoading#', KEY, '5', 'site_model_id', modelId)
```

> 📌 **状态机 vs 矩阵方案的选型**：
> - 各分类走同一解析函数 → 矩阵方案（第六章）
> - 各视图需要完全不同的渲染逻辑/API → 状态机模式（本节）
> - 状态机的 key 命名遵循 Law 51（纯字母+下划线），不用 host 拼前缀

---

## 51.4 lazyRule 的使用边界

手册第二十六章（26.2）已介绍 `lazyRule` 的基本用法，以下补充使用边界说明。

**✅ 推荐用法**：

```javascript
// 1. 发起 API/fetch 请求取数据
// 2. 读写 putMyVar/getMyVar
// 3. 触发页面刷新 refreshPage(false)
// 4. 返回最终协议字符串（video://、pics://、toast://）
```

**❌ 不推荐用法**：

```javascript
// ❌ 在 lazyRule 内调用 setResult()——行为不稳定
url: $(link).lazyRule(function() {
    var d = [];
    // ...
    setResult(d);   // ❌ lazyRule 内 setResult 效果不可预期
    return 'hiker://empty';
})

// ✅ 正确：复杂 UI 逻辑用 $().rule() 沙箱
url: $(link).rule(function() {
    var d = [];
    // ...
    setResult(d);   // ✅ rule() 内 setResult 正常
})
```

**多视频选择的最佳实践**：

```javascript
// lazyRule 内遇到多视频时，存数据 + 切状态 + 刷新，让主页函数渲染
_buildVideoLazyRule: function(postId, host) {
    return $(postId).lazyRule(function(h, apiUrl) {
        var json = JSON.parse(fetch(apiUrl, {
            body: JSON.stringify({ id: input }),
            method: 'POST',
            headers: { 'Content-Type': 'application/json;charset=UTF-8' }
        }) || '{}');
        var videos = json.data && json.data.videoList ? json.data.videoList : [];
        if (videos.length === 0) return 'toast://未找到视频';
        // 单视频直接返回
        if (videos.length === 1) {
            return 'video://' + videos[0].url + ';{Referer@' + h + '/}';
        }
        // 多视频：存到 MyVar，切换到视频列表状态，刷新主页渲染
        putMyVar('site_videos', JSON.stringify(videos));
        putMyVar('site_state', '6');
        refreshPage(false);
        return 'hiker://empty';
    }, host, this._apiUrl);
},
```

---

## 51.5 批量请求注意事项补充

手册第三章（3.3）已说明 `batchFetch` 上限16，以下补充 POST 批量请求的写法：

```javascript
// ✅ POST 接口的 batchFetch
var tasks = [];
for (var p = 1; p <= totalPages; p++) {
    tasks.push({
        url: apiUrl,
        options: {
            headers: {
                'Content-Type': 'application/json;charset=UTF-8',
                'Referer': host + '/'
            },
            body:   JSON.stringify({ id: postId, page: p }),
            method: 'POST'
        }
    });
}
var results = batchFetch(tasks);  // 最多16个并发，超出自动分批串行

// ✅ 解析时每条单独 try...catch，一条失败不影响其他
var allItems = [];
for (var ri = 0; ri < results.length; ri++) {
    try {
        var json = JSON.parse(results[ri] || '{}');
        var list = json.data && json.data.list ? json.data.list : [];
        allItems = allItems.concat(list);
    } catch(e) { log('batchFetch 第' + ri + '条解析失败: ' + e); }
}
```

---

## 51.6 调试技巧补充

| 方法 | 用途 | 示例 |
|---|---|---|
| `toast(msg)` | 运行时弹出简短信息 | `toast('状态: ' + curState)` |
| `return 'toast://' + data` | 在 lazyRule 中输出数据快照 | `return 'toast://' + JSON.stringify(json).substring(0,200)` |
| `log(msg)` | 写入调试日志（不打断流程） | `log('响应长度: ' + (response||'').length)` |
| Python 独立验证 | 在规则外确认 API 格式 | `requests.post(url, data=json.dumps(body))` |
| 截断长字符串 | 避免 toast 溢出 | `JSON.stringify(obj).substring(0, 300)` |

> 📌 **调试顺序建议**：
> 1. 先用 Python 确认 API 请求格式和响应结构
> 2. 再在规则的 `log()` 中打印关键变量
> 3. 用 `toast()` 快速验证某一步的输出值
> 4. 确认无误后删除调试代码

---

## 51.7 本章新增铁律

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 99 | JSON POST 必须 stringify | `body` 必须是 `JSON.stringify()` 字符串，同时设 `Content-Type: application/json` | 对象 body = 表单提交，JSON API 必须手动 stringify |
| Law 100 | lazyRule 内禁用 setResult | `lazyRule` 内不能调用 `setResult()`，复杂 UI 用 `$().rule()` | lazyRule 返回字符串，rule 调 setResult |

---

*手册补充版本 V3.47 · 第五十一章：POST 请求规范、HTML 内嵌 JSON、状态机模式、lazyRule 使用边界*

---

# 第五十二章：API 与 ES6 兼容性完整实测报告（2026年4月最新验证）

本章基于系统性功能测试，全面更新第三十一章的兼容性结论，并补充内置函数的完整可用性清单。

---

## 52.1 ES6 语法完整实测结果

以下结论经过独立测试验证，优先级高于第三十一章的历史记录。

| ES6 特性 | 支持状态 | 备注 |
|---|---|---|
| `let` / `const` | ✅ 支持 | 可完全替代 `var` |
| 箭头函数 `() => {}` | ✅ 支持 | 含完整函数体和简写形式 |
| 模板字符串 `` `${}` `` | ✅ 支持 | |
| 解构赋值 `const {a, b} = obj` | ✅ 支持 | 第三十一章标注"不稳定"，**实测可用** |
| 扩展运算符 `...` | ✅ 支持 | 第三十一章标注"不支持"，**实测可用** |
| `.filter()` / `.map()` 链式 | ✅ 支持 | |
| 对象属性简写 `{ x }` | ✅ 支持 | |
| 可选链 `?.` | ⚠️ 未验证 | 建议仍用 `&&` 替代 |
| 空值合并 `??` | ⚠️ 未验证 | 建议仍用 `\|\|` 替代 |
| 方法简写 `fn() {}` | ❌ 不支持 | 必须写 `fn: function() {}` |
| `async` / `await` | ❌ 不支持 | 引擎单线程，无实际并发效果 |

> ⚠️ **第三十一章修订**：解构赋值和扩展运算符在第三十一章中标注为不支持或不稳定，最新实测结果为**支持**。建议以本章为准。方法简写和 async/await 仍然不支持，结论不变。

---

## 52.2 内置函数完整可用性清单

以下函数经实测验证可用，作为第三章的补充。

**日志与调试**

| 函数 | 状态 | 说明 |
|---|---|---|
| `log(msg)` | ✅ 可用 | 写入调试日志 |
| `toast(msg)` | ✅ 可用 | 弹出短提示 |

**编码与加密**

| 函数 | 状态 | 说明 |
|---|---|---|
| `base64Encode(s)` / `base64Decode(s)` | ✅ 可用 | 标准 Base64 |
| `md5(s)` | ✅ 可用 | MD5 哈希 |
| `encodeURIComponent(s)` | ✅ 可用 | URL 编码 |
| `getCryptoJS()` | ✅ 可用 | 返回 CryptoJS **源码字符串**，必须先 `eval()` 才能使用 |
| `aesEncode(key, data)` / `aesDecode(key, data)` | ✅ 可用 | 引擎内置 AES 快捷函数 |

> ⚠️ **`getCryptoJS()` 最常见陷阱：它返回的是 JS 源码字符串，不是 CryptoJS 对象本身。必须先 `eval(getCryptoJS())` 才能让 `CryptoJS` 变量进入当前作用域，之后才能调用 `CryptoJS.SHA1` / `CryptoJS.MD5` 等方法。**

```javascript
// ❌ 错误：直接调用，CryptoJS 是 undefined
var hash = CryptoJS.SHA1('hello').toString();

// ✅ 正确：先 eval，再调用
eval(getCryptoJS());                              // 把 CryptoJS 源码注入当前作用域
var sha1Hash = CryptoJS.SHA1('hello').toString(); // ✅
var md5Hash  = CryptoJS.MD5('hello').toString();  // ✅

// ── 完整封装（推荐：把 eval 封装在工具函数内，避免污染外部作用域）
sha1: function(str) {
    eval(getCryptoJS());
    return CryptoJS.SHA1(str).toString();
},

md5: function(str) {
    eval(getCryptoJS());
    return CryptoJS.MD5(str).toString();
},

hmacSha256: function(str, key) {
    eval(getCryptoJS());
    return CryptoJS.HmacSHA256(str, key).toString();
},

// ── getCryptoJS 支持的常用算法 ─────────────────────────────────
// CryptoJS.MD5(str).toString()           → MD5 哈希（hex）
// CryptoJS.SHA1(str).toString()          → SHA1 哈希（hex）
// CryptoJS.SHA256(str).toString()        → SHA256 哈希（hex）
// CryptoJS.HmacSHA256(str, key).toString() → HMAC-SHA256（hex）
// CryptoJS.AES.encrypt(str, key).toString() → AES 加密（Base64）
// CryptoJS.AES.decrypt(str, key).toString(CryptoJS.enc.Utf8) → AES 解密
```

> 📌 `eval(getCryptoJS())` 每次调用都会重新解析一遍源码，有性能开销。如果同一函数里多次用到 CryptoJS，只需 eval 一次放在函数顶部即可。不要放在循环里。

**存储**

| 函数 | 状态 | 说明 |
|---|---|---|
| `putMyVar(k, v)` / `getMyVar(k, def)` | ✅ 可用 | 会话级，App 重启丢失 |
| `juItem.set(k, v)` / `juItem.get(k, def)` | ✅ 可用 | 持久化，规则删除才丢失 |

**DOM 解析**

| 函数 | 状态 | 说明 |
|---|---|---|
| `pd(html, rule, baseUrl)` | ✅ 可用 | 返回第一个匹配，自动补全域名 |
| `pdfh(html, rule)` | ✅ 可用 | 返回第一个匹配，原始值 |
| `pdfa(html, rule)` | ✅ 可用 | 返回匹配列表 |

**网络请求**

| 函数 | 状态 | 说明 |
|---|---|---|
| `fetch(url, opts)` | ✅ 可用 | 标准请求 |
| `request(url, opts)` | ✅ 可用，**更完整** | 返回内容比 fetch 更完整，见下 |
| `fetchCodeByWebView(url, opts)` | ✅ 可用 | WebView 渲染后取源码 |

**$ 工具（全部可用）**

| 方法 | 状态 | 说明 |
|---|---|---|
| `$().lazyRule(fn, ...args)` | ✅ 可用 | 懒加载解析 |
| `$().rule(fn, ...args)` | ✅ 可用 | 新开沙箱页面 |
| `$().image(fn, ...args)` | ✅ 可用 | 图片处理管道 |
| `$.toString(fn)` | ✅ 可用 | 函数字符串化 |

**UI 组件**

| 组件 | 状态 |
|---|---|
| 所有 `col_type` 样式 | ✅ 全部支持 |

---

## 52.3 request vs fetch——优先使用 request 的理由

实测发现 `request` 返回内容比 `fetch` 更完整，以下是区别说明：

```javascript
// fetch：部分站点在某些情况下可能返回截断或不完整的响应
var html = fetch(url, { headers: { 'User-Agent': ua } });

// request：与 fetch 参数完全相同，但响应更完整，推荐优先使用
var html = request(url, { headers: { 'User-Agent': ua } });
```

两者参数格式完全一致，可以无缝替换。手册第三章（3.3）注明两者为别名关系，但实际表现有差异时**优先使用 `request`**。

> **⚠️ Law 101（新增）：在有选择的情况下，优先使用 `request()` 而非 `fetch()`，前者返回内容更完整。两者参数格式相同，可直接替换。**

---

## 52.4 修订 Law 34

基于本章实测结果，Law 34 进一步更新：

**Law 34（最终版）**：

| 特性 | 结论 |
|---|---|
| `const` / `let` | ✅ 可用，推荐替代 `var` |
| 箭头函数 | ✅ 可用，但 `$.image()` / `$.lazyRule()` 回调必须用 `function(){}` |
| 模板字符串 | ✅ 可用 |
| 解构赋值 | ✅ 可用 |
| 扩展运算符 `...` | ✅ 可用 |
| 方法简写 `fn() {}` | ❌ 禁止，必须写 `fn: function() {}` |
| `async` / `await` | ❌ 禁止 |
| 可选链 `?.` / 空值合并 `??` | ⚠️ 未验证，建议回避 |

> 📌 **唯一不变的约束**：`$.image()`、`$.lazyRule()`、`$.rule()` 的回调函数必须用 `function(){}`，不能用箭头函数，否则 `input` / `MY_URL` 等注入变量为 `undefined`（Law 68 补充）。

---

*手册补充版本 V3.47 · 第五十二章：2026年4月最新实测——ES6 兼容性全面验证与内置函数清单*

---

# 第五十三章：吃瓜源与聚合源实战踩坑——手册未覆盖的隐性铁律

本章基于麻豆天天转、007吃瓜、Supjav 三个站点的完整开发过程提炼，记录手册前述章节未覆盖、只能靠实战摔出来的坑点。

---

## 53.1 图片加密——两种主流加密模式对照

图集/吃瓜站点的图片加密通常是以下两种之一，症状相同（图片空白或乱码），但解密方式完全不同，必须先从站点 JS 源码中判断类型。

| 类型 | 特征 | 代表站点 | 解密方式 |
|---|---|---|---|
| AES 加密 | 站点 JS 含 `CryptoJS.AES.decrypt` 或 `key`/`iv` 配置 | 007吃瓜 | `$.image()` + `CryptoUtil.AES.decrypt`（第十章）|
| XOR 加密 | 站点 JS 含固定密钥字符串 + 字节循环异或 | 麻豆天天转 | `$.image()` + `FileUtil.toBytes` + 手动循环异或 |

**XOR 解密标准模板**（密钥需从站点 JS 中提取）：

```javascript
_getImageDecrypt: function() {
    return $('').image(function() {
        try {
            var FileUtil = com.example.hikerview.utils.FileUtil;
            var bytes = FileUtil.toBytes(input);  // input 是 InputStream（Law 92）
            if (!bytes || bytes.length < 10) return input;

            var key    = '2019ysapp7527';  // ← 从站点 JS 中提取
            var keyLen = key.length;
            var limit  = Math.min(bytes.length, 100);  // 只需解密前 N 字节

            for (var i = 0; i < limit; i++) {
                var res  = (bytes[i] ^ key.charCodeAt(i % keyLen)) & 0xff;
                bytes[i] = res > 127 ? res - 256 : res;
            }
            return FileUtil.toInputStream(bytes);
        } catch(e) { return input; }
    });
},
```

> 📌 **如何确认是 XOR 还是 AES**：在浏览器 DevTools Sources 中搜索 `^` 运算符配合循环 + 固定字符串 → XOR；搜索 `CryptoJS` 或 `AES` → AES。

---

## 53.2 视频地址藏在 JSON-LD 结构化数据中

部分站点（WordPress 系）不把视频直链写在 HTML 标签属性里，而是放在 `<script type="application/ld+json">` 的 `VideoObject.contentUrl` 字段中。正则匹配 `.m3u8` 会找不到，需要先解析 JSON-LD。

```javascript
// ✅ 优先从 JSON-LD 提取（比正则更准确）
var videoUrl = '';
var jsonLdBlocks = html.match(/<script[^>]*type="application\/ld\+json"[^>]*>([\s\S]*?)<\/script>/gi);
if (jsonLdBlocks) {
    for (var j = 0; j < jsonLdBlocks.length; j++) {
        try {
            var jsonStr = jsonLdBlocks[j]
                .replace(/<script[^>]*>/, '')
                .replace(/<\/script>/, '');
            var obj = JSON.parse(jsonStr);
            // 直接对象
            if (obj['@type'] === 'VideoObject' && obj.contentUrl) {
                videoUrl = obj.contentUrl; break;
            }
            // @graph 数组
            if (obj['@graph']) {
                for (var g = 0; g < obj['@graph'].length; g++) {
                    if (obj['@graph'][g]['@type'] === 'VideoObject' && obj['@graph'][g].contentUrl) {
                        videoUrl = obj['@graph'][g].contentUrl; break;
                    }
                }
                if (videoUrl) break;
            }
        } catch(e) {}
    }
}

// 兜底：正则匹配 m3u8
if (!videoUrl) {
    var m = html.match(/https?:[^"'\s]+\.m3u8[^"'\s]*/);
    if (m) videoUrl = m[0].replace(/&amp;/g, '&');
}

// 相对路径补全
if (videoUrl && videoUrl.indexOf('http') !== 0) {
    videoUrl = host + '/h5/m3u8/' + videoUrl.replace(/^\//, '');
}
```

---

## 53.3 搜索页列表结构与列表页不同——选择器不能复用

同一站点的搜索结果页，HTML 结构可能与分类列表页不同。麻豆天天转的分类页列表项是 `div.videoList > article`，但搜索页是 `div.videoList > a`（直接是链接标签）。

```javascript
// ✅ 公共解析方法中同时尝试两种选择器
_parseArticleList: function(html, host, imgDecrypt) {
    var d = [];
    // 先试 article（分类页结构）
    var items = pdfa(html, 'div.videoList&&article');
    // 没有则试直接 a（搜索页结构）
    if (items.length === 0) items = pdfa(html, 'div.videoList&&a');

    for (var j = 0; j < items.length; j++) {
        // ...
        // ✅ pd 第三参数传 host，自动补全相对路径（Law 3 精神）
        var link = pd(items[j], 'a&&href', host) || pd(items[j], 'self&&href', host);
        // ...
    }
    return d;
},
```

> **⚠️ Law 102（新增）：不同页面类型（列表页、搜索页、标签页）的 HTML 结构可能不同，不能假设选择器通用。公共解析方法应按优先级依次尝试多个选择器，第一个非空结果即停。**

---

## 53.4 详情页多余内容过滤

吃瓜站的详情页正文里通常夹杂广告段落、免责声明、导航链接，不加过滤会把这些内容也渲染到规则里。

```javascript
// ✅ 文字段落过滤模板（根据实际站点调整关键词列表）
var _FILTER_KEYWORDS = [
    '最新地址', '温馨提示', '关键词：', '版权所有', '免责声明',
    '点击进入', '马上注册', '立即下载', '加入我们', '友情链接'
];

var _shouldFilter = function(text) {
    if (!text || text.trim().length < 10) return true;  // 过短
    for (var fi = 0; fi < _FILTER_KEYWORDS.length; fi++) {
        if (text.indexOf(_FILTER_KEYWORDS[fi]) > -1) return true;
    }
    return false;
};

// 在段落渲染时使用
var pText = pdfh(child, 'Text') || '';
if (!_shouldFilter(pText)) {
    d.push({ title: pText.trim(), col_type: 'rich_text' });
}
```

---

## 53.5 flex_button 导致自动翻页失效

**现象**：列表页用了 `flex_button` 显示分类标签，滚动到底部不触发下一页加载，自动翻页永久失效。

**原因**：引擎在计算列表是否到达底部时，无法正确识别 `flex_button` 的高度，导致触发条件永远不满足。

```javascript
// ❌ 错误：列表项用 flex_button，翻页失效
d.push({ title: tagName, col_type: 'flex_button', url: link });

// ✅ 正确：列表项改用 text_2 / movie_2 / movie_3，翻页正常
d.push({ title: tagName, col_type: 'text_2', url: link });
```

> **⚠️ Law 103（新增）：`flex_button` 只能用于纯导航按钮（不依赖翻页的场景）。凡是需要依赖引擎自动翻页加载下一页的列表，列表项必须使用 `movie_*`、`text_*` 等标准列表 col_type，禁止使用 `flex_button`。**

---

## 53.6 聚合页翻页——正则区分首页与子列表页

聚合页（如演员库首页 `/zh/cast/`）和子列表页（如某演员的视频列表 `/zh/cast/actress-name/`）URL 结构相似，翻页后容易互相误判，渲染出错误内容。

```javascript
// ✅ 用正则精确判断当前是聚合首页还是子列表页
var isAggIndex = (function(url) {
    // 匹配：/zh/maker、/zh/cast、/zh/tag（含可选的 /page/N/）
    if (/\/zh\/maker(\/page\/\d+)?\/?$/.test(url)) return true;
    if (/\/zh\/cast(\/page\/\d+)?\/?$/.test(url))  return true;
    if (/\/zh\/tag(\/page\/\d+)?\/?$/.test(url))   return true;
    return false;
})(requestUrl);

if (isAggIndex) {
    // 渲染分类网格
} else {
    // 渲染视频卡片列表
}
```

> **⚠️ Law 104（新增）：聚合页（索引页）翻页后 URL 会变为 `/path/page/N/`，必须在正则中同时匹配原始路径和翻页路径，否则翻页后 `isAggIndex` 变为 `false`，页面类型误判渲染成视频列表。**

---

## 53.7 直接返回 .m3u8 vs video:// 协议的选择

**手册默认写法**：`return 'video://' + m3u8Url + ';{Referer@' + host + '/}'`

**实战发现**：部分站点的 m3u8 链接已经包含鉴权参数，加上 `video://` 前缀后某些播放器反而无法识别，去掉前缀直接返回纯链接反而正常。

```javascript
// 标准写法（多数情况正确）
return 'video://' + playUrl + ';{Referer@' + host + '/}';

// 纯净写法（当上面失效时降级尝试）
return playUrl;  // 直接返回完整 https:// 链接
```

**判断原则**：

| 情况 | 推荐写法 |
|---|---|
| 需要带 Referer / UA 才能播放 | `video://url;{Referer@host/}` |
| 链接已含鉴权参数（`?auth=xxx&token=xxx`）且加前缀后失效 | 直接返回 `url` |
| m3u8 来自第三方 CDN，无防盗链 | 两种均可，优先带 Referer |

> **⚠️ Law 105（新增）：`video://` 前缀不是万能的。当播放器无法识别带前缀的链接时，降级尝试直接返回纯 `https://` 链接。优先添加 `Referer` 和 `User-Agent`，在确认不需要时才去掉。**

---

## 53.8 CF 过盾后禁止提前 return

**现象**：CF 检测到拦截后，只推入过盾按钮然后 `return d`，自动翻页永久失效。

**原因**：引擎收到只含一个按钮的列表后，认为当前页已渲染完毕，记录页面高度。过盾后即使刷新，引擎已不会为其计算下一页，翻页功能彻底失效。

```javascript
// ❌ 错误：提前 return，翻页失效
if (html.indexOf('cf-browser-verification') > -1) {
    d.push({ title: '🛡️ 点击过盾', url: cfUrl, col_type: 'text_center_1' });
    return d;  // ← 这行导致翻页永久失效
}

// ✅ 正确：不提前 return，继续走完整个函数
if (html.indexOf('cf-browser-verification') > -1) {
    d.push({ title: '🛡️ 点击过盾', url: cfUrl, col_type: 'text_center_1' });
    // 不 return，让函数继续执行（后续解析会因 html 内容不足而返回空列表，但结构完整）
}
// 或者过盾成功后 refreshPage(false) 触发重新加载
```

> **⚠️ Law 106（新增）：CF 过盾检测到拦截时，禁止提前 `return`。应推入过盾按钮后继续执行完整函数流程（后续解析自然为空），保证引擎能正确计算页面完整性，不破坏翻页状态。**

---

## 53.9 本章铁律汇总

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 102 | 选择器不能跨页面复用 | 列表页、搜索页、标签页结构可能不同，公共方法应按优先级依次尝试多个选择器 | 一个选择器打天下 = 必翻 |
| Law 103 | flex_button 禁用于翻页列表 | 凡依赖自动翻页的列表，项目必须用 `movie_*`/`text_*`，`flex_button` 导致翻页失效 | flex_button = 翻页杀手 |
| Law 104 | 聚合页翻页用正则区分 | 聚合首页翻页后 URL 变化，必须在正则中匹配原始路径和翻页路径 | 正则加 `/page/\\d+` 分支 |
| Law 105 | 纯净链接降级 video:// | 链接含鉴权参数且 `video://` 前缀导致播放失败时，降级直接返回 `https://` 链接 | 加前缀失败 → 去前缀 |
| Law 106 | CF 过盾禁止提前 return | 检测到 CF 拦截后不能提前 return，否则引擎记录不完整页面高度，翻页永久失效 | 过盾按钮推完继续走，不能 return |

---

*手册补充版本 V3.47 · 第五十三章：吃瓜源与聚合源实战铁律（麻豆天天转/007吃瓜/Supjav）*

---

# 第五十四章：不走二级的列表直播模式——$.lazyRule 参数传递完全指南

本章基于笔盒规则与 dy2020 规则的实战提炼，系统补全手册前述章节对 `$.lazyRule` 不走二级播放模式的描述空白与错误。

> 📌 `$.lazyRule` 的基础参考见**第 26 章 `$ 工具函数完全参考`**（4 大方法对照表 + 基础用法）；本章深入闭包陷阱、参数传递、组合写法等进阶话题。如需把壳子的 `$.toString()` 改写成函数定义，见第 45.10 节。

---

## 54.1 两种播放触发路径对比

手册前述章节默认视频类规则流程为"列表 → 二级函数 → return video://"。但实战中，当视频 URL 可在列表阶段直接获取（API 直接返回 m3u8、详情页只需一次 fetch 即可拿到播放链接）时，应优先使用**不走二级的直播模式**，减少一次页面跳转。

| 模式 | 触发路径 | 适用场景 |
|---|---|---|
| 标准二级模式 | 列表项 url → 二级函数 → `return video://` | 详情页结构复杂、需要渲染剧集列表、选线 |
| **列表直播模式** | 列表项 url = `$.lazyRule(fn, ...)` → 回调直接 `return video://` | API 直接返回 m3u8、或详情页一次 fetch 即可拿到播放链接 |

> ⚠️ **Law 107（新增）：当列表阶段已能获取或一次 fetch 即可拿到播放链接时，优先使用 `$.lazyRule` 直播模式，不要为此单独实现二级函数。二级函数保留给需要渲染剧集列表或多线路选择的场景。**

---

## 54.2 $.lazyRule 核心语法与参数传递

### 完整语法

```javascript
url: $(seed).lazyRule(function(param1, param2, ...) {
    // 此处是独立沙箱，外部变量全部不可见
    // input === seed（$(seed) 中传入的字符串）
    return "video://...";
}, arg1, arg2, ...)
// arg1, arg2... 在调用时被求值，传入回调作为 param1, param2...
```

### `$(seed)` 三种写法的语义

| 写法 | input 值 | 附加效果 | 适用场景 |
|---|---|---|---|
| `$('').lazyRule(fn, a, b)` | 空字符串 | 无 | 回调不需要读 input，参数全靠显式传入 |
| `$(url).lazyRule(fn, url, a)` | url 字符串 | 无 | url 同时作为 input 和第一个显式参数传入（双重保险） |
| `$('#noLoading#').lazyRule(fn, a, b)` | `'#noLoading#'` | 点击时不显示 loading 弹窗 | 筛选按钮、状态切换按钮 |

> ⚠️ **Law 108（新增）：`$(x).lazyRule(fn, a, b)` 中，`x` 成为回调里的 `input`，但 `x` 不会自动作为 `fn` 的第一个参数。`a` 才是 `fn` 的第一个参数。需要在回调中使用 url 时，必须同时在 `$()` 和显式参数列表中传入。**

---

## 54.3 闭包陷阱与逃逸方法

`$.lazyRule` 的回调在独立沙箱中执行，**外部作用域的所有变量（包括循环变量、`this`、闭包引用）全部不可见**。

### ❌ 错误写法（闭包引用外部变量）

```javascript
var host = this.host;
var ua = this.UA;
for (var i = 0; i < list.length; i++) {
    var rawUrl = list[i].vodPreview;
    d.push({
        url: $('').lazyRule(function() {
            // ❌ rawUrl、host、ua 在沙箱中均为 undefined
            return "video://" + rawUrl + ";{Referer@" + host + "/}";
        })
    });
}
```

### ✅ 正确写法（显式传参逃逸）

```javascript
var host = this.host;
var ua = this.UA;
for (var i = 0; i < list.length; i++) {
    var rawUrl = list[i].vodPreview || "";
    d.push({
        url: $(rawUrl).lazyRule(function(m3u8Url, h, u) {
            // ✅ 参数在外部被求值后传入，沙箱内完全可用
            return "video://" + m3u8Url + ";{User-Agent@" + u + "&&Referer@" + h + "/}";
        }, rawUrl, host, ua)
        //   ↑ 逐一传入，每次循环独立求值，无闭包陷阱
    });
}
```

> 📌 **口诀：外部变量进沙箱，必须从参数传。不传就是 undefined，多传不如传够。**
>
> ⚠️ **Law 109（新增）：`$.lazyRule` 回调是独立沙箱，禁止在回调内直接引用外部变量（包括 `this`、循环变量、上层函数局部变量）。所有需要的值必须通过显式参数列表传入，在外部被求值后注入回调。**

---

## 54.4 详情页内联 fetch 模式（一次 fetch 拿播放链接）

当列表项的 url 指向详情页，需要 fetch 一次才能拿到 m3u8 时，完整模板如下：

```javascript
d.push({
    title: title,
    pic_url: picUrl,
    col_type: 'movie_3',
    url: $(detailUrl).lazyRule(function(pUrl, ua, host) {
        var html = fetch(pUrl, {
            headers: { 'User-Agent': ua, 'Referer': host + '/' }
        });
        if (!html) return pUrl + '#嗅探';

        // 优先：url=xxx.m3u8 模式
        var m = html.match(/url=(http[^"'\s]+\.m3u8[^"'\s]*)/);
        if (m) return 'video://' + m[1].replace(/\\/g, '') +
                      ';{User-Agent@' + ua + '&&Referer@' + host + '/}';

        // 次选：直接匹配 m3u8/mp4/flv 链接
        m = html.match(/https?:[^"'\s]+\.(m3u8|mp4|flv)[^"'\s]*/);
        if (m) return 'video://' + m[0] + ';{User-Agent@' + ua + '}';

        // 再次：var url = '...' 赋值模式
        var js = html.match(/var\s+url\s*=\s*['"]([^'"]+)['"]/);
        if (js && js[1].indexOf('http') === 0) return 'video://' + js[1];

        // 兜底：嗅探
        return pUrl + '#嗅探';
    }, detailUrl, this.UA, this.host)
});
```

> ⚠️ **Law 110（新增）：内联 fetch 模式中，正则匹配应按优先级依次降级：`url=xxx.m3u8` → 直接链接正则 → JS 变量赋值 → 嗅探兜底。嗅探兜底必须保留，防止三种正则全部失配时返回空白播放。**

---

## 54.5 动态 token 翻页模式（手动维护翻页链）

手册第二章只讲了 `fypage` 占位符自动翻页。但部分站点的翻页 URL 不是简单页码递增，而是由当前页 HTML 中的 JS 变量动态给出（token 型分页）。此时必须手动用 `putMyVar` 维护翻页链。

```javascript
主页: function() {
    var d = [];
    var host = this.host;
    var KEY = 'sitename_pageurl';  // 用纯字母前缀，避免含 :// 的 key 报错

    var html = '';
    if (MY_PAGE == 1) {
        html = fetch(host + '/path/to/list');
        // 从 html 提取下一页 token 并存储
        var m = html.match(/let\s+url\s*=\s*"(.*?)"/);
        if (m && m[1]) {
            putMyVar(KEY, m[1]);
            html = fetch(host + m[1]);  // 第一页实际内容在 token url
        }
    } else {
        var pageUrl = getMyVar(KEY, '');
        if (!pageUrl) return d;  // token 丢失，停止翻页
        html = fetch(host + pageUrl);
        // 更新 token 为下一页
        var next = html.match(/let\s+url\s*=\s*"(.*?)"/);
        putMyVar(KEY, next ? next[1] : '');
    }

    // 正常解析 html 并 push 列表项...
    return d;
}
```

> ⚠️ **Law 111（新增）：token 型分页站点（翻页 URL 由当前页 HTML 动态给出，不含页码数字）必须用 `putMyVar` 手动维护翻页链。`getMyVar` 的 key 禁止含 `://`，统一用纯字母+下划线前缀命名（如 `sitename_pageurl`）。token 为空时立即 `return d`，防止死循环。**

---

## 54.6 `$('#noLoading#').lazyRule` 组合写法（状态切换按钮标准模板）

手册第四章列出了 `#noLoading#` 标签，但未提供与 `$.lazyRule` 的组合示例。实战中筛选按钮/状态切换按钮的标准写法：

```javascript
// 分类切换按钮标准模板
res.push({
    title: isSelected ? '✦ ' + name + ' ✦' : name,
    col_type: 'scroll_button',
    url: $('#noLoading#').lazyRule(function(key, val, h) {
        putMyVar(h + key, val);
        // 切换父分类时，同时重置子分类
        if (key === '_type') {
            putMyVar(h + '_sub', '');
        }
        refreshPage(false);
        return 'hiker://empty';
    }, '_type', typeId, hostKey)
    // ↑ hostKey 是 host.replace(/https?:\/\//, '') 的结果，作命名空间
});
```

> 📌 **`#noLoading#` 放在 `$()` 里而非 url 字符串末尾，是因为 lazyRule 本身会替换整个 url。标签必须作为 `$()` 的参数传入，不能拼接在 return 值里。**

---

## 54.7 本章铁律汇总

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 107 | 优先列表直播 | API 直接返回播放链接或一次 fetch 可得时，用 lazyRule 直播，不走二级 | 能直播就不跳二级 |
| Law 108 | `$(x)` 不等于参数 | `$(x)` 中的 x 是 input，不自动成为回调第一个参数，需显式重传 | x 是 input，不是参数 |
| Law 109 | 沙箱禁用外部变量 | lazyRule 回调是独立沙箱，所有外部变量必须通过显式参数传入 | 外部变量进沙箱，必须从参数传 |
| Law 110 | 内联 fetch 四级降级 | url= 正则 → 直链正则 → JS变量 → 嗅探兜底，四级依次降级 | 正则失配必须有兜底嗅探 |
| Law 111 | token 翻页手动维护 | token 型分页用 putMyVar 维护翻页链，key 禁含 `://`，token 空立即 return | token 空就停，不能硬翻 |

## 54.8 `$.toString` vs `$().lazyRule` ——聚阅环境下的可靠性差异

两者都能序列化函数在点击时执行，但在**旧版聚阅引擎**里行为有差异：

| 维度 | `$.toString(fn, ...args)` | `$().lazyRule(fn, ...args)` |
|---|---|---|
| 触发方式 | 生成字符串，用于 `url:` 字段直接赋值 | 生成字符串，用于 `url:` 字段 |
| 聚阅兼容性 | ✅ 更可靠，旧版引擎均支持 | 部分旧版偶发执行失败 |
| 参数序列化 | 显式传参，和 lazyRule 相同 | 显式传参 |
| 典型用途 | `putMyVar + refreshPage` 状态切换 | 同上，但可返回 URL/协议 |
| 返回值 | 无（只执行副作用）| 返回字符串作为新 URL |

```javascript
// ── $.toString：只做副作用（putMyVar + refreshPage），不返回 URL ──
url: $.toString(function(key, id) {
    putMyVar(key, id);
    refreshPage(false);
}, 'c1', data.id)

// ── $().lazyRule：需要返回 URL/协议时使用 ──────────────────────
url: $(link).lazyRule(function(ua) {
    var html = fetch(input, { headers: { 'User-Agent': ua } });
    return 'pics://' + extractImgs(html).join('&&');
}, MOBILE_UA)
```

> 📌 状态切换按钮（分类、排序、展开折叠）优先用 `$.toString`，在旧版聚阅下更稳定。需要返回协议字符串（`pics://`/`video://`）时才用 `$().lazyRule`。两者在新版引擎下等价。

---

*手册补充版本 V3.48 · 第五十四章：不走二级的列表直播模式——$.lazyRule 参数传递完全指南*

---

# 第五十五章：多线路链接提取、组合与解析函数协作完全指南

本章基于 Supjav 规则完整开发过程提炼，覆盖"详情页多线路按钮提取 → 中间 API 解析 → 第三方托管服务器适配 → `JSON.stringify` 多线路组合返回 → 解析函数协作"的完整链路，以及手册前述章节的空白与错误点。

---

## 55.1 多线路的核心数据流

多线路播放的完整链路分四个阶段：

```
① 详情页 HTML
   └─ 提取多个线路按钮（name + data-link）
         ↓
② 中间 API（每个 data-link 独立请求）
   └─ 返回重定向，解析 Location header → 第三方服务器 URL
         ↓
③ 第三方服务器适配（turbovid / fc2stream / streamtape / voe …）
   └─ 各服务器有专属解析逻辑，最终拿到 m3u8 / mp4 直链
         ↓
④ 组合返回
   └─ JSON.stringify({ urls: [...], names: [...] })
      引擎自动渲染为多线路选择器
```

> ⚠️ **手册空白**：手册第七章（解析函数）只讲了单 URL `return "video://..."` 的写法，完全未涉及多线路 `JSON.stringify` 返回格式，也未说明与 `$.lazyRule` 的组合用法。

---

## 55.2 详情页线路按钮提取

### 标准 HTML 结构（`.btns` 型）

```html
<div class="btns">
  <div class="btnst">
    <a data-link="a1b2c3d4e5f6" >SERVER 1</a>
    <a data-link="f6e5d4c3b2a1" >SERVER 2</a>
  </div>
</div>
```

### 提取代码

```javascript
var serverBtns = pdfa(html, '.btns&&.btnst&&a');
// 兜底：部分站点无 .btnst 层级
if (serverBtns.length === 0) serverBtns = pdfa(html, '.btns&&a');

var urls  = [];
var names = [];

for (var i = 0; i < serverBtns.length; i++) {
    var btn      = serverBtns[i];
    var lineName = pdfh(btn, 'Text');
    var dataLink = pdfh(btn, 'a&&data-link');

    // ✅ 过滤无效项：无 data-link、无名称、纯标题行
    if (!dataLink || !lineName || lineName === 'SERVER :') continue;

    // ... 后续解析
}
```

> ⚠️ **Law 112（新增）：提取线路按钮时必须同时过滤三类无效项：`data-link` 为空、`lineName` 为空、`lineName` 等于纯标题文字（如 "SERVER :"）。任何一项不过滤都会导致后续 API 请求报错或推入空线路。**

---

## 55.3 中间 API 解析——`onlyHeaders` 获取重定向目标

部分站点的线路 URL 不直接写在 HTML 中，而是通过一个中间 API 返回 302 重定向，真正的第三方服务器 URL 在 `Location` header 里。

```javascript
// data-link 是混淆 id，需先逆序还原
var id     = dataLink.split('').reverse().join('');
var apiUrl = "https://lk1.supremejav.com/supjav.php?c=" + id;
var referer = "https://lk1.supremejav.com/supjav.php?l=" + dataLink;

// ✅ onlyHeaders:true —— 只取响应头，不下载正文，性能更好
var location = JSON.parse(fetch(apiUrl, {
    headers: { "Referer": referer, "User-Agent": ua },
    onlyHeaders: true
})).url.replace(/#.*/, "");  // 去掉 hash 片段
```

> ⚠️ **手册遗漏**：手册第三章 `fetch` 参数表中列出了 `withHeaders` 但没有列出 `onlyHeaders`。两者区别如下：

| 参数 | 返回内容 | 适用场景 |
|---|---|---|
| `withHeaders: true` | 正文 + 响应头（JSON 字符串） | 需要同时读取响应头和正文内容 |
| `onlyHeaders: true` | 仅响应头（JSON 字符串） | 只需追踪重定向目标 URL，不需要正文 |

```javascript
// withHeaders 返回结构示例
var res = JSON.parse(fetch(url, { withHeaders: true }));
var body    = res.body;           // 正文字符串
var headers = res.headers;        // 响应头对象

// onlyHeaders 返回结构示例
var res = JSON.parse(fetch(url, { onlyHeaders: true }));
var redirectUrl = res.url;        // 最终落地 URL（追踪重定向后）
```

> ⚠️ **Law 113（新增）：追踪重定向目标 URL 时，使用 `onlyHeaders: true` 而非 `withHeaders: true`。`onlyHeaders` 不下载响应正文，性能更好，且 `res.url` 直接是重定向链追踪后的最终 URL，无需手动处理 `Location` 字段。**

---

## 55.4 第三方托管服务器适配——多分支解析模板

拿到 `location`（第三方服务器 URL）后，需要根据域名分支处理。不同服务器的播放链接提取方式完全不同：

```javascript
var playUrl = "";

// ── 分支1：turbovid / emturbovid ──────────────────────────────
if (/turbovid|emturbovid/.test(location)) {
    var tvHtml = fetch(location, { headers: { 'Referer': referer, 'User-Agent': ua } });
    var match  = tvHtml.match(/https?.*?\.m3u8/);
    if (match) playUrl = match[0];
}

// ── 分支2：fc2stream / cindyeyefinal ─────────────────────────
else if (/cindyeyefinal|fc2stream/.test(location)) {
    var fHtml  = fetch(location, { headers: { 'Referer': referer, 'User-Agent': ua } });
    var script = fHtml.match(/eval([\s\S]+?)<\/script/);
    if (script) {
        var uas  = eval(script[1]);          // eval 解混淆
        var link = uas.match(/var links[^\;]+/);
        if (link) {
            eval(link[0]);                   // 执行后 links 对象可用
            // 优先 hls4（最高画质），降级 hls3/hls2
            playUrl = links.hls4
                ? "https://xenolyzb.com" + links.hls4
                : (links.hls3 || links.hls2 || "");
        }
    }
}

// ── 分支3：streamtape ─────────────────────────────────────────
else if (/streamtape/.test(location)) {
    var stHtml  = fetch(location, { headers: { 'Referer': referer, 'User-Agent': ua } });
    var pattern = stHtml.match(/\('#(.*)'\)/);
    if (pattern) {
        var srcMatch = stHtml.match(new RegExp("'" + pattern[1] + ".*?=([^;]+)"));
        if (srcMatch) {
            eval("var srclink = " + srcMatch[1]);
            var link2 = "https:" + srclink + "&stream=1";
            // 再次用 onlyHeaders 追踪最终流地址
            playUrl = JSON.parse(fetch(link2, {
                headers: { "Referer": location, "User-Agent": ua },
                onlyHeaders: true
            })).url;
        }
    }
}

// ── 分支4：voe（直接可用，无需二次解析）────────────────────────
else if (/voe/.test(location)) {
    playUrl = location;
}

// ── 收集结果 ──────────────────────────────────────────────────
if (playUrl) {
    urls.push(playUrl);
    names.push(lineName);
}
```

> 📌 **各服务器特征速查：**

| 服务器域名特征 | 链接提取方式 | 说明 |
|---|---|---|
| `turbovid` / `emturbovid` | 正则匹配 `.m3u8` | 直接在 HTML 源码里找 m3u8 |
| `cindyeyefinal` / `fc2stream` | `eval` 解混淆 + `links.hls*` 对象 | JS 混淆，需两次 eval；优先 hls4 |
| `streamtape` | DOM id 逆向 + `onlyHeaders` 追踪 | 构造流 URL 后再追踪一次重定向 |
| `voe` | 直接使用 location | 无需二次解析 |

> ⚠️ **Law 114（新增）：第三方托管服务器必须用域名正则 `/域名特征/.test(location)` 分支处理，禁止用字符串 `indexOf`（因为 URL 中域名位置不固定，可能含子域名前缀）。每个分支必须独立 `try/catch`，单条线路解析失败不能阻断其他线路。**

---

## 55.5 多线路组合返回格式

所有线路收集完毕后，使用 `JSON.stringify` 返回，引擎会自动渲染为多线路选择器：

```javascript
// ✅ 有线路时：多线路格式
if (urls.length > 0) {
    return JSON.stringify({ urls: urls, names: names });
}

// ✅ 无线路时：兜底嗅探（不能直接 return ""）
return detailUrl + "#嗅探";
```

**`JSON.stringify` 多线路返回的结构规范：**

```javascript
// urls 和 names 必须等长，一一对应
{
    urls:  ["https://cdn1.example.com/xxx.m3u8", "https://cdn2.example.com/yyy.m3u8"],
    names: ["SERVER 1",                           "SERVER 2"]
}
```

> ⚠️ **Law 115（新增）：多线路 `JSON.stringify` 返回时，`urls` 数组内的每个元素必须是纯播放链接（`https://...`），不加 `video://` 前缀。引擎在渲染多线路选择器时会自动处理协议前缀。若加了 `video://` 前缀，部分播放器无法识别。**

> ⚠️ **Law 116（新增）：`urls` 与 `names` 数组长度必须严格一一对应。收集时必须同步 push：`urls.push(playUrl); names.push(lineName);`，禁止分开收集后再合并，防止因某条线路解析失败导致下标错位。**

---

## 55.6 兜底 iframe 线路提取（主线路全部失败时）

当 `.btns` 按钮全部解析失败（`urls.length === 0`）时，部分站点有一个 iframe 嵌入的备用线路，可作二级兜底：

```javascript
if (urls.length === 0) {
    var iframeSrc = pdfh(html, '#dz_video iframe&&src');
    if (iframeSrc) {
        // 从 iframe src 的查询参数中提取 data-link
        var lMatch = iframeSrc.match(/l=([a-f0-9]+)/i);
        if (lMatch) {
            var dataLink2 = lMatch[1];
            try {
                var id2      = dataLink2.split('').reverse().join('');
                var apiUrl2  = "https://lk1.supremejav.com/supjav.php?c=" + id2;
                var ref2     = "https://lk1.supremejav.com/supjav.php?l=" + dataLink2;

                var loc2 = JSON.parse(fetch(apiUrl2, {
                    headers: { "Referer": ref2, "User-Agent": ua },
                    onlyHeaders: true
                })).url.replace(/#.*/, "");

                if (/turbovid|emturbovid/.test(loc2)) {
                    var p2 = fetch(loc2, { headers: { 'Referer': ref2, 'User-Agent': ua } });
                    var m2 = p2.match(/https?.*?\.m3u8/);
                    if (m2) {
                        urls.push(m2[0]);
                        names.push("默认");
                    }
                }
            } catch(e) { log("iframe 兜底失败: " + e); }
        }
    }
}
```

> ⚠️ **Law 117（新增）：多线路提取应设置两级兜底：第一级是按钮列表（`.btns` 型）；第二级是 iframe 嵌入备用线路。两级都失败时才降级到 `detailUrl + "#嗅探"`。嗅探是最后兜底，不是默认策略。**

---

## 55.7 `$.lazyRule` 与解析函数的分工

多线路提取通常在 `$.lazyRule` 回调中完成（不走二级），但 `解析` 函数也可以承接同样的逻辑。两者分工如下：

| 场景 | 推荐方式 | 原因 |
|---|---|---|
| 列表页直接点击播放，无剧集选择 | `$.lazyRule` 内联完成 | 不需要二级页面，交互更流畅 |
| 需要先展示剧集列表再选线路 | 二级函数 + `解析` 函数 | 剧集列表需要独立页面渲染 |
| 解析函数作为通用入口（被外部调用） | `解析` 函数 | 支持被其他规则或嗅探框架调用 |

**`解析` 函数承接多线路的写法：**

```javascript
解析: function(url) {
    var ua   = this.UA;
    var host = this.host;

    return $(url).lazyRule(function(playUrl, ua, host) {
        var html = fetch(playUrl, {
            headers: { 'User-Agent': ua, 'Referer': host + '/' }
        });

        var serverBtns = pdfa(html, '.btns&&.btnst&&a');
        var urls = [], names = [];

        for (var i = 0; i < serverBtns.length; i++) {
            // ... 同 55.3~55.5 的提取逻辑
        }

        if (urls.length > 0) return JSON.stringify({ urls: urls, names: names });
        return playUrl + '#嗅探';
    }, url, ua, host);
}
```

> ⚠️ **Law 118（新增）：`解析` 函数内部如需网络请求，必须用 `$(url).lazyRule(fn, ...)` 包裹，不能直接在 `解析` 函数体内 `fetch`。`解析` 函数是同步执行的，直接 `fetch` 会阻塞主线程；`$.lazyRule` 保证在独立线程异步执行。**

---

## 55.8 Cookie 在 lazyRule 沙箱中的传递

`$.lazyRule` 沙箱中无法访问 `this`，因此 `juItem.get`/`juItem.set` 等持久化函数需要特别处理：

```javascript
// ✅ 正确：在外部读取 cookie，通过参数传入沙箱
url: $(plink).lazyRule(function(ua, ck) {
    var headers = { "User-Agent": ua, "Referer": "https://example.com/" };
    if (ck) headers["Cookie"] = ck;
    var html = request(input, { headers: headers });
    // ...
}, this.UA, this._getCookie())  // ← Cookie 在外部读取，作为参数传入
```

```javascript
// ❌ 错误：在沙箱内调用 this._getCookie()（this 不存在）
url: $(plink).lazyRule(function() {
    var ck = this._getCookie();  // ❌ this 是 undefined
})
```

> ⚠️ **Law 119（新增）：`$.lazyRule` 沙箱内无法访问 `this` 和规则的任何方法。需要 Cookie、UA、host 等运行时值时，必须在外部读取后通过参数传入。Cookie 用 `juItem.get` 读取后传参，不要在沙箱内调用 `juItem.get`（沙箱环境下 `juItem.get` 是否可用取决于引擎版本，不可依赖）。**

---

## 55.9 本章铁律汇总

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 112 | 线路按钮三过滤 | data-link 为空、lineName 为空、纯标题行，三者任一必须跳过 | 无link无名无内容，三空必须 continue |
| Law 113 | 追踪重定向用 onlyHeaders | 只需最终 URL 时用 `onlyHeaders:true`，比 `withHeaders` 性能更好 | 追重定向不要正文，onlyHeaders |
| Law 114 | 服务器分支用域名正则 | 第三方服务器用 `/域名/.test(location)` 分支，各分支独立 try/catch | 每个服务器自己的锅自己背 |
| Law 115 | 多线路 urls 不加 video:// | JSON 多线路格式的 urls 数组存纯 https 链接，引擎自动处理协议 | 多线路 urls 是纯链接，不加前缀 |
| Law 116 | urls 与 names 同步 push | 收集时必须同步 push，防止解析失败导致下标错位 | 成对收集，不能分开 |
| Law 117 | 两级兜底再嗅探 | 按钮列表 → iframe 备用 → 嗅探，嗅探是最后手段 | 嗅探是最后一招，不是第一招 |
| Law 118 | 解析函数内 fetch 须用 lazyRule 包裹 | 解析函数同步执行，直接 fetch 阻塞主线程，必须用 lazyRule 异步 | 解析里要 fetch，先包 lazyRule |
| Law 119 | Cookie 沙箱外读取传参 | lazyRule 沙箱内无 this，Cookie/UA/host 必须在外部读取后通过参数传入 | 沙箱没有 this，外部读完再传 |

## 55.10 `fba.*` Cookie 桥接全链路实战

`fba` 桥接对象的**完整 API 列表（30+ 方法）已在第七十一章 fba 桥接 API 完整参考**中分类整理。本节聚焦"Cookie 认证全链路"这一最高频场景，给出 4 阶段标准范式。

> 📌 API 速记：本节用到的 `fba.getCookie(url)` / `fba.putVar(k,v)` / `fba.back()` / `fba.toast(msg)` 均见第 71.1-71.3 节。两个 JS 引擎隔离原则：**`fba.*` 只能在 `extra.js` 沙箱里用，规则主函数里调用会报错**（Law 136）。

**Cookie 认证全链路标准范式（Discuz!/论坛类站点）：**

```javascript
// ── 阶段1：WebView 侧（extra.js 里执行）──────────────────────
// 检测登录态，登录成功后桥接 Cookie
var extra_js = $.toString(function() {
    // 检查登录态标志（按实际站点调整选择器）
    if (document.querySelector('.avatar') || document.cookie.includes('auth=')) {
        var ck = fba.getCookie(location.href);  // 取当前页 Cookie
        fba.putVar('site_ck_temp', ck);         // 写入规则侧临时变量
        fba.toast('登录成功，正在保存...');
        fba.back();
    }
});

// ── 阶段2：规则主函数（主页函数里执行）─────────────────────────
// 从临时变量读取，写入持久存储
var temp_ck = getVar('site_ck_temp', '');
if (temp_ck) {
    juItem.set('site_cookie', temp_ck);  // 持久化，App 重启不丢
    clearVar('site_ck_temp');
}

// ── 阶段3：有效性校验（每次请求前）───────────────────────────
var ck = juItem.get('site_cookie', '');
if (ck) {
    var testHtml = fetch(host + '/profile', { headers: { Cookie: ck } });
    // 双重校验：响应长度 + 关键字
    if (testHtml.length < 3000 || testHtml.indexOf('username') === -1) {
        juItem.set('site_cookie', '');   // 校验失败，清空，触发重新登录
        ck = '';
    }
}

// ── 阶段4：未登录时展示登录入口 ──────────────────────────────
if (!ck) {
    d.push({
        col_type: 'x5_webview_single',
        desc:     '100&&float',
        url:      host + '/login',
        extra:    { js: extra_js }
    });
    return d;
}
```

**关键设计原则：**

| 原则 | 说明 |
|---|---|
| `fba.putVar` 只是临时中转 | 会话级，App 重启丢失；取到后必须立即写入 `juItem.set` |
| 双重校验 | 响应长度阈值 + 关键字，单一条件容易误判（如 302 重定向页也有内容）|
| 校验失败即清空 | 不清空会导致每次都用失效 Cookie 请求，无法触发重新登录流程 |
| `fba.*` 只能在 `extra.js` 里用 | 规则主函数里调用 `fba.*` 会报错，两个 JS 引擎完全隔离 |

## 55.11 `fetchCodeByWebView` 携带 Cookie + JS 执行

手册已记录 `fetchCodeByWebView` 用于 CF 过盾，但它同样支持**携带自定义 Cookie 并启用 JS 执行**来绕过需要登录态才能渲染的内容页：

```javascript
// 携带已登录 Cookie + 启用 JS，用于 JS 渲染后才有内容的详情页
var html = fetchCodeByWebView(url, {
    headers: {
        'User-Agent': MOBILE_UA,
        'Cookie':     juItem.get('site_cookie', '')   // 携带持久化 Cookie
    },
    timeout:           15000,
    javaScriptEnabled: true    // 必须开启，否则 JS 渲染内容取不到
});

// 使用场景：
// 1. 论坛详情页需登录才显示正文（Cookie 从 juItem 取）
// 2. 站点用 JS 动态渲染关键内容（SSR 不完整）
// 3. 需要同时满足"已登录"和"JS执行"两个条件的页面
```

> ⚠️ `javaScriptEnabled: true` 会显著增加请求时间（需等待 JS 执行完成），建议配合较长的 `timeout`（≥10000）。能用 `fetch` 直接拿到内容的不要用 `fetchCodeByWebView`。

---

*手册补充版本 V3.49 · 第五十五章：多线路链接提取、组合与解析函数协作完全指南（Supjav 实战提炼）*

---

# 第五十六章：MissAV 规则开发完全指南

> *基于 MissAV（missav.ai）实战提炼 · 覆盖静态分类、女优/标签二级跳转、surrit.com 解密、多清晰度降级*

---

## 56.1 静态分类 fyclass 模式——URL 是路径字符串，非整数

**问题根因：** `MY_URL` 在 `fyclass` 模式下注入的是 `class_url` 里对应条目的**原始字符串**，不是下标整数。

```javascript
// ❌ 错误：把 MY_URL 当整数处理
var page = parseInt(MY_URL);

// ✅ 正确：MY_URL 就是路径字符串，直接拼接
// class_url 写法示例：
// "/dm265/chinese-subtitle&/dm132/uncensored-leaked&/dm442/4k"
// 当用户选择第一项时 MY_URL === "/dm265/chinese-subtitle"

var url = this.host + MY_URL + "?page=" + MY_PAGE;
```

> ⚠️ **Law 120（新增）：`fyclass` 模式下 `MY_URL` 是 `class_url` 里的原始路径字符串（如 `/dm265/chinese-subtitle`），绝不是数字下标。禁止对 `MY_URL` 做 `parseInt()`，直接拼接到 `host` 后即可。**

**Law 120 补充——`class_url` 写短数字标识 + `_getPath()` 映射的合法例外**

Law 120 禁止对路径字符串做 `parseInt()`，但有一种**刻意设计**的合法用法：当路径包含特殊字符（`&` 等）导致引擎解析出错，或需要在主页函数内根据分类做复杂分流逻辑时，可以主动把 `class_url` 写成短数字标识，再用 `_getPath()` 函数把数字映射回真实路径：

```javascript
// ── 静态分类配置：class_url 写短数字，不写真实路径 ────────────
静态分类: {
    type: '主页', url: 'fyclass',
    class_name: '全部&日本&人气女神&柠檬图库&写真集&下载',
    class_url:  '0&1&2&3&4&5'   // ← 短数字标识，不是真实路径
},

// ── _getPath()：数字映射真实路径（或分流逻辑）──────────────────
_getPath: function() {
    var idx   = parseInt(MY_URL) || 0;   // ← 这里 parseInt 是刻意的
    var paths = [
        '/',                             // 0: 全部
        '/japan/',                       // 1: 日本（视频）
        '/tag/renjinvsheng/',            // 2: 人气女神（标签页）
        '/tag/ninmengtuku/',             // 3: 柠檬图库（标签页）
        '/tag/xiesheng/',                // 4: 写真集
        '/video-and-image-downloads/'   // 5: 下载
    ];
    return paths[idx] || '/';
},

// ── 主页函数：用 _getPath() 取真实路径，再分流 ──────────────────
主页: function() {
    var path = this._getPath();
    var url  = this.host + path;
    var html = fetch(url, { headers: { 'User-Agent': this.UA } });

    // 按路径特征分流渲染逻辑
    if (/tag\//.test(path)) {
        return this._parseTagPage(html);     // 标签云页面
    } else if (/japan|downloads/.test(path)) {
        return this._parseVideoList(html);   // 视频列表
    } else {
        return this._parseImageList(html);   // 图集列表
    }
},
```

**Law 120 的本质与这个例外的区别：**

| | Law 120 禁止的用法 | 合法的短标识用法 |
|---|---|---|
| `class_url` 内容 | 真实路径字符串（`/path/to/page`）| 短数字标识（`0&1&2&3`）|
| `MY_URL` 的值 | 路径字符串，直接拼 host 用 | 数字字符串，需 parseInt 后查表 |
| `parseInt` 结果 | NaN 或错误数字（路径首字符非数字）| 正确索引（0/1/2/3）|
| 设计意图 | 误用，开发者以为 MY_URL 是索引 | 刻意设计，用索引换取分流灵活性 |

> 📌 **判断标准**：如果 `class_url` 里写的是路径（含 `/`），那么 Law 120 适用，直接拼接；如果 `class_url` 里写的是纯数字标识（`0&1&2&3`），那么 `parseInt(MY_URL)` 是合法的索引查表操作。两种用法不可混用。

---

## 56.2 女优/标签二级页面——沙箱模式跳转

**问题根因：** 女优列表和标签页进入后需要渲染**视频列表**，但这些列表页没有"选集"概念，不能走标准二级函数（标准二级会等待剧集选择），必须用沙箱渲染。

```javascript
// ✅ 正确：用 hiker://empty 沙箱跳转，在 erji 函数里调用 _parseVideoItems
{
    title: actressName,
    url: $("hiker://empty##" + link + "?page=fypage").rule($.require("jiekou").parse().erji),
    col_type: "movie_3"
}

// erji 函数骨架
erji: function() {
    var parse = $.require("jiekou").parse();
    var html  = fetch(MY_URL, { headers: { "User-Agent": parse.UA } });
    return parse._parseVideoItems(html);
}
```

> ⚠️ **Law 121（新增）：女优详情页、标签云跳转后的视频列表页，必须用 `$("hiker://empty##" + link).rule(fn)` 沙箱模式渲染，不能走标准二级函数。沙箱内通过 `MY_URL` 获取目标链接，调用公共解析函数渲染视频卡片。**

---

## 56.3 surrit.com UUID 字典解密

MissAV 的播放页视频真实 URL 经过两步混淆：先用数字数组编码，再用字典还原。

```javascript
// 播放页 HTML 中形如：
// var urls = {"encrypted":[12,34,56,...],"m":"..."}
// return p}  ← 这行之后是字典定义

// 步骤1：提取 encrypted 数组和字典
var encMatch  = html.match(/"encrypted"\s*:\s*(\[[^\]]+\])/);
var dictMatch = html.match(/return p\}([\s\S]+?)(?:<\/script|$)/);

var encrypted = JSON.parse(encMatch[1]);    // [12, 34, 56, ...]
var dictStr   = dictMatch[1];

// 步骤2：执行字典定义，建立 index→字符 映射
eval(dictStr);   // 执行后 p 对象可用

// 步骤3：用字典还原真实 URL
var realUrl = encrypted.map(function(idx) { return p[idx] || ""; }).join("");
```

> ⚠️ **Law 122（新增）：surrit.com 的视频 URL 解密分两步：从 `encrypted` 数组取数字索引，再用 `return p}` 之后的字典对象映射还原。正则定位点是 `return p}` 这个固定边界，字典在其后定义。**

---

## 56.4 单线路 video.m3u8 的 Referer 验证

**问题根因：** surrit.com 的 m3u8 文件做了 Referer 白名单校验，必须用视频所在的 surrit.com 页面 URL 作为 Referer，用 missav.ai 会被 403 拦截。

```javascript
// UUID 从 surrit.com 播放页 URL 中提取
// 例：https://surrit.com/abc123-def456/video.m3u8

var uuid    = html.match(/(?:surrit\.com\/)([a-f0-9\-]+)/)[1];
var m3u8Url = "https://surrit.com/" + uuid + "/video.m3u8";
var referer = "https://surrit.com/" + uuid + "/";   // ← 必须是 surrit.com，不能是 missav.ai

return "video://" + m3u8Url + ";{Referer@" + referer + "&&User-Agent@" + this.UA + "}";
```

> ⚠️ **Law 123（新增）：surrit.com 的 m3u8 Referer 必须设为 `https://surrit.com/{uuid}/`，不能用 `missav.ai` 域名。两者都是 MissAV 的域名但 CDN 校验只认 surrit.com 自身。**

---

## 56.5 多清晰度 playlist.m3u8 解析与单线路降级

`video.m3u8` 实际上可能是多清晰度播放列表（`#EXT-X-STREAM-INF`）。多线路 JSON 无法携带自定义 Header（框架限制），因此最终策略是单线路。

```javascript
// video.m3u8 内容示例：
// #EXT-X-STREAM-INF:BANDWIDTH=1000000,RESOLUTION=1280x720
// 720p/video.m3u8
// #EXT-X-STREAM-INF:BANDWIDTH=500000,RESOLUTION=854x480
// 480p/video.m3u8

var m3u8Content = fetch(m3u8Url, {
    headers: { "Referer": referer, "User-Agent": this.UA }
});

var lines  = m3u8Content.split("\n");
var tracks = [];

for (var i = 0; i < lines.length; i++) {
    var line = lines[i].trim();
    if (line.indexOf("#EXT-X-STREAM-INF") === 0) {
        var resMatch = line.match(/RESOLUTION=\d+x(\d+)/);   // 取高度
        var height   = resMatch ? parseInt(resMatch[1]) : 0;
        var trackUrl = (lines[i + 1] || "").trim();
        if (trackUrl && trackUrl.indexOf("#") !== 0) {
            // 相对路径补全
            if (trackUrl.indexOf("http") !== 0) {
                trackUrl = "https://surrit.com/" + uuid + "/" + trackUrl;
            }
            tracks.push({ height: height, url: trackUrl });
        }
    }
}

// 按分辨率降序排列，取最高画质
tracks.sort(function(a, b) { return b.height - a.height; });

// 降级策略：多线路 JSON 不支持携带 Header，统一走单线路
if (tracks.length === 0) return m3u8Url + "#嗅探";
var best = tracks[0].url;
return "video://" + best + ";{Referer@" + referer + "&&User-Agent@" + this.UA + "}";
```

> ⚠️ **Law 124（新增）：MissAV 多清晰度 `playlist.m3u8` 解析后，禁止用多线路 `JSON.stringify` 返回——框架多线路 JSON 不支持携带自定义 Header，全部线路会因缺少 Referer 而 403。正确做法是排序后取最高画质单线路，用 `video://url;{Referer@...}` 格式返回。**

---

## 56.6 多种列表容器——四级降级选择器

MissAV 不同页面（主页 / 搜索结果 / 女优详情）的视频卡片容器 class 不同，需要依次降级：

```javascript
function _parseVideoItems(html) {
    var items = pdfa(html, '.grid&&.group');
    if (items.length === 0) items = pdfa(html, '.thumbnail');
    if (items.length === 0) items = pdfa(html, '.grid-cols-2 .group');
    if (items.length === 0) items = pdfa(html, '.grid .group');

    var d = [];
    for (var i = 0; i < items.length; i++) {
        var item  = items[i];
        var _link = pdfh(item, 'a&&href');
        if (!_link) continue;
        if (_link.indexOf('http') !== 0) _link = this.host + _link;

        // 封面：优先 data-src（懒加载），降级到 src
        var _img = pdfh(item, 'img&&data-src') || pdfh(item, 'img&&src') || '';
        // 补全 // 开头的协议相对 URL
        if (_img.indexOf('//') === 0) _img = 'https:' + _img;

        var _title = pdfh(item, '.text-secondary&&Text') || pdfh(item, 'img&&alt') || '';

        d.push({
            title:    _title,
            pic_url:  _img,
            url:      'video://' + _link,
            col_type: 'movie_3'
        });
    }
    return d;
}
```

> ⚠️ **Law 125（新增）：MissAV 视频卡片选择器需四级降级：`.grid&&.group` → `.thumbnail` → `.grid-cols-2 .group` → `.grid .group`。封面图必须优先取 `data-src`（懒加载属性），`//` 开头的协议相对 URL 必须补全为 `https:`。**

---

## 56.7 页面类型分流——三路正则识别

主页函数需要根据当前路径判断要渲染哪种页面：

```javascript
主页: function() {
    var path = MY_URL.replace(/\?.*/, '');   // 去掉查询参数

    // 女优列表页：/actresses 本身（不含 /actresses/xxx）
    var isActressList = /\/actresses\b/.test(path) && !/\/actresses\//.test(path);

    // 标签/厂商索引页：/genres 或 /makers 结尾
    var isTagIndex = /\/(genres|makers)\s*$/.test(path);

    if (isActressList)  return this._parseActressItems(html);
    else if (isTagIndex) return this._parseTagItems(html);
    else                 return this._parseVideoItems(html);
}
```

| 路径特征 | 正则 | 渲染函数 |
|---|---|---|
| `/actresses`（列表页） | `/\/actresses\b/.test(p) && !/\/actresses\//.test(p)` | `_parseActressItems` |
| `/actresses/xxx`（女优详情） | `MY_URL` 含 `/actresses/` | `_parseVideoItems`（视频列表） |
| `/genres` 或 `/makers` | `/\/(genres\|makers)\s*$/.test(p)` | `_parseTagItems` |
| 其余（视频列表、搜索等） | 默认 | `_parseVideoItems` |

> ⚠️ **Law 126（新增）：MissAV 主页函数必须在进入任何解析逻辑前，用正则对 `MY_URL` 做三路分流：女优列表页、标签/厂商索引页、视频列表页。路径判断需先 `.replace(/\?.*/, '')` 去掉查询参数，再做正则，防止翻页参数干扰识别。**

---

## 56.8 翻页参数拼接——兼容已含 ? 的 URL

```javascript
// ❌ 错误：无脑追加 ?page=
url += '?page=' + MY_PAGE;    // 若 url 已含 ? 则产生双问号

// ✅ 正确：判断是否已有查询参数
url += (url.indexOf('?') > -1 ? '&' : '?') + 'page=' + MY_PAGE;
```

> ⚠️ **Law 127（新增）：翻页参数拼接前必须检测 URL 是否已含 `?`，用三目运算符选择 `&` 或 `?`，防止 URL 中出现双问号导致服务器解析失败。**

---

## 56.9 代码规范——变量命名与 this 指向保护

**变量命名避锋：** 在规则引擎内部，`title`、`url` 等极短变量名可能触发引擎内部正则标志冲突。

```javascript
// ❌ 可能触发引擎 bug
var title = pdfh(item, 'h3&&Text');
var url   = pdfh(item, 'a&&href');

// ✅ 安全写法：下划线前缀
var _title = pdfh(item, 'h3&&Text');
var _url   = pdfh(item, 'a&&href');
```

**this 指向保护：** 在 `$.rule()` 和 `lazyRule` 沙箱内 `this` 不指向 parse 对象。

```javascript
// ✅ 方法1：提前保存 self
var self = this;
url: $(link).lazyRule(function(ua, host) {
    // 使用参数，不用 this
}, self.UA, self.host)

// ✅ 方法2：直接传参（推荐，更明确）
url: $(link).lazyRule(function(ua, host) {
    return "video://" + input + ";{User-Agent@" + ua + "}";
}, this.UA, this.host)
```

> ⚠️ **Law 128（新增）：列表解析循环变量避免使用 `title`、`url`、`name` 等与引擎内部命名可能冲突的极短词，统一加下划线前缀（`_title`、`_url`）。`$.rule()` 和 `lazyRule` 沙箱内 `this` 不可用，所需的 `UA`、`host` 等运行时值必须在外部读取后通过参数传入。**

---

## 56.10 本章功能状态速查 & 铁律汇总

### 功能状态

| 功能 | 状态 | 备注 |
|---|---|---|
| 25 个分类浏览 | ✅ | fyclass 路径字符串直接拼接 |
| 翻页加载 | ✅ | `?` / `&` 自动判断 |
| 关键词搜索 | ✅ | 四级降级选择器兼容搜索结果页 |
| 女优列表/详情 | ✅ | 沙箱模式跳转 |
| 类型/厂商标签页 | ✅ | 正则分流 + 沙箱跳转 |
| 单线路视频播放 | ✅ | surrit.com UUID 解密 + Referer |
| 多线路清晰度选择 | ❌ | 框架限制：多线路 JSON 不支持 Header |

### 铁律汇总

| 编号 | 铁律名称 | 核心要义 | 关键口诀 |
|---|---|---|---|
| Law 120 | fyclass MY_URL 是路径字符串 | `MY_URL` 是 `class_url` 的原始路径，禁止 `parseInt` | MY_URL 是路径，不是数字 |
| Law 121 | 女优/标签二级用沙箱模式 | 无选集列表的二级页面用 `hiker://empty##url` 沙箱渲染 | 没有选集就用沙箱 |
| Law 122 | surrit.com 字典解密两步走 | `encrypted` 数组 + `return p}` 后字典映射，缺一不可 | 数组取下标，字典换字符 |
| Law 123 | m3u8 Referer 必须用 surrit.com | Referer 设为 `surrit.com/{uuid}/`，不能用 missav.ai | Referer 认主人不认门牌 |
| Law 124 | 多清晰度单线路降级 | 多线路 JSON 不支持 Header，取最高画质走单线路 | 多清晰度取最好的，一条线走 |
| Law 125 | 四级选择器 + data-src 优先 | 四级降级兼容不同页面容器；图片取 `data-src` 再取 `src` | 四级降级找容器，懒加载取 data-src |
| Law 126 | 主页三路正则分流 | 女优列表 / 标签索引 / 视频列表，路径去参数后再判断 | 先去问号再分流 |
| Law 127 | 翻页参数 ? 与 & 自动判断 | `url.indexOf('?') > -1 ? '&' : '?'` | 有问号用 &，没问号用 ? |
| Law 128 | 变量下划线前缀 + this 传参 | 循环变量加 `_` 前缀；沙箱所需值外部读取后传参 | 变量加下划线，沙箱传参不传 this |

---

*手册补充版本 V3.50 · 第五十六章：MissAV 规则开发完全指南（missav.ai + surrit.com 实战提炼）*

---

# 第五十七章：间接 m3u8 模式——双重 Base64 接口解密写本地文件

本章记录一种常见的视频防盗链方案：服务端不直接给 m3u8 URL，而是给一个需要携带 Referer 的接口地址，接口返回双重 Base64 编码的 m3u8 文本，客户端解码后写本地缓存文件播放。

---

## 57.1 模式识别

**如何判断站点使用此模式**：

1. DPlayer `data-config` 里的 `video.url` 不是 `.m3u8` 结尾，而是一个接口路径（如 `/api/play?id=xxx`）
2. 直接访问该接口返回一段字符串，不是 `#EXTM3U` 开头的 m3u8 文本
3. 对该字符串做一次或两次 `atob()` / Base64 解码后，出现 `#EXTM3U` 内容

```javascript
// 浏览器 DevTools Console 快速验证：
fetch('/api/play?id=xxx', { headers: { Referer: location.href } })
    .then(r => r.text())
    .then(t => console.log(atob(atob(t)).substring(0, 100)));
// 如果输出 #EXTM3U... → 确认是双重 Base64 模式
```

---

## 57.2 完整实现模板

```javascript
// ── 在二级函数或主页函数中，提取 DPlayer video.url 后 ────────────
// videoUrl = DPlayer data-config.video.url（接口地址，不是 m3u8 直链）
// fullUrl  = 当前文章详情页地址（作为 Referer）

var lazy = $("").lazyRule(function(vUrl) {
    // input = videoUrl（接口地址）
    // vUrl  = 文章详情页地址（Referer）

    // ── 内联 Base64 解码实现 ──────────────────────────────────────
    var Base64 = {
        _keyStr: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=",
        decode: function(input) {
            var output = "";
            var chr1, chr2, chr3, enc1, enc2, enc3, enc4;
            var i = 0;
            input = input.replace(/[^A-Za-z0-9\+\/\=]/g, "");
            while (i < input.length) {
                enc1 = Base64._keyStr.indexOf(input.charAt(i++));
                enc2 = Base64._keyStr.indexOf(input.charAt(i++));
                enc3 = Base64._keyStr.indexOf(input.charAt(i++));
                enc4 = Base64._keyStr.indexOf(input.charAt(i++));
                chr1 = (enc1 << 2) | (enc2 >> 4);
                chr2 = ((enc2 & 15) << 4) | (enc3 >> 2);
                chr3 = ((enc3 & 3) << 6) | enc4;
                output += String.fromCharCode(chr1);
                if (enc3 !== 64) output += String.fromCharCode(chr2);
                if (enc4 !== 64) output += String.fromCharCode(chr3);
            }
            return Base64._utf8_decode(output);
        },
        _utf8_decode: function(utftext) {
            var string = "";
            var i = 0;
            var c, c2, c3;
            while (i < utftext.length) {
                c = utftext.charCodeAt(i);
                if (c < 128) {
                    string += String.fromCharCode(c);
                    i++;
                } else if (c > 191 && c < 224) {
                    c2 = utftext.charCodeAt(i + 1);
                    string += String.fromCharCode(((c & 31) << 6) | (c2 & 63));
                    i += 2;
                } else {
                    c2 = utftext.charCodeAt(i + 1);
                    c3 = utftext.charCodeAt(i + 2);
                    string += String.fromCharCode(((c & 15) << 12) | ((c2 & 63) << 6) | (c3 & 63));
                    i += 3;
                }
            }
            return string;
        }
    };

    try {
        // ① 带 Referer 请求接口，获取 Base64 编码内容
        var content = request(input, {
            headers: { "Referer": vUrl }
        });
        if (!content || content.length < 10) return "toast://接口返回为空";

        // ② 双重 Base64 解码 → m3u8 文本
        var m3u8Text = Base64.decode(Base64.decode(content));
        if (!m3u8Text || m3u8Text.indexOf("#EXTM3U") === -1) {
            return "toast://解码失败: " + (m3u8Text || "").substring(0, 80);
        }

        // ③ 写本地缓存文件（用接口 URL 做 key，避免同文章多视频冲突）
        var cacheFile = "hiker://files/_cache/" + md5(input) + ".m3u8";
        writeFile(cacheFile, m3u8Text);
        return getPath(cacheFile);

    } catch(e) {
        log("视频解析失败: " + e);
        return "toast://解析失败: " + e;
    }
}, fullUrl);

// ── 挂载到卡片 url ────────────────────────────────────────────────
d.push({
    title: "▶ 播放视频",
    url: videoUrl + lazy,   // videoUrl 是接口地址，lazy 是 lazyRule 字符串
    col_type: "text_center_1"
});
```

---

## 57.3 关键细节说明

| 细节 | 说明 |
|---|---|
| `input` 的值 | `$(videoUrl + lazy)` 中，`videoUrl` 是接口地址，引擎把它作为 `input` 传入回调 |
| `Referer` 的作用 | 服务端校验来源，缺少会返回 403 或乱码，必须传文章详情页地址 |
| 解码次数 | 视站点而定，常见 1次或2次，调试时 log 出 `Base64.decode(content).substring(0,50)` 判断 |
| 缓存文件名 | 用 `md5(input)`（接口 URL）而不是 `md5(vUrl)`（文章 URL），避免同文章多视频覆盖同一文件 |
| Base64 内联 | lazyRule 沙箱里无法访问外部对象，Base64 必须在回调内部定义，不能抽到外层 |

---

## 57.4 单重 Base64 变体

部分站点只编码一次，判断方法：对接口返回内容做一次解码后检查是否以 `#EXTM3U` 开头。

```javascript
// 自适应单重/双重解码
var decoded1 = Base64.decode(content);
var m3u8Text;
if (decoded1.indexOf("#EXTM3U") === 0) {
    m3u8Text = decoded1;                    // 单重
} else {
    m3u8Text = Base64.decode(decoded1);     // 双重
}
if (!m3u8Text || m3u8Text.indexOf("#EXTM3U") === -1) {
    return "toast://解码失败";
}
```

---

## 57.5 对比其他视频获取方式

| 方式 | 适用场景 | 代码特征 |
|---|---|---|
| 直接 m3u8 URL | video.url 就是 `.m3u8` 直链 | `return 'video://' + url` |
| 带 Referer 的直链 | m3u8 有防盗链 | `return url + ';{Referer@host/}'` |
| **间接接口 + Base64** | video.url 是接口地址，返回编码内容 | 本章模板 |
| AES 加密视频 | m3u8 本身加密 | `$.image()` + `CryptoUtil.AES` |
| 嗅探兜底 | 以上均失败时 | `return url + '#嗅探'` |

---

*手册补充版本 V3.51 · 第五十七章：间接 m3u8 模式——双重 Base64 接口解密写本地文件（1024吃瓜网实战）*

---



---

# 第五十八章：吃瓜站开发与清洗完全指南

本章合并原第五十八至六十一章，覆盖吃瓜站（WordPress + Mirages/Elementor 主题）的规则骨架、远程调用清洗、高级适配技巧及验证清单，是吃瓜类站点开发的一站式参考。

---

本章基于多个吃瓜站点（1024吃瓜、007吃瓜、海角社区等）的规则清洗实战，系统总结从"依赖壳子远程调用的老规则"到"完全内联、自给自足的新规则"的清洗方法论。

---

## 58.1 远程调用清洗对照表

老规则中常见的壳子远程调用函数，必须全部替换为内联实现（Law 88）：

| 原调用 | 清洗方案 | 说明 |
|---|---|---|
| `rc(url, hours)` | 删除，功能内联 | 远程配置拉取，完全不需要 |
| `gfd()` | 删除 | GitHub 代理域名，不再使用 |
| `fc(url)` | 删除 | 远程配置文件代理，不再使用 |
| `getHtml(url)` | 改为 `request(url, {headers:{...}})` | 内联 fetch 封装 |
| `classTop()` | 改为内联的 `_renderClassRow()` | 分类行渲染内联实现 |
| `getFastestDomain()` | 改为 `batchFetch + 特征检测` | 域名测速内联实现 |
| `getRandomColor()` | 视情况删除或内联 | 吃瓜站标签用 `flex_button`，不需要随机颜色 |

> **⚠️ Law 88 重申：`rc()`/`gfd()`/`fc()` 是壳子专有远程调用，在聚阅/海阔视界规则中会直接报错，清洗时必须全部删除并内联替代实现。**

---

## 58.2 域名自愈内联模板

吃瓜站域名更换频繁，域名自愈是必须实现的功能。完整模板：

```javascript
_getWorkingHost: function() {
    // ① 读缓存（1小时有效，Law 89：必须带时间戳）
    var cached    = juItem.get('site_host', '');
    var cacheTime = parseInt(juItem.get('site_host_time', '0'));
    if (cached && (Date.now() - cacheTime) < 3600000) return cached;

    // ② 从发布页抓取最新域名
    var host = this._fetchPublishHost();
    if (host) {
        juItem.set('site_host', host);
        juItem.set('site_host_time', Date.now() + '');
        return host;
    }

    // ③ 并发测试备用域名列表（batchFetch + 特征关键词验证）
    var backupHosts = this._backupHosts || [];
    if (backupHosts.length > 0) {
        var tasks = [];
        for (var i = 0; i < backupHosts.length; i++) {
            tasks.push({ url: backupHosts[i] + '/', options: { timeout: 5000 } });
        }
        var results = batchFetch(tasks);
        for (var j = 0; j < results.length; j++) {
            if (results[j] && results[j].indexOf('站点特征关键词') > -1) {
                juItem.set('site_host', backupHosts[j]);
                juItem.set('site_host_time', Date.now() + '');
                return backupHosts[j];
            }
        }
    }

    // ④ 全部失败，返回硬编码兜底域名
    return this.host;
},
```

> **⚠️ Law 89 重申：`juItem.set` 缓存域名时必须同时存时间戳（`Date.now() + ''`），否则缓存永不过期，域名更换后无法自动发现新地址。**

---

## 58.3 图片 AES 解密模板（带头部跳过）

部分吃瓜站图片在标准 AES 加密前还有固定字节数的头部数据需要跳过：

```javascript
_getImageLazy: function() {
    var key = this._imgKey;   // 从站点 JS 提取
    var iv  = this._imgIv;    // 从站点 JS 提取
    return $('').image(function(k, iv_s) {
        // input 是引擎下载好的 InputStream（Law 92，不能 fetch(input)）
        var CryptoUtil = $.require('hiker://assets/crypto-java.js');
        var textData = CryptoUtil.Data.parseInputStream(input);
        // 跳过头部字节（具体字节数从站点 JS 确认，此处示例为 276）
        textData.bytes = textData.bytes.slice(276);
        var decrypted = CryptoUtil.AES.decrypt(
            textData,
            CryptoUtil.Data.parseUTF8(k),
            { mode: 'AES/CTR/NoPadding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
            // ⚠️ Law 93：padding 模式从站点 JS 确认，此处是 NoPadding + CTR
        );
        return decrypted.toInputStream();
    }, key, iv);
},
```

**如何确认头部字节数**：在站点 JS 中搜索 `.slice(` 或 `.subarray(`，找到截断偏移量。

---

## 58.4 DPlayer 视频地址提取

吃瓜站普遍使用 DPlayer 播放器，视频地址在 `.dplayer` 元素的 `data-config` 属性中：

```javascript
// ── 提取 DPlayer data-config ──────────────────────────────────
var players = pdfa(html, 'body&&.dplayer');
for (var i = 0; i < players.length; i++) {
    var cfgStr = pdfh(players[i], 'self&&data-config') || '';
    if (!cfgStr) continue;
    // HTML 实体还原（&quot; → "，&amp; → &）
    cfgStr = cfgStr.replace(/&quot;/g, '"').replace(/&amp;/g, '&');
    try {
        var cfg = JSON.parse(cfgStr);
        // url2 优先于 url（部分站点 url2 是高清版）
        var vUrl = (cfg.video && (cfg.video.url2 || cfg.video.url)) || '';
        if (!vUrl) continue;
        // 去掉反斜杠转义
        var slash = String.fromCharCode(92);
        vUrl = vUrl.split(slash).join('');
        // 使用（视具体站点决定是直接播放还是走间接接口解密）
        d.push({
            title: '▶ 播放视频' + (players.length > 1 ? ' ' + (i + 1) : ''),
            url: 'video://' + vUrl + ';{Referer@' + host + '/}',
            col_type: 'text_center_1'
        });
    } catch(e) { log('DPlayer 解析失败: ' + e); }
}
```

**常见变体**：
- `cfg.video.url`：标准字段
- `cfg.video.url2`：部分站点的高清线路，优先使用
- `cfg.video.url` 是接口地址而非 m3u8：走第五十七章的双重 Base64 解密流程

---

## 58.5 分类状态管理规范

吃瓜站通常是矩阵方案（多行分类按钮），状态用 `getMyVar`/`putMyVar` 管理：

```javascript
// ── state key 命名规范 ────────────────────────────────────────
// 用 host 派生唯一前缀，避免不同规则的 key 冲突（Law 51）
var hKey = host.replace(/https?:\/\//, '').replace(/[\/:.]/g, '_');
// 例：https://example.com → example_com

// ── 读当前状态 ────────────────────────────────────────────────
var curCurl = getMyVar(hKey + 'curl0', '');     // 当前选中分类路径
var curIdx  = getMyVar(hKey + 'cindex0', '0'); // 当前选中下标

// ── 分类按钮点击时写状态 ──────────────────────────────────────
url: $('#noLoading#').lazyRule(function(h, rIdx, idx, val) {
    putMyVar(h + 'cindex' + rIdx, idx + '');
    putMyVar(h + 'curl'   + rIdx, val);
    // 切换大分类时，重置所有子分类状态
    if (rIdx === 0) {
        for (var n = 1; n < 5; n++) {
            putMyVar(h + 'cindex' + n, '0');
            putMyVar(h + 'curl'   + n, '');
        }
    }
    refreshPage(false);
    return 'hiker://empty';
}, hKey, rowIdx, i, val)
```

---

## 58.6 吃瓜站详情页——图文混排标准结构

吃瓜站详情页是图文混排内容，不用 `line/list` 选集模式，用 `noShow + extenditems` 完全自定义（Law 94/95）：

```javascript
二级: function(url) {
    var host = this._getWorkingHost();
    var html = request(url, { headers: { 'User-Agent': this.UA, 'Referer': host + '/' } });
    if (!html || html.length < 100) {
        return { vod_name: '加载失败', list: [['']], noShow: { 封面:true, 简介:true, 排序:true, 选集:true }, extenditems: [] };
    }

    var title = pdfh(html, 'h1&&Text') || '';
    var d = [];

    // 标题
    d.push({ title: title, col_type: 'text_1', url: url, extra: { lineVisible: false } });
    d.push({ col_type: 'line' });

    // 视频（DPlayer）
    // ... 见 58.4 ...

    // 正文图文按 HTML 顺序遍历（Law 95）
    var nodes = pdfa(html, 'body&&article.ql-editor&&*');
    for (var i = 0; i < nodes.length; i++) {
        var node = nodes[i];
        var imgSrc = '';
        try { imgSrc = pdfh(node, 'img&&src') || pdfh(node, 'img&&data-src'); } catch(e) {}
        if (imgSrc && imgSrc.indexOf('blob:') !== 0 && imgSrc.indexOf('http') === 0) {
            d.push({ pic_url: imgSrc, url: 'pics://' + imgSrc, col_type: 'pic_1_full' });
            continue;
        }
        var txt = '';
        try { txt = pdfh(node, 'Text'); } catch(e) {}
        if (txt && txt.trim().length > 10) {
            d.push({ title: txt.trim(), col_type: 'rich_text' });
        }
    }

    return {
        vod_name:    title,
        vod_pic:     '',
        // ✅ Law 94：list 必须给 [['']]
        list:        [['']] ,
        noShow:      { 封面: true, 简介: true, 排序: true, 选集: true },
        extenditems: d
    };
},
```

---

## 58.7 吃瓜站清洗检查清单

拿到一个老规则时，按以下顺序逐项核查：

| # | 检查项 | 处理方式 |
|---|---|---|
| 1 | 含 `rc()`/`gfd()`/`fc()` | 全部删除，功能内联（Law 88）|
| 2 | 域名缓存无时间戳 | 补加 `juItem.set('xxx_time', Date.now() + '')` |
| 3 | `$.image()` 内 `fetch(input)` | 删掉 fetch，直接 `parseInputStream(input)`（Law 92）|
| 4 | padding 写死 PKCS7 | 从站点 JS 确认，可能是 NoPadding/CTR（Law 93）|
| 5 | 头部跳过字节数不对 | 搜站点 JS 的 `.slice(N)` 确认偏移量 |
| 6 | `lazyRule` 内访问 `this`/`self` | 改为参数传入（Law 49）|
| 7 | 列表卡片 `url: 'video://' + htmlPageUrl` | 改为 `$(link).lazyRule(...)` 取真实 m3u8 |
| 8 | `img:` 字段 | 改为 `pic_url:` |
| 9 | `flex_button` 用于翻页列表 | 改为 `movie_*`/`text_*`（Law 103）|
| 10 | `extenditems` 时 `list:[]` | 改为 `list:[['']]`（Law 94）|

---

## 58.8 本章新增铁律

| 编号 | 铁律名称 | 核心要义 |
|---|---|---|
| Law 109 | `rule()`/`lazyRule()` 回调外部变量不可访问 | 回调是序列化沙箱，`this`/`self`/`parse` 全不可见，所有依赖值必须通过参数传入 |
| Law 110 | Cookie/UA/host 外部读取后传参 | `lazyRule` 内无法读取外部对象属性，需在调用前读取后作为参数传入 |

---

*手册补充版本 V3.51 · 第五十八章：吃瓜类规则清洗完全指南*

---

---

# 第五十九章：高级适配技巧（吃瓜站进阶）

本章基于 1024吃瓜网规则的完整开发总结，记录手册通用模板之外、针对复杂站点的特化适配方法。

---

## 59.1 域名自愈双重保障方案

手册第十三章给出了基础的域名自愈模板。对于更换频率高的站点，推荐**发布页探测 + 备用域名并发测速**的双重保障方案：

```javascript
_getWorkingHost: function() {
    // ① 缓存检查（1小时有效）
    var cached    = juItem.get('site_host', '');
    var cacheTime = parseInt(juItem.get('site_host_time', '0'));
    if (cached && (Date.now() - cacheTime) < 3600000) return cached;

    var ua = this.UA;

    // ② 优先从发布页探测最新域名（普通 fetch，失败再降级）
    try {
        var pubHtml = request(this.发布页, { headers: { 'User-Agent': ua }, timeout: 8000 });
        if (pubHtml) {
            // 从发布页提取最新域名（正则按实际站点调整）
            var m = pubHtml.match(/href="(https?:\/\/[a-z0-9\-]+\.gtykezq\.cc[^"]*)"/);
            if (!m) m = pubHtml.match(/lineAry\s*=\s*[^\[]*\[[^\]]*"([^"]+\.gtykezq\.cc)"/);
            if (m) {
                var newHost = m[1].match(/^https?:\/\/[^\/]+/)[0];
                juItem.set('site_host', newHost);
                juItem.set('site_host_time', Date.now() + '');
                return newHost;
            }
        }
    } catch(e) { log('发布页探测失败: ' + e); }

    // ③ 发布页失败：并发测试备用域名列表
    var backups = this._backupHosts || [];
    if (backups.length > 0) {
        var tasks = [];
        for (var i = 0; i < backups.length; i++) {
            tasks.push({ url: backups[i] + '/', options: { headers: { 'User-Agent': ua }, timeout: 5000 } });
        }
        var results = batchFetch(tasks);
        for (var j = 0; j < results.length; j++) {
            // 用站点特征关键词验证（比纯状态码更可靠）
            if (results[j] && results[j].indexOf('站点特征词') > -1) {
                juItem.set('site_host', backups[j]);
                juItem.set('site_host_time', Date.now() + '');
                return backups[j];
            }
        }
    }

    // ④ 全部失败时清除缓存，下次强制重试
    juItem.set('site_host', '');
    juItem.set('site_host_time', '0');
    return this.host;
},
```

**与手册基础方案的差异**：

| 维度 | 基础方案 | 双重保障方案 |
|---|---|---|
| 探测方式 | 单一 `fetchCodeByWebView` | 普通 `fetch` + 备用域名 `batchFetch` |
| 失败处理 | 返回硬编码域名 | 清除缓存，下次强制重探 |
| 适用场景 | 有 CF 的发布页 | 发布页可直接 fetch 的站点 |

---

## 59.2 列表选择器精确化

通用模板用 `body&&article` 或 `body&&.post`。当站点有大量广告混入时，需要用更精确的选择器定向提取：

```javascript
// ✅ 精确选择器：限定在特定容器内，过滤掉 Advertisement 节点
var items = pdfa(html, 'div[role="main"]&&article:has(h2)');

// 遍历时再做一次 itemtype 过滤（双重保险）
for (var i = 0; i < items.length; i++) {
    var it = items[i];
    var itemtype = '';
    try { itemtype = pdfh(it, 'self&&itemtype'); } catch(e) {}
    if (itemtype && itemtype.indexOf('Advertisement') > -1) continue;

    var title = '';
    try { title = pdfh(it, '.post-card-title&&Text'); } catch(e) {}
    // 过滤站点特有的垃圾标题
    if (!title || /更新通知|^高端约炮$|^广告$/.test(title.trim())) continue;
    // ...
}
```

---

## 59.3 封面图从 script 标签提取

部分站点的封面图不在 `img` 标签里，而是通过 `loadBannerDirect()` 等 JS 函数动态加载，URL 藏在 `script` 标签的内容中：

```javascript
// ✅ 从 script 内容提取封面
var img = '';
try {
    var scriptContent = pdfh(it, 'script&&Html') || '';
    var imgMatch = scriptContent.match(/loadBannerDirect\('([^']+)'/);
    if (imgMatch) img = imgMatch[1];
} catch(e) {}

// 降级：再试 img 标签
if (!img) {
    img = pd(it, 'img&&data-src') || pd(it, 'img&&src') || '';
}
```

---

## 59.4 视频 url2 优先策略

DPlayer `data-config` 中部分站点提供了 `url2` 字段作为高清备用线路，提取时 `url2` 优先：

```javascript
var cfg = JSON.parse(cfgStr);
// url2（高清/备用）优先于 url（标清/主线）
var vUrl = '';
if (cfg.video) {
    vUrl = cfg.video.url2 || cfg.video.url || '';
}
// 去掉反斜杠转义（JSON 里的 \/ 转义）
var slash = String.fromCharCode(92);
vUrl = vUrl.split(slash + '/').join('/').split(slash).join('');
```

---

## 59.5 多路由主页函数——if/else 分流

当主页函数需要同时处理首页、分类列表、标签云、归档等多种完全不同结构的页面时，用 `if/else if` 按路由分流（Law 38 精神）：

```javascript
主页: function() {
    var host = this._getWorkingHost();
    var _c   = (MY_URL === 'fyclass' || !MY_URL) ? '' : MY_URL;

    // 构建请求 URL（按路由类型分别拼接）
    var url;
    if (!_c) {
        // 首页：/page/N/
        url = host + '/page/' + MY_PAGE + '/';
    } else if (_c === 'tags') {
        // 标签云：/tags/N/（N为页码，首页为1）
        url = host + '/tags/' + MY_PAGE + '/';
    } else if (_c === 'archives') {
        // 归档：/archives/page/N/
        url = host + '/archives/page/' + MY_PAGE + '/';
    } else {
        // 普通分类：/category/slug/N/
        url = host + '/category/' + _c + '/';
        if (MY_PAGE > 1) url += MY_PAGE + '/';
    }

    var html = request(url, { headers: { 'User-Agent': this.UA } });
    if (!html || html.length < 100) return [];

    // 按路由类型渲染不同内容
    if (_c === 'tags') {
        return this._renderTagCloud(html, host);
    } else if (_c === 'archives') {
        return this._renderArchives(html, host);
    } else {
        return this._renderVideoList(html, host);
    }
},
```

---

## 59.6 标签云子页面——沙箱内完全内联

点击标签云的某个标签后，需要展示该标签下的视频列表（含翻页）。由于 `$().rule()` 沙箱内无法调用外部 `二级` 函数，必须把列表渲染逻辑完全内联：

```javascript
// 标签云中每个标签的 url
url: $('hiker://empty##' + tagLink + '##fypage').rule(function() {
    // MY_URL = "hiker://empty##https://xxx/tag/yyy/##fypage"
    var parts = MY_URL.split('##');
    var tagUrl = parts[1] || '';
    if (MY_PAGE > 1) tagUrl = tagUrl.replace(/\/$/, '') + '/page/' + MY_PAGE + '/';

    var html2 = request(tagUrl, {
        headers: { 'User-Agent': 'Mozilla/5.0 (Linux; Android 10) ...' }
    });
    var d2 = [];
    if (!html2 || html2.length < 100) { setResult(d2); return; }

    // ✅ 完全内联：不调用任何外部函数
    var items = pdfa(html2, 'div[role="main"]&&article');
    for (var j = 0; j < items.length; j++) {
        var it2    = items[j];
        var title2 = '';
        try { title2 = pdfh(it2, '.post-card-title&&Text'); } catch(e) {}
        if (!title2) continue;
        var link2 = '';
        try { link2 = pd(it2, 'a&&href'); } catch(e) {}
        if (!link2) continue;
        if (link2.indexOf('http') !== 0) link2 = 'https:' + link2;
        d2.push({ title: title2.trim(), url: link2, col_type: 'movie_2' });
    }
    setResult(d2);
})
```

> 📌 **`$().rule()` 沙箱的约束**：沙箱内能用的只有引擎内置函数（`request`、`fetch`、`pdfa`、`pdfh`、`pd`、`setResult` 等），外部 `parse` 对象的所有方法全部不可访问。规则越复杂，内联的代码量越大——这是设计上的权衡，无法回避。

---

## 59.7 双重广告过滤

手册基础模板只过滤文字关键词，部分站点需要节点级 + 关键词双重过滤：

```javascript
// 节点级过滤（通过 itemtype 属性识别广告节点，在选择器阶段剔除）
var itemtype = '';
try { itemtype = pdfh(it, 'self&&itemtype'); } catch(e) {}
if (itemtype && itemtype.indexOf('Advertisement') > -1) continue;

// 文字关键词过滤（处理正文段落时）
var adKeywords = [
    '备用地址', '永久海外地址', '永久地址', '最新地址',
    '最新域名', '回家地址', '入口', '建议使用', 'Chrome', 'Safari',
    '温馨提示', '重要提示', '关键词', '复制网址', '1024吃瓜'
];
var isAd = false;
for (var k = 0; k < adKeywords.length; k++) {
    if (text.indexOf(adKeywords[k]) > -1) { isAd = true; break; }
}
if (isAd) continue;

// 长度过滤（过短的文字通常是无意义标注）
if (text.length < 20) continue;
```

---

## 59.8 本章技巧汇总

| 技巧 | 适用场景 | 手册对应章节 |
|---|---|---|
| 发布页 fetch + 备用 batchFetch 双重保障 | 发布页偶尔不稳定的站点 | 第十三章 + 本章 59.1 |
| 容器限定 + itemtype 双重过滤 | 广告大量混入列表的站点 | 第四章 + 本章 59.2 |
| script 标签提取封面 | img 标签懒加载或动态加载封面的站点 | 本章 59.3 |
| `url2` 优先降级 `url` | DPlayer 多线路站点 | 第五十八章 59.4 |
| if/else 多路由主页 | 同一规则处理多种页面结构 | 第三十八章 + 本章 59.5 |
| `rule()` 沙箱完全内联 | 标签云/分类子页需要独立翻页 | 第二十六章 + 本章 59.6 |
| 节点级 + 关键词双重广告过滤 | 广告深度嵌入正文的站点 | 第五章 + 本章 59.7 |

---

*手册补充版本 V3.51 · 第五十九章：吃瓜站高级适配技巧（1024吃瓜网实战）*

---

---

# 第六十章：完整规则骨架模板（吃瓜站）

本章提供一份可直接复制使用的吃瓜站规则骨架，覆盖 WordPress + Mirages/Elementor 主题的标准结构，包含域名自愈、图片解密、DPlayer 视频、图文混排详情页的完整实现。

---

## 60A.1 站点特征速查

| 特征 | 说明 |
|---|---|
| 站点类型 | WordPress + Mirages/Elementor 主题 |
| 内容类型 | 图文混合（视频 + 图片 + 文字） |
| 域名特点 | 频繁更换，需域名自愈 |
| 图片加密 | AES-128-CBC，固定 key/iv（从站点 JS 提取）|
| 视频播放 | DPlayer，`data-config` JSON 配置 |
| 页面结构 | 首页文章列表 → 详情页图文混排 |

---

## 60A.2 完整规则骨架

```javascript
var parse = {
    作者: 'dev',
    版本: '20260422',
    UA: 'Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Mobile Safari/537.36',

    // ── AES 密钥（从站点 JS 提取，Law 93：必须从源码确认）
    _key: 'f5d965df75336270',
    _iv:  '97b60394abc2fbe1',

    // ── 发布页和备用域名
    _publishUrl:  'https://发布页.com/',
    _backupHosts: ['https://备用1.com', 'https://备用2.com'],

    页码: { '主页': 1, '搜索': 1 },

    静态分类: {
        type: '主页',
        url: 'fyclass',
        class_name: '首页&分类1&分类2',
        class_url:  '&cate1&cate2'
    },

    // ── 域名自愈（fetchCodeByWebView + batchFetch 双重保障）
    _getWorkingHost: function() {
        var cached     = juItem.get('site_host', '');
        var cachedTime = parseInt(juItem.get('site_host_time', '0'));
        if (cached && (Date.now() - cachedTime) < 3600000) return cached;

        var ua   = this.UA;
        var host = this._backupHosts[0];

        try {
            var html = fetchCodeByWebView(this._publishUrl, {
                timeout: 8000,
                headers: { 'User-Agent': ua },
                javaScriptEnabled: true
            });
            if (html && html.length > 100) {
                // 从发布页提取域名列表（选择器按实际站点调整）
                var urls = [];
                var btns = pdfa(html, '.box&&.btnLink');
                for (var i = 0; i < btns.length; i++) {
                    var domain = pdfh(btns[i], 'Text');
                    if (domain && domain.indexOf('.') > -1) urls.push('https://' + domain);
                }
                if (urls.length > 0) {
                    // batchFetch 并发测速，用页面特征关键词验证
                    var tasks = [];
                    for (var j = 0; j < urls.length; j++) {
                        tasks.push({ url: urls[j] + '/', options: { timeout: 5000 } });
                    }
                    var results = batchFetch(tasks);
                    for (var k = 0; k < results.length; k++) {
                        if (results[k] && results[k].indexOf('post-card') > -1) {
                            host = urls[k];
                            break;
                        }
                    }
                }
            }
        } catch(e) { log('域名自愈失败: ' + e); }

        juItem.set('site_host', host);
        juItem.set('site_host_time', Date.now() + '');
        return host;
    },

    // ── 图片 AES 解密（Law 92：input 是 InputStream，不能 fetch(input)）
    _getImageLazy: function() {
        var key = this._key;
        var iv  = this._iv;
        return $('').image(function(k, iv_s) {
            var CryptoUtil = $.require('hiker://assets/crypto-java.js');
            var dec = CryptoUtil.AES.decrypt(
                CryptoUtil.Data.parseInputStream(input),
                CryptoUtil.Data.parseUTF8(k),
                { mode: 'AES/CBC/PKCS7Padding', iv: CryptoUtil.Data.parseUTF8(iv_s) }
            );
            return dec.toInputStream();
        }, key, iv);
    },

    // ── 公共列表解析（主页和搜索共用）
    _parseList: function(html, host, imgLazy) {
        var d     = [];
        var items = pdfa(html, 'div[role="main"]&&article:has(h2)');
        for (var i = 0; i < items.length; i++) {
            var it    = items[i];
            // itemtype 过滤广告节点
            var itype = '';
            try { itype = pdfh(it, 'self&&itemtype'); } catch(e) {}
            if (itype && itype.indexOf('Advertisement') > -1) continue;

            var title = '';
            try { title = pdfh(it, 'h2&&Text'); } catch(e) {}
            if (!title || /更新通知|^高端约炮$/.test(title.trim())) continue;

            var link = pd(it, 'a&&href', host);
            if (!link) continue;

            // 封面从 script 标签提取（Law 59.3）
            var bgUrl = '';
            try {
                var sc = pdfh(it, 'script&&Html') || '';
                var sm = sc.match(/'([^']+\.(jpe?g|png|webp))'/i);
                if (sm) bgUrl = sm[1].indexOf('http') === 0 ? sm[1] : host + '/' + sm[1];
            } catch(e) {}
            // 降级：img 标签
            if (!bgUrl) bgUrl = pd(it, 'img&&data-src') || pd(it, 'img&&src') || '';

            var desc = '';
            try { desc = pdfh(it, '.post-card-info&&Text'); } catch(e) {}

            d.push({
                title:    title.trim(),
                desc:     desc,
                pic_url:  bgUrl ? bgUrl + imgLazy : '',
                url:      link,
                col_type: 'pic_1_card'
            });
        }
        return d;
    },

    // ── 主页
    主页: function() {
        var host    = this._getWorkingHost();
        var ua      = this.UA;
        var imgLazy = this._getImageLazy();
        var _c      = (MY_URL && MY_URL !== 'fyclass') ? MY_URL : '';

        var url = !_c
            ? host + '/page/' + MY_PAGE + '/'
            : host + '/category/' + _c + '/' + MY_PAGE + '/';

        var html = request(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });
        if (!html || html.length < 100) return [];
        return this._parseList(html, host, imgLazy);
    },

    // ── 二级（图文混排，noShow + extenditems，Law 94/95）
    二级: function(url) {
        var host    = this._getWorkingHost();
        var ua      = this.UA;
        var imgLazy = this._getImageLazy();
        var html    = request(url, { headers: { 'User-Agent': ua, 'Referer': host + '/' } });
        if (!html || html.length < 100) {
            return { vod_name: '加载失败', list: [['']], noShow: { 封面:true, 简介:true, 排序:true, 选集:true }, extenditems: [] };
        }

        var title = '';
        try { title = pdfh(html, 'h1&&Text'); } catch(e) {}
        var d = [];

        d.push({ title: title, desc: pdfh(html, '.post-meta&&Text') || '', col_type: 'text_center_1', url: url, extra: { lineVisible: false } });
        d.push({ col_type: 'line' });

        // DPlayer 视频提取
        var players = pdfa(html, 'body&&.dplayer');
        for (var i = 0; i < players.length; i++) {
            var cfgStr = '';
            try { cfgStr = pdfh(players[i], 'self&&data-config') || ''; } catch(e) {}
            cfgStr = cfgStr.replace(/&quot;/g, '"').replace(/&amp;/g, '&');
            if (!cfgStr) continue;
            try {
                var cfg  = JSON.parse(cfgStr);
                var vUrl = cfg.video && (cfg.video.url2 || cfg.video.url) || '';
                var slash = String.fromCharCode(92);
                vUrl = vUrl.split(slash).join('');
                if (vUrl) {
                    d.push({
                        title:    '▶ 播放视频' + (players.length > 1 ? ' ' + (i + 1) : ''),
                        url:      'video://' + vUrl + ';{Referer@' + host + '/}',
                        col_type: 'text_center_1',
                        extra:    { lineVisible: false }
                    });
                }
            } catch(e) { log('DPlayer 解析失败: ' + e); }
        }
        if (players.length > 0) d.push({ col_type: 'line' });

        // 正文图文按 HTML 顺序遍历（Law 95）
        var nodes = [];
        try { nodes = pdfa(html, 'body&&.post-content&&*'); } catch(e) {}

        var adKeywords = ['最新地址', '备用地址', '永久地址', '推荐使用', '温馨提示', '关键词', '入口'];
        for (var j = 0; j < nodes.length; j++) {
            var node = nodes[j];
            // 图片
            var imgSrc = '';
            try { imgSrc = pdfh(node, 'img&&data-xkrkllgl') || pdfh(node, 'img&&data-src') || pdfh(node, 'img&&src'); } catch(e) {}
            if (imgSrc && imgSrc.indexOf('http') === 0 && imgSrc.indexOf('blob:') !== 0) {
                d.push({ pic_url: imgSrc + imgLazy, url: 'pics://' + imgSrc, col_type: 'pic_1_full' });
                continue;
            }
            // 文字
            var txt = '';
            try { txt = pdfh(node, 'Text'); } catch(e) {}
            txt = (txt || '').trim();
            if (txt.length < 10) continue;
            var isAd = false;
            for (var k = 0; k < adKeywords.length; k++) {
                if (txt.indexOf(adKeywords[k]) > -1) { isAd = true; break; }
            }
            if (!isAd) d.push({ title: txt, col_type: 'rich_text' });
        }

        return {
            vod_name:    title,
            vod_pic:     '',
            list:        [['']], // ✅ Law 94：extenditems 时必须给非空二维数组
            noShow:      { 封面: true, 简介: true, 排序: true, 选集: true },
            extenditems: d
        };
    },

    // ── 搜索
    搜索: function(key) {
        var host    = this._getWorkingHost();
        var imgLazy = this._getImageLazy();
        var url     = host + '/search/' + encodeURIComponent(key) + '/' + MY_PAGE + '/';
        var html    = request(url, { headers: { 'User-Agent': this.UA } });
        if (!html || html.length < 100) return [];
        return this._parseList(html, host, imgLazy);
    }
};
```

---

## 60A.3 关键点速查

| 项目 | 实现方式 |
|---|---|
| 域名自愈 | `fetchCodeByWebView` 提取域名列表 + `batchFetch` 并发测速 |
| 图片解密 | `$.image()` + `CryptoUtil.AES.decrypt`，input 直接传入 |
| 视频提取 | `pdfa('.dplayer')` → `data-config` JSON → `url2` 优先 `url` |
| 列表封面 | 优先从 `<script>` 正则提取，降级 `img[data-src]` |
| 详情页 | `extenditems` 图文混排，`list: [['']]`，`noShow` 全隐藏 |

---

---

# 第六十一章：清洗步骤与验证清单

本章是第五十八章清洗指南的补充，提供清洗完成后的逐项验证方法，确保清洗质量。

---

## 61A.1 清洗步骤速查

| 步骤 | 操作 |
|---|---|
| 1 | 删除所有 `rc()` 调用，包括开头的公共库加载行 |
| 2 | 替换 `getHtml(url)` → `request(url, {headers:{...}})` + 长度检查 |
| 3 | 删除 `classTop()`，分类整合到 `静态分类` |
| 4 | 替换 `imgDec(key, iv)` → `_getImageLazy()` 内联 |
| 5 | 删除文件缓存代码（`getdTemp`/`writeFile`/`_chchePath`）|
| 6 | 删除位置记忆代码（`pageAdd`/`pageMoveto`）|
| 7 | 将 `$.toString() + eval()` 改为直接函数调用 |
| 8 | 删除样式函数（`strong`/`color`/`fontcolor` 等）|
| 9 | 删除或简化评论区代码 |
| 10 | 简化分类联动为纯静态分类 |

---

## 61A.2 验证清单

清洗完成后逐项检查：

| # | 检查项 | 验证方法 |
|---|---|---|
| 1 | 无 `rc(` 字符串 | 全文搜索 `rc(` |
| 2 | 无 `gfd(` 字符串 | 全文搜索 `gfd(` |
| 3 | 无 `fc(` 字符串 | 全文搜索 `fc(` |
| 4 | 无 `$.toString(` | 全文搜索 `$.toString` |
| 5 | 无 `eval(parse.` | 全文搜索 `eval(parse` |
| 6 | 静态分类配置完整 | 检查 `class_name` 和 `class_url` 条数一致 |
| 7 | 域名自愈正常 | 清除缓存后加载首页，log 确认 host |
| 8 | 列表可加载 | 切换每个分类，验证列表出现 |
| 9 | 翻页正常 | 滑动到底部触发第二页 |
| 10 | 详情页正常 | 点击任意卡片，验证图文出现 |
| 11 | 视频可播放 | 点击播放按钮，验证 m3u8 加载 |
| 12 | 图片可显示 | 验证解密图片正常显示（不是乱码）|
| 13 | 搜索正常 | 输入关键词，验证结果出现 |

---

## 61A.3 壳子函数完整替代对照

| 壳子函数 | 替代方案 | 章节参考 |
|---|---|---|
| `rc()` / `gfd()` / `fc()` | 删除，所有逻辑内联 | Law 88 |
| `getHtml(url)` | `request(url, {headers})` + `!html \|\| html.length < 100` | Law 98 |
| `classTop()` | 删除，改用 `静态分类` | 第三章 |
| `imgDec(key, iv, 'AES')` | `_getImageLazy()` 内联 `$.image()` | 第十章、第六十章 |
| `getFastestDomain(urls)` | `batchFetch(tasks)` + 特征关键词检测 | 第五十八章 |
| `pageAdd()` / `pageMoveto()` | 删除，不需要位置记忆 | — |
| `getdTemp()` / `writeFile()` | 删除（或保留用于 m3u8 缓存场景，见第五十七章）| 第五十七章 |
| `strong()` / `color()` / `fontcolor()` | 删除，直接用纯文本 | — |
| `$.toString() + eval()` | 改为直接函数内联 | Law 90 |
| `getRandomColor()` | 吃瓜站删除；需要颜色时内联 HSL 计算 | 第五十九章 |

---

---

# 第六十七章：静态分类与矩阵方案选型指南

本章明确两种分类方案的适用边界，补全手册第六章（矩阵方案）和第三章（静态分类）的选型说明。

---

## 67.1 选型决策树

```
站点有二级函数（需详情页/选集/简介）？           ← 第一层硬过滤（Law 67）
  ├─ 有 → ✅ 直接走矩阵方案，跳过下面所有判断
  │       （rule() 沙箱里 http url 永远跳浏览器，
  │         有二级的站点不能用独立标签页/fyclass 单维方案）
  └─ 无（漫画 pics:// / 直链 video:// / Next.js 无二级）
       │
       ▼
     分类数量 ≤ 20 个？
       ├─ 是 → URL 规律？（如 /category/slug/）
       │         ├─ 是 → ✅ 静态分类（fyclass）
       │         └─ 否 → 矩阵方案（手动 scroll_button）
       └─ 否 → 需要多行联动筛选？
                 ├─ 是 → ✅ 矩阵方案
                 └─ 否 → 静态分类（拆分多规则）
```

> ⚠️ **第一层为硬过滤（V3.66 修订）：** 不再让"分类数量 + URL 规律"覆盖"有二级"这条更高优先级。详见第 32 章 Law 67 沙箱限制；旧版决策树会把"有二级 + 分类少 + URL 规律"导向静态分类，但实际 rule() 沙箱内 http 链接跳浏览器，最终走不到二级函数。

---

## 67.2 静态分类适用场景

| 条件 | 说明 |
|---|---|
| 分类数量 ≤ 20 | 超出后 scroll 区域过长影响体验 |
| URL 有固定规律 | `/category/{slug}/`、`/dm{id}/{slug}` 等 |
| 不需要多级联动 | 单行分类，无子筛选 |
| 清洗旧规则 | 优先简化为静态分类 |

```javascript
// ✅ 静态分类标准配置
静态分类: {
    type: '主页',
    url: 'fyclass',
    class_name: '首页&分类1&分类2&分类3',
    class_url:  '&cate1&cate2&cate3'
    // 首页对应空字符串，主页函数里用 !_c 判断
}
```

---

## 67.3 矩阵方案适用场景

| 条件 | 说明 |
|---|---|
| 分类数量 > 20 | 需要分组展示 |
| 多行联动筛选 | 如"分类 + 排序 + 地区"三行互斥 |
| URL 结构不规律 | 需要用 `getMyVar` 动态拼接 URL |
| 原规则已稳定运行 | 不值得简化时保留 |

---

## 67.4 清洗时的简化策略

| 原结构 | 简化方案 |
|---|---|
| 单行分类 + 无联动 | → 直接改为静态分类 |
| 两行联动（分类+排序）| → 保留矩阵，精简按钮数量 |
| 多行复杂联动 | → 保留矩阵，但提取主分类到静态分类 |
| 标签云独立页 | → 单独用 `$().rule()` 沙箱处理，不纳入静态分类 |
| 热门榜切换 | → 并入主分类的 `scroll_button` 行 |

---

## 67.5 特殊路由处理（tags / archives）

部分站点有标签云（`/tags/`）、归档（`/archives/`）等特殊路由，URL 翻页格式与普通分类不同：

```javascript
// 主页函数中按路由类型分别拼接翻页 URL
var url;
if (!_c) {
    url = host + '/page/' + MY_PAGE + '/';              // 首页
} else if (_c === 'tags') {
    url = host + '/tags/' + MY_PAGE + '/';              // 标签云（页码直接在路径里）
} else if (_c === 'archives') {
    url = host + '/archives/page/' + MY_PAGE + '/';    // 归档
} else {
    url = host + '/category/' + _c + '/' + MY_PAGE + '/';  // 普通分类
}
```

> 📌 **标签云不建议放进静态分类**：标签云是索引页，点击后需要独立翻页，放进静态分类会导致点击标签后无法翻页。建议在主页函数的渲染逻辑里单独处理，或用 `$().rule()` 沙箱新开页面。

---

*手册补充版本 V3.51 · 第六十章：吃瓜站完整规则骨架模板 · 第六十一章：远程调用清洗验证清单 · 第六十二章：静态分类与矩阵方案选型指南*

---

# 第六十五章：吃瓜站 DPlayer API 懒加载播放模式

吃瓜站（WordPress + Mirages/Elementor 主题）的视频播放统一使用 DPlayer，`data-config` JSON 中嵌入加密 API 地址和 AES 密钥。点击播放时才请求 API 解密获取 m3u8，避免提前请求造成的额外开销。

> 📌 本章是**第 46 章 WordPress 骨架**的视频专题延伸。如需完整吃瓜站规则骨架（含主页/列表/详情/评论/广告过滤），先读第 46.10 节标准模板；DPlayer 视频提取部分调用本章流程。整体地图见第 46 章章首。

---

## 65.1 标准提取流程

```
html → pdfa("body&&.dplayer") → 逐个提取 data-config JSON
→ 解析 data-api / data-key / data-iv
→ lazyRule(function(apiUrl, key, iv, host, UA) { ... })
→ 点击播放时 → fetchPC(apiUrl) → JSON 解析
→ 三种响应格式：video_url(明文) / url(明文) / data(AES加密Base64)
→ AES 解密得到 m3u8 → return "video://" + m3u8 + ";{Header}"
```

---

## 65.2 完整代码（吃瓜站标准模板）

```javascript
var playlist = [];
try {
    var dplayers = pdfa(html, "body&&.dplayer");
    log("[二级] 找到 " + dplayers.length + " 个 DPlayer");

    for (var idx = 0; idx < dplayers.length; idx++) {
        var dplayer = dplayers[idx];

        // 兼容单引号和双引号两种 data-config 写法
        var configMatch = dplayer.match(/data-config='([^']+)'/);
        if (!configMatch) configMatch = dplayer.match(/data-config="([^"]+)"/);
        if (!configMatch) continue;

        var configStr = configMatch[1]
            .replace(/&quot;/g, '"')
            .replace(/&#34;/g, '"');
        var config;
        try { config = JSON.parse(configStr); } catch(e) { continue; }

        // 兼容 data-api / api 两种键名
        var api  = config['data-api'] || config.api;
        var _key = config['data-key'] || config.key;
        var _iv  = config['data-iv']  || config.iv;
        if (!api) continue;

        // 补全相对路径
        var fullApi = api;
        if (api.indexOf("http") !== 0) {
            var cleanApi = api.replace(/^\/+/, '');
            fullApi = host + "/" + cleanApi;
        }

        // ✅ lazyRule：点击时才请求 API，不提前消耗请求（Law 61精神）
        var lazyUrl = $(fullApi).lazyRule(function(apiUrl, key, iv, h, u) {
            try {
                // fetchPC：API 端通常需要 PC UA 才能正常响应
                var resp = fetchPC(apiUrl, {
                    method:  "GET",
                    headers: { "User-Agent": u, "Referer": h + "/" },
                    timeout: 15000
                });
                if (!resp) return "#嗅探";

                var payload  = JSON.parse(resp);
                var videoUrl = null;

                // 格式1：明文 video_url
                if (payload.video_url && payload.video_url.indexOf("http") === 0) {
                    videoUrl = payload.video_url;

                // 格式2：明文 url
                } else if (payload.url && payload.url.indexOf("http") === 0) {
                    videoUrl = payload.url;

                // 格式3：AES-128-CBC 加密的 Base64 data
                } else if (payload.data && key && iv) {
                    var CryptoUtil = $.require("hiker://assets/crypto-java.js");
                    var decrypted  = CryptoUtil.AES.decrypt(
                        CryptoUtil.Data.parseBase64(payload.data),
                        CryptoUtil.Data.parseUTF8(key),
                        { mode: "AES/CBC/PKCS7Padding", iv: CryptoUtil.Data.parseUTF8(iv) }
                    );
                    var decText  = decrypted.toText();
                    var m3u8M    = decText.match(/https?:[^\s"'<>]+\.m3u8[^\s"'<>]*/i);
                    if (m3u8M) videoUrl = m3u8M[0];
                }

                if (videoUrl) {
                    return "video://" + videoUrl +
                           ";{User-Agent@" + u + "&&Referer@" + h + "/}";
                }
                return "#嗅探";
            } catch(e) {
                log("API解析失败: " + e);
                return "#嗅探";
            }
        }, fullApi, _key, _iv, host, this.UA);  // ✅ this.UA 在循环外读取后传入

        playlist.push({
            title: "▶ 视频 " + (idx + 1),
            url:   lazyUrl
        });
    }
} catch(e) {
    log("[二级] 解析视频失败: " + e);
}

// ── 兜底：直接从页面正则匹配 m3u8 ──────────────────────────────
if (playlist.length === 0) {
    var m3u8Matches = html.match(/https?:[^\s"'<>]+\.m3u8[^\s"'<>]*/gi) || [];
    for (var m = 0; m < m3u8Matches.length; m++) {
        playlist.push({
            title: "▶ 视频 " + (m + 1),
            url: "video://" + m3u8Matches[m] +
                 ";{User-Agent@" + this.UA + "&&Referer@" + host + "/}"
        });
    }
}
```

---

## 65.3 API 响应三种格式对照

| 格式 | 特征 | 处理方式 |
|---|---|---|
| `video_url` | 明文 m3u8 URL | 直接使用 |
| `url` | 明文 m3u8 URL | 直接使用 |
| `data` | Base64 + AES-128-CBC 加密 | `data-key`/`data-iv` 解密后正则提取 m3u8 |

---

## 65.4 关键要点

| 要点 | 说明 |
|---|---|
| `fetchPC` 而非 `fetch` | API 端通常需要 PC UA，`fetch` 的移动端 UA 会被拒绝 |
| `Referer` 必须带 | 部分 API 校验来源页面 |
| `timeout: 15000` | API 响应可能较慢，默认超时会导致失败 |
| 返回 `"#嗅探"` | 解析失败时引擎自动尝试 WebView 嗅探，比 `toast://` 更友好 |
| 正则 `[^\s"'<>]+` | 严格限定 m3u8 URL 边界，避免匹配到 HTML 属性内容 |
| `this.UA` 在外部读取 | `lazyRule` 沙箱内不能访问 `this`，必须在调用处传入（Law 110）|

---

# 第六十六章：正文广告过滤——关键词 + 长度双重过滤

吃瓜站正文中普遍混入"最新地址"、"回家地址"、"TG群"等广告段落，以及过短的无意义标注文字。本章提供完整的过滤方案。

---

## 66.1 广告关键词池（吃瓜站通用）

```javascript
_adKeywords: [
    "备用地址", "永久海外地址", "永久地址", "最新地址",
    "最新域名", "回家地址", "入口", "建议使用", "Chrome", "Safari",
    "温馨提示", "重要提示", "关键词", "复制网址", "1024吃瓜",
    "请收藏", "网址发布", "邮箱获取", "发送邮件", "忘记网址",
    "找不到地址", "最新网址", "官方地址", "回家路", "防走丢",
    "收藏本站", "网址导航", "地址发布", "发布页", "TG群",
    "Telegram", "电报群", "QQ群", "加群"
]
```

---

## 66.2 过滤函数

```javascript
_isAdText: function(text) {
    if (!text || text.length < 20) return true;   // 过短直接过滤
    for (var i = 0; i < this._adKeywords.length; i++) {
        if (text.indexOf(this._adKeywords[i]) !== -1) return true;
    }
    return false;
}
```

---

## 66.3 正文图文混排应用（图片不受文字过滤影响）

```javascript
// ✅ 正确写法：图片提取独立于文字过滤逻辑之外
var nodes = pdfa(html, 'body&&.post-content>p:not(:has(.content-file))');
for (var pi = 0; pi < nodes.length; pi++) {
    var item  = nodes[pi];
    var pText = pdfh(item, "p&&Text");

    // ── 文字：通过过滤才添加 ──────────────────────────────────
    if (pText && !this._isAdText(pText)) {
        var pUrl        = pdfh(item, "a&&href");
        var isArchiveLink = pUrl && /archive/i.test(pUrl);
        extendItems.push({
            title:    isArchiveLink
                      ? "'''''<font color=\"#FF0000\">" + pText + "</font>"
                      : pText,
            url:      pUrl || "hiker://empty",
            col_type: isArchiveLink ? "text_1" : "rich_text"
        });
    }

    // ── 图片：始终提取，不受文字过滤影响 ─────────────────────
    // ⚠️ 不能放在上面 if 块内，否则广告段落中的图片会一起丢失
    var imgNodes = pdfa(item, "p&&img");
    for (var ii = 0; ii < imgNodes.length; ii++) {
        var src = pdfh(imgNodes[ii], "img&&data-xkrkllgl")
               || pdfh(imgNodes[ii], "img&&data-src")
               || pdfh(imgNodes[ii], "img&&src");
        if (src && src.indexOf("http") === 0) {
            extendItems.push({
                title:    "",
                pic_url:  src + imgDecrypt,
                col_type: "pic_1_full"
            });
        }
    }
}
```

---

## 66.4 关键陷阱

| 陷阱 | 说明 |
|---|---|
| 图片放在文字 `if` 块内 | 广告段落中的图片会一并丢失，图文穿插顺序被破坏 |
| 用 `continue` 跳过整段 | `if (!pText) continue` 会导致纯图片段落（无文字）被完全跳过 |
| 关键词过多 | 建议控制在 30 个以内，过多影响性能 |
| 长度阈值 | `< 20` 适合中文站点，英文站点可适当调低至 `< 10` |

> **⚠️ Law 95 补充：图文穿插时，图片提取逻辑必须独立于文字过滤逻辑，不能因文字被过滤而导致同节点的图片也丢失。**

---

*手册补充版本 V3.52 · 第六十章：吃瓜站 DPlayer API 懒加载播放模式 · 第六十一章：正文广告过滤*

---

# 第六十二章：高级 UI 组件与多功能规则设计模式

本章基于"禁漫天堂"聚阅规则（作者：流苏）提炼，记录手册前述章节未覆盖的高级 UI 组件、交互模式和多功能规则架构技巧。

---

## 62.1 页面主题标识——URL 参数控制页面样式

`$().rule()` 的 URL 参数不只是传数据，还可以控制页面渲染主题：

```javascript
// 沉浸式主题（大图背景，适合漫画/图集详情页）
$('hiker://empty?#immersiveTheme#').rule(function() { ... })

// 全屏主题（无导航栏，适合登录/设置页）
$('hiker://empty?#fullTheme#').rule(function() { ... })

// 游戏主题（带顶部图片，适合章节列表）
$('hiker://empty?page=fypage&#gameTheme#').rule(function() { ... })

// 不记录历史（弹窗性质的页面，返回后不残留历史）
$('hiker://empty?#fullTheme##noHistory#').rule(function() { ... })

// 组合使用
$('hiker://empty?page=fypage&#fullTheme##noHistory#').rule(function() { ... })
```

---

## 62.2 setPreResult + deleteItemByCls——骨架屏加载动效

在数据请求前先渲染顶部 UI 和 loading 占位符，数据加载完成后替换，避免用户看到白屏：

```javascript
主页: function() {
    var d  = [];   // 主内容区
    var d_ = [];   // 顶部固定区（先渲染）

    if (MY_PAGE == 1) {
        // ── 顶部 UI（立即渲染）────────────────────────────────
        d_.push({ title: '分类A', col_type: 'scroll_button', url: '...' });

        // loading 占位符（带 cls 标记）
        d_.push({
            url: 'hiker://empty',
            col_type: 'card_pic_3',
            extra: { cls: 'loading_' }   // ← 标记 cls
        }, {
            img: 'https://example.com/loading.gif',
            url: 'hiker://empty',
            col_type: 'card_pic_3',
            extra: { cls: 'loading_' }
        });

        setPreResult(d_);  // ← 立即渲染顶部，用户看到导航和加载动画
    }

    // ── 耗时的数据请求（此时用户已看到顶部 UI）────────────────
    var data = fetch(apiUrl);
    // ... 解析数据填充 d ...

    deleteItemByCls('loading_');  // ← 删除所有 cls='loading_' 的占位符
    setResult(d);
}
```

> 📌 `setPreResult(d_)` 立即渲染不等待，`setResult(d)` 渲染主内容并替换 loading。两者配合实现骨架屏效果。

---

## 62.3 Cate 通用分类行渲染函数

把分类行渲染抽象为通用函数，支持图标、长按菜单、自定义样式：

```javascript
Cate: function(list, stateKey, d, col, longClick) {
    col       = col       || 'scroll_button';
    longClick = longClick || [];

    // list 格式：[{ title: 'A&B&C', id: '1&2&3', img: 'url1&url2&url3' }]
    var defaultId = list[0].id.split('&')[0];

    list.forEach(function(data) {
        var titles = data.title.split('&');
        var ids    = data.id.split('&');
        var imgs   = data.img ? data.img.split('&') : [];
        var curId  = getMyVar(stateKey, defaultId);

        titles.forEach(function(title, index) {
            var isSel = (curId == ids[index]);
            d.push({
                title: isSel
                    // 选中态：加粗+彩色（用 strong 辅助函数）
                    ? "'''''<strong><font color=#FF6699>" + title + "</font></strong>"
                    : title,
                img:      imgs[index] || '',
                col_type: col,
                url: $('#noLoading#').lazyRule(function(k, id) {
                    putMyVar(k, id);
                    refreshPage(false);
                    return 'hiker://empty';
                }, stateKey, ids[index]),
                extra: {
                    longClick:       longClick,
                    backgroundColor: isSel ? '#20FA7298' : ''  // 选中背景色
                }
            });
        });
        d.push({ col_type: 'blank_block' });
    });
    return d;
},
```

**调用示例**：
```javascript
var cats = [{ title: '首页&最新&热门', id: '1&2&3' }];
this.Cate(cats, 'site_tab', d_, 'scroll_button', longClickMenu);
```

---

## 62.4 select:// 协议——原生下拉选择弹窗

比 `hikerPop` 更轻量的选择弹窗，适合选项较少（≤10个）的场景：

```javascript
// 返回 select:// 协议字符串，引擎弹出原生选择框
url: $.toString(function() {
    var options = ['选项A', '选项B', '选项C'];
    var config = {
        title:         '请选择',
        options:       options,
        selectedIndex: parseInt(getItem('my_sel', '0')),  // 当前选中项
        col:           2,   // 每行几列（1或2）
        js: $.toString(function() {
            // input = 用户选中的选项文字
            setItem('my_sel', options.indexOf(input) + '');
            toast('已选择：' + input);
            refreshPage(false);
        })
    };
    return 'select://' + JSON.stringify(config);
})
```

**完整参数说明：**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `title` | string | ✅ | 弹窗顶部标题 |
| `options` | string[] | ✅ | 选项列表，只支持字符串 |
| `selectedIndex` | number | ❌ | 默认高亮索引（从0开始），不传则无选中态 |
| `col` | number | ❌ | 列数，支持 1/2/3，默认 1 |
| `js` | string | ✅ | 选中后执行的 JS，`input` = 选中项文字，必须用 `$.toString()` 序列化 |

**三种列数实战写法（R星精选日期三级联动）：**

```javascript
// ── 单列：年份选择 ───────────────────────────────────────────
url: $('#noLoading#').lazyRule(function() {
    var years = [];
    for (var y = 2025; y <= new Date().getFullYear(); y++) years.push(y + '');
    return 'select://' + JSON.stringify({
        title: '切换年份',
        options: years,
        selectedIndex: years.indexOf(getMyVar('movie_year', '2025')),
        col: 1,
        js: $.toString(function() {
            putMyVar('movie_year', input);
            refreshPage(false);
        })
    });
});

// ── 双列：月份选择 ───────────────────────────────────────────
url: $('#noLoading#').lazyRule(function() {
    var months = Array.from({ length: 12 }, function(_, i) {
        return (i + 1).toString().padStart(2, '0');
    });
    return 'select://' + JSON.stringify({
        title: '切换月份',
        options: months,
        selectedIndex: months.indexOf(getMyVar('movie_month', '01')),
        col: 2,
        js: $.toString(function() {
            putMyVar('movie_month', input);
            refreshPage(false);
        })
    });
});

// ── 三列：日期选择（根据年月动态计算天数）─────────────────────
url: $('#noLoading#').lazyRule(function() {
    var y    = getMyVar('movie_year', '2025');
    var m    = getMyVar('movie_month', '01');
    var last = new Date(parseInt(y), parseInt(m), 0);
    var days = [];
    for (var i = 1; i <= last.getDate(); i++) {
        days.push(String(i).padStart(2, '0'));
    }
    return 'select://' + JSON.stringify({
        title: '切换日期',
        options: days,
        selectedIndex: days.indexOf(getMyVar('movie_day', '01')),
        col: 3,
        js: $.toString(function() {
            putMyVar('movie_day', input);
            refreshPage(false);
        })
    });
});
```

> ⚠️ `input` 是选中的**文字**而非索引，需自己 `indexOf` 找位置。`col` 超过3时效果未定义。弹窗选完后不会自动刷新页面，必须在 `js` 里手动调 `refreshPage`。

---

## 62.5 hikerPop 图标弹窗——selectCenterIcon

适合选项较多且需要图标展示的弹窗（登录菜单、标签云弹窗等）：

```javascript
url: $('#noLoading#').lazyRule(function(items) {
    var hikerPop = $.require('http://your-server/hikerPop.js');
    var iconList = items.map(function(item) {
        return {
            title: item.name,
            icon:  item.icon || 'https://example.com/default.png',
            url:   item.id
        };
    });
    hikerPop.selectCenterIcon({
        iconList: iconList,
        title:    '请选择',
        columns:  2,        // 每行列数
        click: function(input) {
            // input = 用户点击项的 url 字段值
            return $('hiker://empty?page=fypage&#fullTheme#').rule(function(selected) {
                // 处理选中
            }, input);
        }
    });
    return 'hiker://empty';
}, dataArray)
```

---

## 62.6 avatar col_type——头像+标题+描述行

适合用户信息、作者信息等场景，左侧圆形头像 + 右侧标题和描述：

```javascript
d.push({
    title: account ? account.username : '点击登录',
    desc:  account ? 'LV.' + account.level + ' ' + account.level_name : '',
    img:   account ? avatarUrl : 'https://example.com/default_avatar.png',
    url:   loginLazyRule,
    col_type: 'avatar',
    extra: {
        longClick: contextMenuItems  // 长按菜单
    }
});
```

---

## 62.7 col_type: 'input' 输入框组件

在列表页内嵌搜索/输入框，支持实时响应和默认值：

```javascript
d.push({
    title: '搜索',
    desc:  '请输入关键词',
    col_type: 'input',
    url: $.toString(function() {
        putMyVar('search_kw', input);
        refreshPage(false);
        return 'hiker://empty';
    }),
    extra: {
        defaultValue: getMyVar('search_kw', ''),
        onChange: $.toString(function() {
            putMyVar('search_kw', input);  // 实时保存，不触发刷新
        }),
        // pageTitle: '搜索结果'  // 可选：设置页面标题
    }
});
```

> 📌 `url` 里的代码在用户**提交**（按回车/确认）时执行；`onChange` 在每次**输入变化**时执行。两者都可以访问 `input` 变量获取当前输入值。

---

## 62.8 storage0——跨页面大对象缓存

`getMyVar`/`putMyVar` 适合存小字符串，`storage0` 适合缓存 API 返回的大型 JSON 对象：

```javascript
// ✅ 缓存 API 数据（避免翻页时重复请求）
var cacheKey = 'site_data_page_' + MY_PAGE;
if (!storage0.getMyVar(cacheKey)) {
    var data = JSON.parse(fetch(apiUrl));
    storage0.putMyVar(cacheKey, data);    // 直接存对象，不需要 JSON.stringify
}
var cachedData = storage0.getMyVar(cacheKey);  // 取出即是对象

// ✅ 页面关闭时清理缓存（避免数据过期）
addListener('onClose', $.toString(function(key) {
    clearMyVar(key);
}, cacheKey));
```

**三层存储完整对比（R星精选实战提炼）：**

| 维度 | `juItem` | `storage0` | `putMyVar`/`getMyVar` |
|---|---|---|---|
| 作用域 | 当前规则 | 当前规则 | 当前规则 |
| 生命周期 | **持久化**（App 重启/规则删除前不丢） | 会话级（App 重启后丢失） | 会话级（App 重启后丢失） |
| 存储类型 | 字符串（存对象需手动 stringify） | **任意对象**（自动处理） | 字符串（存对象需手动 stringify） |
| 典型用途 | token、动态域名、CF Cookie | 分类列表、演员列表等大型 API 数据 | 分类选中 ID、排序方式、展开折叠状态 |
| 读取 | `juItem.get(k, def)` | `storage0.getMyVar(k)` | `getMyVar(k, def)` |
| 写入 | `juItem.set(k, v)` | `storage0.putMyVar(k, v)` | `putMyVar(k, v)` |
| 沙箱内可用 | ✅ | ✅ | ✅ |

```javascript
// ── 推荐分层用法 ──────────────────────────────────────────────

// juItem：存需要 App 重启后仍有效的凭证
juItem.set('token', token);
juItem.set('host', 'https://example.com/api/');

// storage0：存接口返回的分类数据（只请求一次，翻页复用）
if (!storage0.getMyVar('prefix_fenlei')) {
    storage0.putMyVar('prefix_fenlei', this.post('manhua/fenglei'));
}

// putMyVar：存用户当前操作状态（切换分类、排序、展开折叠）
putMyVar('prefix_tab', '1');
putMyVar('prefix_sort', 'time');
var fold = getMyVar('prefix_fold', '0');  // 第二参数是默认值
```

---

## 62.9 $().b64().lazyRule()——Base64 编码传参

当参数中含有特殊字符（如 URL、JSON 字符串）时，用 `.b64()` 对参数做 Base64 编码，避免序列化时特殊字符被转义破坏：

```javascript
// 普通传参（特殊字符可能被破坏）
url: $(normalString).lazyRule(function() { ... })

// b64 编码传参（安全传递含特殊字符的参数）
url: $('#noLoading#').b64().lazyRule(function(encodedId) {
    toast('已复制');
    return 'copy://' + encodedId;
}, data.id + '')
```

---

## 62.10 addListener('onClose') ——页面关闭回调

在页面关闭时执行清理逻辑（清缓存、重置状态等）：

```javascript
// 在 rule() 沙箱或二级函数开头注册
addListener('onClose', $.toString(function(id) {
    clearMyVar('site_detail_' + id);   // 清除该详情页的缓存
    clearMyVar('site_page_' + id);
}, contentId));
```

---

## 62.11 多功能规则主页分发架构

当一个规则需要承载多个完全不同的功能模块（首页、分类、视频、论坛、用户中心等）时，推荐用状态变量分发的架构：

```javascript
主页: function() {
    // ── 第一页：渲染导航 tab，setPreResult 先出 ────────────────
    if (MY_PAGE == 1) {
        // 渲染 tab 按钮 + loading 占位符
        setPreResult(d_);
    }

    // ── 按 tab 状态分发到不同渲染函数 ─────────────────────────
    var tab = getMyVar('site_tab', '1');
    if      (tab == '1') { this._renderHome(); }
    else if (tab == '2') { this._renderCate(); }
    else if (tab == '3') { this._renderVideo(); }
    else if (tab == '4') { this._renderForum(); }

    deleteItemByCls('loading_');
    setResult(d);
},
```

> 📌 **关键**：`setPreResult(d_)` 在数据请求**之前**调用，让用户先看到导航；`deleteItemByCls('loading_')` 在数据填充**之后**调用，删除占位符。这个顺序不能颠倒。

---

## 62.12 rich_text 富文本与 HTML 标签

`col_type: 'rich_text'` 的 `title` 字段支持部分 HTML 标签，可用于文字样式：

```javascript
// 彩色文字
title: '<font color=#FF6699>' + text + '</font>'

// 加粗彩色（需要 ''''' 前缀激活富文本）
title: "'''''<strong><font color=#FF6699>" + text + "</font></strong>"

// 超链接（在富文本中可跳转）
title: "<a href='" + url + "'>" + text + "</a>"

// 混合使用
desc: "''''❤️ " + likes + "喜欢  " + comments + "评论\n更新：" + date
```

---

*手册补充版本 V3.52 · 第六十二章：高级 UI 组件与多功能规则设计模式（禁漫天堂规则实战提炼）*

---

# 第六十三章：动态解析器 + 多态加密 + 跨规则跳转（综合视频站实战提炼）

本章基于含 `player_xxx` 内联 JSON + 站点自托管解析器 JS + 第三方 AES 解密接口的视频站规则开发实战提炼，记录了手册其他章节未覆盖的几个关键模式。

---

## 63.1 `player_xxx` 内联 JSON 提取

部分视频站把播放配置以 `player_xxxxxxxx = {...}` 变量的形式内联在详情页 HTML 里（变量名后缀随机），用正则提取后直接 JSON.parse：

```javascript
// 匹配 player_ 开头的变量赋值，取 = 后到 < 之前的 JSON 字符串
var raw = html.match(/r player_.*?=(.*?)</);
if (!raw) return 'toast://未找到播放配置';
var playerData = JSON.parse(raw[1].trim());

// playerData 结构示例：
// { url: "...", from: "jiexi", encrypt: "0"|"1"|"2", ... }
```

---

## 63.2 encrypt 字段三态解码

`playerData.encrypt` 标记了 URL 的编码方式，必须按值分支处理：

```javascript
var url = playerData.url;

if (playerData.encrypt == '1') {
    url = unescape(url);                      // URL 编码，unescape 还原
} else if (playerData.encrypt == '2') {
    url = unescape(base64Decode(url));        // Base64 再 URL 编码，两步还原
}
// encrypt == '0' 或不存在：明文，直接用

// 还原后判断类型
if (/mp4|mkv|avi/.test(url)) {
    return url;                               // 直链，直接返回
}
// 否则走解析器逻辑（见 63.3）
```

| encrypt 值 | 编码方式 | 解码方式 |
|---|---|---|
| `'0'` 或不存在 | 明文 | 直接用 |
| `'1'` | URL 编码 | `unescape(url)` |
| `'2'` | Base64 + URL 编码 | `unescape(base64Decode(url))` |

---

## 63.3 动态读取站点自托管解析器地址

站点把解析器 JS 路径配置在 `/static/player/{from}.js` 里，规则运行时动态读取，跳过注释行取有效 `src`：

```javascript
// playerData.from 是解析器名称，如 "jiexi"、"dplayer"
var jxJs  = request(MY_HOME + '/static/player/' + playerData.from + '.js');

// 提取所有 src="..." 并跳过注释行
var srcPattern = /src="([^"]*)"/g;
var srcs = [];
var m;
while ((m = srcPattern.exec(jxJs)) !== null) {
    // 判断该匹配所在行是否是注释行
    var lineStart = jxJs.lastIndexOf('\n', m.index) + 1;
    var lineEnd   = jxJs.indexOf('\n', m.index);
    var line      = jxJs.substring(lineStart, lineEnd === -1 ? jxJs.length : lineEnd);
    if (!line.trim().startsWith('//')) {
        srcs.push(m[1]);
    }
}

// 取最后一个有效 src（或第二个，视站点约定而定）
var jxBase = MY_HOME + srcs[srcs.length - 1].split("'+")[0];
// jxBase 即为解析器的实际入口地址前缀
```

> 📌 `split("'+")`：解析器 JS 里的 src 通常写成 `src="' + host + '/path'"` 的字符串拼接形式，`split("'+")`  取引号内的静态部分即可。

---

## 63.4 时间戳衍生 AES key + POST 解析接口

解析器 JS 里包含 `playPageUrl`、`secretKeySeed`、`timestamp` 等变量，通过 `eval` 注入当前作用域后 POST 到解析接口，返回加密 URL，再用时间戳衍生的 MD5 key 解密：

```javascript
// ── Step1：eval 解析器 JS，注入 playPageUrl / secretKeySeed / timestamp ──
var jxHtml = request(jxBase + encodeURIComponent(url));
eval(jxHtml.match(/const playPageUrl[\s\S]*?const timestamp\s*=\s*".*?";/)[0]);

// ── Step2：POST 到解析接口 ──────────────────────────────────────────────
var time = Math.floor(Date.now() / 1000);
var body = {
    vkey:      playPageUrl,
    code:      secretKeySeed,
    t:         time,
    signature: CryptoJS.MD5(time.toString()).toString()   // 签名 = MD5(时间戳)
};
var postRes = JSON.parse(fetch('https://api.example.com/webvideo.php', {
    method:  'POST',
    headers: { 'Content-Type': 'application/json', 'Origin': MY_HOME },
    body:    JSON.stringify(body)
}));
if (postRes.code !== 200) return 'toast://解析失败';

// ── Step3：时间戳衍生 key/iv，AES-CBC-Pkcs7 解密 ────────────────────────
eval(getCryptoJS());
var md5Key = CryptoJS.MD5(timestamp + 'YourSaltHere').toString();
var key    = CryptoJS.enc.Utf8.parse(md5Key.substring(16));   // 后16位为key
var iv     = CryptoJS.enc.Utf8.parse(md5Key.substring(0, 16)); // 前16位为iv
var playUrl = CryptoJS.AES.decrypt(postRes.url, key, {
    iv:      iv,
    mode:    CryptoJS.mode.CBC,
    padding: CryptoJS.pad.Pkcs7
}).toString(CryptoJS.enc.Utf8);
```

**key 衍生规律速查（从站点 JS 里确认 salt）：**

| 步骤 | 操作 |
|---|---|
| `md5Key` | `MD5(timestamp + salt).toString()` |
| `key` | `md5Key.substring(16)`（后16字符）|
| `iv` | `md5Key.substring(0, 16)`（前16字符）|
| 模式 | CBC + Pkcs7 |

> 📌 `timestamp` 和 `salt` 在解析器 JS 里，用 `eval` 注入后即可在当前作用域使用。`eval(getCryptoJS())` 必须在使用 `CryptoJS` 前调用（见 3.5.2 节）。

---

## 63.5 fallbackurl 多线路拆分

部分解析接口返回含 `/fallbackurl/` 分隔符的多线路 URL，需拆分为多线路 JSON 格式：

```javascript
if (playUrl && playUrl.indexOf('fallbackurl') !== -1) {
    var urls  = playUrl.split('/fallbackurl/').map(function(u) { return u.trim(); });
    var names = urls.map(function(_, i) {
        return urls.length > 1 ? 'HD' + (i + 1) : '默认';
    });
    return JSON.stringify({ urls: urls, names: names }) + '#isVideo=true#';
}
return playUrl + '#isVideo=true#';
```

**多线路 JSON 格式规范：**

```javascript
// 引擎识别的多线路格式
JSON.stringify({
    urls:  ['https://cdn1.example.com/v.m3u8', 'https://cdn2.example.com/v.m3u8'],
    names: ['HD1', 'HD2']
}) + '#isVideo=true#'
```

---

## 63.6 跨规则跳转（夸克网盘 / 百度云盘）

当解析结果是网盘链接时，可直接跳转到对应的专用规则页面处理，无需在本规则内实现网盘解析：

```javascript
if (/pan\.quark\.cn|drive\.uc\.cn/.test(playUrl)) {
    // 跳转到夸克网盘规则
    return 'hiker://page/quarkList?rule=Quark.简&page=fypage&realurl=' + encodeURIComponent(playUrl);
}
if (/pan\.baidu\.com/.test(playUrl)) {
    // 跳转到百度云盘规则
    return 'hiker://page/fxlj?rule=百度云盘&realurl=' + encodeURIComponent(playUrl);
}
```

**跨规则跳转 URL 格式：**

```
hiker://page/{页面名}?rule={规则名}&{参数}={值}
```

| 字段 | 说明 |
|---|---|
| `页面名` | 目标规则里的页面标识（如 `quarkList`、`fxlj`）|
| `rule=` | 目标规则的名称（必须已安装）|
| 附加参数 | 透传给目标规则的参数，用 `MY_PARAMS.xxx` 读取 |

> ⚠️ 目标规则必须已安装且名称完全匹配，否则跳转失败无提示。网盘链接建议先 `encodeURIComponent` 再拼接，避免链接里的 `&` 破坏 URL 参数解析。

---

## 63.7 本章模式完整流程图

```
请求详情页 HTML
    ↓
正则提取 player_xxx JSON → JSON.parse → { url, from, encrypt }
    ↓
按 encrypt 值解码 url（0=明文 / 1=unescape / 2=unescape+base64Decode）
    ↓
判断 url 类型：
  ├─ mp4/mkv 直链 → 直接 return
  ├─ 夸克/百度网盘 → hiker://page 跨规则跳转
  └─ 其他（需解析）→ 读 /static/player/{from}.js 取解析器入口
                          ↓
                    eval 解析器 JS → 注入 playPageUrl / secretKeySeed / timestamp
                          ↓
                    POST 解析接口（带 MD5 签名）→ 返回加密 URL
                          ↓
                    时间戳衍生 MD5 key → AES-CBC 解密 → 得到 m3u8
                          ↓
                    含 fallbackurl → 拆分多线路 JSON
                          ↓
                    return playUrl + '#isVideo=true#'
```

---

*手册补充版本 V3.55 · 第六十三章：动态解析器 + 多态加密 + 跨规则跳转（综合视频站实战提炼）*

---

# 第六十四章：Discuz! 论坛站规则开发 + 变量驱动路由状态机

本章基于 Discuz! 系论坛（含 sjs 型、本地 bbs 型站点）规则开发实战提炼，记录了 Discuz! 特有的数据结构处理方式，以及变量驱动路由这一区别于 switch-case 和 cate:// 的第三种状态机模式。

---

## 64.1 变量驱动路由状态机

区别于手册已有的 `switch-case` 状态机（按 tab 值分发）和 `cate://` 联动分类，**变量驱动路由**通过变量的值或空状态直接控制数据源选择，不依赖 UI 控件事件：

```javascript
主页: function() {
    var d = this.d;

    // sjs_board_url 非空时：直接请求板块页，跳过 mode 逻辑
    var boardUrl = juItem.get('sjs_board_url', '');
    if (boardUrl) {
        return this._renderBoard(boardUrl, d);
    }

    // sjs_board_url 为空时：按 mode 决定渲染路径
    var mode = getMyVar('sjs_mode', 'guide');
    if (mode === 'guide') {
        return this._renderGuide(d);      // 引导页：显示版块列表
    } else if (mode === 'forumlist') {
        return this._renderForumList(d);  // 论坛列表页
    }

    return d;
}
```

**与 switch-case 状态机的区别：**

| 维度 | switch-case 状态机 | 变量驱动路由 |
|---|---|---|
| 触发方式 | UI 控件点击 → putMyVar → refreshPage | 变量值/空状态直接决定分支 |
| 状态存储 | `putMyVar` 会话级 | 可混合 `juItem`（持久）+ `getMyVar`（会话）|
| 典型场景 | tab 切换、分类切换 | 登录态决定渲染路径、板块URL决定内容源 |
| 优先级 | 平级 if-else | 可有优先级（先判断高优先级变量）|

> 📌 变量驱动路由的核心思路：**变量的值或是否为空，决定走哪条数据路径**。登录后写入 `juItem.set('board_url', url)` 则后续直接进入板块，未登录则 `board_url` 为空走引导流程——这是一种隐式的状态切换，比显式的 UI 按钮更适合需要跨越页面生命周期的路由决策。

---

## 64.2 Discuz! formhash POST 搜索

Discuz! 的搜索走 CSRF 保护，先取 `formhash` 令牌，再用 POST 提交搜索表单：

```javascript
搜索: function(keyword) {
    var d = this.d;
    var ck = juItem.get('site_cookie', '');

    // ── Step1：取 formhash（防 CSRF 令牌）────────────────────
    var indexHtml = fetch(host + '/search.php', {
        headers: { Cookie: ck }
    });
    var formhash = (indexHtml.match(/formhash"\s+value="([^"]+)"/) || [])[1];
    if (!formhash) return d;  // 未登录或 Cookie 失效

    // ── Step2：POST 搜索表单 ─────────────────────────────────
    var searchRes = fetch(host + '/search.php?mod=forum', {
        method:  'POST',
        headers: {
            'Cookie':       ck,
            'Content-Type': 'application/x-www-form-urlencoded',
            'Referer':      host + '/'
        },
        body: 'formhash=' + formhash
            + '&srchtxt=' + encodeURIComponent(keyword)
            + '&searchsubmit=yes'
            + '&srchfid=0'
    });

    // ── Step3：从响应里取 searchid（见 64.3）────────────────
    var searchid = this._extractSearchId(searchRes);
    if (!searchid) return d;

    // ── Step4：请求搜索结果页 ────────────────────────────────
    var resultHtml = fetch(host + '/search.php?mod=forum&searchid=' + searchid + '&page=' + MY_PAGE, {
        headers: { Cookie: ck }
    });
    // 解析结果页...
    return d;
}
```

---

## 64.3 REPORTURL Base64 间接取 searchid

Discuz! 搜索结果页不直接暴露 `searchid`，而是藏在 `REPORTURL` 变量的 Base64 编码值里：

```javascript
_extractSearchId: function(html) {
    // Step1：正则取 REPORTURL 赋值
    var reportMatch = html.match(/REPORTURL\s*=\s*'([^']+)'/);
    if (!reportMatch) return '';
    var reportUrl = reportMatch[1];

    // Step2：Base64 解码（用 android.util.Base64，避免 atob 多字节问题）
    var bytes   = android.util.Base64.decode(reportUrl, android.util.Base64.DEFAULT);
    var decoded = new java.lang.String(bytes, 'UTF-8') + '';

    // Step3：从解码后的字符串取 searchid
    var sidMatch = decoded.match(/searchid=(\d+)/);
    return sidMatch ? sidMatch[1] : '';
}
```

**为什么用 `android.util.Base64` 而不是 `base64Decode`：**

`REPORTURL` 经过 URL 编码后再 Base64，可能包含多字节字符，Rhino 引擎的 `atob` 对非 ASCII 字节处理不稳定。`android.util.Base64.decode` + `new java.lang.String(bytes, 'UTF-8')` 是最可靠的组合（Law 37 延伸）。

---

## 64.4 Discuz! 特有 HTML 反混淆清洗

Discuz! 论坛为防止爬取，在帖子正文插入多种干扰元素，需专门清洗：

```javascript
_cleanDiscuzContent: function(html) {
    return html
        // ① 隐藏干扰文字（display:none 的 span）
        .replace(/<span[^>]*style="[^"]*display\s*:\s*none[^"]*"[^>]*>[\s\S]*?<\/span>/gi, '')

        // ② jammer 混淆字符（Discuz! 内置反爬字体类）
        .replace(/<font[^>]*class="[^"]*jammer[^"]*"[^>]*>[\s\S]*?<\/font>/gi, '')

        // ③ ASCII 随机噪声（1-3个可见 ASCII 字符夹杂空格的模式）
        .replace(/[!-~]{1,3}\s*[!-~]{1,3}\s*[!-~]{1,3}/g, '')

        // ④ 去除剩余 HTML 标签，保留换行
        .replace(/<br\s*\/?>/gi, '\n')
        .replace(/<[^>]+>/g, '')
        .replace(/&nbsp;/g, ' ')
        .replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&amp;/g, '&')
        .trim();
}
```

**三种干扰元素说明：**

| 干扰类型 | HTML 特征 | 效果 |
|---|---|---|
| 隐藏 span | `style="display:none"` | 视觉不可见，但爬虫会读到 |
| jammer font | `class="jammer"` | Discuz! 专用混淆字体，渲染为不可见字符 |
| ASCII 噪声 | 随机可见 ASCII 字符 + 空格 | 在正文里插入乱码干扰 NLP/搜索 |

> 📌 清洗顺序重要：先去隐藏元素和 jammer，再去 ASCII 噪声，最后去标签。顺序反了会导致标签里的 ASCII 字符被误删，清洗后 HTML 结构残留。

---

## 64.5 本章铁律补充

| Law | 名称 | 核心要义 |
|---|---|---|
| Law 120 | Discuz! 先取 formhash | POST 搜索前必须先 GET 取 formhash，缺令牌会 403 |
| Law 121 | REPORTURL 间接取 searchid | Discuz! searchid 藏在 REPORTURL 的 Base64 里，不在 URL 参数上 |
| Law 122 | Discuz! 三层反混淆 | 隐藏 span → jammer font → ASCII 噪声，三层按序清洗 |
| Law 123 | fba.* 只在 extra.js 里用 | WebView 桥接 API 只能在 extra.js 沙箱内调用，规则主函数里不可用 |
| Law 124 | Cookie 双重校验 | 长度阈值 + 关键字，单一条件易误判；校验失败即清空触发重登 |

---

## 64.6 禁区/敏感内容开关模式

需要区分"普通内容"和"限制内容"两组分类，并给用户一个持久化的开关：

```javascript
// ── 开关状态持久化（getItem 跨 App 重启保留）─────────────────
var isOpen = getItem('nsfw_open', '0');  // '0'=关闭 '1'=开启

// ── 两组分类列表缓存在 storage0 ──────────────────────────────
if (!storage0.getMyVar('cate_normal')) {
    var all = JSON.parse(fetch(host + '/api/categories'));
    storage0.putMyVar('cate_normal', all.filter(function(c) { return !c.nsfw; }));
    storage0.putMyVar('cate_nsfw',   all.filter(function(c) { return c.nsfw; }));
}
var categories = isOpen === '1'
    ? storage0.getMyVar('cate_normal').concat(storage0.getMyVar('cate_nsfw'))
    : storage0.getMyVar('cate_normal');

// ── 开关按钮（confirm 确认弹窗）─────────────────────────────
d.push({
    title: isOpen === '1' ? '🔴 关闭禁区' : '🟢 开启禁区',
    col_type: 'scroll_button',
    url: $.toString(function() {
        var cur = getItem('nsfw_open', '0');
        if (cur === '0') {
            confirm({
                title: '提示',
                content: '确认开启限制内容？',
                confirm: $.toString(function() {
                    setItem('nsfw_open', '1');
                    refreshPage(false);
                }),
                cancel: $.toString(function() {})
            });
        } else {
            setItem('nsfw_open', '0');
            refreshPage(false);
        }
    })
});
```

> 📌 `getItem`/`setItem` 是壳子函数，效果等同于 `juItem.get`/`juItem.set`，在聚阅引擎下均持久化存储。两组分类列表用 `storage0` 缓存避免重复请求，开关切换只需重新 `concat` 合并即可。

---

## 64.7 综合坑点速查（实战提炼）

| 坑 | 根本原因 | 解决方案 |
|---|---|---|
| `CryptoJS` 未定义 | `eval(getCryptoJS())` 在旧版聚阅不生效 | 改用 `$.require('hiker://assets/crypto-java.js')` 的 `CryptoUtil` |
| 沙箱内点击空白 | `token`/`deviceId` 在沙箱内 `juItem` 不可用 | 沙箱外取好后通过参数显式传入（见 32.9 节）|
| 子分类点进去空白 | 嵌套沙箱 `host`/`token` 参数漏传 | 逐层显式传参，推荐用 `ctx` 对象打包所有依赖 |
| 图片空白 | `$.image()` 回调用了箭头函数 | 必须用 `function()`，Law 68 |
| 搜索不能翻页 | 搜索函数内部直接渲染而不走主页 | 改为只写 `putMyVar + refreshPage`，由主页接管（见 29.3.2a 节）|
| 切换 tab 后内容残留 | 子分类状态变量未清空 | 切换父分类时同步清空所有子 key（Law 84）|
| `encData` 解密返回空 | padding 或 mode 写错 | 从站点 JS 源码逐行确认 mode/padding，ECB 无需 iv |
| `this` 在 `lazyRule` 内 undefined | 沙箱隔离 | 把 `this.xxx` 在沙箱外取好后作为参数传入 |
| `$.toString` 比 `lazyRule` 更可靠 | 旧版聚阅 `lazyRule` 偶发失效 | 状态切换按钮优先用 `$.toString`（见 54.8 节）|
| `juItem` 沙箱内不可用 | 部分引擎版本沙箱隔离 `juItem` | 统一在沙箱外调用 `juItem.get` 后传入 |
| 签名校验失败 | 参数名/顺序与站点 JS 不一致 | 从抓包逐行核对，MD5/SHA256 的拼接顺序必须完全一致 |
| `getItem` 不存在报错 | 壳子专用函数，聚阅不支持 | 全部替换为 `juItem.get`/`juItem.set` |

---

*手册补充版本 V3.55 · 第六十四章：Discuz! 论坛站规则开发 + 变量驱动路由状态机*


---

# 第六十八章：App 逆向规则开发完全指南（DEX 分析 + Python 验证 + 规则落地）

本章基于国产视频 App（AES-CBC + 签名 Header + S3 重定向播放）规则开发实战提炼，总结"Python 测试先行 → 链路验证 → 规则落地"的完整流程。

---

## 68.1 核心方法论：Python 测试先行

**不要直接在规则里试错。** 先用 Python 把接口调通，确认每一步的请求参数、加密算法、数据格式，保证链路完整可用，再按手册规范转为规则代码。

```
APK 逆向（jadx） → 提取 key/iv/域名/Header
        ↓
Python 脚本验证每一步接口
        ↓
确认链路完整 → 按规则规范落地
```

---

## 68.2 DEX 逆向关键词速查

用 jadx 打开 APK 后，按以下关键词定向搜索：

| 搜索词 | 找到的内容 |
|---|---|
| `DOMAIN` / `BuildConfigs` | 真实 API 域名、备用域名 |
| `OkHttpConfig` / `App-api-verify` | 签名 Header 注入拦截器 |
| `ApiEncryptUtils` | 加解密方法、密钥、IV |
| `VodParseListener` / `GetJsonData` | 解析结果回调、返回数据结构 |
| `PlayerInfoBean` / `PlayUrlBean` | 播放地址字段名 |
| `API_APP_VOD_PARSE` | 解析接口调用位置 |
| `Urls` | 接口路径常量 |
| `init` / `typeFilterVodList` | 接口路径定义 |

---

## 68.3 AES-CBC 签名 Header 体系

从 OkHttpConfig 拦截器提取的必需请求头：

```javascript
// 签名算法：AES-CBC-PKCS7，key/iv 从 DEX 提取
var key = '247D7D65913BF2FF';   // 示例，按实际 DEX 替换
var iv  = '247D7D65913BF2FF';

// 构建签名 header
var timestamp = Math.floor(Date.now() / 1000) + '';
eval(getCryptoJS());
var keyParsed = CryptoJS.enc.Utf8.parse(key);
var ivParsed  = CryptoJS.enc.Utf8.parse(iv);
var sign = CryptoJS.AES.encrypt(
    CryptoJS.enc.Utf8.parse(timestamp), keyParsed,
    { iv: ivParsed, mode: CryptoJS.mode.CBC, padding: CryptoJS.pad.Pkcs7 }
).toString();

var headers = {
    'app-api-verify-time': timestamp,
    'app-api-verify-sign': sign,
    'app-user-device-id':  juItem.get('deviceId', ''),  // 32位随机hex，见 3.4.4
    'app-version-code':    '128',
    'app-user-token':      juItem.get('token', ''),      // 游客为空
    'Content-Type':        'application/x-www-form-urlencoded',
    'User-Agent':          'okhttp/3.14.9'
};
```

---

## 68.4 接口响应解密

```javascript
_decrypt: function(word) {
    eval(getCryptoJS());
    var keyParsed = CryptoJS.enc.Utf8.parse(this._key);
    var ivParsed  = CryptoJS.enc.Utf8.parse(this._iv);
    return CryptoJS.AES.decrypt(word, keyParsed, {
        iv: ivParsed,
        mode: CryptoJS.mode.CBC,
        padding: CryptoJS.pad.Pkcs7
    }).toString(CryptoJS.enc.Utf8);
},

// 使用
var raw    = fetch(apiUrl, { headers: this._buildHeaders(), method: 'POST', body: 'vod_id=' + id });
var obj    = JSON.parse(raw);
var data   = JSON.parse(this._decrypt(obj.data));
```

> 📌 如果 `eval(getCryptoJS())` 报 CryptoJS 未定义，改用 `CryptoUtil` 方案（见 10.5 节用法五）。

---

## 68.5 播放地址链路

**标准模式（三步）：**

```
列表接口 → vod_id
    ↓
vodDetail 接口 → vod_play_list → { url, parse_api }
    ↓
vodParse 接口 → encrypt(url) → 真实 m3u8
```

**降级模式（vodParse native so 不通时）：**

```
vodDetail 接口 → url（首集直链）
    ↓
直链 302 重定向 → S3 签名地址
    ↓
return input + ';{Referer@host/&&User-Agent@okhttp/3.14.9}'
（播放器自动追重定向，不需要手动解析 S3 地址）
```

```javascript
// 降级模式规则写法
解析: function(url) {
    // url 是加密后的首集地址，先解密
    var realUrl = this._decrypt(url);
    // 直接返回，播放器会自动追 302 重定向到 S3
    return realUrl + ';{Referer@' + this.host + '/&&User-Agent@okhttp/3.14.9}';
},
```

---

## 68.6 Python 验证模板

```python
import base64, time, json, requests
from Crypto.Cipher import AES
from Crypto.Util.Padding import pad, unpad

KEY = b'247D7D65913BF2FF'   # 按实际替换
IV  = b'247D7D65913BF2FF'
HOST = 'https://api.example.com'
UA   = 'okhttp/3.14.9'

def encrypt(word: str) -> str:
    cipher = AES.new(KEY, AES.MODE_CBC, IV)
    return base64.b64encode(cipher.encrypt(pad(word.encode(), 16))).decode()

def decrypt(word: str) -> str:
    cipher = AES.new(KEY, AES.MODE_CBC, IV)
    return unpad(cipher.decrypt(base64.b64decode(word)), 16).decode()

def build_headers(token=''):
    ts   = str(int(time.time()))
    sign = encrypt(ts)
    return {
        'app-api-verify-time': ts,
        'app-api-verify-sign': sign,
        'app-user-device-id':  'abcdef1234567890abcdef1234567890',
        'app-version-code':    '128',
        'app-user-token':      token,
        'User-Agent':          UA,
        'Content-Type':        'application/x-www-form-urlencoded',
    }

# 1. 获取详情页
r    = requests.post(HOST + '/api.php/getappapi.index/vodDetail',
           headers=build_headers(), data=b'vod_id=2699')
data = json.loads(decrypt(json.loads(r.text)['data']))

# 2. 提取首集 URL
first_url = data['vod_play_list'][0]['urls'][0]['url']
print('首集地址:', first_url)

# 3. 测试播放（验证重定向）
r2 = requests.get(first_url, headers={'User-Agent': UA,
     'Referer': HOST + '/'}, allow_redirects=True)
print(f'最终状态码: {r2.status_code}')
print(f'最终 URL: {r2.url[:80]}')
```

---

## 68.7 Python → 规则代码映射表

| Python 写法 | 规则对应写法 |
|---|---|
| `encrypt(word)` | `this._encrypt(word)`（内联 CryptoJS/CryptoUtil）|
| `decrypt(word)` | `this._decrypt(word)` |
| `requests.post(url, headers=h, data=b)` | `fetch(url, {headers: h, body: b, method: 'POST'})` |
| `requests.get(url, headers=h)` | `request(url, {headers: h})` |
| `allow_redirects=True` | `return input + ';{Header}'`（播放器自动追重定向）|
| `DeviceUtils.getUniqueDeviceId()` | 随机 32 位 hex，`juItem.set` 持久化（见 3.4.4 节）|
| `BuildConfigs.DOMAIN` | `parse.host` |
| `handler.post(runnable)` | 不需要，规则内同步执行 |

---

## 68.8 踩坑速查

| 问题 | 根本原因 | 解决方案 |
|---|---|---|
| 接口返回 `data:[]` 空 | 缺少签名 header | 加 `app-api-verify-time` / `app-api-verify-sign` |
| 签名不对，接口 401 | key/iv 在 native so 里 | 先用 DEX 里 Java 层的 key/iv，大多数 App 通用 |
| `vodParse` 接口不通 | 核心解析在 native so，Python 调不到 | 降级：直接测试首集 URL 是否可追重定向播放 |
| S3 HEAD 请求 403 | S3 对 HEAD 限制 | 改用 GET 测试，播放器 GET 请求能正常下载 |
| 播放器不追重定向 | UA/Referer 不匹配 | `return url + ';{Referer@host/&&User-Agent@okhttp/3.14.9}'` |
| `CryptoJS` 未定义 | 旧版聚阅 eval 不生效 | 改用 `CryptoUtil`（见 10.5 节）|
| DEX 找不到 key | key 硬编码在 so 中 | 用 frida hook AES 调用捕获运行时 key |

---

## 68.9 本章铁律补充

| Law | 名称 | 核心要义 |
|---|---|---|
| Law 126 | Python 测试先行 | 接口链路 Python 验证通过后再落地规则，禁止直接在规则里试错 |
| Law 127 | DEX 优先 Java 层 | key/iv 先在 DEX Java 层找，native so 是最后手段 |
| Law 128 | 重定向交给播放器 | S3/CDN 302 重定向不需要手动解析，`return url + ';{Header}'` 即可 |
| Law 129 | 嵌套沙箱逐层透传 | host/token/deviceId 必须从直接外层形参取，严禁跨层引用，口诀：hh→hhh→hhh2 |

---

*手册补充版本 V3.66 · 第六十八章：App 逆向规则开发完全指南*

---

> **第六十九章：URL 协议大全（hiker://、x5://、协议路由）**

URL 协议是规则跳转、UI 弹窗、文件操作的核心入口。除 `video://`/`pics://`/`web://`/`webview://` 等已在前文出现的协议外，下表汇总全部可用协议。**请求时引擎自动剥除协议前缀**，无需手动清理。

## 69.1 系统页面跳转（hiker://*）

| 协议 | 用途 | 仅小程序内 | 高级用法 |
|---|---|---|---|
| `hiker://home` | 展开全部小程序 | 否 | `hiker://home@规则名`（带补偿：`@规则1\|\|规则2\|\|http://fallback`） |
| `hiker://bookmark` | 跳转书签页 | 否 | — |
| `hiker://history` | 跳转历史记录 | 否 | `hiker://history?rule=xxx` 指定规则 |
| `hiker://collection` | 跳转收藏页 | 否 | `?group=分组名` 弹分组、`?rule=xxx` 指定规则 |
| `hiker://download` | 跳转下载中心 | 否 | — |
| `hiker://search` | 搜索页 | 小程序+网页 | `?s=词&group=分组&rule=规则&simple=false` |
| `hiker://js` | 网页插件管理 | 是 | — |
| `hiker://adUrl` | 网址拦截管理 | 是 | — |
| `hiker://adRule` | 元素拦截页 | 是 | — |
| `hiker://setting` | 设置页 | 是 | — |
| `hiker://settingMore` | 更多设置页 | 是 | — |
| `hiker://webdav` | 一键备份到 WebDav | 是 | 需先配置 WebDav 账号 |
| `hiker://webRule` | 开启 Web 编辑模式 | 是 | — |
| `hiker://debug` | 开发助手页 | 是 | `?url=xxx` 自动加载源码 |
| `hiker://explore` | 文件管理界面 | 是 | `?path=hiker://files/cache` 或绝对路径 |
| `hiker://localMedia` | 本地音视频媒体库 | 是 | — |
| `hiker://webSearch` | 跳转网页搜索 | 是 | — |
| `hiker://empty` | 空返回（占位 URL） | 是 | 见前文，用于矩阵静态分类、子页面跳转 |
| `hiker://page/xxx` | 跳转子页面 | 是 | `?rule=xxx` 跨规则跳子页面，extra 传 url 覆盖加载链接 |

```javascript
// 实战：长按按钮跳收藏分组
{ title: '我的追番', col_type: 'icon_4', url: 'hiker://collection?group=追番' }

// 实战：跳指定规则的搜索结果（不允许切换规则）
{ url: 'hiker://search?s=' + word + '&rule=' + MY_RULE.title }
```

## 69.2 弹窗/交互协议

| 协议 | 格式 | 说明 |
|---|---|---|
| `toast://` | `toast://提示文本` | 短暂浮窗提示 |
| `confirm://` | `confirm://内容.js:'toast://点确认了'` | 确认框，`.js:` 后是点确认时执行的代码 |
| `input://` | `input://{"value":"默认值","hint":"提示","js":"'toast://'+input"}` | 输入框；`value` 支持 `{{clipboard}}` 自动填剪贴板 |
| `select://` | `select://{"title":"选择","options":["A","B"],"col":3,"js":"'toast://'+input,MY_INDEX"}` | 下拉选择；`options` 支持 `{title,icon}` 加图标；`selectedIndex` 预选；`attachView:true` 依附按钮；`bottom:true` 底部弹出 |
| `copy://` | `copy://内容.js:'http://后续链接'` | 复制到剪贴板，可不带 `.js:` |
| `rule://口令` | `rule://base64口令` | 完整 base64 编码的规则口令，点击导入 |
| `海阔视界...` | `海阔视界，xxx￥home_rule￥{...}` | 见第七十章，云口令格式 |

```javascript
// 实战：弹输入框 + 用户输入立即搜索
return 'input://' + JSON.stringify({
    hint: '输入关键词',
    js: $.toString(() => 'hiker://search?s=' + input + '&rule=' + MY_RULE.title)
});

// 实战：弹底部菜单
return 'select://' + JSON.stringify({
    title: '选择线路',
    options: lineNames,
    bottom: true,
    js: $.toString(() => 'toast://选了第' + MY_INDEX + '条')
});
```

## 69.3 文件 & 系统操作协议

| 协议 | 格式 | 说明 |
|---|---|---|
| `download://` | `download://http://x.com/1.mp4` | 直接下载（视频/音频/APK） |
| `share://` | `share://hiker://files/a.txt` | 系统分享弹窗（文件需存在） |
| `editFile://` | `editFile://hiker://files/a.txt` 或 `...@js=保存后执行的代码` | 进入编辑器，保存触发回调，`input` 为文件路径 |
| `openFile://` | `openFile://hiker://files/a.txt` | 第三方软件打开文件 |
| `fileSelect://` | `fileSelect://log(input)` | 系统选文件后执行 JS；`input` 为软件可读路径；原始路径用 `MY_PATH`（需用户授权全部文件） |

> ⚠️ `fileSelect://` 选到的文件**不是原始路径**，系统会把文件复制到软件目录并加 `_fileSelect_` 前缀，软件下次启动会自动清理。

## 69.4 X5/网页/视频路由协议

| 协议 | 用途 |
|---|---|
| `x5://http://a.com` | X5 全屏打开网页 |
| `x5WebView://http://a.com` | 刷新当前页面的 X5 组件内容（见 5.7 x5_webview_single） |
| `x5Play://http://a.com/1.mp4` | 二级强制用 X5 播放器播放（忽略嗅探） |
| `web://http://a.com` | 强制用网页打开（绕过当前规则的二级解析） |
| `webview://http://a.com;get;UTF-8;{}` | 用 WebView 加载并提取源码（同 `fetchCodeByWebView`） |
| `video://...` | 标准视频协议（见第八章） |
| `pics://url1&&url2` | 多图模式（漫画/图集） |
| `x5Rule://链接@JS代码` | X5 嗅探资源（TBS 内核） |
| `webRule://链接@JS代码` | 系统 WebView 嗅探资源（推荐用此替代 x5Rule） |

```javascript
// 实战：webRule:// 嗅探音频地址（已支持 extra 设 UA/Referer/blockRules）
{
    title: '提取音频',
    url: 'webRule://' + pageUrl + '@' + $.toString(() => {
        var a = document.querySelector('audio');
        return a ? a.src : null;
    }),
    extra: {
        ua: PC_UA,
        referer: 'http://host.com',
        blockRules: ['.png', '.jpg']
    }
}
// 等价的代码方式：
var url = executeWebRule(pageUrl, $.toString(() => {
    var a = document.querySelector('audio');
    return a ? a.src : null;
}), { ua: PC_UA, timeout: 10000, checkTime: 100 });
```

## 69.5 协议路由本章铁律

| Law | 核心要义 |
|---|---|
| Law 130 | `select://`/`input://`/`confirm://` 三协议的 JSON 字段都是字符串，`js` 字段的内容是 JS 代码字符串，用 `$.toString()` 包裹 |
| Law 131 | `MY_INDEX` 仅在 `select:// js` 里可用；`input` 变量仅在协议回调里可用（区分输入框和列表元素的同名变量） |
| Law 132 | `x5Rule://` 用 TBS 内核已不推荐，新规则用 `webRule://`（系统内核，更稳定） |

---

> **第七十章：自动导入与云口令体系**

聚阅/海阔的"复制口令打开软件即导入"机制基于固定字符串模板。规则可以**生成口令分享**，也可以**主动写入触发导入**。所有口令都遵循：

```
海阔视界，<可自定义说明>￥<标识>￥<内容>
```

注意分隔符是**全角￥**（U+FFE5），不是 `$`。

## 70.1 标识对照表

| 标识 | 内容形式 | 用途 |
|---|---|---|
| `home_rule_url` | https://xxx/rule.json | 首页规则合集订阅 |
| `home_rule` | `{'title':'VIP', ...}` 规则对象 | 单条小程序规则 |
| `search_engine_url` | https://xxx/search.json | 搜索引擎合集 |
| `search_engine_v2` | `{'title':'VIP', ...}` 引擎对象 | 单条搜索引擎 |
| `js_url` | `global_movie@https://xxx/插件.js` 或 `域名_序号_名称@https://...` | 网页插件 |
| `ad_url_rule` | `https://xxx/rule&&xn--` | 广告网址拦截 |
| `bookmark` | `{'title':'VIP', ...}` 书签对象 | 书签规则 |
| `bookmark_url` | hiker://files/share/share-bookmarks.json | 书签合集 |
| `file_url` | `hiker://files/a.txt@https://xxx/源文件` | 本地文件订阅 |
| `fast_play_urls` | https://xxx/list.txt | 快速播放白名单 |
| `xt_dialog_rules` | bbs.fy-sys.com | 嗅探弹窗黑名单 |
| `ad_subscribe_url` | `{"urlV2":"https://ad.txt","domBlockRuleUrl":"https://dom.txt"}` | 广告拦截订阅 |
| `home_sub` | https://xxx/sub.json | 小程序规则订阅 |
| `publish_account` | `账号@密码` | 云仓库账密 |
| `require_url` | https://xxx/test.js | 远程依赖更新 |
| `web-proxy` | `{"name":"x","match":"x"}` 或数组 | 浏览器代理规则 |

```javascript
// 实战：规则内一键导出当前规则为云口令
var rule = { title: MY_RULE.title, rule: '...' };
var token = '海阔视界，' + rule.title + '小程序￥home_rule￥' + JSON.stringify(rule);
copy(token);
return 'toast://口令已复制，请在软件中粘贴或拖入剪贴板检测';
```

## 70.2 `@import=js:` —— 写文件即触发的高级口令

最强的口令模式：剪贴板内容自带 `@import=js:` 时，软件会把 `@import=js:` 前的内容作为 `input`，执行后面的 JS 代码。

```text
# 范例：让用户复制下面整段，打开软件即写入文件
云口令，复制整条口令打开软件即可导入
https://xxx.cn/test.js@import=js:writeFile('hiker://files/cache/test.js', fetch(input))

# 范例：让 JS 决定要导入什么（动态拉规则）
云口令，复制整条口令打开软件即可导入
https://xxx.cn/test.json@import=js:fetch(input)
```

| 规则 | 说明 |
|---|---|
| 第一行（"云口令..."）可自定义，仅作识别提示 | |
| `@import=js:` **必须紧跟换行符** | 前一行换行后即 `input` 的值 |
| `input` 不能包含换行符 | 多行内容需先 base64 编码 |
| JS 代码作用域和规则 JS 完全一致 | 可用 fetch/writeFile/publishRule/.. |

## 70.3 主动调用 `publishRule` 上传云仓库

```javascript
var rule = { title: 'JuYue1', rule: 'js:...' };
publishRule(rule);                    // 单条
publishRule([rule1, rule2, rule3]);    // 批量
// 软件会弹窗征求用户同意，同意后才真正上传
```

需先用 `publish_account` 口令导入云仓库账号（或在设置内填写）。

## 70.4 本章铁律

| Law | 核心要义 |
|---|---|
| Law 133 | 云口令分隔符是全角￥，规则代码里手写时确认是 `\uFFE5` |
| Law 134 | `@import=js:` 适合"一键安装/更新"，不要嵌套 `@import=js:`（行为未定义） |
| Law 135 | `publishRule` 必须用户同意，规则里直接调用会弹确认框，**不允许静默上传** |

---

> **第七十一章：fba (fy_bridge_app) 桥接 API 完整参考**

`fy_bridge_app` 是 X5/WebView 组件内可用的桥接对象，简写 `fba`。规则侧（JS 规则）与 WebView 侧（`extra.js`、`x5Rule://`、`javascript:` 链接）调用方式略有差异——**`javascript:` 模式下必须先 `eval(fy_bridge_app.getInternalJs())` 才能用 request 等方法**。

## 71.1 UI/页面控制

| 方法 | 说明 |
|---|---|
| `fba.setAppBarColor('#ffffff')` | 设置状态栏颜色 |
| `fba.setWebTitle(title)` | 设置网页标题 |
| `fba.setWebUa(ua)` | 设置当前域名的 UA |
| `fba.refreshPage(true)` | 刷新所在的二级页面（仅 X5WebView 内） |
| `fba.refreshX5Desc('float&&255')` | 刷新 X5 组件的 desc（不重载网页） |
| `fba.back()` | 关闭当前 WebView |
| `fba.toast('提示')` | 显示 toast |

## 71.2 媒体/资源

| 方法 | 说明 |
|---|---|
| `fba.playVideo(url)` | 播放单视频/音频 |
| `fba.playVideos(JSON.stringify([{title,url,use:true}]))` | 播放选集；带 lazyRule 时附加 `codeAndHeader:";get"` + `originalUrl` |
| `fba.showPic(url)` | 大图显示 |
| `fba.saveImage('http://1.png\|\|http://2.png','hiker://files/1.png')` | 保存图片到相册（多地址竖线分隔，失败自动重试下一个） |
| `fba.clearM3u8Ad('http://1.m3u8')` | 清除 m3u8 内的 `#EXT-X-DISCONTINUITY` 广告段（自动取当前页 header） |
| `fba.isVideoOrMusic(url)` | 判断链接是不是音视频 |

## 71.3 数据交互

| 方法 | 说明 |
|---|---|
| `fba.getCookie('http://a.com/')` | 取当前网页域名的 Cookie（即使 http-only 也能取到） |
| `fba.getUa()` | 取当前网页 UA |
| `fba.getHeaderUrl(url)` | 把请求时 Header 拼成 `url;{K@V}` 形式（参数必须是 `_getUrls()` 加载过的链接） |
| `getRequestHeaders(url)` | 取请求 Header 对象（同上要求） |
| `fba.putVar(k, v)` / `fba.getVar(k)` / `fba.clearVar(k)` | 字符串型全局变量，软件本次运行内有效；规则侧 `putVar/getVar` 可读写同一份 |
| `fba.writeFile(path, content)` | 写文件 |
| `request(url, opts)` | 同步请求；页面加载完成后才能用，否则用 `request00`；`url` 支持 `hiker://files/` |
| `requestAsync(url, [param], [key], callback)` | 异步请求；`callback(key, result)` |

## 71.4 跳转/导入

| 方法 | 说明 |
|---|---|
| `fba.importRule('海阔视界，...')` | 触发规则导入流程 |
| `fba.openThirdApp('legado://xxxx')` | 用 intent 唤起第三方 APP |
| `fba.newPage(title, url)` | X5 内开新页面（避免返回键销毁整个 X5） |
| `fba.open(JSON.stringify({rule, title, url, group:'', col_type:'', findRule:'', preRule:'', extra:{}}))` | 跳转到视界原生二级页（取代废弃的 `toDetailPage`） |

## 71.5 解析/规则桥接

| 方法 | 说明 |
|---|---|
| `fba.parseDomForHtml(html, rule)` | 同规则侧 pdfh |
| `fba.parseDomForArray(html, rule)` | 返回**字符串数组的 JSON**，需 `JSON.parse` |
| `fba.parseLazyRule('http://x@lazyRule=.js:input')` | 同步解析 lazyRule（**会阻塞整个网页加载**） |
| `fba.parseLazyRuleAsync(url, $.toString(() => console.log(input)))` | 异步解析，回调里用 `input` 取结果 |
| `fba.parsePaste('http://x.com')` | 解析云剪贴板 |
| `fba.getInternalJs()` | 在 `javascript:` 模式下必须先 `eval(fba.getInternalJs())` 才能用 request/requestAsync |

## 71.6 完整用例：X5 内自动登录 + Cookie 回传

```javascript
{
    col_type: 'x5_webview_single',
    desc: 'float&&500',
    url: parse.host + '/login',
    extra: {
        canBack: true,
        js: $.toString(() => {
            // 等用户手动登录完成，监听 DOM 检测登录成功
            var checker = setInterval(() => {
                if (document.querySelector('.user-info')) {
                    clearInterval(checker);
                    var ck = fba.getCookie(location.href);
                    fba.putVar('site_cookie', ck);
                    fba.toast('登录成功，正在返回');
                    fba.back();
                }
            }, 1000);
        })
    }
}

// 规则主函数读回 Cookie 并持久化
var cookie = getVar('site_cookie', '');
if (cookie) {
    juItem.set('site_cookie', cookie);
    clearVar('site_cookie');  // 中转完即清
}
```

## 71.7 本章铁律

| Law | 核心要义 |
|---|---|
| Law 136 | 写在 `extra.js` 里的代码运行在 WebView 沙箱，**不能引用规则侧的 `this`/`parse`/`juItem`**，要回传数据走 `fba.putVar` 中转 |
| Law 137 | `fba.parseLazyRule` 会阻塞整个页面，能用 `parseLazyRuleAsync` 就不要用同步版 |
| Law 138 | `javascript:` 协议下用 `eval(fba.getInternalJs())` 解锁 request/requestAsync，否则方法不存在 |
| Law 139 | `fba.getHeaderUrl` / `getRequestHeaders` 只对 `_getUrls()` 收集过的链接有效，没出现在嗅探记录里的链接返回空 |

---

> **第七十二章：进阶 JS API 速查（DNS / Java / 代理 / 任务）**

本章汇总海阔/聚阅引擎里的高级原生函数，多数为系统级能力，**需用户授权**后才能正常工作。

## 72.1 自定义 DNS / IP Ping

```javascript
// 单条注册（影响全局 fetch/request/require/首页/搜索）
registerDNS({ 'www.themoviedb.org': '1.1.1.1 2.2.2.2' });   // 多 IP 空格分隔
registerDNS({ '.themoviedb.org': '1.1.1.1' });               // 子域名半匹配
registerDNS({ 'a.com': 'https://dns.alidns.com/dns-query' }); // DoH

// 检测可达 IP
var ok = ipping('1.1.1.1', 2000);              // 单 IP 探测
var ip = findReachableIP(['1.1.1.1', '2.2.2.2'], 2000);  // 批量并发，找到一个立刻返回

// 实战：动态选 IP 后注册
var ip = findReachableIP(['1.1.1.1', '2.2.2.2']);
if (ip) registerDNS({ 'a.com': ip });

// fetch 单次自定义 DNS（不全局）
var html = fetch(url, { dns: 'https://dns.alidns.com/dns-query' });
```

首次调用 `registerDNS` 会弹授权框，用户拒绝则无效。

## 72.2 Java 字节码加载（loadJavaClass）

```javascript
// 1. 下载 dex
requireDownload('https://xxx/d.dex', 'hiker://files/cache/t.dex');

// 2. 加载类 → newInstance
var Test = loadJavaClass('hiker://files/cache/t.dex', 'com.test.code.TestCode');
log(Test.hello());

// 3. 携带 so 文件（按当前 ABI 放在 cache/dir 下）
var abi = getCpuAbi();   // arm64-v8a / armeabi-v7a / ...
loadJavaClass('hiker://files/cache/t.dex', 'com.test.code.TestCode', 'hiker://files/cache/dir/a.so');
loadJavaClass('hiker://files/cache/t.dex', 'com.test.code.TestCode', 'hiker://files/cache/dir');

// 4. 只要 Class，不要实例
var Clz = findJavaClass('hiker://files/cache/t.dex', 'com.test.code.TestCode');
```

> ⚠️ `loadJavaClass` 为高危方法，**每次执行都弹授权框**，且只能在用户主动操作的链路上调用，启动时直接执行会被拦截。

## 72.3 后台代理服务器（startProxyServer）

把 JS 函数包装成一个本地 HTTP 端点，常用于给播放器喂动态生成的 m3u8 或带特殊解密的流。

```javascript
// 基础：返回 m3u8 内容
var url = startProxyServer($.toString(() => {
    return fetch(base64Decode(MY_PARAMS.u));
}));
url = url + '?u=' + base64Encode('http://1.com/origin.m3u8');
// 把 url 直接给播放器，每次请求都会执行 JS

// 进阶：自定义状态码 + Header（用于 302 转跳本地文件等）
startProxyServer($.toString(() => {
    return JSON.stringify({
        body: 'aa',
        headers: { 'Content-Type': 'text/html', 'Location': 'http://b.com' },
        statusCode: 302
    });
}));

// 高级：返回 InputStream / byte[]（图片解密、视频拼接）
startProxyServer($.toString(() => {
    return { body: fetch('http://1.com', { inputStream: true }) };
}));
```

| 注意 | 说明 |
|---|---|
| 默认 Content-Type 为 `application/vnd.apple.mpegurl`（即 m3u8） | 想返回别的格式必须用扩展模式自定义 |
| 想给播放器播 m3u8，**URL 必须包含 `.m3u8` 字样** | 否则可能识别不到 |
| 每次链接要带不同参数（如 `?u=xxx`） | 否则播放进度会串 |

## 72.4 定时任务（registerTask / unRegisterTask）

```javascript
var id = registerTask('myTask', 5000, $.toString((p) => {
    log('每 5 秒一次：' + p.tick);
}, { tick: Date.now() }));

// 任务自动绑定当前页面，离开页面即清理；也可手动注销
unRegisterTask(id);
```

| 限制 | 说明 |
|---|---|
| 最小间隔 500ms | 实际有 ±500ms 误差 |
| 任务耗时不要超过间隔 | 否则会被延后 |
| `id` 传 `null` | 软件自动生成 uuid，用 registerTask 返回值拿到 |

## 72.5 二维码 / 第三方 APP / 系统能力

```javascript
// 二维码
createQRCode('文本', true);                          // 第二参数 true 显示输入框先编辑
var path = createQRCodeToFile('文本');               // 保存到本地文件，路径可放 img 显示
startQRScanPage();                                    // 跳扫码界面

// 唤起第三方 APP
openAppIntent('com.tencent.mm', 'com.tencent.mm.MainActivity', {
    title: 'test',
    scan: true,
    intentData: 'http://a.com/1.mp4'    // 走 intent data
});

// 文件操作
saveImage('http://1.png||http://2.png', 'hiker://files/1.png');
fileExist('hiker://files/1.png');
var fpath = getPath('hiker://files/a.txt');          // → file:///storage/.../a.txt
shareDirectory('hiker://files/_cache/test');         // 分享文件夹（生成 hkpkg/合集）
shareDirectory('hiker://files/_cache/test', '测试.hkpkg');

// 系统信息
var v   = getAppVersion();   // 整数版本号
var ip  = getIP();           // 手机 IP
var ua  = MOBILE_UA;         // 内置 UA 常量
```

## 72.6 媒体与列表辅助 API

| 方法 | 说明 |
|---|---|
| `cacheM3u8(url, [opts], [name])` | 把 m3u8 索引缓存到本地，返回 `file://...##原地址` |
| `batchCacheM3u8([{url, options}, ...])` (`bcm`) | 批量缓存（同 batchFetch） |
| `cacheM3u8WithPngProxy(url, opts, name)` | 处理 `image/png` 伪装的 m3u8（自动转 ts） |
| `convertM3u8WithPngProxy(content, {headers})` | 直接处理已下载的 m3u8 文本 |
| `clearM3u8Ad(url, opts, rules)` | 立即清广告段，返回可播链接 |
| `clearM3u8AdLazy(url, opts, rules)` | 生成 lazyRule 字符串，点击时才清 |
| `fixM3u8(baseUrl, content)` | 修正相对路径的 m3u8/ts/key |
| `hexToBytes(hex)` | 16 进制串 → Uint8Array |
| `writeHexFile(path, hex)` | 16 进制直接写二进制文件（搭配 `fetch(url,{toHex:true})`） |
| `hexToBase64(hex)` | 16 进制 → base64 |
| `convertBase64Image(url)` | 图片转 `data:image/jpeg;base64,...` |
| `refreshVideoUrl(url, restart)` | 不退播放器刷新播放地址（支持多线路对象） |
| `setPageReverse(true/false)` | 选集倒序/顺序（`isPageReversed` 读取） |
| `setLastChapterRule(rule)` | 动态修改"最新章节"规则；JS 写法见 help_js 31-251 行 |

## 72.7 弹出原生 UI（showSelectOptions / showLoading）

```javascript
// 下拉选择（同 select:// 但能在 JS 里直接弹）
showSelectOptions({
    title: '选择性别',
    options: ['男', '女', '保密'],
    col: 3,
    selectedIndex: 0,
    bottom: true,
    js: "'toast://选了' + input + '（' + MY_INDEX + '）'"
});

// 带图标（自动两列，不支持 col）
showSelectOptions({
    title: '渠道',
    options: [
        { title: 'A 站', icon: 'http://1.png' },
        { title: 'B 站', icon: 'http://2.png' }
    ],
    js: "'toast://选了' + input"
});

// Loading
showLoading('加载中');
hideLoading();

// 立即弹确认（不点击触发）
confirm({
    title: '更新提示',
    content: '是否立即更新？',
    confirm: $.toString(() => { refreshPage(); }),
    cancel: $.toString(() => {})
});
```

## 72.8 本章铁律

| Law | 核心要义 |
|---|---|
| Law 140 | `loadJavaClass` / `registerDNS` 等高危 API 首次调用都会弹授权，**不要在规则启动入口直接调用**，否则用户体验差 |
| Law 141 | `startProxyServer` 的回调内禁止直接引用外部变量，所有上下文走 `MY_PARAMS` 传递 |
| Law 142 | `registerTask` 启动的任务**必须**在不需要时主动 `unRegisterTask`，并在回调里 try/catch 防止页面销毁后 `updateItem` 抛错 |
| Law 143 | `cacheM3u8`/`fixM3u8`/`clearM3u8AdLazy` 三件套优先组合用，能不手写 m3u8 拼接就不要写 |

---

> **第七十三章：URL 标签全集（含进阶标签）**

第四章列出常用标签，本章是**完整对照表**，补充了进阶用法。所有标签真正请求时会被剥除，纯标识用途。

## 73.1 完整标签清单

| 标签 | 作用 | 关键说明 |
|---|---|---|
| `#noLoading#` | 不显示加载弹窗 | 见 4.1 节适用场景判断 |
| `#noHistory#` | 不记录足迹 | 标记加在**前一级**链接上 |
| `#noRecordHistory#` | 不记录历史记录 | 加在页面 url |
| `#noRefresh#` | 禁止下拉刷新 | 加在页面 url |
| `#immersiveTheme#` | 沉浸式（标题栏不占位） | 仅二级/子页面 |
| `#fullTheme#` | 全屏 | 仅二级/子页面 |
| `#readTheme#` | 阅读模式（点击/音量翻页、进度记忆） | 配合 `col_type:'rich_text', extra:{click:true}` |
| `#gameTheme#` | 游戏模式（全屏 + 右上角菜单） | 仅二级/子页面 |
| `#autoPage#` | 滚到底自动加载父级下一项 | 章节按钮加，不含 `fypage` |
| `#autoCache#` | 自动缓存页面（秒开） | 别用在正文页（缓存 key = 链接+标题 md5） |
| `#cacheOnly#` | 只用缓存，不发网络（无需再加 autoCache） | 离线阅读，需下拉手动刷 |
| `#memoryPage#` | 翻页位置自动记忆 | 下拉刷新清除记忆 |
| `#background#` | 后台播放音频 + 通知栏前台通知 | 用于 X5 播音频防被杀 |
| `#originalSize#` | 大图查看强制原图加载 | 超大图慎用，可能 OOM |
| `#isVideo=true#` / `#ignoreVideo=true#` | 强制是/不是视频 | 嗅探辅助 |
| `#isMusic=true#` / `#ignoreMusic=true#` | 强制是/不是音频 | 嗅探辅助 |
| `#ignoreImg=true#` | 强制非图片 | 链接含 jpg 但实际是页面 |
| `#ignoreM3U8#` | 强制非 m3u8 | 链接含 .m3u8 但不是流 |
| `#isM3u8` / `#isM3u8#` | 强制识别 m3u8（绕过 content-type 校验） | 链接含 .mp4 但实际是 m3u8 必备 |
| `#pre#` / `#noPre#` | 强制 / 禁止预加载 | 时效性 URL 用 `#noPre#` |
| `#concat#` | 连接多段视频 | 多段地址用 `#concat#` 拼接传给播放器 |
| `#fastPlayMode#` | 极速播放（边下边播） | 用户黑名单中无效；下载繁忙时无效 |
| `#threads=xxx#` | 仅与 `#fastPlayMode#` 连用 | 3-32 线程，默认随用户设置 |
| `#checkMetadata=false#` | 音频禁止读取内置封面/标题 | 用外部传入的元数据 |
| `#isJiexi=1#` | 触发聚阅内置阅读/播放器 | 漫画/小说章节 URL **必须** |
| `#noCookie#` | 请求头 Cookie 字段留空 | `fetch(url,{headers:{Cookie:'#noCookie#'}})` |

## 73.2 标签组合用法

```javascript
// 小说章节 URL（聚阅）：触发阅读器 + 阅读模式 + 自动翻章
url: chapterUrl + '#isJiexi=1#readTheme##autoPage#'

// 离线小说目录：秒开 + 翻页记忆
url: tocUrl + '#autoCache##memoryPage#'

// 完全离线：只读缓存
url: tocUrl + '#cacheOnly##memoryPage#'

// 极速播放高线程
url: 'https://x.com/big.mp4#fastPlayMode##threads=16#'

// 后台音频
url: 'hiker://page/player#background#'
```

## 73.3 `#autoCache#` 高级用法（cacheCode）

```javascript
// 在解析规则首行注册"读缓存前先执行"的代码（用来给 x5 注入变量）
cacheCode($.toString((t) => { putVar('a', t); }, JSON.stringify(payload)));

// 让 cacheCode 直接返回缓存内容（取代默认列表）
cacheCode(() => [{ title: '来自 cacheCode', col_type: 'text_1' }]);
```

仅 `#autoCache#`/`#cacheOnly#` 页面生效，常用于首屏混合缓存。

## 73.4 本章铁律

| Law | 核心要义 |
|---|---|
| Law 144 | 聚阅章节 URL 缺 `#isJiexi=1#` 会进网页而不是阅读器；这是聚阅独有，海阔无需 |
| Law 145 | `#autoCache#` 缓存 key 由 `链接+页面标题` md5 生成，**翻页/不同分类必须用不同 url** |
| Law 146 | `#fastPlayMode#` 在用户黑名单或下载器繁忙时静默降级到普通播放，不用判断结果 |

---

> **第七十四章：扩展生态接口（WebDav / EPUB / 小程序包 / 弹幕）**

## 74.1 WebDav 开放接口

```javascript
var webdav = buildWebDav('http://x.com/dav', user, password);

// 列出目录
var list = JSON.parse(webdav.list());

// 文件操作
for (var i = 0; i < list.length; i++) {
    log(list[i].playUrl);                                    // 直接播放
    log('download://' + list[i].playUrl);                    // 下载
    webdav.download(list[i].name, 'hiker://files/cache/_fileSelect_' + list[i].name);  // 显式下载
}
webdav.upload('a.mp4', 'hiker://files/cache/a.mp4');
webdav.delete('a.mp4');
webdav.makeDir('视频');

// 进入子文件夹：用 list 子元素的 url 重新 build
var childDav = buildWebDav(list[0].url, user, password);
```

## 74.2 EPUB 接口

```javascript
// 解析章节目录（耗时操作，建议缓存结果）
var chapters = getEpubChapters('hiker://files/x.epub');
log(chapters[0].title + ' - ' + chapters[0].url);

// 获取正文（推荐用 getEpubContent，避免老接口 getEpubContent0）
var content = getEpubContent('hiker://files/x.epub', chapters[0].url);

// 元数据
var meta = getEpubMetadata('hiker://files/x.epub');

// 正文用 rich_text 渲染，正文页链接搭配 #readTheme##autoPage# 享受阅读模式
d.push({ col_type: 'rich_text', title: content, url: chapters[1].url + '#readTheme##autoPage#' });
```

## 74.3 小程序包 / 合集包

| 后缀 | 内容 |
|---|---|
| `.hk小程序.zip` | rule.json + libs.zip（远程依赖） + data.zip（私有数据） + data2.zip（不参与备份的数据） + require.json |
| `.hk合集.zip` 或 `.hkpkg` | 多个小程序、规则、口令、json 合集；支持 `*.hkshell` 脚本 |

**hkshell 脚本指令（类似 Dockerfile，目前只支持 COPY）：**

```text
COPY a.txt hiker://files/cache/b.txt          # 文件改名复制
COPY a.txt hiker://files/cache/                # 保留原名复制
COPY data hiker://files/cache/data2            # 文件夹整体复制
```

**hkshell 高级模式（JS 形式）：**

```text
js: let count = copyFiles('a.txt', 'hiker://files/cache/', MY_DIR);
// 子页面调用时 MY_DIR 不会自动注入，需要从父页面手动透传
```

**规则内打包分享：**

```javascript
shareDirectory('hiker://files/_cache/myrule');                    // 默认 .hk合集.zip
shareDirectory('hiker://files/_cache/myrule', '我的规则.hkpkg');   // 自定义文件名
```

## 74.4 小程序弹幕搜索（danmu#）

让自己的规则在播放器内提供弹幕搜索：把一个子页面标识设为 `danmu#开头`，软件会在播放器搜索弹幕时调起。

```javascript
// 子页面 danmu#Q站 的解析规则
// input = 用户输入的片名
var keyword = input;
var d = [{
    title: keyword + ' 第一话',
    url: 'https://danmu.example.com/' + keyword + '.xml'
}, {
    title: keyword + ' 第二话',
    url: $('https://danmu.example.com/lazy/' + keyword).lazyRule(() => {
        // 二级懒加载：返回字符串=最终 url；返回数组=进入选集页
        return fetch(input);
    })
}];
setResult(d);
```

| 字段 | 说明 |
|---|---|
| 子页面标识必须以 `danmu#` 开头 | 一个规则可有多个：`danmu#1`、`danmu#2` 会同时显示 |
| 返回数组每项含 `title` + `url` | url 可含 `@lazyRule=`，点击时执行 |
| 弹幕格式 | B 站 XML、JSON（`[{text,time}]`）、`web://` 加载 WebView |

## 74.5 小程序处理外部 JS 文件（intent#js#）

把一个子页面标识改为 `intent#js#`，当外部 APP 唤起本软件打开 `.js` 文件（非 `global_*` 网页插件）时，软件会列出所有带 `intent#js#` 的小程序让用户选择，选中后执行该子页面规则，并注入 `input = 文件路径`。

## 74.6 投屏开放接口

软件内启动 DLNA/网页投屏后，提供本机 HTTP 接口：

| 接口 | 说明 |
|---|---|
| `GET /playUrl` | 当前播放地址；手机端切换地址会自动刷新 |
| `GET /playUrl?enhance=true` | 增强：返回 `{title, url, headers, jumpStartDuration, jumpEndDuration}` |
| `GET /getPlayList` | 选集列表字符串数组 |
| `GET /playMe?index=0&title=第一集` | 切到指定选集，返回 true=已生效；接着轮询 `/playUrl` 拿新地址 |
| `GET /playNext` | 下一集 |

## 74.7 聚合搜索代理 (`extra.rules`)

让一个 input 元素聚合多个规则一起搜：

```javascript
{
    col_type: 'input',
    title: '聚合搜索',
    url: "'hiker://search?s=' + input",
    extra: {
        rules: "JSON.stringify([{title:'A站',rule:'...'}, {title:'B站',rule:'...'}])",
        delegateOnlySearch: true   // 只传 searchFind/title，其它属性继承父规则
    }
}
```

- `rules` 是**一段 JS 代码字符串**，运行后返回**规则数组的 JSON 字符串**
- 软件自带搜索规则不能用此功能；只能在首页用，也可以在搜索解析里返回 `hiker://search?s=xxx` 跳转聚合搜索

## 74.8 Python (Chaquopy) 集成

```javascript
// 1. 先让用户导入 Chaquopy 依赖 apk
// 2. 在解析规则中初始化（path 为依赖 apk 路径）
initChaquopy('hiker://files/cache/chaquopy.apk');

// 3. 现在可以执行 Python 代码（详细用法见官方 demo 链接）
```

完整文档：<https://www.123865.com/s/fajA-nNTEh>

## 74.9 浏览器代理规则（addWebProxyRule）

```javascript
addWebProxyRule({
    name: 'test.com',
    match: '.*test\\.com/.*',
    replace: 'input.replace("1", "2")',         // input 是原始 url
    requestHeaders: { 'x-requested-with': 'android', 'dns': '192.1.1.122' },
    responseHeaders: [
        { match: 'content-security-policy', replace: '' }
    ]
});

// 移除
removeWebProxyRule('test.com');
```

只影响 `x5_webview_single` 组件、X5/webRule 加载的请求；不影响 fetch/request。

## 74.10 广告订阅订阅格式

```json
{
  "urlV2": "https://example.com/ad_v2.txt",
  "domBlockRuleUrl": "https://example.com/domBlockRules.txt"
}
```

- `urlV2`：网址过滤（"广告网址拦截"分享导出的格式）
- `domBlockRuleUrl`：网页元素过滤（"网页元素拦截"分享导出的格式）

## 74.11 本章铁律

| Law | 核心要义 |
|---|---|
| Law 147 | WebDav 子文件夹必须用 `buildWebDav(子url, user, pwd)` 重新构造对象，不要在原对象上拼路径 |
| Law 148 | `getEpubChapters` 耗时，必须缓存（`storage0.putMyVar`），不要每次进页面都重新解析 |
| Law 149 | `danmu#` 子页面解析规则不能依赖规则侧的 `parse.host` / `juItem`，沙箱内只能用 `input`（关键词）+ 自带 API |
| Law 150 | 聚合搜索 `extra.rules` 是 JS 代码字符串而非 JSON 数组，**返回值必须是规则数组的 JSON 字符串**（`JSON.stringify`） |

---

*手册补充版本 V3.66 · 第六十九～七十四章：基于 help/*.json 完整补全*

补充内容覆盖：
- 第 69 章：URL 协议大全（hiker://* / select:// / input:// / x5*:// 等 30+ 协议）
- 第 70 章：自动导入云口令体系（16 种标识 + `@import=js:` + `publishRule`）
- 第 71 章：fba 桥接 API 完整参考（30+ 方法，分 UI/媒体/数据/跳转/解析 5 类）
- 第 72 章：进阶 JS API（DNS / Java 字节码 / 代理服务器 / 定时任务 / 二维码 / m3u8 全套）
- 第 73 章：URL 标签全集（4 章基础上补充 #autoCache# cacheCode、#threads=#、#noCookie# 等）
- 第 74 章：扩展生态（WebDav / EPUB / 小程序包 hkshell / 弹幕 danmu# / 聚合搜索 / Python）

---

> **第七十五章：四维 scroll_button 筛选 + 远程模板二级（BBYS 实战模式）**

本章以 **BBYS（bbys.app）** 聚合 API 站为模板，拆解两个高复用度的 UI 范式：**四维联动筛选行**（顶层 Tab → 分类 → 地区 → 年份）和**远程模板渲染的二级页**（数据 / 渲染分离架构）。两个模式相互独立，可单独移植到任意 API 型规则。

## 75.1 模式总览

| 模块 | 函数位置 | 核心机制 |
|---|---|---|
| 四维筛选行 | 主页 / 分类 | 4 行 `scroll_button` + `getMyVar/putMyVar` 双 key（`*_index` + value） |
| 父子级联清空 | 切顶层 Tab 时 | 一次 `clearMyVar` 全量重置子级索引和值 |
| 远程模板二级 | 二级 | `requireCache` 加载远程渲染器，二级 `return` 结构化对象由模板消费 |
| 多线路聚合 | 二级 | `$$$` 拆线路、`#` 拆选集、`$` 拆名称/地址；`playerMap` 做 from→显示名映射 |

## 75.2 四维筛选行——双 key 状态设计

### 75.2.1 为什么要双 key

| key | 用途 | 取值示例 |
|---|---|---|
| `xxx_index` | UI 选中态高亮判断 | `"0"`, `"1"`, `"2"` |
| `xxx` | 真正拼到请求 URL 的值 | `""`（全部）, `"日本"`, `"2026"` |

**核心矛盾：** "全部" 选项对应的请求值是空串 `""`，未选过的初始值也是空串。如果只用 value 做高亮判断，"全部"和"未选"无法区分；用 index 标记 UI、用 value 拼参数，两个职责分离。

```javascript
// ❌ 单 key 的坑
var area = getMyVar("area", "");
title: area == val ? "<b>" + name + "</b>" : name   // "全部" 永远显示选中
// ✅ 双 key
var idx = getMyVar("area_index", "0");
title: idx == i.toString() ? "<b>" + name + "</b>" : name
```

### 75.2.2 通用筛选行渲染函数（可直接复用）

```javascript
_renderFilter: function(d, names, vals, varKey, color) {
    var idxKey  = varKey + "_index";
    var curIdx  = getMyVar(idxKey, "0");
    for (var i = 0; i < names.length; i++) {
        var selected = curIdx == i.toString();
        d.push({
            title: selected
                ? "‘‘’’<b><font color=#ffffff>" + names[i] + "</font></b>"
                : names[i],
            url: $("#noLoading##noHistory#").lazyRule(function(v, idx, k, ik) {
                putMyVar(ik, idx);
                putMyVar(k,  v);
                refreshPage();
                return "hiker://empty";
            }, vals[i], i.toString(), varKey, idxKey),
            col_type: "scroll_button",
            extra: { backgroundColor: selected ? color : "gary" }
        });
    }
    d.push({ col_type: "blank_block" });
}

// 调用——一行一个维度
this._renderFilter(d, areaName, areaVal, "area", "#98D8AA");
this._renderFilter(d, yearName, yearVal, "year", "#F7DC6F");
```

**`#noLoading##noHistory#` 双标签：** 切筛选条件无 loading 闪屏 + 不污染足迹（参见 4.1 节）。

### 75.2.3 父子级联清空

顶层 Tab（电影/剧集/动漫/综艺）切换时，**所有子级筛选必须重置**，否则会发出"动漫 + 欧美地区"这种数据库不存在的组合查询：

```javascript
url: $("#noLoading##noHistory#").lazyRule(function(ct, si) {
    putMyVar("ctitle", ct);
    putMyVar("sub",    si);
    // ⚠️ 切顶层必须级联清子级（index 和 value 都要清）
    clearMyVar("area_index");  clearMyVar("area");
    clearMyVar("year_index");  clearMyVar("year");
    clearMyVar("class_index"); clearMyVar("class");
    refreshPage();
    return "hiker://empty";
}, x, subs[i])
```

### 75.2.4 父级决定子项的 classMap

二级筛选项动态跟着顶层 Tab 变（电影下是"剧情/动作/喜剧"，动漫下是"日漫/国漫/热血"）——静态分类的五维矩阵做不到这种动态联动：

```javascript
var classMap = {
    电影: ["全部","剧情","动作","喜剧","爱情","科幻","恐怖"],
    剧集: ["全部","国产","港台","日韩","欧美","悬疑"],
    动漫: ["全部","日韩动漫","国漫","热血","冒险"],
    综艺: ["全部","大陆综艺","日韩综艺","真人秀"]
};
var classVal = classMap[getMyVar("ctitle", "电影")] || ["全部"];
```

### 75.2.5 仅首页渲染筛选条

```javascript
if (MY_PAGE == "1") {
    // 渲染 4 行筛选 + blank_block 分隔
}
// 翻页时筛选条 push 都跳过，只重新拉数据列表
// 收益：省请求 / 保留用户滚动位置 / 列表更顺滑
```

### 75.2.6 配色规范

```javascript
var colors = ["#F4A7B9", "#7EC8E3", "#98D8AA", "#F7DC6F"];
// [0]=类型行（顶层 Tab）  [1]=分类行  [2]=地区行  [3]=年份行
```

每行一个主色，视觉上能秒辨"我现在在哪一层做筛选"。**未选中态用 `"gary"`**（注意这是 BBYS 原规则的拼写，引擎遇到非法色按默认灰渲染——可改为 `"#cccccc"` 更显式）。

### 75.2.7 请求 URL 空值丢弃

```javascript
var url = host + "/api.php/web/filter/vod?type_name=" + sub + "&page=" + MY_PAGE;
var area = getMyVar("area", "");
if (area) url += "&area=" + encodeURIComponent(area);   // 空 = 全部 = 不带参
var year = getMyVar("year", "");
if (year) url += "&year=" + year;
var cls  = getMyVar("class", "");
if (cls)  url += "&class=" + encodeURIComponent(cls);
```

中文值必须 `encodeURIComponent`，纯数字（year）可省。

## 75.3 远程模板二级——数据 / 渲染分离

### 75.3.1 标准列表返回 vs 模板返回

| 模式 | 返回值 | 适用场景 |
|---|---|---|
| 标准列表 | `return d`（数组） | 每条规则 UI 都不同，灵活自由 |
| 远程模板 | `return { detailObj, line, list, ... }`（对象） | 多个规则共用同一种二级 UI |

**远程模板返回对象的标准字段：**

```javascript
requireCache("http://yourcdn.com/二级新样式.js", 24);  // 远程渲染器，缓存 24h
return {
    detailObj: header(false, {
        片名: title, 图片: img,
        类型: detail.vod_class || "",
        状态: detail.vod_remarks || "",
        年份: detail.vod_year || ""
    })[0],
    detail1: "‘‘’’" + title + "\n主演：" + actor + " 导演：" + director,  // 富文本
    detail2: "类型:" + cls + "  地区:" + area + " 年份:" + year,
    desc:    intro,    // 简介纯文本
    img:     img,
    line:    tabs,     // 线路名数组：["资源 A", "资源 B", ...]
    list:    lists     // 二维选集数组：[[{title,url}, ...], [{title,url}, ...]]
                       // line[i] ↔ list[i] 严格一一对应
};
```

| 字段 | 类型 | 模板用法 |
|---|---|---|
| `detailObj` | object | header 函数返回数组的第 0 项，包含片名/图片/分级信息 |
| `detail1` | string（富文本） | 第一行简介，支持 `‘‘’’` 着色 |
| `detail2` | string | 第二行元数据 |
| `desc` | string | 长简介，模板里通常展开/折叠 |
| `img` | string | 大图片地址 |
| `line` | string[] | 线路 Tab 名 |
| `list` | item[][] | 每条线路下的 episode 数组 |

### 75.3.2 多线路聚合数据流（MACCMS API 通用）

```
detail.vod_play_from = "线路1$$$线路2$$$线路3"
detail.vod_play_url  = "第1集$地址1#第2集$地址2$$$..."
                          └─#─┘ 拆每集
                          └─$─┘ [0]=集名 [1]=地址
                       └──$$$──┘ 拆线路（与 from 同序）
```

**完整拆解模板：**

```javascript
var playFrom = detail.vod_play_from.split("$$$");
var playUrl  = detail.vod_play_url.split("$$$");

for (var i = 0; i < playFrom.length; i++) {
    var from = playFrom[i];
    var str  = playUrl[i];
    if (!from || !str) continue;

    var eps = str.split("#");
    var names = [];
    for (var j = 0; j < eps.length; j++) {
        var p    = eps[j];
        var name = p.split("$")[0];                  // 集名
        // var addr = p.split("$")[1];               // 实际播放地址（如需直链时取）
        if (name) names.push({ title: name, nid: j + 1 });
    }
    var item = {};
    item[from] = names;
    episodeTab.push(item);
}
```

### 75.3.3 `playerMap`——线路 from → 显示名映射

API 返回的 `from` 通常是英文标识（如 `kk`, `bjm3u8`），实际展示需要中文友好名。优先级：**接口给的 `show` > 自定义映射 > from 原值**。

```javascript
var playerMap = {};
// 1) 接口返回的 vodplayer.show 字段
(djson.vodplayer || []).forEach(function(p) {
    playerMap[p.from] = p.show;
});
// 2) 兜底：from 自身做名字
playerMap[from] = playerMap[from] || from;
// 3) 聚合搜索接口的 site_name 也并入
playerMap[site_key] = site_name || site_key;

// 最终展示
tabs.push(playerMap[key] || key);
```

### 75.3.4 选集标题清洗与样式

```javascript
var cleanTitle = ep.title.replace(/第(\d+)集/, "$1").replace(/第|集|话|期/g, "");

// 跳过预告/纯享
if (ep.title.indexOf("预告") !== -1 || ep.title.indexOf("纯享") !== -1) continue;

episodeList.push({
    title: cleanTitle,
    url: "video://" + host + "/play/" + vid + "#sid=" + encodeURIComponent(key)
         + "&nid=" + encodeURIComponent(ep.nid),
    // 自动布局：选集多且标题短 → 五列；否则两列
    col_type: episodes.length > 3 && cleanTitle.length < 5 ? "text_5" : "text_2",
    extra: {
        videoRules:  ["videoType=1", "*.m3u8"],
        blockRules:  [".png", ".jpg"],
        ua: PC_UA
    }
});
```

**`extra.videoRules` / `blockRules`** 是给 `video://` 嗅探器的钩子，加快定位 m3u8、屏蔽图片资源。

### 75.3.5 line / list 对齐保护

线路下没有任何有效选集时，**必须把 line 也 pop 掉**，否则模板会出现"空 Tab"：

```javascript
if (episodeList.length > 0) {
    lists.push(episodeList);
} else {
    tabs.pop();   // ⚠️ 失败时丢线路名，保持 tabs 与 lists 长度一致
}
```

### 75.3.6 远程模板模式的风险

| 风险 | 缓解 |
|---|---|
| 远程文件下线 → 整规则崩 | 24h 缓存（`requireCache(url, 24)`）+ 自建/可控 CDN |
| 字段变更 → 旧规则全军覆没 | 模板侧做向后兼容；新字段用可选 |
| 第三方托管 | 优先托管到自己掌握的域名，不要用社区临时服务器 |

> ⚠️ 与 Law 88（远程依赖风险）部分冲突。生产规则慎用此模式，**只在统一 UI 收益足够大时使用**（如一套 6+ 规则共用同一渲染器）。

## 75.4 完整模板（可直接复用骨架）

```javascript
var parse = {
    作者: "dev",
    版本: "20260513",
    host: "https://example.com",
    UA:   "Mozilla/5.0 ...",
    页码: { 主页: true, 分类: true },

    _headers: { "x-client": "...", "web-sign": "..." },

    // 顶层 Tab → 子分类映射（依站点接口设计）
    _classMap: {
        电影: ["全部","剧情","动作","喜剧"],
        剧集: ["全部","国产","港台","日韩"],
        动漫: ["全部","日漫","国漫"],
        综艺: ["全部","真人秀","脱口秀"]
    },
    _colors: ["#F4A7B9", "#7EC8E3", "#98D8AA", "#F7DC6F"],

    静态分类: {
        type: '主页',
        url:  'hiker://empty##fyclass##fypage',
        class_name: '电影&剧集&动漫&综艺',
        class_url:  '电影&剧集&动漫&综艺'
    },

    // 通用筛选行
    _renderFilter: function(d, names, vals, varKey, color) {
        var idxKey = varKey + "_index";
        var cur    = getMyVar(idxKey, "0");
        for (var i = 0; i < names.length; i++) {
            var sel = cur == i.toString();
            d.push({
                title: sel ? "‘‘’’<b><font color=#ffffff>" + names[i] + "</font></b>" : names[i],
                url: $("#noLoading##noHistory#").lazyRule(function(v, idx, k, ik) {
                    putMyVar(ik, idx);
                    putMyVar(k,  v);
                    refreshPage();
                    return "hiker://empty";
                }, vals[i], i.toString(), varKey, idxKey),
                col_type: "scroll_button",
                extra: { backgroundColor: sel ? color : "gary" }
            });
        }
        d.push({ col_type: "blank_block" });
    },

    分类: function() {
        var d = [], host = this.host;
        var ctitle = getMyVar("ctitle", "电影");
        setPageTitle("分类≡" + ctitle);

        if (MY_PAGE == "1") {
            // ── 1. 顶层 Tab（切换时清子级）──
            var tops    = Object.keys(this._classMap);
            var curIdx0 = tops.indexOf(ctitle);
            for (var i = 0; i < tops.length; i++) {
                var x   = tops[i];
                var sel = ctitle == x;
                d.push({
                    title: sel ? "‘‘’’<b><font color=#ffffff>" + x + "</font></b>" : x,
                    url: $("#noLoading##noHistory#").lazyRule(function(ct) {
                        putMyVar("ctitle", ct);
                        ["area","area_index","year","year_index","class","class_index"].forEach(clearMyVar);
                        refreshPage();
                        return "hiker://empty";
                    }, x),
                    col_type: "scroll_button",
                    extra: { backgroundColor: sel ? this._colors[0] : "gary" }
                });
            }
            d.push({ col_type: "blank_block" });

            // ── 2. 三行筛选 ──
            var classNames = this._classMap[ctitle] || ["全部"];
            var classVals  = classNames.map(function(x){ return x === "全部" ? "" : x; });
            this._renderFilter(d, classNames, classVals,
                ["全部","2026","2025","2024","2023","2022","更早"],
                "class", this._colors[1]);
            this._renderFilter(d,
                ["全部","中国大陆","香港","台湾","日本","韩国","美国"],
                ["","中国大陆","香港","台湾","日本","韩国","美国"],
                "area", this._colors[2]);
            this._renderFilter(d,
                ["全部","2026","2025","2024","2023","2022"],
                ["","2026","2025","2024","2023","2022"],
                "year", this._colors[3]);
        }

        // ── 3. 拉数据 ──
        var url = host + "/api.php/web/filter/vod?type_name=" + ctitle + "&page=" + MY_PAGE;
        var area = getMyVar("area", ""); if (area) url += "&area="  + encodeURIComponent(area);
        var year = getMyVar("year", ""); if (year) url += "&year="  + year;
        var cls  = getMyVar("class","");if (cls)  url += "&class=" + encodeURIComponent(cls);
        try {
            var list = JSON.parse(request(url, { headers: this._headers })).data || [];
            list.forEach(function(v) {
                d.push({
                    title: v.vod_name,
                    img:   v.vod_pic,
                    desc:  (v.vod_remarks||"") + (v.vod_year ? " · " + v.vod_year : "")
                         + (v.vod_douban_score > 0 ? " · 豆瓣" + v.vod_douban_score : ""),
                    url:   host + "/play/" + v.vod_id,
                    col_type: "movie_3",
                    extra: { id: v.vod_id }
                });
            });
        } catch (e) { log("分类错误:" + e.message); }
        return d;
    },

    二级: function(url) {
        var vid = url.replace(this.host + "/play/", "");
        var detailUrl = this.host + "/api.php/web/vod/get_detail?vod_id=" + vid;
        var playerMap = {}, episodeTab = [];
        try {
            var dj     = JSON.parse(request(detailUrl, { headers: this._headers }));
            var detail = dj.data[0];
            var img    = detail.vod_pic;
            var title  = detail.vod_name || "";
            var intro  = (detail.vod_content || "").replace(/<[^>]+>/g, "").trim();

            (dj.vodplayer || []).forEach(function(p){ playerMap[p.from] = p.show; });
            var froms = detail.vod_play_from.split("$$$");
            var urls  = detail.vod_play_url.split("$$$");
            for (var i = 0; i < froms.length; i++) {
                if (!froms[i] || !urls[i]) continue;
                playerMap[froms[i]] = playerMap[froms[i]] || froms[i];
                var eps   = urls[i].split("#");
                var names = [];
                for (var j = 0; j < eps.length; j++) {
                    var nm = eps[j].split("$")[0];
                    if (nm) names.push({ title: nm, nid: j + 1 });
                }
                var t = {}; t[froms[i]] = names; episodeTab.push(t);
            }

            var tabs = [], lists = [];
            for (var i = 0; i < episodeTab.length; i++) {
                var key  = Object.keys(episodeTab[i])[0];
                tabs.push(playerMap[key] || key);
                var ep   = episodeTab[i][key];
                var arr  = [];
                for (var j = 0; j < ep.length; j++) {
                    var ct = ep[j].title.replace(/第(\d+)集/,"$1").replace(/第|集|话|期/g,"");
                    if (/预告|纯享/.test(ep[j].title)) continue;
                    arr.push({
                        title: ct,
                        url: "video://" + this.host + "/play/" + vid
                           + "#sid=" + encodeURIComponent(key)
                           + "&nid=" + encodeURIComponent(ep[j].nid),
                        col_type: ep.length > 3 && ct.length < 5 ? "text_5" : "text_2",
                        extra: { videoRules: ["videoType=1","*.m3u8"], blockRules:[".png",".jpg"], ua: PC_UA }
                    });
                }
                if (arr.length > 0) lists.push(arr); else tabs.pop();   // 对齐保护
            }

            requireCache("http://your-cdn.com/二级新样式.js", 24);
            return {
                detailObj: header(false, {
                    片名: title, 图片: img,
                    类型: detail.vod_class || "",
                    状态: detail.vod_remarks || "",
                    年份: detail.vod_year || ""
                })[0],
                detail1: "‘‘’’" + title + "\n主演：" + (detail.vod_actor||"")
                       + " 导演：" + (detail.vod_director||""),
                detail2: "类型:" + (detail.vod_class||"未知")
                       + "  地区:" + (detail.vod_area||"未知")
                       + " 年份:" + (detail.vod_year||"未知"),
                desc:  intro, img: img,
                line:  tabs,  list: lists
            };
        } catch (e) { log("二级错误:" + e.message); }
    },

    解析: function(url) { return url; }
};
```

## 75.5 本章铁律

| Law | 名称 | 核心要义 |
|---|---|---|
| Law 151 | 双 key 筛选 | 多维筛选 UI 用"`*_index` 高亮 + value 请求"双 key 分离，避免"全部=空值"导致选中态丢失 |
| Law 152 | 级联清空 | 父级筛选切换必须 `clearMyVar` 全部子级 index+value，否则会出现数据库不存在的组合查询 |
| Law 153 | 仅首页渲染筛选条 | 翻页时跳过筛选行 push，保留滚动位置 + 省请求；条件用 `if (MY_PAGE == "1")` |
| Law 154 | line/list 对齐保护 | 远程模板二级里某线路 episode 为空时，**必须 `tabs.pop()`** 保持 line/list 长度一致，否则模板出现空 Tab |
| Law 155 | playerMap 三级降级 | 线路显示名优先级：接口 `show` > 自定义映射 > from 原值；`playerMap[k] = playerMap[k] \|\| k` 兜底 |
| Law 156 | 远程模板需托管自有 CDN | `requireCache` 远程渲染器只在自有/可控域名上使用，且必须设缓存时长（24h+），社区临时服务器禁用 |

---

*手册补充版本 V3.66 · 第七十五章：基于 BBYS（bbys.app）实战提炼*

铁律新增 Law 130 ～ Law 150（共 21 条）。

---

> **第七十六章：ACG 漫画站多分类聚合规则（多页内容抓取 + 分类分流）**

本章基于 ACG 类站点实战提炼：**一个规则同时处理 7 个分类**（漫画、图集、全彩、写真、动画、有声、网漫），每个分类的列表页翻页格式不同，内容页需支持多页图集批量抓取。核心是**配置驱动分流 + 复用多页抓取函数**。

## 76.1 模式总览

| 模块 | 实现 | 核心思想 |
|---|---|---|
| 分类配置 | `_cats` 数组，每条含 `name/path/type/选择器` | 数据驱动，加新分类只动数组 |
| 主页分流 | 按 `_cat.type` 走不同分支（pics/video/audio/webtoon） | pics 类型全部走 `_multiPageRule` |
| 多页内容抓取 | `_multiPageRule(url, host)` | 探测最大页码 → batchFetch 并发抓取 → 去重拼 `pics://` |
| 搜索 / 二级 | 复用 `_multiPageRule` | 一套抓取逻辑，三处入口 |

## 76.2 `_cats` 配置驱动模式

```javascript
_cats: [
    { name: '漫画', path: '/h/',             type: 'pics',    listSel:'...', titleSel:'...', hrefSel:'...' },
    { name: '图集', path: '/hentai/',        type: 'pics',    /* ... */ },
    { name: '全彩', path: '/tags/full-color.html', type: 'pics', /* ... */ },
    { name: '写真', path: '/cos/',           type: 'pics',    /* ... */ },
    { name: '动画', path: '/donghua/',       type: 'video',   /* ... */ },
    { name: '有声', path: '/audio/',         type: 'audio',   /* ... */ },
    { name: '网漫', path: '/webtoon/',       type: 'webtoon', /* ... */ }
],

// 主页函数分流
主页: function() {
    var _cat = this._cats[parseInt(getMyVar('cat_idx', '0'))];
    // ... 列表抓取 ...
    list.forEach(function(it) {
        var item = { title: ..., img: ..., url: ... };
        if (_cat.type === 'pics') {
            item.url = $(item.url).rule(function(host) {
                return this._multiPageRule(input, host);
            }.bind(this), this.host);
        }
        // video/audio/webtoon 保持原 url，走二级
        d.push(item);
    });
}
```

## 76.3 `_multiPageRule`——多页内容并发抓取

**问题：** 漫画/图集/写真等内容页图片分散在多个子页（如 `/cos/800221.html` → `/cos/800221-2.html` → ...），需自动发现总页数并并发抓取。

**核心算法：**

```javascript
_multiPageRule: function(url, host) {
    // 1. 从 URL 提取 prefix + id + suffix
    //    例：/cos/800221.html → prefix=/cos/, id=800221, suffix=.html
    var m = url.match(/^(.*\/)([^\/]+?)(\.[a-z]+)$/);
    if (!m) return 'toast://URL 格式无法识别';
    var prefix = m[1], id = m[2], suffix = m[3];

    // 2. 请求第一页，正则探测最大页码
    var firstHtml = request(url);
    // ⚠️ prefix 含 / 等正则特殊字符，必须转义
    var safePrefix = prefix.replace(/[\\^$.*+?()[\]{}|]/g, '\\$&');
    var safeId     = id.replace(/[\\^$.*+?()[\]{}|]/g, '\\$&');
    var re = new RegExp(safePrefix + safeId + '-(\\d+)' + suffix.replace(/\./g, '\\.'), 'g');
    var maxPage = 1, mm;
    while ((mm = re.exec(firstHtml)) !== null) {
        var p = parseInt(mm[1]);
        if (p > maxPage) maxPage = p;
    }

    // 3. 构造任务数组（第一页已抓到，剩余 maxPage-1 页）
    var tasks = [];
    for (var i = 2; i <= maxPage; i++) {
        tasks.push({ url: prefix + id + '-' + i + suffix });
    }
    var pages = [firstHtml].concat(batchFetch(tasks));   // 引擎自动分批（>16 并发）

    // 4. 每页提取图片，去重拼 pics://
    var pics = [];
    var seen = {};
    pages.forEach(function(html) {
        var imgs = pdfa(html, '.content-page&&img');
        imgs.forEach(function(img) {
            var src = pdfh(img, 'img&&data-original') || pdfh(img, 'img&&src');
            if (src && !seen[src]) { seen[src] = 1; pics.push(src); }
        });
    });

    return 'pics://' + pics.join('&&');
}
```

| 关键点 | 说明 |
|---|---|
| `prefix` 正则转义 | 必须 `replace(/[\\^$.*+?()[\]{}|]/g, '\\$&')`，否则 `/` `.` 等被吃掉 |
| `batchFetch` 并发上限 16 | 超过自动分批串行，**无需手动处理**（参见 3.3 节铁律） |
| 第一页已抓必须复用 | tasks 从 i=2 开始，省一次请求 |
| 图片 URL 去重 | `seen` 字典防止跨页重复（部分站点首图重复出现） |

## 76.4 不同分类列表翻页格式

实战发现同一站点不同分类翻页格式不一致：

| 分类 | 第 1 页 | 第 N 页 | 翻页拼装 |
|---|---|---|---|
| 普通分类（`/h/` `/hentai/`） | `/h/` | `/h/?page=2` | `path + (MY_PAGE>1 ? '?page='+MY_PAGE : '')` |
| 全彩 tags 型 | `/tags/full-color.html` | `/tags/full-color-page-2.html` | `path.replace('.html', '') + '-page-' + MY_PAGE + '.html'` |
| 写真 cos 型 | `/cos/` | `/cos/2.html` | `path + (MY_PAGE>1 ? MY_PAGE+'.html' : '')` |

```javascript
// 推荐做法：把翻页逻辑挂在 _cat 配置里
_cats: [{
    name: '全彩',
    path: '/tags/full-color.html',
    type: 'pics',
    pageBuild: function(p) {
        return p === 1 ? this.path : this.path.replace('.html', '') + '-page-' + p + '.html';
    },
    /* ... */
}]
// 主页里统一 _cat.pageBuild(MY_PAGE)
```

## 76.5 搜索页选择器三级降级

**问题：** 搜索结果页 `ul#list` 可能不存在（有时是 `div.list`，有时纯 `li` 字符串），`pdfa` 返回空。

**三级降级模板：**

```javascript
搜索: function(key, page) {
    var url = this.host + '/q/' + encodeURIComponent(key) + '-' + page + '.html';
    var html = request(url);
    var d = [];

    // ① 优先：精确选择器
    var items = pdfa(html, 'body&&ul#list&&li');

    // ② 降级 1：宽松选择器
    if (items.length === 0) items = pdfa(html, 'body&&div.list&&li');
    if (items.length === 0) items = pdfa(html, 'body&&li');

    // ③ 降级 2：字符串切割保底（pdfa 都失败时）
    if (items.length === 0) {
        items = html.split('<li>').filter(function(s) {
            return s.indexOf('</li>') > -1 && s.indexOf('href=') > -1;
        });
    }

    items.forEach(function(item) {
        var href  = pdfh(item, 'a&&href');
        var title = pdfh(item, 'a&&Text');
        if (href && title) d.push({ title: title, url: href });
    });
    return d;
}
```

## 76.6 二级网漫章节复用 `_multiPageRule`

**网漫的二级页是章节列表**，需要返回 `line/list` 结构；每个章节 URL 仍然是多页图集格式。**章节 URL 直接复用 `_multiPageRule`** 即可，无需新写函数：

```javascript
二级: function(url) {
    var html = request(url);
    var chapters = pdfa(html, 'dd&&.chapters&&a');
    var list = [];
    chapters.forEach(function(ch) {
        var _full  = pd(ch, 'a&&href', this.host);
        var _title = pdfh(ch, 'a&&Text');
        list.push({
            title: _title,
            // ✅ 章节 url 走 $().rule 沙箱，沙箱内调用 _multiPageRule
            url: $(_full).rule(function(host) {
                return this._multiPageRule(input, host);
            }.bind(this), this.host)
        });
    });
    return { line: ['章节'], list: [list] };
}
```

## 76.7 错误排查速查

| 现象 | 排查步骤 |
|---|---|
| 列表为空 | 检查 `listSel` / `hrefSel` / `titleSel` 与页面实际 DOM 是否一致；用 `log(html.length)` 确认 fetch 成功 |
| 翻页第 2 页空白 | `log(_pageUrl)` 打印翻页 URL，确认格式（带不带 `?` `-page-` `2.html`）|
| 内容页不出图 | 检查 prefix 正则是否转义；用 `log` 输出 maxPage 和 pics.length |
| 搜索无结果 | 确认 URL 格式（如 `/q/{keyword}-1.html`），`encodeURIComponent` 后用浏览器手动访问验证 |

## 76.8 本章铁律

| Law | 名称 | 核心要义 |
|---|---|---|
| Law 157 | 配置驱动多分类 | 分类用 `_cats` 数组配置 + `type` 分流，新增分类只改数组 |
| Law 158 | prefix 正则必须转义 | `replace(/[\\^$.*+?()[\]{}|]/g, '\\$&')` 转义 6+ 特殊字符，遗漏一个就匹配失败 |
| Law 159 | 第一页结果复用 | 多页抓取时 tasks 从 i=2 开始，第一页已在探测最大页时拿到，省一次请求 |
| Law 160 | 选择器三级降级 | 精确选择器 → 宽松选择器 → `html.split('<tag>')` 字符串切割保底 |
| Law 161 | 翻页格式按分类挂配置 | 不同分类翻页格式不同，写在 `_cat.pageBuild` 函数里，**严禁主页函数 if/else 硬编码** |
| Law 162 | `pics:// + batchFetch` 是图集站标配 | 多页图集首选；超过 16 页引擎自动分批，不需要手动循环 |

---

*手册补充版本 V3.66 · 第七十六章：基于 ACG 漫画站多分类聚合实战提炼*

铁律新增 Law 157 ～ Law 162（共 6 条）。本次开发新发现并修复手册旧版错误：**`$().image()` 回调里的 `input` 是 InputStream,不是 `$()` 括号内的字符串**——第 26 章已同步修正。

---

> **第七十七章:`buildDynCate` 动态分类聚合(零配置生成 class 树)**

本章基于 苏苏阅读 / tutaow 类站点实战提炼:**主页函数零硬编码**,直接从 HTML 的导航/筛选/排行模块中**动态抽取分类与子分类**,免去手写 `_cats` 数组。核心是 `buildDynCate(d, html, 定位列表)` 一行扫平所有 class 节点。

## 77.1 函数签名

```javascript
buildDynCate(d, html, 定位列表)
```

| 参数 | 类型 | 说明 |
|---|---|---|
| `d` | Array | 主页结果数组。函数会向其 `push` 一个或多个 `col_type:'icon_2_round'`/`'icon_5'`/`'class'` 类型的分类块 |
| `html` | String | 已经 `fetch(MY_URL)` 拿到的页面 HTML 全文 |
| `定位列表` | Array<Object> | 分类源描述,每项是一组 `{一级分类, 子分类}` 选择器对,**按顺序依次解析,生成多个独立分类条** |

> 函数内部使用 `pdfa(html, 一级分类)` 抓父容器,再 `pdfa(parent, 子分类)` 抓每个 chip(分类按钮),最后塞进 `d`。**调用前 `d` 必须已声明,调用后由开发者自行 `setResult(d)`**。

## 77.2 `定位列表` 元素结构

```javascript
let 定位列表 = [{
    一级分类: 'body&&.stui-header__menu||.hl-nav',
    子分类:   'body&&li:not(:matches(首页|资讯|专题|短视频|APP下载|音乐|留言|最新|排行))'
}, {
    一级分类: 'body&&.stui-screen__list||.hl-filter-wrap:not(:matches(字母))',
    子分类:   'body&&li:has(a:not(:empty)):lt(12)'
}, {
    一级分类: 'body&&.hl-rb-title',
    子分类:   'body&&a'
}];
```

- `一级分类`:用 `||` 连接的**降级链**,左侧不存在则用右侧,适配双模板/换肤站。
- `子分类`:**伪类组合拳**——`:not(:matches(...))` 黑名单、`:has(...)` 必有子节点、`:lt(N)` 截断长度、`:empty` 排空、`:gt(0)` 跳首项。
- 三个元素分别对应:**顶部导航**、**筛选条**、**右侧排行榜**——一次性把三块独立分类全提出来。

## 77.3 标准调用模板

```javascript
var parse = {
    主页: function() {
        let d = [];
        MY_URL = getMyVar('header.url', 'https://www.tutaow.com/type/1.html');
        let html = fetch(MY_URL);
        if (html) {
            let 定位列表 = [{
                一级分类: 'body&&.stui-header__menu||.hl-nav',
                子分类:   'body&&li:not(:matches(首页|资讯|专题|短视频|APP下载|音乐|留言|最新|排行))'
            }, {
                一级分类: 'body&&.stui-screen__list||.hl-filter-wrap:not(:matches(字母))',
                子分类:   'body&&li:has(a:not(:empty)):lt(12)'
            }, {
                一级分类: 'body&&.hl-rb-title',
                子分类:   'body&&a'
            }];
            buildDynCate(d, html, 定位列表);
        }
        setResult(d);
    }
}
```

## 77.4 与 `_cats` 数组模式对比

| 维度 | `_cats` 静态配置(第七十六章) | `buildDynCate` 动态抽取(本章) |
|---|---|---|
| 分类来源 | 写死在规则里 | 实时从页面 DOM 抽 |
| 站点改版 | 改数组、改选择器 | 只要导航 DOM 不变就自动跟随 |
| 适用场景 | 分类固定、翻页格式各异的图集/漫画站 | 视频/小说门户、苹果 CMS 系模板 |
| 子分类联动 | 翻页 / 内容页要按 type 分流 | 子分类直接作为下一级跳转 URL,无需分流 |

## 77.5 常见坑

| 现象 | 原因 | 解法 |
|---|---|---|
| `d` 里没东西 | `一级分类` 选择器没命中 | 用 `log(pdfa(html, sel).length)` 验证父节点数 |
| 子分类多了"首页"等垃圾项 | 黑名单不全 | `:not(:matches(关键词1\|关键词2))` 补关键词,**用 `\|` 不是中文竖线** |
| 排行榜 chip 重复 | 同一节点被多个 `定位列表` 元素命中 | 用 `:lt(N)` 截断、或在子分类用更精确的 class 限定 |
| 顶部导航没数据但筛选条有 | 第一组 `一级分类` 失败,但函数不抛错 | `buildDynCate` 对每组独立处理,失败静默——确认时逐组注释验证 |

## 77.6 本章铁律

| Law | 名称 | 核心要义 |
|---|---|---|
| Law 163 | DOM 即配置 | 能从页面 DOM 直接抽的分类,绝不在 JS 里硬编码;站点改版成本归零 |
| Law 164 | `一级分类` 必须用 `\|\|` 降级 | 模板站经常多套皮肤,左失败右兜底,**不写降级等于赌运气** |
| Law 165 | 子分类必上黑名单 | `:not(:matches(首页\|关于))` 是标配,导航里 90% 的垃圾项靠它过滤 |
| Law 166 | `buildDynCate` 不调用 `setResult` | 函数只 push,**setResult 由开发者显式调用**,便于和静态条目混排 |
| Law 167 | `定位列表` 顺序即展示顺序 | 数组从前到后渲染,把最重要的导航条放第一项 |

---

*手册补充版本 V3.66 · 第七十七章:基于 tutaow 类苹果 CMS 站点动态分类实战提炼*

